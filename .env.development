# 开发环境：axios baseURL 使用 dev server 的 /api 代理
VITE_API_BASE_URL=/api

# 开发默认 fallback：先真实登录，网络级失败才切换 demo（规格 §13.1）
VITE_DEMO_MODE=fallback

# 仅 Vite dev server 使用，不暴露给客户端；替换为真实后端地址
PROXY_TARGET=http://localhost:8080
