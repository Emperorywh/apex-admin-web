# P22 避障模板 — 交付与联验记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
- **run 标识**：run-20260919-p22（2026-09-19，串行单任务轮；分支 apple-rxx）
- **任务卡**：TASKS.md §P22；批次 B3；前置 B2 样板门禁（已满足）
- **旧来源**：`C:\code\dd\src\pages\ObstacleAvoidance`（index.tsx + ObstacleTemplateModal + ObstacleAvoidanceTemplateData.ts + index.less）
- **新目标**：`src/pages/obstacle-avoidance/ObstacleAvoidance/ObstacleAvoidance.tsx` + `src/features/obstacle-avoidance` + `src/services/obstacle-avoidance`
- **契约依据**：default_OpenAPI.json `dispatcher/obstacleAvoidance` 五 operation（SHA-256 A82E…49C7C 基线内）

## 1. 交付范围（整页重写，替换迁移过渡占位）

### 1.1 服务层（owner=P22，contracts.md 第 5 节）

| operation | method/path | 形态 |
| --- | --- | --- |
| pageObstacleAvoidance | GET `/fms/v1/dispatcher/obstacleAvoidance/pageObstacleAvoidance` | query 平铺 pageNo/pageSize/query（G04 口径，联验再实证） |
| createObstacleAvoidance | POST `…/createObstacleAvoidance` | body=AddParam{obstacleAvoidanceName, parameters[]} |
| updateObstacleAvoidance | POST `…/updateObstacleAvoidance` | body=UpdateParam{id, obstacleAvoidanceName, parameters 整体替换} |
| deleteObstacleAvoidance | POST `…/deleteObstacleAvoidance` | `?id=`（OpenAPI 声明 required query 参数；旧实现 POST+URLSearchParams form body 同为 Spring @RequestParam 可接受形态，按文档 query 实证通过） |

**不落地 operation**：getObstacleAvoidanceList（GET 全量）——旧仓库唯一活跃消费者为地图嵌套修改页（H02 本期暂缓）安全区面板的模板选项加载，本页不可达（P16 getAllChargePiles / P18 getAirShowerDoors 同口径），H02 迁移时由该页 owner 接入。

### 1.2 预置模板数据（`features/obstacle-avoidance/components/obstacleTemplateData.ts`）

- 14 个模板项（前方/后方导航激光、四方避障激光、避障相机、叉尖避障等，与旧 TEMPLATE_META 逐项一致）× 每项 64 个选项；
- 选项 value 规则 `${key}_index_${i}`（i 从 0 起）为**协议提交值**（avoid 字段），后端存储+历史回显依赖该规则——文件头注明不得变更；
- label「默认」/「自定义N」为待翻译简中形态，渲染侧 `translateAvoidLabel` 正则反解经 t() 翻译（数字拼死形态直接 t() 查不到 key，旧实现同问题其在消费侧反解）。

### 1.3 列表页（整页重写）

- Apex request 模式：服务端分页/内建取消/错误重试；行 ID=id（int64 经 stringFieldRowId）；列偏好 `obstacle-avoidance:main`；**操作列默认钉右**（defaultPinning `end:['actions']`，P14–P18 同款；首轮联验发现遗漏已补）；
- 列结构逐列核对旧版：避障名称（弹性，缺失留白）+ 操作（旧版 width 200 fixed right → Apex 钉右；编辑 primary/删除 danger，无权限隐藏）；
- 展开行子表（Apex data 模式，禁 antd Table）：避障参数名称/避障参数类型（协议原值，未知标识不臆造中文）/是否启用避障参数（enable true→启用/false→未启用 旧实现两态映射，缺失留白）；无明细不可展开；子表行 ID=名称+类型复合（参数无独立主键）；
- 删除升级 confirmCommand danger（旧版 Popconfirm 仅「确认删除当前数据?」一问，DoD 8 升级；确认框五要素+行级防连点）；
- 搜索：名称框（旧 360px 同宽）+查询按钮，提交回首页（旧 onSearch 同语义）；无手动刷新按钮（按钮纪律）；配置数据不轮询（DoD 7）。

### 1.4 双模式弹窗（ObstacleTemplateModal）

- 标题 新增避障模板/编辑避障模板，宽 1000；名称必填 maxLength 64 showCount（labelCol 6/18 新规范）；底部 清空/取消/确定（清空=显式丢弃草稿入口）；
- Transfer 穿梭框：14 预置模板、titles 可选/已选模板、actions 添加/移除、搜索占位 搜索模板名称、styles.section 400×320（旧 listStyle 同尺寸）；勾选态与已选集合分离受控（勾选取消不污染草稿）；
- 参数配置表：**Apex data 模式**（业务表格统一 Apex 纪律，P07 MappingFormModal/P13 DispatchConfigTable 同款行内控件；行数≤14 关闭虚拟化防失焦），4 列=避障名称（模板名当前语言）/避障参数类型（下拉 label=`${文案} (${value})` 识别具体标识 + popupRender 底部自定义添加（antd v6 对旧 dropdownRender 的替代；Input maxLength 32+添加按钮，Enter 提交）+自定义项 value 按 `${key}_index_${base+extra}` 规则生成）/是否启用（受控 Switch）/移除（link danger，等同 Transfer 左移并清理配置与自定义项）；
- 校验链（旧实现同序）：名称必填→「请至少选择一项避障模板」→逐项「请为"{name}"选择避障参数类型」；
- **编辑回填算法逐分支等价旧实现**：avoid 精确=模板 key 或 `${key}_` 前缀匹配→回退 name=模板名匹配→历史自定义项 `${key}_index_N` 还原标签与选项→**完全不匹配的参数旧实现静默丢弃：本实现保留该提交语义（保存按已选模板重建 parameters），但丢弃计数以 warning 状态文本可见化（无按钮）——P22 专项「未知值按契约处理」不静默伪装，且不发明旧版没有的编辑控件**；
- 文案翻译时机：回填 effect 不经 t()（自定义项 label 存待翻译形态，渲染侧统一翻译）——语言切换实时生效且不触发回填重跑（防草稿被重置）；
- 草稿：关闭保留（destroyOnHidden=false）+失败留稿+useTabDirtyGuard+提交防重复（submitting 早退+按钮 loading）。

### 1.5 路由与权限

- `mission-cluster-obstacle-avoidance` 解除 pending，挂 `PERM.OBSTACLE_AVOIDANCE_VIEW` 守卫，meta.i18nNamespaces=['obstacleTemplate']；
- 按钮码 PERM_BUTTON.OBSTACLE_AVOIDANCE_ADD/UPDATE/DELETE 页面内控制入口（无权限隐藏，旧 §7/§8.2 同边界）；
- 联验登录响应实证四码 `obstacle-avoidance:view/add/update/delete` 平铺下发（登记 contracts 第 2 节）。

### 1.6 四语言

- `obstacleTemplate` 命名空间四语言分片（zh-CN 即 key 本体）；
- en 旧真译逐条沿用（Obstacle Name/Obstacle Param …/Add Obstacle Template/Available/Selected/Parameters/Default/Custom …/创建-更新-删除-查询 避障数据 …/14 模板名全量真译）；**插值改 {{var}}**：'自定义{index}'→'自定义{{index}}'（Custom {{index}}），与 P10 crossMap '地图 {{index}}' 同形态（P22 联验首轮发现旧单花括号插值不替换——i18next 默认 {{var}}，已修）；
- 繁日韩 B1 基线补译：14 模板名与「預設/自訂{index}」「デフォルト/カスタム{index}」「기본값/사용자 정의 {index}」旧真译逐条沿用；其余 key（旧资源为简中复制）补译——繁中 避障範本/範本/參數設定/搜尋/儲存 台湾惯用语；日文 障害物回避テンプレート（与 menu 对齐）/テンプレート選択/候補·選択済みテンプレート/パラメータ設定/避障パラメータ種別/避障を追加；韩文 장애물 회피 템플릿（与 menu 对齐）/템플릿 선택/파라미터 설정/장애물 회피 파라미터 유형/장애물 회피 추가；
- 公共词与 P11 nodeEdgeGroup 分片对齐：确定=Confirm/確認/확인、查询=Query/照会/조회、清空=Clear/クリア/지우기、移除=Remove/移除/이동；
- i18n.ts 四语言懒加载表各增 1 行；i18n-map.md 已登记。

## 2. 门禁（最终代码态复跑）

| 门禁 | 结果 |
| --- | --- |
| pnpm lint | 通过（386 文件 0 警告 0 错误） |
| pnpm typecheck | 通过 |
| pnpm check:structure | 通过（384 源文件，无违规导入方向/无硬编码主机） |
| pnpm build | 通过（tsc -b + vite build；chunk 警告为既有 apex-table-react 体积） |
| 调试残留 grep | 0（console./debugger/TODO/FIXME 于本任务新文件） |

typecheck 曾抓出并修复：Select 漏导入、translateAvoidLabel 签名与 TFunction 不兼容、findTemplate 回调内 TS 收窄失效（avoid 局部变量收敛）、Transfer moveKeys（React.Key[]）索引 Record<string,…> 需 String 收敛、popupRender（antd v6）。

## 3. 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local [登录凭据已脱敏]；python API 层先行、浏览器会话独立）

### 3.1 API 层（curl 形态经 python urllib）

- 登录 code=200 + permissions 平铺真实下发 obstacle-avoidance:view/add/update/delete 四码；
- 无令牌 pageObstacleAvoidance → 1000000 用户未登录对照；带令牌 GET 平铺 query `?pageNo=1&pageSize=10` code=200（G04 口径再实证），基线 total=0 真实空态；
- create code=200：parameters 数组顺序保持、enable boolean 落库正确、name=模板名简中 key 原样（旧实现 name: templateMap[key].name 同语义）；
- update code=200：id 定位+名称+parameters 整体替换全字段落库，**无 G18 类缺陷**；
- 筛选 query=「P22联验模板A改」命中 total=1、不存在的名称 total=0；
- delete POST+`?id=` code=200 后端自清理，终态审计 **total=0 零残留**。

### 3.2 浏览器整页（IAB rAF/RO 垫片）

- **守卫回跳**：未登录直访 /mission-cluster/obstacle-avoidance → /login?redirect=%2Fmission-cluster%2Fobstacle-avoidance；
- **登录落点闭环**：登录成功回跳 redirect 目标页（含 toast 登录成功）；退出登录真实回跳登录页；
- **列表渲染**：空态（暂无数据+共 0 条+翻页禁用）→ 创建后行渲染（避障名称/编辑/删除）；
- **必填拦截**：空表单确定 →「请输入避障名称」零请求；填名称确定 →「请至少选择一项避障模板」零请求（校验链同旧序）；
- **新增全链路**：Transfer 勾选 2 模板→添加→参数配置表出现两行默认类型（默认 (forward_nav_index_0)）→改选「自定义2 (forward_nav_index_2)」→相机行开 Switch→确定→提交成功列表刷新（弹窗 DOM 卡 leave 动画为 P17 沉淀⑪已知伪象，React 状态路径已关闭——列表刷新+onSucceeded 完整执行）；
- **展开行子表**：3 列真实渲染，avoid 协议原值+启用/未启用映射与保存一致；
- **编辑回显**：名称/已选模板 2 项/自定义类型「自定义2 (forward_nav_index_2)」还原/开关状态逐行一致；改名提交列表实时更新（全字段落库无 G18 类缺陷）；
- **删除确认层**：五要素齐出（标题 删除避障模板/对象 ·P22浏览器联验模板改/影响 永久删除不可恢复/提交≠完成附注/danger 红键）；取消路径零新请求；确认删除请求发出+列表回空态+后端权威核实 total=0；
- **列偏好闭环**：面板隐藏避障名称列→确定→列消失（操作列钉右保持）→reload→偏好持久（名称列仍隐藏）→恢复默认→出厂空态两列回归→清 localStorage 偏好键→reload→**代码默认钉右回归 TH[data-pinned=end]**（P18 同款回归路径）；
- **五语言**：en-US/zh-TW/ja-JP/ko-KR html lang+表头+新增按钮逐语言实证；en 弹窗标题 Add Obstacle Template/Select Template/Available/Selected/Parameters/Front Nav Laser（模板名翻译）抽查通过；简中基线回归；
- **双主题双宽度四组截图**：light-1366/light-1920/dark-1366/dark-1920（均含主行+展开行子表+钉右+分页，`.run-lock/shots-p22/`）；
- **收尾**：测试数据后端清理（终态 total=0 零残留）、真实登出、页签关闭、dev server 停止、令牌临时文件清除。

## 3.3 联验环境备忘

- 后端 root 单会话互踢（P09 沉淀①）两度发生：API 层复核终态的登录把浏览器会话踢失效（守卫重定向登录页）——API 层与浏览器会话仍需严格分时使用；
- IAB 垫片时序竞态（P17 ⑫「时好时坏」）本轮多次复现，沉淀增强版垫片见 AGENTS 第 5 节（observe 后先立即 fire 再 400ms 间隔续 fire 至连续 8 轮非零，覆盖任意晚的布局完成）；PageCache 页签保活下「同页签菜单导航」不会卸载重挂载组件（P10 ① 手法的前提是关闭页签后重开或页签层动画完成），关闭页签→菜单重开为可靠重挂载路径。

## 4. 观察项登记

- **G24**（gaps.md）：apex-table-react「唯一业务数据列被隐藏」边界行为——仅剩钉右操作列时行区与 BodyState 均不渲染（表头/分页/total 正常、数据模型有行）；恢复默认立即恢复。P03–P18 各页从未触发（隐藏的都是非唯一数据列）。页面不特判不兜底（列偏好是用户显式操作，恢复默认可还原），登记提示。
- 无后端契约缺陷（增改删全字段落库、无 G18/G21/G22/G23 类形态）。

## 5. 公共文件改动清单

| 文件 | 改动 | 影响已核 |
| --- | --- | --- |
| src/router/definitions.tsx | 本页 meta 解除 pending+i18nNamespaces（唯一改动点纪律） | 全局路由树其余未动（typecheck/build 实证） |
| src/i18n/i18n.ts | 四语言 loaders 各增 obstacleTemplate 1 行 | 既有分片不受影响 |
| src/constants/auth/permission.constants.ts | 无改动（OBSTACLE_AVOIDANCE_* 四码 T00.4 已定义，本页消费） | — |
| docs/* | contracts/operations/i18n-map/gaps/RUN_STATE/TASKS 联动 | 本文件 |

## 6. 结论

P22 适用验收（A07–A11/A13/A15/A18、通用 DoD、I01–I08 适用项）全部满足并有证据；TASKS.md P22 行已勾选。下一任务：P23 车辆动作（B3 队首未完成项）。
