# P01 登录 · 交接记录

> 当前状态（2026-09-21 audit 第二次复核轮）：实现保留；复核通过为 verification=partial、gate=ready。A04/I06 两项真实场景 deferred 归 V01（与 T00/P02 同口径），不勾选 TASKS。验收记录：`docs/migration/evidence/P01/run-0142d6d6-3479-4d63-bbb4-1b3257fab3c3/acceptance.json`。
**批次/前置**：B1 / T00（已放行）
**入口/目标**：`/login` → `src/pages/auth/Login/Login.tsx`；私有模块/语言分片 `auth-login`（i18n 命名空间 `auth`）
**旧来源**：`C:\code\dd\src\pages\Login\index.tsx`
**历史交付**：2026-09-18 定时轮交付（提交 5eda9af）；旧「已勾选」按 2026-09-20 复核纪律不再作为验收凭证

## 交付内容

1. **品牌资源服务**（`src/services/system/brand/brand.service.ts`，P01/P27 共用）：
   - `fetchSystemImageBlob(placementKey)`：GET `/systemLogos/{placementKey}/file`，文件通道透传 Blob（非 JSON content-type 不套 Result 解包）；无需认证（旧系统登录前调用）。
   - `isUsableImageBlob`：非空且 `image/*` 校验，不盲信媒体内容；P27 扩展上传（PUT upsert）与品牌变更通知，contracts.md 第 5 节登记。
   - 登录页消费 `loginBackground`（旧系统同源 key）；失败/无图/非图片回退默认壁纸，不阻塞登录。
2. **登录页**（`Login.tsx`）：品牌背景 blob→objectURL→全屏 cover，卸载精确 revoke；语言切换入口挂载。
3. **登录页五语言切换**（`LoginLanguageSwitch/`）：I01 登录前可切五语言；语言名自名不译；仅 dispatch `localeChanged`，App.tsx 统一执行 `changeAppLanguage`（预加载失败自动回滚），不双写语言来源。
4. **登录反馈**（`LoginForm.tsx`）：成功 `message.success` 在真实返回之后；失败 `apiErrorMessage` 展示后端原文。表单横排 labelCol 6/18。
5. **五语言**：`auth` 命名空间四语言分片（en-US 沿用旧系统真译 "Login successful"；旧繁/日/韩本为简中回退，T00.8 基线即定稿）。

## 复用 T00 基座（不另造认证状态）

- 会话：`useLogin` → `auth.service`（MD5 单点 G03 / Bearer 请求层组装 / 空字段诚实默认，无伪管理员）→ `authSlice`（token+权限持久化，不存密码）。
- 落点：`routeAccess.resolveLandingPath`（activated=false → 授权页；**P34 交付解除 /dashboard pending 后，有权限首页即登录落点（D29）**；否则首个有权限已迁移业务页；全不可用 → 无权限页）。
- 回跳：`resolveSafeRedirectPath`（仅站内+存在+当前可用；外站/未知/占位/暂缓页一律拒绝）。
- 多窗口退出同步：`authBridge` storage 监听清空事件；并发过期收敛归请求层（T00.3）。

## 验证证据

### 2026-09-21 audit 第二次复核轮（run-0142d6d6，代码基线 bbe05cb）

- **受检文件哈希核对**：上轮 24 受检文件中 22 个零变更；2 个变更文件（`definitions.tsx`/`i18n.ts`）为 P33/P34/P35/P36/P37/P40/P43 登记授权扩展（T00 audit run-b11c9c11 已逐 hunk 审查），本轮再从 P01 视角复核——login 节点与 auth 四语言注册行零改动。哈希重绑见 `code-files.json`。
- **真实只读复测 8/8**（`run-p01-audit2-readonly.mjs`，经 dev 代理 → 192.168.0.158:8888）：错误凭据 code=1000010；正确凭据 MD5 → activated=true/155 权限码；systemLogos items=0；loginBackground 404；**dashboard-realtime:view 在权限码中（落点前提）**；pageUsers total=3；无令牌对照 1000000；登出 code=200。
- **浏览器针对性复测 5 项**（`browser-ui-result.json`，IAB 1280×720 深色，en-US 持久化恢复）：登录页渲染正常（品牌 404 回退默认壁纸实证）；**真实登录落点实测 = /dashboard**（上轮落 /order-record 的结论随 P34 交付更新，D29 兑现）；Dashboard 整页渲染完整且 KPI 轮询数据跳动；登出清理（守卫保留站内回跳参数+表单清空+persist auth 无 token）；外站回跳拒绝复验（evil.example.com → 落站内 /dashboard）。截图仅本地留存（`screenshots/`，git 排除）。
- **静态门禁**：复用 T00 audit（run-b11c9c11）在 392e831 的全量复绿结果（lint 543 文件 0/0+typecheck+structure 529+build 1.24s）；392e831..bbe05cb 仅 docs 提交，git diff 证实 src/package.json/pnpm-lock.yaml 零变化。OpenAPI 哈希复核一致。
- IAB 输入管线冻结两次复现（同页签 goto 后快照冻结、新页签 Playwright click 超时）：第一次登录落点经 cua 真实指针完成；第二次登录提交与登出经 evaluate 合成指针序列（诊断垫片，覆盖边界已登记）。

### 历史复核轮（audit-20260920-p01-44fc5e6，基线 44fc5e6）

- 真实登录链路复测 5/5（错误凭据 1000010 为上上轮缺口补测）；浏览器 UI 验收 1366/1920 双视口、浅色+深色诊断、五语言切换（en/ja html lang 同步+输入草稿保留）、错误凭据 aria 快照、外站回跳拒绝、刷新恢复；静态门禁复用同日 T00 audit a9a3cc0 复绿结果。验收记录在原目录保留。

## 登记的限制与 deferred（归 V01）

- **A04 deferred**：真实 token 过期收敛与双窗口退出同步需长会话/双窗口环境；机制级（持久化恢复/登出清理/无虚构刷新）两轮均实证。
- **I06 deferred**：真实网络级语言资源加载失败不白屏需构造网络故障；品牌 404 资源缺失分支两轮实测不白屏。
- **品牌图成功显示路径**：联调环境未上传品牌图（items=0、file 404），成功路径以代码审查+失败回退实证覆盖；有图环境或 P27 上传完成后顺带复验。
- **activated=false → 授权页落点**：当前环境 activated=true 无法复现；分支逻辑为 T00.4/T00.9 实证，P02 专用环境补真实激活链路。
- **普通用户细分权限**：环境仅 root 账号（超管短路）；落点/守卫逻辑代码审查覆盖，普通用户矩阵归 P31/P32/V01。

## DoD 适用性

不适用：表格（DoD4/5）、写操作批量（DoD8）、文件传输（DoD9）、地图关联（DoD10）、统计（DoD14）。适用项全部 passed：真实服务协议、权限状态区分、取消与失败、草稿（输入保留）、五语言、外观终端（1366/1920 双主题）、验证证据、交接收尾（无 mock 残留）。
