# P01 登录 · 交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
**状态**：✅ 已完成（2026-09-18 定时轮交付并勾选）
**批次/前置**：B1 / T00（已放行）
**入口/目标**：`/login` → `src/pages/auth/Login/Login.tsx`；私有模块/语言分片 `auth-login`（i18n 命名空间 `auth`）
**旧来源**：`C:\code\dd\src\pages\Login\index.tsx`

## 交付内容

1. **品牌资源服务**（新增 `src/services/system/brand/brand.service.ts`，P01/P27 共用）：
   - `fetchSystemImageBlob(placementKey)`：GET `/systemLogos/{placementKey}/file`，请求层文件通道透传 Blob（非 JSON content-type 不套 Result 解包）；无需认证（旧系统登录前调用，实证可用）。
   - `isUsableImageBlob`：非空且 `image/*` 校验，不盲信媒体内容。
   - 登录页消费 `loginBackground`（旧系统同源 key）；外壳顶栏 logo 由 P27 复用。
2. **登录页改造**（`Login.tsx`）：
   - 品牌背景层：blob → objectURL → 全屏 cover `<img>`；卸载精确 revoke；失败/无图/非图片回退默认壁纸，不阻塞登录（规格 P01）。
   - 挂载语言切换入口（见 3）。
3. **登录页五语言切换**（新增 `src/features/auth/components/LoginLanguageSwitch/`）：
   - I01 登录前可切换五语言（登录页 public 无外壳顶栏，独立于 Header 入口）。
   - 语言名自名不译；仅 dispatch `localeChanged`，App.tsx 统一执行 `changeAppLanguage`（预加载失败自动回滚原语言），不双写语言来源。
4. **登录成功反馈**（`LoginForm.tsx`）：成功后 `message.success(t('登录成功'))`（A03 真实结果反馈，不提前冒充成功）。
5. **五语言资源定稿**：`auth` 命名空间四语言补 `'登录成功'`（en-US 沿用旧系统真译 "Login successful"；旧系统繁/日/韩登录文案本为简中回退，T00.8 基线译文即为定稿）。

## 复用 T00 基座（不另造认证状态）

- 会话：`useLogin` → `auth.service`（MD5 单点/Bearer 由请求层组装/空字段诚实默认）→ `authSlice`（token+权限持久化，不存密码）。
- 落点：`routeAccess.resolveLandingPath`（activated=false → 授权页；首页优先；否则首个有权限已迁移业务页；全不可用 → 无权限页）。
- 回跳：`resolveSafeRedirectPath`（仅站内+存在+当前可用；外站/未知/占位一律拒绝）。
- 多窗口退出同步：`authBridge` storage 监听 `persist:apex-admin:auth` 清空事件。

## 真实联验证据（2026-09-18，经 dev 代理 → 192.168.0.158:8888，凭据来源 `.env.local`）

| 场景 | 结果 |
| --- | --- |
| 错误凭据（UI 提交） | 停留登录页 + 后端真实错误「用户名或密码错误」（code=1000010） |
| 正确凭据（root + MD5） | 「登录成功」提示 → 落点规则生效；API 层 code=200、activated:true、155 权限码 |
| 外站回跳 `?redirect=https://evil.example.com/...` | 拒绝外站，落站内规则落点 |
| 无可用业务页落点 | 全业务页 migrationPending → `/no-permission`「无权限」明确反馈 |
| 刷新会话恢复 | 刷新后仍站内（持久化恢复，不被踢回登录页） |
| 直访未迁移深链接 `/order-record` | 迁移占位页 + 外壳正常，零业务请求 |
| 品牌接口失败不阻塞 | 该环境 `loginBackground` 404、列表 data:[]，登录页正常回退默认壁纸，登录流程完整走通 |
| 五语言切换 | en-US / zh-TW / ko-KR 实测（html lang、品牌、表单、按钮逐项正确，语言自名不译）；ja-JP 同链路 T00.8 已验；刷新后语言偏好恢复 |
| 视觉 | 1366×768 截图确认：玻璃卡片居中、右上角语言按钮、表单完整 |

`pnpm check`（215 文件 lint 0 警告 0 错误 / typecheck / 结构检查 213 源文件）+ `pnpm build` 全部通过。

## 登记的限制（非缺口）

- **品牌图成功显示路径**未在联调环境验证：环境未上传任何品牌图（列表空、file 404），成功路径以代码自查 + 失败回退实证覆盖；后续有图环境（或 P27 品牌上传完成后）可顺带复验。
- **activated=false → 授权页落点**：当前环境 activated:true 无法复现；`resolveLandingPath` 分支逻辑 + 1001000 语义为 T00.4/T00.9 实证。P02 完成授权页后在专用环境补真实激活链路。
- **并发过期收敛**（业务码 1000000 → sessionExpired → 回登录）与**多窗口退出同步**为请求层/authBridge 机制，T00.3/T00.6 已实证；登录页正确消费。真实 token 过期需长会话环境，V01 统一验收复核。

## DoD 适用性

不适用：表格（4–10 无表格/地图/统计/文件项）。适用项全部满足：真实服务协议、权限状态区分、五语言随页交付、外观终端（截图）、验证证据、交接收尾（无 mock 残留，测试注入会话与浏览器标签已清除）。
