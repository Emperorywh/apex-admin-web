# P38 完整任务详情——任务交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
| 字段 | 内容 |
| --- | --- |
| 任务 / 负责人 / 基线 | P38 完整任务详情；ZCode 串行调度（run-20260918-p38-91d11aa1）；分支 apple-rxx，基线 f48a87d（P05 交付后） |
| 开始 / 更新日期 | 2026-09-18 / 2026-09-18 |
| 实施状态 | **已完成**（前端交付 + 带令牌真实联验通过；工作区页签/独立窗口双形态、实体页签隔离、P03 往返闭环实证） |
| 验证状态 | **已通过**（带令牌只读全链路 + 双主题双宽度整页截图 + 五语言）；登记例外：无写操作（本页纯只读），令牌 TTL 过期收敛场景顺带实证 |

## 依据

- 规格：第 3.3（任务详情行）、7（详情默认工作区页签+独立窗口入口、快速预览不替代完整详情路由）、6（Apex 表格）、8.1（页签/草稿）、18（国际化）；D08（独立详情形态）。
- 旧路由：`C:\code\dd\.umirc.ts` `/order-info`（layout:false，access=order-record:view）。
- 旧可达组件（递归清点）：`OrderInfo/index.tsx`（主体 Descriptions column=3 + mission 服务端分页 antd Table + ExpandedActions 展开）→ `ExpandedActions/index.tsx`（动作子表，11 列含耗时/条件标识/动作参数 Popover）。
- **参数形状核对结论（TASKS「核对旧调用点参数」）**：旧源码中**没有任何导航到 /order-info 的调用点**（grep 全仓仅 .umirc.ts 与 .umi 生成物）——旧详情均为列表弹窗快速预览，/order-info 是直访 URL 形态；旧页面 `location.search.substring(1)` 取整段 query 作 orderTaskKey，即历史链接为裸值 `?KEY`（无参数名）。
- API 哈希：A82E9B5F…49C7C（开工复核与规格一致）。
- 前置交接：P03.md（详情弹窗契约/列偏好接线/orderRecord 命名空间）、T00.md（contracts 第 1–7 节）。

## 形态与参数契约（本任务新增，登记 contracts）

1. **工作区页签形态（默认）**：`order-info` 路由从布局外独立页移入受保护根（`root.children`，相对段 `order-info`，完整路径保持 `/order-info` 历史地址不变）。直访/导航进入即作为工作区页签打开：页签 key = pathname + 规范化 search，不同 `orderKey` 自动获得独立页签、独立 Activity 缓存实例与独立请求 scope（实体页签隔离）。`hideInMenu: true` 保证不进 Dock 菜单、也不作为登录落点候选（`isLandingCandidate` 排除 hideInMenu，已核实）。
2. **独立窗口形态**：页面工具栏「独立窗口」按钮经 `openStandaloneWindow('/order-info', { orderKey })` 打开同一路径——新窗口受同一认证+权限守卫，会话/语言/主题来自持久化；同实体复用同一窗口（按「路由+排序参数」命名）。窗口内为完整外壳形态（与 P39/P40 接入时的形态一致）。
3. **参数单一真相**：命名参数 `?orderKey=<任务编号>` 为新导航标准（`buildOrderInfoPath`）；历史裸值 `?KEY` 及其经页签 search 规范化（URLSearchParams 重序列化）写回地址栏的 `?KEY=` 变体，均由 `parseOrderInfoSearch` 兼容解析为 orderKey（A22 历史链接可用）。

## 操作映射（来源 → 新实现 → operation → 权限 → 状态）

| 旧操作 | 新实现 | operation（已核对 OpenAPI） | 权限 | 状态 |
| --- | --- | --- | --- | --- |
| 直访 /order-info 按任务上下文查询 | `pages/order-info/OrderInfo`（工作区页签/独立窗口双形态，参数解析见 orderDetailNavigation） | POST /fms/v1/dispatcher/orderRecord/getOrderRecordDetail（{orderTaskKey,pageNo,pageSize}） | order-record:view（守卫按 meta.perm） | **带令牌联验通过**（真实 orderKey 主体+mission 渲染） |
| 主体 Descriptions（transformOrderInfo 17 字段，column=3） | OrderDetailPanel 主体区域（字段全集同旧口径：编号/名称/类型/状态/任务ID/工艺/优先级/指定车辆/组/执行车辆/三个时间/创建人/失败/挂起/取消原因），枚举映射+未知原值，空值留白，时间 displayDateTime | 同一响应喂数据（不重复请求） | 同上 | **联验通过**（17 项渲染、枚举映射、空值留白、秒级时间） |
| mission 服务端分页表（antd Table） | OrderDetailPanel mission 表格：ApexTableReact request 模式（服务端分页 missionPage.records/total、行 ID=orderMissionKey、翻页/每页条数），G09 不启用排序 | 同上 | 同上 | **联验通过**（共 2 条/翻页控件/行 ID 稳定） |
| ExpandedActions 动作子表 | MissionActionsTable（Apex data 模式完整小集合）：11 列含耗时（起止齐备时 HH:mm:ss，缺失留白）、动作参数 Popover+复制、执行结果 Tooltip | mission 行内 actions（同一响应） | 同上 | **联验通过**（pick/HARD/耗时 00:00:01，与 P03 弹窗联验数据一致） |
| 弹窗快速预览（列表行详情） | OrderInfoModal 迁入 features/order-detail 并改为「壳+OrderDetailPanel」；footer 新增「完整详情」按钮（关弹窗+navigate 到 /order-info?orderKey=…） | 同上 | order-record:view | **联验通过**（弹窗回归+完整详情导航+新页签生成） |
| 返回来源 | 工作区页签切换即返回（PageCacheHost 保留列表快照：筛选/页码/滚动位置实证）；独立窗口形态直接关窗 | — | — | **联验通过**（P03→P38→P03 往返） |
| 缺参数/任务不存在/无权限/会话过期 | 缺参数：StateBlock gap「缺少任务编号参数」（不发起空参请求）；任务不存在：后端 code=200+data=null（真实实证）→ StateBlock gap「任务不存在或已被删除」；无权限：路由守卫；会话过期：请求层 1000000 单飞收敛 | — | — | **联验通过**（三分支分别实证；会话过期随令牌 TTL 自然触发实证） |
| 独立窗口（D08 新增形态，旧系统无） | 页面工具栏按钮 → openStandaloneWindow | — | 同守卫 | **联验通过**（新窗口渲染完整外壳+主体，同会话） |

选项消费：无新增（详情纯只读，不消费下拉选项）。

## 文件归属

新增（features/order-detail 为 P38 私有域）：
- `src/features/order-detail/components/OrderDetailPanel.tsx`（详情业务组件：单请求喂主体+mission 表，完整页/弹窗共用）
- `src/features/order-detail/components/OrderInfoModal.tsx`（自 order-record 迁入并改造：壳+Panel+「完整详情」入口）
- `src/features/order-detail/components/MissionActionsTable.tsx`（自 order-record 迁入，逻辑不变）
- `src/features/order-detail/components/OrderDetailPanel.module.css`
- `src/features/order-detail/orderDetailNavigation.ts`（参数名/构造/解析单一真相，含裸值双变体兼容）
- `src/constants/order/orderDisplayOptions.ts`（四个枚举展示常量提升至共享常量层）
- `src/pages/order-info/OrderInfo/OrderInfo.tsx` / `OrderInfo.module.css`（整页重写）
- 五语言分片：`src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/orderInfo.ts`（4 key：完整详情/独立窗口/缺少任务编号参数/任务不存在或已被删除/打开独立窗口失败提示）

修改（交接登记）：
- `src/router/definitions.tsx`：order-info 移入受保护根+meta 变更（摘除 migrationPending/standaloneMeta）；order-record meta 增加 orderInfo 命名空间（弹窗「完整详情」按钮文案预载）。
- `src/i18n/i18n.ts`：orderInfo 四语言懒加载登记。
- `src/features/order-record/components/orderRecordOptions.ts`：常量定义改为 re-export 共享常量（消费者导入路径不变）。
- `src/pages/order-record/OrderRecord/OrderRecord.tsx`：OrderInfoModal 导入路径更新（一行，P03 交接）。
- `src/services/order-record/order.service.ts`：getOrderRecordDetail 返回类型如实改为 `Promise<OrderRecordDetailDto | null>`（任务不存在时 data=null 实证），消费者 Panel 已判空。

删除：`src/features/order-record/components/OrderInfoModal.tsx`、`MissionActionsTable.tsx`（迁出后删除；MissionActionsTable 唯一消费者是 OrderInfoModal，OrderInfoModal 唯一消费者是 OrderRecord.tsx 页面，均已处理）。

## 共享契约

- 消费：请求协议/Result 解包/1000000 收敛（第 1 节）；路由守卫/实体页签/独立窗口/登录回跳（第 2、4 节）；Apex locale/主题映射/toBackendPage/stringFieldRowId（第 3 节）；StateBlock（第 3 节）；orderRecord 命名空间既有文案（i18n-map 第 30 行）。
- 新增/变更（已登记 contracts 变更日志）：任务详情参数契约（orderKey 命名+裸值兼容）；实体页签契约落地首个消费者（order-info 移入受保护根，P39/P40 接入时同构迁移）；getOrderRecordDetail 返回可空。
- 页签标题：页签栏显示路由 meta.title「任务详情」（i18n-menu 已有五语言）；不同任务的页签以 key 区分（标题不带实体标识为既有 TabsBar 机制，实体标识在页面工具栏展示任务编号并可复制）。
- 草稿：本页无表单输入，不消费 useTabDirtyGuard；轮询：旧实现无轮询，按普通查询纪律不接入 useVisiblePolling。

## 接口缺口

| 编号 | 本页具体化 | 状态 |
| --- | --- | --- |
| G09 | mission 表全部列 enableSorting:false，不发虚构 sort | 已按契约处理 |
| G10 | 行 ID=orderMissionKey 字符串业务键；missionPage.total/size 以 JSON number 承载（真实规模 2 条/52 万列表环境下无精度异常） | 已验证 |
| 新增登记 | 任务编号不存在时后端仍返回 code=200 且 data=null（2026-09-18 带令牌实证）；页面以「任务不存在或已被删除」区分呈现，服务层签名已如实标注可空 | 已验证可用（行为实证） |

## 国际化

- 详情字段/表格/动作子表文案全部沿用 orderRecord 命名空间（P03 已交付四语言旧真译，弹窗与完整页共用，零重复 key）。
- P38 新增 orderInfo 命名空间四语言分片（5 key：完整详情/独立窗口/缺少任务编号参数/任务不存在或已被删除/打开独立窗口失败提示）；en 沿用既有术语（Task ID/Details），繁日韩按 B1 基线补译并对齐 orderRecord 术语（タスク番号/작업 번호）。
- **接线教训（沉淀 AGENTS 第 2 节）**：本项目 `nsSeparator=false`（中文 key 即文案），t() 不得带「ns:」前缀——前缀整体被当作 key 显示字面量；跨命名空间复用组件用 `useTranslation(['a','b'], { nsMode: 'fallback' })` + 无前缀 key。
- 语言边界：后端原文（任务名/原因/动作描述/参数 JSON）不翻译；未知枚举显示原值；html lang 随切换同步（四语言实测）。

## 验证

工程门禁（2026-09-18，联验后复跑）：
- `pnpm lint`：0 警告 0 错误（262 文件）
- `pnpm typecheck`：通过
- `pnpm check:structure`：通过（260 源文件，无违规导入方向/硬编码主机）
- `pnpm build`：通过（tsc -b + vite build）
- 调试残留 grep：0

运行时验证（dev 代理 192.168.0.158:8888，凭据 .env.local [登录凭据已脱敏]）：
- **API 层**：登录 code=200 activated=true；pageOrderRecords 取真实 orderKey（total=526,400）；getOrderRecordDetail 返回主体（orderState=IN_QUEUE）+missionPage（current=1/size=10/total=2/records=2，mission 含 mapName/stationName/actions）；**不存在任务编号：HTTP 200 + code=200 + data=null 实证**（呈现语义据此定）。
- **浏览器**（1366×768 与 1920×945）：
  - 未登录直访 `/order-info?orderKey=…` → 守卫回跳 `/login?redirect=%2Forder-info%3ForderKey%3D…`（参数完整保留）；登录后按 redirect 回跳详情 ✓
  - 工作区页签形态：顶栏「任务详情」页签生成、Dock/顶栏外壳完整（路由移入受保护根后实证）✓
  - 整页渲染：工具栏（订单详情+编号复制+独立窗口按钮）+ 主体 17 项（枚举映射/空值留白/秒级时间）+ mission 表 2 行 + 行展开动作子表（pick/HARD/耗时 00:00:01/参数 Popover）+ 分页器同视口 ✓
  - 弹窗回归+完整详情入口：列表行详情弹窗（壳+Panel 正常）→「完整详情」→ 弹窗关闭 + 新详情页签打开 ✓
  - 实体页签隔离：两个不同 orderKey 的详情页签并存（data-tab-key 三个不同 key），旧页签内容隔离（切回仍为旧 orderKey）✓
  - P03→P38→P03 往返：列表页签快照保留（筛选/页码/表格横向滚动位置/统计轮询递增 526,640→526,672）✓
  - 独立窗口：按钮 → 新窗口打开同路径（标题「任务详情 · 调度系统」），完整外壳+主体渲染，html lang 同步 ✓
  - 缺参数 `/order-info` → StateBlock「缺少任务编号参数」✓；不存在任务 → StateBlock「任务不存在或已被删除」✓；裸值兼容 `?KEY` 与规范化变体 `?KEY=` → 正常解析渲染 ✓
  - 五语言：en-US/zh-TW/ja-JP/ko-KR 逐语言（Standalone Window/獨立視窗/独立ウィンドウ/독립 창 + Order Details/訂單詳情/オーダー詳細/오더 상세）+ html lang 同步 + 会话过期（令牌 TTL 约 15 分钟）自然触发 1000000 收敛跳登录、重新登录恢复 ✓
  - 双主题双宽度四组截图：1920 深/浅、1366 深/浅全部通过（布局无撑破、分页器与 Dock 同视口、深色主题变量映射正确）✓
- 验证残留清理：真实登出链路退出（回 /login）、浏览器验证页签全部关闭、dev server 已停止。

## 未完成 / 后续

1. P39 交付后：/vehicle-info 与本页形态对照复核（同构迁移实体路由到受保护根，参数契约 vehicleKey 待 P39 按旧调用点核对）。
2. 页签标题不带实体标识为既有 TabsBar 机制（title=路由 title）；如需「任务详情-编号」形态页签标题，须扩展 tabsSlice/TabEntry（公共文件，留 V01 评估）。
3. 本页无写操作、无轮询、无文件传输——A12/A14/A16 不适用（已按 DoD 逐项核对）。
