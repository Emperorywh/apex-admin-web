# P33 任务统计（/analyze-visual/order-statistics）交接记录

| 字段 | 内容 |
| --- | --- |
| 任务 / 负责人 / 基线 | P33；ZCode 自动轮（sess_b566e27e，run-3057971e-9304-4217-8018-ebab094591e0）；分支 apple-rxx，基线 14d567db（P43 交付 HEAD），实现提交见验收记录 codeCommit |
| 状态 | implementation=implemented；verification=partial（deferred 归 V01 与跨页口径）；gate=ready |
| 开始 / 更新日期 | 2026-09-20 / 2026-09-20 |

## 依据

- 规格 `docs/SPEC_dispatch_migration_20260917.md` §3.3（任务统计行）、§8.2（失败恢复）、§11.2（指标口径/空值纪律）、§11.3（时间口径）、§18（五语言）；API 基线 `default_OpenAPI.json` SHA-256 `A82E9B5F...49C7C`（未变化，本轮复核）。
- 旧来源 `C:\code\dd\src\pages\AnalyzeVisual\OrderStatistics`（index.tsx + QuantityStatistics + EfficiencyStatistics + index.less，全量 4 文件，无可达弹窗/子表/文件操作）；旧参考 `C:\code\dd\docs\SPEC_metric_calculation_hint.md`（口径提示形态参考，目标项目 P33 私有实现，不预建 P34–P36 共享组件）。
- **范围事实（D24 例外登记）**：旧 `.umirc.ts` 中 `/analyze-visual/order-statistics` 路由处于注释状态（不可达），页面源码存在但无菜单入口；规格 3.3 与 TASKS P33 明确将其列为迁移项（用户决策优先于旧路由现状），权限码沿用旧 `constants/permission.ts` 的 `statistics:order:view`，目标路由 `definitions.tsx` 既有登记一致。目标项目侧无凭空造码。

## 操作映射（逐 operation 核对 OpenAPI）

| 旧调用 | 新实现 | method/path | operationId | 权限 | 状态与证据 |
| --- | --- | --- | --- | --- | --- |
| 数量统计查询（quantityParams → 图表） | `useStatisticsQuery` + `orderQuantityStatistics` | POST `/fms/v1/report/orderStatisticsReport/orderQuantityStatistics`（body=OrderQuantityStatisticsParam） | orderQuantityStatistics | 路由守卫 statistics:order:view（页内无按钮码，与旧一致） | 已验证（真实只读：默认 6 行；WORK+SUCCEEDED 收敛 1 行等价；无令牌 code=1000000 拒绝） |
| 效率统计查询（params → 三柱图） | `useStatisticsQuery` + `orderEfficiencyStatistics` | POST `/fms/v1/report/orderStatisticsReport/orderEfficiencyStatistics`（body=OrderEfficiencyStatisticsParam，**契约无 orderStates**——旧效率页签也无该筛选，等价） | orderEfficiencyStatistics | 同上 | 已验证（真实只读：默认 2 类型行；WORK 收敛 1 行；字段/秒值非负抽查通过） |
| 车辆下拉（getSimpleVehicles） | `useStaticOptions(fetchSimpleVehicles)`（P03 代建共享选项契约直接消费，不复制请求） | GET `/fms/v1/dispatcher/vehicle/getSimpleVehicles` | getSimpleVehicles | 同上 | 已验证（真实 56 项加载；浏览器下拉打开正常） |
| 旧 `message.warning/error(查询…出错)` | 失败统一 StateBlock offline（无重试按钮，按钮纪律），筛选保留 | —（呈现层收敛，规格 D15/8.2） | — | — | 已验证（会话失效真实场景触发：图表区 StateBlock、筛选保留） |

查询性质 POST 分类：两个统计 POST 均为纯查询语义（统计报表查询，无副作用），按 TASKS §1 归入只读查询，可安全重查/自动恢复。

## 文件归属

- 新增 `src/services/report-order/`（types+service，owner=P33）：两个统计 operation。
- 新增 `src/features/analyze-visual/order-statistics/`：`statistics.ts`（纯计算：sumQuantityByState / sumEfficiencyAverages / formatDuration）、`hooks/useStatisticsQuery.ts`（防乱序/取消级联/失败清空/可见有限退避自动重查，上限 3 次、间隔复用 `constants/polling.constants`）、`components/`（OrderStatisticsFilter / QuantityStatisticsPanel / EfficiencyStatisticsPanel / StatisticsBarChart（echarts 按需注册）/ MetricHint + module.css）。
- 重写 `src/pages/analyze-visual/OrderStatistics/OrderStatistics.tsx`（PagePlaceholder → 双页签整页）+ module.css。
- 公共文件修改（消费者影响已核）：`src/router/definitions.tsx` 仅本页 meta 行解除 pending + i18nNamespaces（路由级影响：本页进入登录落点候选，守卫不变）；`src/i18n/i18n.ts` 四语言懒加载表各增 1 行 `report-order`（消费方仅本页）；`package.json`/`pnpm-lock.yaml` 新增 `echarts@6.1.0`（官方 npm 正式版，2026-09-20 安装，按需注册不引入全量包）。
- 枚举复用：`constants/order/orderDisplayOptions.ts`（P38 提升的共享常量，顺序/文案与旧 orderRecord 常量逐项一致，未改动）。

## 共享契约

- 消费：`fetchSimpleVehicles`（contracts 第 5 节）、`ORDER_TYPE_OPTIONS`/`ORDER_STATE_OPTIONS`（constants/order）、`useTheme`（主题解析）、`StateBlock`（统一状态块）、轮询常量（退避参数）。
- 新增（供 P35 复用）：**效率统计口径与计算源**为 `features/analyze-visual/order-statistics/statistics.ts`——Σ各类型平均值（orderAverageTime/ExecutionTime/WaitTime，单位秒，toFixed(2)），「各类型平均值合计 ≠ 整体平均」口径已在图表提示与四语言分片说明；数量统计按状态分组求和（未知状态保留原值）。P35 接入 vehicleStatisticsReport/taskStatistics 时不得另写第二份同义计算。
- 图表技术决策：echarts 6.1.0 按需注册（规格 11.2 明示 ECharts 可沿用）；标题/口径提示在 React 层（参考旧 SPEC_metric_calculation_hint 的 ChartPanel 结构），echarts 仅绘图。`features/dashboard` 模板 mock 图表组件未消费、未改动（P34 owner）。

## 接口缺口

- 无新增 G 编号。G15（统计口径/时区）页面侧落实：单位（秒）按 OpenAPI 字段注释，时区口径为部署时区（本页时间筛选为本地表单序列化 `yyyy-MM-dd HH:mm:ss`，与旧实现等价，不发时区字符串）；「各类型平均值合计」口径已显式说明（公式验证留 V01 真实数据复核）。
- G10（int64）：number/orderAverage* 按 JSON number 承载（P21/P22 同口径），本页数量级（≤10^6）无精度风险。

## 国际化

- 新增 `report-order` 命名空间四语言分片（en 旧真译沿用+新增补译；繁/日/韩按 B1 基线补真实译文，术语与 orderRecord/terminology 对齐）。zh-CN key 即文案，无资源文件。
- 筛选标签/占位/枚举 label 复用 `orderRecord` 命名空间（`useTranslation(['report-order','orderRecord'], { nsMode: 'fallback' })`，AGENTS §6 模式；路由 meta.i18nNamespaces=['report-order','orderRecord'] 声明齐）。
- 口径说明长文案以**完整简中文案为 key**（zh-CN key 即文案纪律——短 key 会在简中显示六个字，实测发现并修正）。
- 无新增 i18n-missing 条目（本页所有 key 四语言均交付）。观察项（不阻塞）：en「时/分/秒」旧真译为 Hour/minute/second，拼接展示效果（如 "1Hour2minute"）沿用旧系统现状，未擅自改写；如需本地化时长格式（Intl.DurationFormat）留 V01 统一裁决（I03 边界）。

## 验证

- 静态门禁（代码提交前复跑两次，i18n 修正后复绿）：`pnpm check` lint 0 warning 0 error（491 文件）/ typecheck 通过 / structure 483 文件通过；`pnpm build` 1.18s 成功。
- 真实只读 8/8（`docs/migration/evidence/P33/run-3057971e-.../readonly-result.json`，经 Vite 同源代理 10.11.2.67 后端）：登录 activated=true；数量统计默认 6 行、筛选 WORK+SUCCEEDED 收敛 1 行且行值落在筛选集合；效率统计默认 PARK|WORK 2 行、字段秒值非负；效率筛选 WORK 收敛 1 行；getSimpleVehicles 56 项；无令牌 code=1000000 用户未登录；登出清理。
- 浏览器整页验收（真实登录、真实数据、IAB Chromium 1920×945 与 1366×768、深/浅主题、简中/EN/한국어）：菜单导航守卫直达；数量图按状态聚合（取消/队列中/执行中/成功四柱、柱顶标签 0 值不显示、8 色循环）；效率图三值柱（3 色板、柱顶秒转「X时X分X秒」、y 轴「时间（秒）」）；筛选联动真实生效（工作任务+成功 → 单柱与只读脚本等价）；口径提示完整长文案（简中实测）；语言切换图表/筛选/页签动态重建不刷新整页；主题切换图表重绘；1366 布局完整无溢出、图表 ResizeObserver 自适应；登出清理（回 /login 带 redirect 参数、auth 持久化清空）。截图 `screenshots/`（本地留存，8 张，local_only）。
- 失败态：会话失效真实场景触发图表区 StateBlock offline（清空数据、筛选保留、无重试按钮），符合按钮纪律与 D15。失败自动重查（可见有限退避 3 次）逻辑为代码审查证据（真实环境不可见窗口分支未构造，同 T00/P01 的 IAB 环境限制口径）。

## 未完成 / 后续

| 项 | 类别 | 说明 | 负责人 / 解除条件 |
| --- | --- | --- | --- |
| A04/I06（真实 token 过期收敛、资源加载失败不白屏） | cross_page | 与 T00/P01/P02/P21/P43 同口径 deferred 归 V01 长会话复核 | V01 |
| A24（长时多页签/真实规模稳定性、图表重建资源回收） | cross_page | 本轮图表 dispose/重建机制级已实现（Activity 隐藏卸载即释放）；长时观测归 V01 统一收口 | V01 |
| G15 公式真实数据复核 | site_acceptance | 「各类型平均值合计」口径与旧系统一致性的后端语义最终确认（本轮已按契约+旧实现等价交付并显式说明口径） | V01 |
| 繁體/日本語运行时抽查 | cross_page | en/ko 已实测两条加载路径（拉丁 fallback + CJK 分片），zh-TW/ja 分片与 ko 同构、typecheck 保证 key 一致；运行时逐语言切换留 V01 五语言全量回归 | V01 |
| en 时长单元词拼接效果 | 观察项 | 沿用旧系统真译，不改写；是否引入本地化时长格式由用户裁决 | V01/用户 |

上轮遗留补全：P21/P43 两轮的 runner 运行记录（`evidence/runs/run-6a494b98-*.json`、`run-94e66b2a-*.json`）此前未入库，本轮随交付一并补交（acceptanceRecord 引用完整性）。
