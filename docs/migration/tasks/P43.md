# P43 模板个人中心与公共入口清理 — 任务交接记录

| 字段 | 内容 |
| --- | --- |
| 任务 / 负责人 / 基线 | P43 模板个人中心与公共入口清理；AI 执行者（run-94e66b2a-1c48-4651-adde-ae4a9513e6a7，定时轮）；分支 apple-rxx，基线 02923b3（P21 交付后） |
| 状态 | 实施状态：已合并（本地提交）；验证状态：本轮全项通过（静态门禁 + 真实登录浏览器整页验收，2026-09-20）；阻塞：无 |
| 依据 | TASKS §6 P43；规格 §3.1（个人中心仅保留可接真实接口部分/无菜单 CRUD）、§5.4（不调用 /users/me）、§12（零 mock）、G01/G02/G12；目标项目 `src/pages/profile` 及相关公共入口（本任务旧来源为目标模板自身，非旧仓库页面）；OpenAPI 无菜单 CRUD/个人资料修改 operation |

## 来源 → 目标操作映射（P43 为清理收口任务，逐项核对结论）

| 模板公共入口 | 核对结论 | 处理 | 状态与证据 |
| --- | --- | --- | --- |
| `/profile` 个人中心页 | 账户信息全部来自真实登录会话（user.username/state/level + roles 数组），不调用任何业务接口 | 保留并按 AGENTS 第 3 节纪律收敛（见下） | 浏览器实测：root/启用/等级 2/角色空数组留白；页面 remount XHR 计数=0（不调用 /users/me，G01/G02 纪律实证） |
| 资料编辑区 ProfileForm | 调度后端无资料修改接口（G12 已确认缺失） | Alert 明确呈现不可用（五语言），不渲染伪造编辑表单、不用示例数据补字段 | 页面实测文案「调度系统暂未提供资料修改接口，当前以登录账号 root 的信息为准。」零请求 |
| 模板菜单管理页 Menu + MenuForm + useMenuTree + menu.service（虚构 REST `/menus` CRUD） | OpenAPI 无菜单 CRUD；路由未挂载（definitions.tsx 无 `/system/menu`）；消费者核查=仅链内互引，无外部消费者 | **全链删除**（6 文件 + en-US/system.ts 26 个 menu 死键 + i18n 注册行） | 删除后全局 grep 零残留引用；lint/typecheck/structure/build 全绿佐证无悬空导入 |
| 模板遗留 system 命名空间（i18n） | 唯一消费者即 menu 链 | en-US/system.ts 整文件删除 + 四语言表注册行移除（繁日韩本无 system 分片） | i18n-missing.md/i18n-map.md 登记同步更新 |
| 重复用户/角色入口（pages/system/user、role 链） | P31/P32 已分别删除（对接虚构 REST `/users`/`/roles`） | 无残留，本轮 grep 复核确认 | 本轮复核登记 |
| services/system 下其余服务（brand/database-backup/sys-log/system-log/system-version） | 真实调度契约实现（/systemLogos、/getDataBases、/pageSysLogs、/pageSystemLogs、/getSystemVersions 等），P25/P26/P27/P28/P30 页面 owner 交付的活代码 | **保留不动**（不误删共享模块） | 逐一核对消费者后保留 |
| Header 用户菜单「个人中心」入口 | 真实导航（navigate ROUTE_PATHS.profile），Dropdown click 触发 | 保留 | 浏览器实测：菜单项 root/未分配角色/个人中心/退出登录，点击落 /profile |
| profile 页对用户/角色选项的消费（P31/P32 交接待复核项） | profile 只消费登录返回的 roles 数组，不调用 getRoles/getUsers | 无需接入选项服务 | 本轮核查登记，P31/P32 交接挂账关闭 |

## 文件归属

- 删除：`src/pages/system/menu/Menu/Menu.tsx`、`src/features/system/menu/components/MenuForm/MenuForm.tsx`、`src/features/system/menu/hooks/useMenuTree.ts`、`src/services/system/menu/menu.service.ts` + `.types.ts`、`src/types/system/menu/menu.types.ts`、`src/i18n/locales/en-US/system.ts`
- 修改：`src/pages/profile/Profile/Profile.tsx`（状态枚举纪律收敛 + 角色空数组留白 + 头注释收口表述）；`src/features/profile/components/ProfileForm/ProfileForm.tsx`（头注释收口表述）；`src/i18n/i18n.ts`（en 表删 system 注册；繁日韩表各 +1 行 profile 注册；注释示例同步收敛）
- 新增：`src/i18n/locales/{zh-TW,ja-JP,ko-KR}/profile.ts` 三语言分片（10 键，与 en-US/profile.ts 同构）
- 登记：`docs/migration/i18n-missing.md`（profile 缺失条目移除、system 条目随死代码清除）、`docs/migration/i18n-map.md`（概括行 + P43 行更新）、`docs/migration/gaps.md`（G12 前端侧收口）

## 共享契约

- 无新增/变更服务与 DTO；profile 页零业务接口（会话数据直读 redux auth 切片）
- i18n：profile 命名空间四语言分片齐备（en 既有 10 键沿用，「停用」键改「禁用」与 P31/P32 术语收敛一致；繁日韩 B1 基线补译，术语对齐 menu/access-user：使用者名稱/ユーザー名/사용자명、啟用-停用/有効-無効/사용-중지、角色/ロール/역할）
- 公共文件影响：`src/i18n/i18n.ts`（消费者=全站语言加载；改动为删死注册+补 profile 注册，build 全绿佐证）；definitions.tsx 本轮零改动（profile 路由已挂载、i18nNamespaces 已声明）

## 接口缺口

- G12（模板个人资料/菜单/角色用户 REST DTO 与调度不同）：前端侧收口完成——menu 链删除、profile 零请求、无接口能力明确不可用呈现；gaps.md 已登记（2026-09-20）
- G01/G02（/users/me、refresh 无契约）：profile 不调用，维持已确认缺失状态

## 国际化

- profile 四语言分片 10 键同构；{{username}} 为 i18next 插值形态
- 五语言实测（真实登录会话，IAB Chromium）：zh-CN/en-US/zh-TW/ja-JP/ko-KR 逐语言验证页面文案 + 页签标题（个人中心/My Profile/個人中心/マイプロフィール/내 프로필 · 調度系統）+ html lang 同步，全绿；繁日韩新分片真实消费（此前为回退简中）
- i18n-missing.md 移除 profile 繁日韩缺失登记；「模板遗留 system 命名空间」登记条目随死代码清除

## 验证

- 静态门禁四项全绿：pnpm lint（477 文件 0 警告 0 错误）/ typecheck / check:structure（470 源文件）/ build（1.21s 成功；chunk 警告为既有 apex-table-react 体积现象）
- 浏览器整页（IAB Chromium，真实登录 root 会话，凭据取自 .env.local 未入记录）：
  - 未登录直访 `/profile` → 回跳 `/login?redirect=%2Fprofile`（守卫带合法回跳）；登录成功落回 redirect 目标 `/profile`
  - 页面渲染：账户信息（用户名 root/状态 启用/等级 2 全部真实会话数据）；状态枚举收敛（ENABLED→启用）生效；角色行=登录返回 roles=[] 真实空数组留白（无「—」，AGENTS 第 3 节留白纪律真实数据实证）；编辑资料 Alert 不可用说明
  - 入口导航：Header 头像 Dropdown（click）菜单 root/未分配角色/个人中心/退出登录；关闭页签后经菜单重开 → 落 /profile，window 级 XHR/fetch 计数器实证 **页面 remount 零业务请求**
  - 五语言切换（见上节）；双主题双视口四组截图 `screenshots/`（1920×945 与 1366×728 × 浅色/深色，仅本地留存）；reload 后 dark+zh-CN 持久化恢复实测
  - 真实登出：URL 回 /login；persist:apex-admin:auth 终态 token=null/user=null；语言偏好保留（规格 18.4 允许）；dev server 已停止
- 环境伪象登记（非产品缺陷，AGENTS 已有沉淀）：①antd 两字按钮「登 录」空格匹配陷阱+IAB 合成点击需 PointerEvent 序列（㉑/㉑ 同款，PointerEvent 序列派发后全流程正常）；②暗色截图合成层旧帧（浅色帧/全黑）经 viewport 往返强制重合成解决（⑱）；③Dropdown 关闭动画 DOM 残留不 remove DOM、点空白关闭（⑪）
- 防御分支代码审查登记：状态未知枚举原值/缺失留白分支（`user.state ?? ''`）与 roles 空数组留白在同一代码路径——空数组路径已经真实数据实证，未知枚举路径运行时受后端值域控制无法在真实环境构造（类型为 'ENABLED'|'DISABLED' 联合），代码审查通过

## 未完成 / 后续

- 无本任务遗留。A04/I06 长会话/资源故障场景按 T00/P01/P02/P21 同口径归 V01 复核
- P31/P32 交接的「P43 复核 profile 对用户/角色选项消费」挂账随本轮关闭（结论：无消费，不接入选项服务）
