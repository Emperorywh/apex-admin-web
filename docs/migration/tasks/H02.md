# H02 地图编辑暂缓说明 · 交接记录

**状态**：✅ 已完成（2026-09-18 定时轮交付并勾选）
**批次/前置**：B1 / T00（已完成）
**入口/目标**：`/map-through/map-nest-modify` → `src/pages/map-through/MapNestModify/MapNestModify.tsx`
**私有模块/语言分片**：无新增（复用 common/menu 基座命名空间，与 i18n-map 冻结表「H02 复用 common+menu」一致）
**旧来源**：`C:\code\dd\src\pages\MapThrough\MapNestModify`（index.tsx + NestGraph 完整画布编辑器（GraphStage/GraphBar/GraphMenu/ContextMenu）+ NestPanel 属性面板（EnableModify/MapDescription/MapPaneTabs）+ Konva，本期整体不迁移）

## 交付内容

1. **页面**（MapNestModify.tsx 重写）：清除模板期 PagePlaceholder 占位，改为复用 T00 统一暂缓组件 `MigrationPending`（title=地图编辑）的页面级呈现；多行简体中文注释登记 D07 暂缓边界与资源归属。页面无业务请求、无 Konva/编辑器代码、无任何地图编辑副作用（新增/改删节点边、保存、发布等）。
2. **路由标记**：map-nest-modify 从 `pending: true` 转为 `migrationDeferred: true`（H01 建立的语义：本期确定不迁移、页面正常加载渲染统一说明）。
3. **落点与回跳**：复用 routeAccess.isLeafAvailable 既有 deferred 排除——本页不作为登录落点候选、登录回跳不放行（root 登录落点保持 /no-permission）。菜单可见性与直访守卫不受影响。
4. **权限核对**：旧 `.umirc.ts` `/map-through/map-nest-modify` access=PERM.MAP_EDIT_VIEW（map-edit:view）= 新 definitions.tsx businessMeta 挂码，两侧常量值一致未改；有权限用户菜单/直访一致可达，无权限用户守卫拦截。
5. **查询上下文保留**：直访 `?mapId=42&tab=edit`（旧入口地图 ID 上下文形态）参数原样保留在地址栏，页面不消费、不清洗、不跳转——既不死链也不自动跳旧系统。
6. **资源归属隔离**（验收要求）：P09 地图列表的增改删/版本/导入导出/发布推送、P07 选点/只读渲染均不受本页影响——本页仅是路由叶子呈现，不改任何共享地图组件与 service；MigrationPending 为 H01 已交付公共组件，本轮零改动。

## 操作映射

| 旧操作 | 新实现 | Method + Path | 权限 | 状态/证据 |
| --- | --- | --- | --- | --- |
| /map-through/map-nest-modify 入口（Dock 菜单/直访） | MapNestModify.tsx 渲染统一暂缓说明 | — | map-edit:view（守卫） | ✅ 浏览器实测 |
| 旧页编辑器业务（Konva 画布/属性面板/启用编辑） | 不迁移（D07 本期暂缓） | — | — | ✅ 页内零 /fms 请求、无编辑器代码加载 |
| 旧深链/查询上下文（地图 ID 等） | URL 参数保留不清洗 | — | — | ✅ ?mapId=42&tab=edit 实测保留 |

## 真实联验证据（2026-09-18，dev 代理 192.168.0.158:8888，1366×768）

| 场景 | 结果 |
| --- | --- |
| 未登录直访 /map-through/map-nest-modify?mapId=42&tab=edit | 守卫送回 `/login?redirect=%2Fmap-through%2Fmap-nest-modify%3FmapId%3D42%26tab%3Dedit`（含查询参数） |
| root 登录（请求带该 redirect） | 落点 `/no-permission`——deferred 页不抢占落点、回跳被拒 |
| 已登录直访 | 统一暂缓说明渲染（标题+「该功能正在迁移中」）；页签「地图编辑」正常创建；Dock「地图管理」子菜单含「地图编辑」入口且分组高亮 |
| 业务隔离 | 页面加载后 performance 资源条目中 /fms 请求 0 条；页面代码路径无任何网络/编辑器代码 |
| 五语言 | zh-CN（初始态）/en-US「Map Editor」/zh-TW「地圖編輯」/ja-JP「マップ編集」/ko-KR「맵 편집」标题+说明逐语言实测；html lang 随语言同步 |
| 深浅主题 | 玻璃面板两主题截图：标题/说明均落面板内可读（MigrationPending H01 修复直接受益） |
| 历史往返 | back/forward 稳定在本页 URL，无重定向风暴 |
| 收尾 | 清测试会话后直访再次被守卫拦回登录页；语言/主题已复位（zh-CN/light）；页签已关；dev server 已停 |
| 工程门禁 | pnpm check（228 文件 lint 0 警告 0 错误 / typecheck / 结构检查 226 源文件）+ pnpm build 全过 |

## 公共文件改动记录（影响消费者）

| 文件 | 改动 | 消费者影响 |
| --- | --- | --- |
| `src/router/definitions.tsx` | map-nest-modify 标记 pending→migrationDeferred | 仅该节点变化；routeAccess/菜单/守卫逻辑零改动（复用 H01 既有 deferred 语义） |
| `src/pages/map-through/MapNestModify/MapNestModify.tsx` | 整页重写 | 无其他消费者 |

## DoD 适用性

- 不适用：表格（4/5）、草稿（7）、写入（8）、文件（9）、地图（10，本页无地图渲染；只读/选点归 P07）、统计（14）。
- 适用已满足：可达业务完整（1，旧页业务整体不迁移=暂缓语义，无死链）、权限与状态区分（3，MAP_EDIT_VIEW 守卫；暂缓≠无权限）、五语言（11，menu/common 既有五语言复用，无新增 key、无缺译）、外观终端（13，两主题×1366×768 截图）、验证证据（15）、交接收尾（16，模板占位清除、无 mock）。
- 追踪项：A01/A02（/map-through/map-nest-modify 为暂缓说明呈现）、A05（原权限保留）、A17（旧页业务文案随编辑器整体不迁移，无遗留死资源）、A20（五语言+两主题+最低视口）、A22（菜单别名一致、回跳拒绝）；I02（旧资源去向：菜单 key 走 menu 分片真译、页内编辑器业务文案随业务不迁移、说明文案复用 common 五语言）、I06（无回退、无缺译）。

## 对后续任务的影响

- **H03**：复用 `migrationDeferred` 标记 + 页面级 MigrationPending 模式（与 H01/H02 相同写法）。
- **P07/P09**：本页未触碰任何地图共享组件/service/类型；地图列表与选点页面交付时不受影响。
- 无未完成项、无接口缺口（本页无业务接口）。
