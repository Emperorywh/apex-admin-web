# P30 数据库备份 · 交接记录

**状态**：✅ 已完成（2026-09-20 定时轮交付并勾选；7.63GB 大文件完整落盘留现场环境——流式真文件字节/真实进度/取消中止已实证，完整下载到磁盘不做不合理磁盘占用）
**批次/前置**：B3 / B2 样板门禁（均已完成）
**入口/目标**：`/system-involve/database-backup` → `src/pages/system-involve/DatabaseBackupManagement/DatabaseBackupManagement.tsx`
**私有模块/语言分片**：`database-backup`（页面私有，四语言分片 24 键）
**旧来源**：`C:\code\dd\src\pages\SystemInvolve\DatabaseBackupManagement\index.tsx`（+index.less，无其他可达子组件）

## 交付内容

1. **服务层**（`src/services/system/database-backup/`，owner=P30，contracts.md 第 5 节）：
   - `getDataBases()`：GET `/dataBase/getDataBases`（无参 → 库名 string[]）。
   - `getDataBaseBackupFiles({database})`：GET query 平铺 → `BackupFileRecord[]`（fileName/fileSize/fileSizeReadable/createTime/filePath 五字段，全集返回无分页参数）。
   - `downloadDataBaseBackupFile({database, backupFileName}, onDownloadProgress)`：GET query 双参数（OpenAPI 均 required）→ Blob + 文件名解析（content-disposition 优先 RFC 5987，回退行记录文件名=旧版 a.download 同语义）。与旧页面 a 标签 GET 同形态（旧 api/index.ts 的 POST 版本为页面未消费的死代码，不迁移）。
2. **整页重写**（DatabaseBackupManagement.tsx + .module.css）：
   - 库选择行：「备份数据库：」标签 + Select 300px allowClear（旧版 search_bar 等价）；页面加载即查、默认选中第一个；**清空仅更新选中不重查**（旧版 effect 判空跳过同语义，XHR 计数实证 0 请求）。
   - 备份文件列表：ApexTableReact request 模式（P25 同款——getDataBaseBackupFiles 无分页参数，request 回调内客户端切片分页 rowCount=全集长度，不虚构服务端分页语义）；未选库时不发请求返回空结果；失败态由表格内建承载（重试仅在表格内部，按钮纪律）；库切换经 ref 读当前值 + reload 重查（Apex 内建取消前次）。
   - 列结构逐列核对旧版：文件名（弹性+悬浮完整值）/文件大小（fileSizeReadable 后端预格式化串，缺失留白）/创建时间（displayDateTime 秒级展示——后端实测 23 字符毫秒串 `2026-09-20 02:14:19.830`，P26/P28 同款既有能力覆盖；协议值不改）/路径（600+悬浮完整值）/操作（120，下载按钮）；行 ID=fileName（stringFieldRowId 雪花守卫）；列偏好 `database-backup:main` + defaultPinning 操作列钉右（用户偏好覆盖）。
   - 下载：请求层 `downloadGet` Blob 通道 + TransferProgressModal 真实进度弹窗（复用 P25）+ beginTransfer 传输管理器登记（切页继续/关页提示）+ useTransferSpeed + 取消二次确认（transfer.cancel() 真实中止）；错误 JSON/HTML 不落伪文件（DoD 9）。旧版 a 标签直下载不可观测无失败处理，按 A16 真实进度纪律升级；旧版伴随提示「浏览器即将开始下载，请注意查看下载栏」随形态升级移除（新形态有真实进度+完成反馈，差异登记）。
3. **权限（专项验收：核对原权限清单，不把缺少路由 access 当公开页）**：旧 `.umirc.ts` 该路由**漏写 access**（同组其余页面均有），但旧权限清单真实存在——`PERM.SYSTEM_DATABASE_BACKUP_VIEW: "system:database-backup:view"`（permission.ts:69）+ 菜单映射 `{ code, path: "/system-involve/database-backup" }`（permission.ts:355）；按钮码 `system:database-backup:download`（permission.ts:235，PERM_BUTTON 既有）。收敛：新项目路由挂 `system:database-backup:view` 守卫（两码后端下发已实证，非凭空造码）；下载按钮码条件渲染隐藏、操作列保留空列（旧 §7.2）。
4. **「两级列表均使用 Apex」解读登记**：页面两级数据=库列表+备份文件列表。旧版库列表为 Select 下拉**选择控件**（非表格），等价迁移保持下拉形态；表格型列表（备份文件）已 Apex 化。若按字面把库列表也 Apex 表格化将改变旧版交互形态，与等价迁移纪律冲突，登记解读依据待验收确认。
5. **路由**（definitions.tsx 单点）：`system-involve-database-backup` 摘除 pending，挂 `PERM.SYSTEM_DATABASE_BACKUP_VIEW`（本轮新增常量），`i18nNamespaces: ['database-backup']`。
6. **五语言**：`database-backup` 命名空间四语言分片交付（24 键）。en-US 旧真译逐条沿用（`创建时间`=Created/`操作`=Action 与 systemVersion 一致；错误反馈前缀拼接改 `{{msg}}` 插值形态）；下载弹窗类旧资源缺失按 P25 风格补译并与 systemVersion 术语对齐；zh-TW/ja-JP/ko-KR B1 基线真实译文（資料庫/データベース/데이터베이스 系）。zh-CN 中文 key 即文案无需分片。菜单项「数据库备份管理」四语言既有译文沿用。
7. **不擅自增加**：无创建备份、无恢复数据库入口（旧版亦无，交付边界）；无轮询（旧版同语义）；无手动刷新按钮（按钮纪律，库切换即重查）。

## 操作映射

| 旧操作 | 新实现 | Method+Path | operationId | 权限 | 状态/证据 |
| --- | --- | --- | --- | --- | --- |
| 备份数据库下拉（getDataBases，默认选第一个） | 页面加载查询 + Select（清空不重查） | GET /fms/v1/dataBase/getDataBases | getDataBases | system:database-backup:view（守卫） | ✅ 带令牌联验通过（fms/rcs_flow 两库；无令牌 1000000） |
| 备份文件列表（全量 Table 不分页） | Apex request 模式（客户端切片，rowCount=全集） | GET /fms/v1/dataBase/getDataBaseBackupFiles?database= | getDataBaseBackupFiles | system:database-backup:view（守卫） | ✅ 带令牌联验通过（fms 10×7.5GB / rcs_flow 10×276KB；五字段齐备；缺参后端 500 记录） |
| 下载备份文件（a 标签 GET 直下+message.info 提示） | downloadGet Blob+真实进度弹窗+传输管理器+取消确认（升级登记） | GET /fms/v1/dataBase/downloadDataBaseBackupFile?database=&backupFileName= | downloadDataBaseBackupFile | system:database-backup:download（无权限隐藏保留空列） | ✅ 联验通过（小文件 275KB 完成反馈实证；大文件走马灯真实增长+取消中止实证；PGDM 魔数 64KB 实证；7.63GB 完整落盘留现场） |
| 未落地（旧页面未消费） | api/index.ts 的 downloadDataBaseBackupFile POST 版本（死代码） | — | — | — | 已核对不落地（OpenAPI 与实际页面行为均 GET） |

## 真实联验证据（2026-09-20，run-20260920-p30-030xxx，环境 192.168.0.158:8888）

**API 层（python 脚本 8/8 通过 + 2 项契约形态如实记录，验证后删除）**：

- 登录 code=200；权限两码 `system:database-backup:view`/`system:database-backup:download` 真实下发（155 权限中）。
- `getDataBases` 带令牌 code=200 → `["fms", "rcs_flow"]`；无令牌 1000000。
- `getDataBaseBackupFiles?database=fms` code=200 → 10 条五字段齐备（fileSize=8197800249 字节=7.63 GB 大文件实锤）；rcs_flow 10×~276KB。
- **契约实证①**：`downloadDataBaseBackupFile` 流式响应无 Content-Length（application/octet-stream），魔数 `PGDM`（PostgreSQL dump 自定义头）真实文件；content-disposition `filename=V2_fms_20260911020000.dump`（ASCII 形态）。
- **契约实证②（安全观察项）**：下载端点**无令牌不返回 1000000** 而返回业务码 7000140「数据库备份文件不存在」——请求直接进入业务逻辑，该端点不做会话校验（P27 systemLogos 公开族同型观察）；前端正常带令牌不受影响，登记 contracts 待后端确认。
- 缺参查询（无 database）后端 500「失败」——页面从不缺参请求，仅记录。
- 不存在文件名 → code=7000140 JSON（页面下载 catch 如实呈现错误消息，不落伪文件）。
- 只读+下载零写副作用；终态零残留。

**浏览器整页（dev 代理 192.168.0.158:8888，真实登录 root，IAB rAF+RO 永续 fire 垫片）**：

- 守卫：未登录直访 → `/login?redirect=%2Fsystem-involve%2Fdatabase-backup`；登录后 redirect 回跳目标页闭环。
- 整页渲染：库下拉默认选中 fms；表格 10 行真实数据；5 列头+列宽与代码一致（150/150/200/600/120）；创建时间秒级展示；共 10 条分页正确。
- 切库：fms → rcs_flow（列表刷新为 275KB 文件）→ 切回 fms（7.63GB 文件）；**antd v6 Select 选项选中需 PointerEvent 序列**（纯 MouseEvent 无效，AGENTS 新沉淀㉑）。
- 小文件下载（rcs_flow 275KB）：下载触发 → 进度弹窗 → `备份文件下载完成` 反馈 ×2（两次独立下载均成功）；download 事件真实发生。
- 大文件下载（fms 7.63GB）：不确定进度走马灯（**99%+active=P25 组件既定形态**，antd 以 99% 模拟走马灯不显示真实百分比）+已接收 MB 真实增长（15.77→39.66MB）；取消下载 → 确认弹窗五要素（标题+正文+danger 取消键+继续键）→ `已取消下载` info 反馈+请求真实中止。
- 首轮取消曾现「网络不可达」：定性为**残留弹窗死 DOM 误点伪象**（AGENTS ⑦/⑪——点了 React 已 resolve 的旧实例，取消信号未发出，在途流式连接随后断流如实报错）；整页 reload 清残留后干净环境重测取消路径通过，产品代码无缺陷。
- 清空库：XHR 计数 0 请求+表格数据保留（旧版语义等价）。
- 列偏好四步闭环：隐藏路径列 → 确定（原生 dialog 的「确定(Enter)」按钮）→ localStorage `apex-table:columns:[...,"database-backup:main"]` 落盘（filePath=false）→ reload 持久 → 恢复默认+确定 → 出厂全列回归。
- 五语言逐语言：en-US/zh-TW/ja-JP/ko-KR/zh-CN html lang 同步+库标签/文件名列头/下载按钮逐语言正确（Backup Database:/備份資料庫：/バックアップデータベース：/백업 데이터베이스:）。
- 双主题双宽度四组截图：`.run-lock/shots-p30/`（light-1920/dark-1920/dark-1366/light-1366，布局完整无溢出，暗色主题正确）。
- 真实登出 → `/login`；页签关闭+dev server 停止+联验脚本删除+调试残留 0。

## 差异登记（收敛项，非缺陷）

1. **下载形态升级**：旧 a 标签直下载（不可观测、无失败处理）→ GET Blob+真实进度弹窗+传输管理器+取消确认（A16 纪律）；旧「浏览器即将开始下载…」提示随形态移除。
2. **分页形态**：旧 antd Table pagination=false 全量渲染 → Apex 客户端切片分页（rowCount=全集长度，数据同源全量；P25 同款决策）。
3. **进度形态**：下载端点流式无 Content-Length → 本页下载恒走不确定进度路径（走马灯+已接收 MB），已知百分比/ETA 仅在响应带总长时出现（G11 只承诺真实能力）。
4. **时区口径**：创建时间 displayDateTime 部署时区秒级展示（后端毫秒串截秒显示，协议值不改；P26/P28 既有能力）。
5. **未落地死代码**：旧 api/index.ts 的 downloadDataBaseBackupFile POST 版本（页面实际走 a 标签 GET，OpenAPI 亦 GET）。
6. **安全观察项（后端）**：下载端点无令牌直达业务逻辑（7000140 而非 1000000）——见 API 层契约实证②；不影响前端交付，登记待后端确认是否补鉴权。
7. **大文件完整落盘**：7.63GB×10 的 fms 库备份完整下载到磁盘不做（磁盘+时间代价不合理）；流式真文件字节（PGDM 头）、真实进度、取消中止均已实证，完整落盘留现场环境（TASKS「大文件待验收明确」项）。

## 门禁

lint 434 文件 0 警告 0 错误 / typecheck / structure 432 源文件 / build 全部通过（2026-09-20，chunk 警告为既有 apex-table-react 体积）。
