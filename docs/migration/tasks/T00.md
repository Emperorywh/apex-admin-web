# T00 基础设施 — 任务记录

## 任务 / 负责人 / 基线

| 字段 | 内容 |
| --- | --- |
| 任务 | T00 基线、请求/会话/权限、表格、页签与五语言公共基础设施（B0，无实施前置，可跨多轮执行） |
| 负责人 | 定时调度串行执行（T00.1：run-20260917-121624-7468；T00.2：run-20260917-123037-7641） |
| 分支 / 提交 | `apple-rxx` / 基线 `b09d285` → T00.1 提交 `0fd6dd9` → T00.2 本轮提交（见 git log） |
| 开始日期 | 2026-09-17 |
| 最近更新 | 2026-09-17（T00.2 完成） |

## 状态

| 维度 | 状态 |
| --- | --- |
| 实施状态 | **进行中**（T00.1、T00.2 已完成；T00.3–T00.9 未开始） |
| 验证状态 | T00 整体未验证；T00.1 文档清点即交付；T00.2 自动检查四项全部通过（见验证节） |
| 阻塞原因 | 无 |

## 里程碑进度

| 里程碑 | 状态 | 说明 |
| --- | --- | --- |
| T00.1 基线及分工 | ✅ 完成（2026-09-17） | 交付物见下节 |
| T00.2 依赖与环境 | ✅ 完成（2026-09-17，本轮） | 依赖/代理/时区/结构门禁全部落地，`pnpm check` + build 实测通过 |
| T00.3 请求与认证 | ⬜ 未开始 | 下一轮起点：Result/ApiError、`code=200` 解包、假登录移除、真实会话持久化 |
| T00.4 权限与路由 | ⬜ 未开始 | |
| T00.5 Apex 与状态 | ⬜ 未开始 | apex-table-react 已安装（T00.2），运行时适配在此落地 |
| T00.6 页签与传输 | ⬜ 未开始 | |
| T00.7 共享业务能力 | ⬜ 未开始 | 时间/时区展示工具消费 `DEPLOY_TIMEZONE` |
| T00.8 五语言基座 | ⬜ 未开始 | |
| T00.9 冻结交接 | ⬜ 未开始 | |

## 本轮（T00.2）交付物与证据

1. **npm latest 重核与安装**：antd latest = 6.6.4（项目 `^6.6.1` 兼容）；`apex-table-react@0.1.0` 安装成功并提交 pnpm 锁文件（peerDeps react ^18‖^19 兼容 React 19）。类型兼容性以临时探针验证（引用 `ApexTableReact`/`zhCN` 过 `tsc -b` 全量），验证后探针已删除，不留演示代码。
2. **dev 代理与前缀契约**：`vite.config.ts` 默认目标 `http://localhost:8000` → `http://10.11.2.67:8888`（`APEX_DEV_PROXY_TARGET` 可覆盖）；代理键 `/api` → `/fms`，**不做 rewrite**，上游最终路径准确为 `/fms/v1/...`；`request.constants.ts` 的 `DEFAULT_API_BASE_URL` `/api/v1` → `/fms/v1`（`VITE_API_BASE_URL` 覆盖机制保留）。生产由同源反向代理转发 `/fms`。
3. **部署时区**：`src/vite-env.d.ts` 登记 `VITE_DEPLOY_TIMEZONE`；新增 `src/constants/datetime.ts` 定义 `DEPLOY_TIMEZONE`（缺省 `Asia/Shanghai`），为全站唯一定义点；dayjs 展示工具 T00.7 接入消费。
4. **check:structure 修复（G16）**：新增 `scripts/check-structure.mjs`（规则依据 CLAUDE.md）：禁 `index.tsx`；`@/` 唯一别名（禁多级相对导入与伪绝对导入）；导入方向门禁（services→组件层、components|hooks→pages|features、跨 features 业务域穿透）；业务源码禁硬编码 `http(s)/ws(s)` URL 与 IPv4。**违规注入自测**：5 类违规样本（深层相对/伪绝对/services→features/跨 feature/硬编码 IP URL/index.tsx）逐一被抓后清理；期间修复两处脚本缺陷（同目录相对导入误报伪绝对、跨 feature 因一级目录判定漏报）。
5. **README/CLAUDE.md 失效依据修正**：全仓库确认 `docs/macos_ui_ux_design_guide_v3.md` **不存在**（勘误：T00.1 曾登记「文件现存」，不实）；README/CLAUDE.md 引用改为「以现有布局/token 为视觉基线」，代理与基础路径描述同步更新。

**影响说明**：基础路径切换后，现有模板 service 的相对请求将指向 `/fms/v1`；模板旧协议（无 envelope/RFC 9457/Cookie 刷新）与调度协议不匹配，模板页面真实联调在 T00.3 协议重写前不可用——属迁移预期，已在 contracts.md 变更日志登记。

## 上一轮（T00.1）交付物与证据

1. **OpenAPI 核验**：`C:\yangwenhua\ApexTableReact\default_OpenAPI.json` SHA-256 = `A82E9B5F6EF5E564DEEE16E4E74713B28FCCB35CD0A651E3631CFD9D63D49C7C`（与规格一致）；238 operation / 408 schema 脚本实测一致。endpoint 全表 → `docs/migration/openapi-inventory.md`。
2. **45 个旧叶子清点与目标映射** → `docs/migration/baseline.md` 第 2 节（含目录/重定向结构、目标树现状核对：全部叶子已有路由节点）。特殊权限点（载具类型/数据库备份无 access、order-info/vehicle-info 复用码、服务器资源别名）→ baseline 第 5 节。
3. **历史实现排除登记** → baseline 第 6 节（Elevator/AutoDoor 历史、ConveyorLine、sysAction、重复用户角色页等）。
4. **检查与依赖故障登记** → baseline 第 7/10 节。
5. **台账初始化**：`gaps.md`（G01–G16）→ `contracts.md` → `operations.md` → `i18n-map.md` / `i18n-missing.md` / `terminology.md` → `RUN_STATE.json`（48 项串行队列）。
6. **旧五语言资源规模实测**：zh-CN/en-US 2092 顶层 key、zh-TW/ja-JP/ko-KR 1478，与规格 18.1 一致（baseline 第 8 节）。
7. **基线检查实测**：`pnpm lint` ✅、`pnpm typecheck` ✅、`pnpm check:structure` ❌（T00.2 已修复）。

## 文件归属（实际修改）

**T00.2（本轮）**：

- `package.json` / `pnpm-lock.yaml` / `pnpm-workspace.yaml` — 安装 `apex-table-react@0.1.0`（workspace 文件为 pnpm 自动生成的供应链策略例外登记）
- `vite.config.ts` — 代理目标与前缀契约
- `src/services/request/request.constants.ts` — `DEFAULT_API_BASE_URL`（公共文件，owner T00；影响全部 service 消费者，已登记 contracts.md 变更日志）
- `src/vite-env.d.ts` — 登记 `VITE_DEPLOY_TIMEZONE`、更新 `VITE_API_BASE_URL` 注释
- `src/constants/datetime.ts` — 新增部署时区常量
- `scripts/check-structure.mjs` — 新增结构门禁脚本
- `README.md` / `CLAUDE.md` — 修正失效视觉指南引用与代理/路径描述

**T00.1（上一轮）**：`docs/migration/` 全部台账与 `tasks/T00.md`，未触碰应用代码。

## 共享契约

本轮只登记未实现：协议契约框架见 `contracts.md` 第 1–2 节；共享只读选项 owner 归 T00（第 5 节，operation 待 T00.7 逐项核对登记）。

## 接口缺口

G01–G16 已按规格第 13 章初始化为「待确认」→ `gaps.md`。G16 的脚本缺失部分已在目标仓库实测复现。

## 验证

**T00.2（2026-09-17）**：

| 命令 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 152 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过（含临时 apex-table-react 类型探针，验证后删除；删除后复跑仍通过） |
| `pnpm check:structure` | ✅ 150 源文件通过；违规注入自测 6 处样本全部正确拦截（深层相对/伪绝对/services→features/跨 feature/硬编码 IP+URL/index.tsx） |
| `pnpm build` | ✅ 通过 |
| `pnpm add apex-table-react@latest` | ✅ 安装 0.1.0，锁文件通过 pnpm 供应链策略校验 |

无真实接口验证（10.11.2.67:8888 可达性/登录验证属 T00.3，须记录环境证据）；本轮不探测真实服务器。

**T00.1（2026-09-17）**：`pnpm lint` / `pnpm typecheck` 通过；哈希与旧语言资源规模脚本核验通过；`pnpm check` 当时失败于 check:structure 缺失（本轮已修复）。

## 未完成 / 后续（下一轮入口）

1. **T00.3 请求与认证**（下一轮起点）：Result/ApiError 与 `code/message/data/timestamp` 解包；分页/查询/时间/精度适配；取消与并发失效收敛；移除假登录与虚构 `/users/me`、`/auth/logout`、静默刷新；真实登录最小路径（G01/G02/G03 证据）；写请求不自动重放。
2. 之后 T00.4 → T00.9 顺序推进；每里程碑完成即更新 RUN_STATE.json 与本文件。
3. 待核实遗留：`10.11.2.67:8888` 联调环境可达性与登录凭据（T00.3 需要用户提供或确认专用测试环境）；旧 `APEX_DEV_LEGACY_TARGET` 变量仅存于用户本地 `.env.local`（gitignore，无人消费），不动它。
