# T00 基础设施 — 任务记录

## 任务 / 负责人 / 基线

| 字段 | 内容 |
| --- | --- |
| 任务 | T00 基线、请求/会话/权限、表格、页签与五语言公共基础设施（B0，无实施前置，可跨多轮执行） |
| 负责人 | 定时调度串行执行（T00.1：run-20260917-121624-7468；T00.2：run-20260917-123037-7641；T00.3：run-20260917-140500-13667；T00.4：run-20260917-133558-8461；T00.5：run-20260917-151640-9160；T00.6：run-20260917-154343-9436；T00.7：run-20260917-164052-9742；T00.8：run-20260917-183059-10593；T00.9：run-20260917-193052-10903） |
| 分支 / 提交 | `apple-rxx` / 基线 `0fd6dd9` → T00.2 提交 `8b6ed2f` → 各轮提交见 git log |
| 开始日期 | 2026-09-17 |
| 最近更新 | 2026-09-18（G03 补验完成并关闭：MD5/Bearer/1001000 全部真实实证；出现新外部阻塞 G17——联调系统未激活，带令牌业务只读不可达，T00 维持不勾选） |

## 状态

| 维度 | 状态 |
| --- | --- |
| 实施状态 | **里程碑 T00.1–T00.9 全部完成**；**G03 已关闭（2026-09-18 真实凭据联验）**：MD5 确认必需（MD5 成功/明文 1000010）、Bearer 确认被识别（带令牌 1001000 vs 无令牌 1000000）、1001000 实证=`系统没有被激活`、登录返回 UserAuth 与前端 DTO 完全一致、root 真实身份确认（id=2/level=2/155 权限码）。T00 整体勾选**受阻 G17（新）** |
| 验证状态 | 机制级验证齐备（T00.4 17 场景、T00.6 9 组场景、T00.7 26 项断言+地图渲染、T00.8 五语言全链路、T00.9 四项检查复跑）+ **认证链路真实联验通过**（2026-09-18：登录成功路径/MD5/Bearer/1001000/root 身份）；**受阻 G17**：带令牌业务只读（pageUsers 返回 1001000 系统未激活）；**待消费页联验**：Apex 真实列表（P03/P05）、useVisiblePolling（首个实时页）、传输文件通道（P08/P09/P25）、地图动画（P07 真实浏览器）、antd 控件弹窗文案（P03 等）、跨窗口退出真实双窗口复验 |
| 阻塞原因 | G17：联调服务器软件未激活（`activated:false` → 业务接口全部 `1001000 系统没有被激活`），softwareActivation 属副作用操作不自动执行。解除条件：用户激活联调环境或提供已激活后端。激活后 T00 即可勾选（放行证据「授权只读可用」届时以带令牌业务只读复核收尾），随后下一轮领取 P01 |

## 里程碑进度

| 里程碑 | 状态 | 说明 |
| --- | --- | --- |
| T00.1 基线及分工 | ✅ 完成（2026-09-17） | 交付物见下节 |
| T00.2 依赖与环境 | ✅ 完成（2026-09-17） | 依赖/代理/时区/结构门禁全部落地，`pnpm check` + build 实测通过 |
| T00.3 请求与认证 | ✅ 完成（2026-09-17） | 调度协议请求层 + 真实登录/登出 + 会话持久化 + 多窗口退出；协议形状与业务码真实实证；登录成功路径待凭据（登记受阻范围） |
| T00.4 权限与路由 | ✅ 完成（2026-09-17） | 权限码契约、路由守卫、动态落点、迁移占位、菜单剪枝、按钮码 hook；连带修复 T00.3 持久化恢复与多窗口键名两处缺陷 |
| T00.5 Apex 与状态 | ✅ 完成（2026-09-17） | 五语言 ApexLocale 包、主题变量映射、分页换算/行 ID 纯函数、列偏好官方适配器约定、统一状态块；公开 API 核验（虚拟化/展开/编辑/双 ref）类型探针实证；不做二次封装（用户决策），页面直接组装 |
| T00.6 页签与传输 | ✅ 完成（2026-09-17） | 脏状态/LRU 豁免、统一动作确认（关闭/刷新/批量/登出）、beforeunload、可见串行轮询 hook、独立传输生命周期、实体页签与独立窗口；真实浏览器 9 组机制场景实证 |
| T00.7 共享业务能力 | ✅ 完成（2026-09-17） | 时间/时区、统计空值/单位、控制确认/批量反馈、选项失效、只读地图（含 services/map）；依赖升级 react 19.3.0 + react-konva 19.3.0 + konva 9.3.20 |
| T00.8 五语言基座 | ✅ 完成（2026-09-17） | SUPPORTED_LANGUAGES 扩五语言、normalizeLanguage 精确归一化（zh-TW/ja-JP/ko-KR 不误映射）、四语言懒加载基座（common/menu/auth/error/map 全量）、umi_locale 一次迁移、settings 恢复单一真相源收敛、antd/dayjs/html lang 同步、切换失败回滚；真实浏览器全链路实证 |
| T00.9 冻结交接 | ✅ 完成（2026-09-17，本轮） | contracts.md 冻结（i18n 契约落地实况、变更日志补全、第 9 节冻结登记）；公共导出清点与契约核对一致；各页 namespace/文件归属终版冻结（i18n-map.md 45 入口表）；过渡期 mock 退出运行路径复核成立；真实纵向样板确认；T00 退出检查 A/I 证据汇总 |

## 本轮（T00.9）交付物与证据

本轮为冻结交接轮，无代码改动（`git status` 干净起步、四项检查复跑通过），全部交付为台账核对与冻结登记。

### 1. 公共导出清点（与 contracts.md 核对一致）

逐文件核对 T00 交付的公共模块实际导出（grep `^export`）与契约表登记：

| 模块 | 实际导出 | 契约 |
| --- | --- | --- |
| `src/i18n/i18n.ts` | SUPPORTED_LANGUAGES/AppLanguage/DEFAULT_LANGUAGE/BASE_NAMESPACES/normalizeLanguage/readStoredLanguage/preloadNamespaces/changeAppLanguage + 默认 i18next | 第 7 节 ✅ |
| `src/services/request/` | api（Result 解包）、RESULT_CODES、ResultDto/BackendPageQuery/BackendPageResult/ApiError | 第 1 节 ✅ |
| `src/constants/auth/permission.constants.ts` | PERM/PermCode/PERM_BUTTON/PermButtonCode/ROOT_ONLY_CODES | 第 2 节 ✅ |
| `src/utils/auth/permission.ts` + `src/router/routeAccess.ts` | isRootUser/flattenMenuCodes/hasButtonCode；AccessContext/buildAccessContext/hasMenuAccess/isLeafAvailable/resolveLandingPath/resolveDirectoryLandingPath/resolveSafeRedirectPath | 第 2 节 ✅ |
| `src/utils/table/` + `useColumnPreferences`/`useApexLocale`/StateBlock | toBackendPage(+No/Size)/stringFieldRowId/createTableColumnPreferences/useColumnPreferences/resolveApexLocale/useApexLocale/StateBlock(noPermission/gap/offline) | 第 3 节 ✅ |
| `useTabDirtyGuard`/`useTabActionGuard`/`useVisiblePolling`/`polling.constants`/transferManager/`useTransfers`/TransferWatcher/`openStandaloneWindow` | 契约第 4 节全部入口（beginTransfer/findActiveTransfersIn/isOrphanedTransfer 等；POLL_BASE_INTERVAL_MS=5000/×2/60s） | 第 4 节 ✅ |
| `src/utils/datetime/` + `src/constants/datetime.ts` | DEPLOY_TIMEZONE/deployDayjs/parseBackendDateTime/formatInDeployTimezone/displayDateTime/UNPARSEABLE_DATETIME | 第 7 节 ✅ |
| `src/utils/stats/metricFormat.ts` | UNCOMPUTABLE/isComputableNumber/formatMetricValue/safeRatio/formatPercent/formatWithUnit/secondsToHours | 变更日志 T00.7 行 ✅ |
| `src/utils/command/` | CommandOutcome/BatchItemOutcome/BatchOutcomeSummary/confirmCommand/summarizeBatchOutcomes/summarizeWholeBatch/formatBatchSummaryText（文件实际为 commandConfirm.**ts**，修正 T00.7 轮记录中的 .tsx 笔误） | 变更日志 T00.7 行 ✅ |
| `src/utils/options/` + `useStaticOptions` | matchOptionById/missingOptionLabel/findMissingOptions/useStaticOptions | 第 5 节 ✅ |
| `src/services/map/` + ReadOnlyMap（12 文件） | fetchMapGraph/fetchSimpleMaps；ReadOnlyMap 默认导出 + hooks（useMapSelection/useMapStageInteraction/useMapBlink/useMapGraph）+ 纯函数（geometry/mount/styles） | 第 5.1 节 ✅ |

### 2. 过渡期 mock 退出运行路径复核（成立）

引用链实测：`order.mock.ts`/`dashboard.mock.ts` → 仅被同目录 `order.service.ts`/`dashboard.service.ts` 引用 → service 仅被 `features/order-record`/`features/dashboard` 的 hooks（useOrderList/useDashboardOverview）引用 → hooks 仅被页面组件引用 → 页面组件经 `definitions.tsx` `loadPage` React.lazy 加载 → **order-record 与 dashboard 路由均标记 `pending: true`，projections.tsx 对 pending 页渲染 MigrationPending 占位、不触发 lazy 加载**（projections.tsx:216）。T00.4 真实浏览器已实证迁移页零请求。结论：mock 文件本体运行不可达；按 TASKS.md 归属，「移除 order.mock.ts 和运行引用」随 P03、「移除 dashboard.mock.ts 及其引用」随 P34 执行，T00.9 不越权删除。

### 3. 真实纵向样板确认

P03/P05 将采用的全链路组装方式各环节已分别实证：路由定义→守卫→动态落点（T00.4，17 场景）；页签/草稿/取消 scope（T00.6，9 组场景）；表格直接组装 `ApexTableReact` + `useApexLocale`/`toBackendPage`/`stringFieldRowId`/`useColumnPreferences`/StateBlock（T00.5，类型探针以页面对接方式全量组装通过 `tsc -b` 后删除）；请求层 Result/取消/健康（T00.3，真实端点实证）；五语言切换（T00.8，真实浏览器）。**缺口保持登记**：链条的「真实接口数据」一环（登录成功 + 带令牌只读列表）受 G03 阻塞，纵向样板的端到端联验随 B2a（P03/P05）完成——与 TASKS.md B2 门禁定义一致（真实只读未验证不得宣称门禁通过）。

### 4. contracts.md 冻结与各页归属终版

- contracts.md：第 7 节 i18n 契约由「已确认」粗粒度更新为 T00.8 落地实况（归一化规则/单一真相源/切换时序/第三方 locale/命名空间冻结指引）；变更日志补 T00.8 行与全表冻结行；新增第 9 节「冻结登记」（冻结范围、变更规则、放行状态——明确本登记不构成 B1 放行，放行以 G03 补验证据为准）。
- i18n-map.md：新增「各页命名空间/文件归属终版（T00.9 冻结）」表——45 个旧叶子入口逐一登记任务/目标入口/命名空间/前端文件域；规则=目标路由 path 核心段 camelCase，三处业务命名例外（P06 carrierType 避免载具/车辆混淆、P35 taskReport 与 P36 faultAlarm 避免与 P34 dashboard 命名空间混淆）；P03/P31/P32/P34/P01/P42/P43 沿用现有目录，不制造平行实现；H01–H03 复用 common（D07 暂缓说明统一组件）。

### 5. T00 退出检查（A/I 编号公共部分证据汇总）

| 编号 | T00 公共部分证据 | 缺口（归属） |
| --- | --- | --- |
| A03 真登录 | 错误凭据真实反馈实证（1000010）；直通登录/伪管理员数据已删除 | 登录成功路径联验受阻 G03（P01 前补验） |
| A04 登录恢复与退出 | 持久化恢复实证（T00.4）；过期清理回登录机制实证；无虚构刷新接口（已删除） | 跨窗口退出真实双窗口复验（键名缺陷已修正） |
| A05 权限 | 17 场景真实浏览器实证（守卫/落点/独立页不公开） | 真实账号菜单树/root 身份/1001000 行为（G03） |
| A06 协议 | `/fms/v1` 路径/Result/业务码/取消/健康实证（T00.3） | 逐 endpoint DTO/分页联调随各页（G04） |
| A07 全表格替换 | apex-table-react@0.1.0 官方安装锁定；现存业务叶子全部 pending 不加载页面代码，应用内无违规表格渲染 | 真实表格消费随 P03/P05 起 |
| A08/A09 表格语义/状态 | 纯函数与适配器约定交付（类型探针全量组装实证） | 真实列表行为随 P03/P05 联验 |
| A10 请求竞争 | 取消静默/防乱序/失败清区机制落地（请求层+useStaticOptions+useMapGraph+表格内建） | 运行联验随消费页 |
| A11 查询失败 | 失败清区+StateBlock offline 显式重试机制落地 | 运行联验随消费页 |
| A12 实时生命周期 | useVisiblePolling 机制落地（可见串行/退避/恢复即查） | 运行联验随首个实时页（P34/P05/P39/P40） |
| A13 编辑草稿 | 脏登记/LRU 豁免/统一确认/beforeunload 9 组场景实证（T00.6） | 页面草稿真实内容随各编辑页 |
| A16 文件 | 独立传输生命周期/进度/取消边界/孤儿提示实证（T00.6） | 真实文件通道随 P08/P09/P25 |
| A17 地图及关联 | ReadOnlyMap 渲染/选点/回填/失效保留原值实证（T00.7） | 动画与带令牌联验随 P07（rAF 抑制/G03） |
| A20 语言、主题、终端 | 五语言切换全链路实证（T00.8）；亮暗主题随模板既有 token | 1366×768 桌面核查随页面任务交付 |
| A22 首页与链接 | D29 落点/无可用页反馈/别名守卫实证（T00.4） | 旧地址兼容重定向随 P34/P05 |
| A23 依赖与检查 | 官方 npm 版本+锁文件；本轮四项检查复跑通过 | — |
| I01 五语言入口与规范化 | 五语言切换/恢复/不误映射实证（T00.8） | — |
| I02 旧资源完整迁移 | 基座五命名空间四语言全量交付；映射规则与归属冻结 | 页面私有译文随各页补全（T00 盘点不代表通过） |
| I04 切换与草稿 | 切换不重载/菜单即时重算实证（T00.8） | 缓存页/表单/图表随页面任务联验 |
| I05 第三方与辅助文案 | antd/Apex/dayjs 五语言映射落地（typecheck 强制） | 控件弹窗按钮随首个消费者页面联验 |
| I06 回退与资源故障 | 页面私有分片回退简中不炸/非法残留归一化实证（T00.8） | — |
| I07 后端协同 | Accept-Language 实证（含上传通道单点 setRequestLanguage） | 后端导出文件语言边界随文件页登记 |

### 验证证据（T00.9，2026-09-17）

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 213 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过 |
| `pnpm check:structure` | ✅ 211 源文件通过 |
| `pnpm build` | ✅ 通过（tsc -b + vite build） |
| mock 退出运行路径 | ✅ 引用链实测 + pending 页不触发 lazy（见第 2 节） |
| 公共导出清点 | ✅ 与 contracts.md 一致（修正 commandConfirm.ts 记录笔误） |

无现场副作用操作；本轮无真实后端请求，无代码改动。

## 上一轮（T00.8）交付物与证据

### 1. 五语言类型与归一化（`src/i18n/i18n.ts` 重写）

- `SUPPORTED_LANGUAGES = ['zh-CN','zh-TW','en-US','ja-JP','ko-KR']`；`AppLanguage` 五语言联合类型，`supportedLngs` 同步。
- `normalizeLanguage` 精确归一化：`en*→en-US`、`ja*→ja-JP`、`ko*→ko-KR`；**繁中变体（zh-tw/zh-hk/zh-mo/zh-hant*）保留为 zh-TW 不再并入简中**；zh-cn/zh/zh-hans/未知一律回退 zh-CN。
- 懒加载后端按「语言 × 命名空间」查表：zh-CN 返回空资源（key 即文案）；查无 loader 的命名空间**返回空资源而非报错**，i18next 走 fallbackLng 自然回退简中（页面私有分片未交付时不炸页面）。
- `changeAppLanguage`：dayjs locale 五语言映射表（zh-cn/zh-tw/en/ja/ko 静态注册）、`document.documentElement.lang`、请求层 `setRequestLanguage`（含上传通道）同步；**预加载失败抛错且不改变任何语言状态**（失败保留原语言的机制基础）。

### 2. 四语言基座资源（新增 15 文件）

- `src/i18n/locales/{zh-TW,ja-JP,ko-KR}/{common,menu,auth,error,map}.ts`：五个基座命名空间 × 3 语言全量交付（common 81 键、menu 60 键、auth 8 键、error 7 键、map 6 键），key 集合与 en-US 一致（脚本校验）且四语言插值占位一致。
- 旧系统既有真译一律沿用不重新发明（`重试`→重試/再試行/재시도、`權限管理/権限管理/권한 관리`、`載具類型/キャリアタイプ/캐리어 유형`、`使用者管理/ユーザー管理/사용자 관리`、`即時看板/リアルタイムダッシュボード/실시간 대시보드`、`任務統計報表/タスク統計レポート/작업 통계 보고서`、`告警碼管理/アラームコード管理/알람 코드 관리`、`多地圖點邊組合/マルチマップノードエッジグループ/멀티 맵 노드-엣지 그룹`、`ロール管理/역할 관리`、`故障・アラート/고장 및 알림`）；旧资源中值=简中的占位键不构成既有译文，按新译补齐。
- zh-TW 采用台湾惯用语（登入/登出、網路、伺服器、儲存、設定、分頁、使用者）；「工艺」在 zh-TW 译「流程」（台湾「工藝」指手艺易歧义），ja/ko 译「工程/공정」；已登记 terminology.md。
- i18n-missing.md 中 StateBlock 7 键、T00.6 页签/传输 11 键、T00.7 命令/批量/选项失效 8 键、map 6 键的回退登记全部补齐移除；页面私有命名空间（profile/system/orderRecord/dashboard）四语言未交付，回退登记保留归页面任务。

### 3. 语言偏好单一真相源与一次迁移（`src/i18n/i18n.ts` + `src/store/store.ts`）

- `readStoredLanguage` 为持久语言偏好唯一读取入口：读 `apex-admin:lang`；无则读旧 key `umi_locale` **一次迁移**（写入新 key、删除旧 key），经 normalizeLanguage 归一化。
- settings 持久化 `migrate`：恢复的 locale **以独立语言 key 为单一真相源**（`locale: readStoredLanguage()`），持久化切片里的 locale 只是运行时镜像——修复「旧 key 刚迁移、镜像仍存旧语言时 App effect 把语言拉回旧值、覆盖迁移结果」的双来源冲突（浏览器验证中发现即修复）；theme 异常回退亮色。settingsSlice 导出 `SettingsState` 供 migrate 构造完整状态。
- 恢复路径统一经 normalizeLanguage 收敛后，早期残留的不受支持语言值（T00.7 验证曾注入 ja-JP 导致 useSuspense 挂起的场景）自然消除。

### 4. antd locale 与切换失败回滚（`src/App/App.tsx` + `src/layouts/.../Header/Header.tsx`）

- App.tsx：`ANTD_LOCALES: Record<AppLanguage, Locale>` 五语言映射（zh_CN/zh_TW/en_US/ja_JP/ko_KR），ConfigProvider 与 i18next、dayjs、html lang 同帧切换；语言切换**预加载失败时 catch 回滚** `localeChanged(normalizeLanguage(i18n.language))`，恢复「设置=实际」一致，不挂起、不留半翻译状态。
- Header.tsx：语言菜单扩五项，语言名用各自语言书写（简体中文/繁體中文/English/日本語/한국어），用户无需先懂当前界面语言即可识别。

### 验证证据（T00.8，2026-09-17，Vite dev + 内置浏览器；验证后 localStorage 测试数据已清除）

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 213 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过 |
| `pnpm check:structure` | ✅ 211 源文件通过 |
| `pnpm build` | ✅ 通过（tsc -b + vite build） |
| 五语言切换 | ✅ zh-CN→zh-TW→ja-JP→ko-KR→en-US 各轮：html lang 与持久化 key 同步、主导航菜单文案逐语言正确（繁中 儀表板/調度監控/流程設定…；日文 ダッシュボード/タスク管理/工程設定…；韩文 대시보드/작업 관리/공정 설정…；英文 Dashboard/Tasks/Process Configuration…） |
| 繁中/日/韩不误映射 | ✅ zh-TW 精确落位（html lang=zh-TW，非 zh-CN）；normalizeLanguage 变体规则经代码审查 + 注入值验证 |
| 旧真译沿用 | ✅ 繁中菜单「權限管理」、日文「権限管理」、韩文「권한 관리」等旧真译在真实页面呈现 |
| 页面私有命名空间回退 | ✅ ja-JP 下个人中心（profile 分片未交付四语言）回退简中，页面正常不炸 |
| 持久化恢复 | ✅ 切 en-US 刷新后保持 en-US，菜单英文正常懒加载 |
| umi_locale 一次迁移 | ✅ 注入 `umi_locale=zh-TW` + 删除新 key 后刷新：语言落位繁中、新 key 写入 zh-TW、旧 key 删除；**首轮验证暴露镜像覆盖缺陷（settings 持久化 locale 拉回 en-US），修复为单一真相源后复验通过** |
| 非法语言值恢复 | ✅ 持久化残留 `fr-FR`（模拟旧版本）刷新后归一化 zh-CN，页面正常渲染不挂起（T00.7 发现的 useSuspense 挂起边界收敛实证） |
| 日文登录页资源 | ✅ 登出后登录页日文完整（ディスパッチシステム/ユーザー名/パスワード/ログイン，auth+common 命名空间） |
| 退出后语言偏好保留 | ✅ 登出清理会话后 `apex-admin:lang=ja-JP` 保留（语言非会话数据） |
| antd locale 五语言弹窗按钮 | ⛔ 映射表全覆盖（typecheck 强制）+ zh-CN「登 录」空格特征实证；确认框按钮等控件文案随首个消费者页面（P03 等）真实联验 |
| 切换失败保留原语言 | ⚠️ 机制级：changeAppLanguage 预加载失败抛错不改状态 + App catch 回滚；真实网络失败场景未构造（IAB 无法可靠模拟资源加载失败），随页面任务联调覆盖 |

无现场副作用操作；登录页/工作台均为只读观测，未向后端发送写请求；验证用会话注入（root/administrator，与 T00.4/T00.6 同一方式）经登出与手动清除，localStorage 测试键（auth/settings/lang/umi_locale）全部移除，浏览器页签已关闭；dev server 已停止。

## 上一轮（T00.7）交付物与证据

### 1. 时间/时区展示工具（`src/utils/datetime/`）

- `deployDayjs.ts`：dayjs utc/timezone 插件全站单点装配，缺省时区固定为 `DEPLOY_TIMEZONE`（唯一定义点 `src/constants/datetime.ts`）；其他模块禁止自行 import 'dayjs/plugin/*'。
- `datetimeDisplay.ts` 纯函数：`parseBackendDateTime`（墙钟字符串 `yyyy-MM-dd HH:mm:ss` 按部署时区解释——已实证的 systemLogos createTime 形态；带偏移 ISO 换算到部署时区；纯日期=部署时区零点；毫秒时间戳；**无法识别返回 null 不猜**）；`formatInDeployTimezone`/`displayDateTime`（缺失统一 '—'，不显示空白）。

### 2. 统计空值/单位基础工具（`src/utils/stats/metricFormat.ts`）

- 规格 11.2 空值纪律的代码化：真实 0 显示 0、缺失/null/undefined → '—'（**绝不填 0**）、`safeRatio` 除零→null（不产生 Infinity/NaN/虚假 0%）、`formatPercent`（0→'0.0%' 与缺失区分）、`formatWithUnit`（缺失时单位不孤悬，单位词由调用方传 t() 结果）、`secondsToHours`（totalDurationSeconds 已证实秒→小时）。
- 具体指标公式（利用率分母、状态集合、日边界）归各报表页面 owner，本模块不做公式。

### 3. 控制确认/未知/批量反馈契约（`src/utils/command/`）

- `command.types.ts`：`CommandOutcome = 'accepted' | 'completed' | 'unknown'`（**命令已接受 ≠ 完成**，展示阶段与接口实际语义一致）+ `BatchItemOutcome`/`BatchOutcomeSummary`。
- `commandConfirm.ts`：`confirmCommand({title, targets, impact, danger, okLabel})` 统一破坏性操作确认——列前 5 个对象（余者"等 N 个对象"）+ 影响说明 + 固定附注"提交≠完成，以实际状态核实"；`summarizeBatchOutcomes` 只归纳真实逐项响应（成功/失败/未知），响应缺项计入未知不静默；`summarizeWholeBatch` 供整批结果诚实呈现（不编造逐项成功）。

### 4. 共享选项加载与失效处理（`src/hooks/useStaticOptions.ts` + `src/utils/options/optionFallback.ts`）

- `useStaticOptions(fetcher, enabled)`：全量只读选项（getSimpleVehicles/getAGVActions/getRoles/getElevators/getOrderTemplates/getSimpleMaps/getVehicleGroups/get*Drivers 等）统一加载——页签 scope 信号取消（切页/关页自动中止）、防乱序（仅最后一次响应落库）、**真正失败清空 options + error**（不留旧数据冒充成功）、取消静默、fetcher 经 ref 消费。
- `optionFallback.ts`：`matchOptionById`（失效→missing）、`missingOptionLabel`（保留原始标识 + 不可用说明）、`findMissingOptions`（保存前批量失效检测）；「不静默选第一项」是页面行为红线（DoD 10）。

### 5. 只读地图共享能力（`src/services/map/` + `src/components/ReadOnlyMap/`，11 文件）

- `map.service.ts`：`fetchMapGraph`（`GET /dispatcher/map/getMapInfo?mapId=`——**响应 schema 未在 OpenAPI 定义，按旧可达实现解析 `data.currentMapInfoVersion.mapJson.{nodes,edges}` 并登记联调差异点**；结构异常抛错，不以空画布冒充成功）；`fetchSimpleMaps`（`GET /dispatcher/map/getSimpleMaps`，过滤缺 mapId 条目）。
- `ReadOnlyMap.tsx`（forwardRef + 默认导出供 React.lazy）：数据供给二选一（mapId 内部加载 / graph 外部直供）；三层渲染（路径视觉层 listening=false + 路径加宽命中层 + 节点层 sceneFunc 几何绘制）；**能力边界=只读渲染/平移缩放/选点/定位高亮/回填**，不引入地图编辑、监控、回放、车辆图层（H01/H02 范围）；失败态组件内闭环（StateBlock offline + 显式重试）；`ref` 暴露 fitView/zoom/选中/定位/reload。
- 纯函数层：`mapGraphGeometry`（贝塞尔/箭头/标签）、`mapGraphMount`（y 取反挂载、正反向样式）、`mapGraphStyles`（节点类型色/选中青环/命中区宽度，历史拼写 warehouse_font 显式保留）；hooks：`useMapSelection`（选中 Set + 完整 DTO 回填、selectMode 变化清不匹配项、失效 ID 回显静默忽略）、`useMapStageInteraction`（以指针为中心缩放/fitView/定位动画）、`useMapBlink`（定位闪烁）、`useMapGraph`（数据加载：取消静默/防乱序/失败清区带错误文本）。
- i18n：共享 `map` 命名空间（en-US 6 键已交付，`i18n.ts` 懒加载表已注册）；消费页面在路由 meta.i18nNamespaces 声明 `'map'`。

### 6. 依赖变更（T00 为依赖 owner，记录版本与原因）

| 包 | 变更 | 原因 |
| --- | --- | --- |
| react / react-dom | ^19.2.8 → ^19.3.0（minor） | react-konva@19.3.0 peer 要求 react ^19.3.0 |
| konva | 新增 ^**9.3.20**（非 latest 10.5.0） | react-konva peer 支持 ^9；**与旧系统实装版本一致**（SPEC_node_mapping_map_picker §2.3）；10.5.0 未在本轮完成等价正面验证，不无控制跟随发布（规格 §1） |
| react-konva | 新增 ^19.3.0 | React 19 官方适配版（react-reconciler 0.34 + scheduler 0.28） |
| @types/react | ^19.2.x → 19.3.0 | 与 react 同步 |
| vite.config.ts | optimizeDeps.include: ['react-konva'] | 预声明优化：避免首个消费者触发"动态发现 → 整页二次 reload"；本轮曾实测陈旧预打包产物导致双 React 实例（Invalid hook call），预声明后消除 |

### 验证证据（T00.7，2026-09-17）

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 198 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过 |
| `pnpm check:structure` | ✅ 196 源文件通过 |
| `pnpm build` | ✅ 通过（tsc -b + vite build） |
| 纯函数断言（真实模块经 Vite dev 动态加载，26 项） | ✅ 全部通过：挂载转换 6 项（y 取反/样式/箭头/标签/反向红/控制点归一）、时间 5 项（墙钟=部署时区/带偏移换算/纯日期零点/不猜 null/缺失占位）、统计 7 项（0 正常/缺失 '—'/除零 null/比值/百分比 0 与缺失/单位不孤悬/秒转时）、选项失效 4 项（命中/missing/保留原值标签/批量检测）、命令契约 4 项（三态归纳/缺项计未知/整批不编造） |
| 只读地图渲染（React 19.3.0 + react-konva 19.3.0 + konva 9.3.20 真实实例化） | ✅ 三层 canvas 真实绘制：路径层（正向灰 #BDBDBD + 反向红 #E57373）、命中层（不可见）、节点层（work 蓝/charge 绿/park 红 + n3 选中青环 #00E5FF 像素级确认） |
| 选中回显与回填 | ✅ `initialSelectedIds:['n3']` → onChange 携带完整 DTO（id/name/type/x/y/mapId）；pointerdown/up 命中节点 → onChange 回填完整对象（选点→回填链路端到端） |
| hitFunc 命中区 | ✅ 手动 drawHit 后 hit canvas 出现 colorKey 像素（命中检测正常；此前 0 像素系 IAB 环境 rAF 抑制导致 batchDraw 延迟，非代码缺陷） |
| i18n map 命名空间 | ✅ zh-CN 下 loadNamespaces 完成、t('节点') 等 key 即文案；en-US/map.ts 已注册懒加载 |
| fitView / 定位闪烁 / 滚轮缩放动画 | ⛔ 待 P07 真实浏览器联验：IAB 自动化环境 rAF 被抑制（实测 requestAnimationFrame 不执行），动画类无法自动化；场景渲染/命中/回填已用同步路径实证 |
| getMapInfo / getSimpleMaps 带令牌真实联验 | ⛔ 受阻：登录凭据（G03）；getMapInfo 响应 schema 未在 OpenAPI 定义，联调时逐字段核实并登记差异 |

无现场副作用操作；机制验证以真实模块派发（无 mock、无演示页入库），探针文件验证后删除（src/devProbeT00Map.tsx），测试注入数据未入 redux-persist 持久化（验证后清理语言与设置键），不发送任何真实写请求（地图数据为构造几何，仅机制验证；graph 模式不发请求）。

## 上一轮（T00.6）交付物与证据

### 1. 脏状态保护（`tabsSlice` + `useTabDirtyGuard`）

- `TabEntry` 增加 `dirty`/`dirtyLabel`：页面经 `useTabDirtyGuard(isDirty, label?)` 同步草稿状态到所属页签；草稿数据本身留在页面组件内存（Activity 隐藏不丢），标记只是跨页签元数据；组件卸载（关页/淘汰/刷新重建）cleanup 自动清标记，杜绝「幽灵脏页签」。
- LRU 淘汰跳过 dirty 页签（规格 8.1：草稿不能静默丢弃）：宁可临时超出 `PAGE_CACHE_MAX_ENTRIES=10` 也不淘汰正在编辑的页签。程序化验证：12→13 页签越过上限时脏页签保留、最旧干净页签淘汰、缓存 11（超限暂存）。
- 顺带清理 `tabsSlice` 内预先存在的重复 `TabsState` 接口声明（接口合并死代码，owner 文件内缺陷修复）。

### 2. 统一动作确认（`useTabActionGuard` + `beforeunload`）

- 关闭当前/其他/左侧/右侧/全部、刷新当前页签、退出登录一律经 `useTabActionGuard`：列出受影响脏页签（路由标题 + 脏对象说明）后才执行；TabsBar 的批量动作按「真实移除范围」（与 reducer 保留语义严格对应）计算受影响集合，无旁路。
- 传输提示三档策略：`none`（刷新不影响传输）、`continue-after-close`（关页传输继续 + 完成消息提示）、`terminate`（登出本机终止，注明服务端处理不保证已撤销）。Header 登出确认复用同一 hook（extraContent 保留既有提示文案）。
- BasicLayout 挂 `beforeunload`：仅存在脏页签时监听（preventDefault + returnValue），无脏不挂。对照实验：置脏后 `reload` 被拦下（store 状态未变=页面未重载）；清脏后正常重载（新文档、持久化会话恢复）。

### 3. 可见串行轮询（`useVisiblePolling` + `polling.constants`）

- 契约：页签激活（Activity visible）+ 文档可见才刷新；串行（一次请求完成后约 5 秒发起下一次，间隔自完成时刻起算，不并发堆积）；重新可见/激活立即刷新（恢复即查）；失败 ×2 退避至 60s 上限、成功复位；主动取消（scope 信号中止）静默停止不算失败；后台保留最后快照。
- 配置集中定义（`POLL_BASE_INTERVAL_MS=5000`/`POLL_BACKOFF_MULTIPLIER=2`/`POLL_MAX_BACKOFF_MS=60000`），实时页面禁止私设定时器；`refresh` 必须只读查询并透传 signal；refresh 引用经 ref 消费（页面内联闭包不重启轮询循环，语言切换到点的请求自然携带新 Accept-Language）。
- 运行时行为待首个实时页消费者（P34/P05/P39/P40）联验——机制交付轮无实时页面可挂载，与 T00.5 表格联验同一衔接模式。

### 4. 独立传输生命周期（`transferManager` + `useTransfers` + `TransferWatcher`）

- 模块级单例持有 AbortController，不挂页面 scope：切页（Activity 隐藏、effect 清理）、关页、LRU 淘汰不误杀传输；普通查询取消（RequestScopeProvider）与传输互不影响（规格 10.3）。`beginTransfer` 返回句柄：`signal` 传请求层、`setProgress(loaded, total)`（total 未知 = null 不确定进度）、`markProcessing()`（字节 100% ≠ 完成）、`succeed()/fail(reason)` 依业务结果判定、`cancel()` 仅本机终止（aborted ≠ 服务端回滚，文案注明）。
- 状态追踪三入口：`useTransfers(tabKey?)`（页面进度 UI 订阅，useSyncExternalStore，快照引用稳定）、`findActiveTransfersIn`（关闭确认一次性查询）、`TransferWatcher`（BasicLayout 内零 UI 桥）：孤儿传输（承载页签已关/不隶属页签）终态以一次性 antd message 提示——**不新增用户可见全局任务中心**；终态记录 15 秒 TTL 自动清理。
- 会话结束清理：`isAuthenticated=false` 时终止全部进行中传输并清空全部记录（含终态残留），登出确认框列明影响（规格 5.3 用户数据清理）。

### 5. 实体页签与独立窗口

- 实体隔离机制 = 页签 key 含规范化 search（参数名稳定排序）：`/order-info?id=1001` 与 `?id=1002` 自动获得独立页签、独立 Activity 缓存实例与独立请求 scope；同参数复用同一页签。浏览器实证：同路由不同 id → 两个独立页签（cached 各自为 true）。工作区页签默认形态要求实体路由位于受保护根内——P38/P39/P40 接入时由统筹应用 definitions.tsx 差异（当前布局外路由即独立窗口/全屏形态）。
- `openStandaloneWindow(path, params?, options?)`：仅站内绝对路径；「路由+排序参数」命名窗口（同实体复用、异实体独立）；居中 1280×800 popup；成功后切断 opener（不能用 noopener——其下 window.open 恒返回 null 无法检测拦截）。浏览器实证：独立窗口打开 `/order-info?id=88`，共享持久化会话经同一认证+权限守卫放行，渲染迁移占位（P38 完成后即完整详情）。

### 验证证据（T00.6，2026-09-17）

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 176 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过 |
| `pnpm check:structure` | ✅ 174 源文件通过 |
| `pnpm build` | ✅ 通过（tsc -b + vite build） |
| 实体页签隔离 | ✅ 同路由 `?id=1001`/`?id=1002` → 两个独立页签，独立 cached 实例（DOM 与 store 一致） |
| 脏页签关闭确认 | ✅ 点关闭弹「确认继续关闭页签？」并列出「任务管理（机制验证草稿）」，页签未被直接关闭；取消后保留 |
| 脏+传输组合确认 | ✅ 弹窗同时列出脏页签与「这些页签仍有正在进行的传输：关闭后传输会继续…」 |
| 确认关闭后传输继续 | ✅ 页签关闭，传输 phase 仍 transferring（Activity/关页不误杀）；完成后孤儿 toast「"机制验证.bin" 传输完成」（一次性，非任务中心） |
| beforeunload 离开提示 | ✅ 对照实验：置脏刷新被拦（store 未重置）；清脏后正常重载（新文档、持久化会话恢复）。IAB 环境自动处理原生对话框，拦截行为经状态对照实证 |
| LRU 不淘汰脏页签 | ✅ 应用主 store（整页重载后）与全新模块实例双重验证：脏页签保留、最旧干净页签淘汰、缓存超限暂存（11>10） |
| 独立窗口 | ✅ 打开 `/order-info?id=88`：独立窗口加载应用、守卫放行（共享持久化会话）、渲染迁移占位 |
| 登出清理链路 | ✅ sessionExpired → 跳登录（带回跳）、传输 signal.aborted=true、管理器记录清空（含终态残留）、测试令牌从持久化清除 |
| useVisiblePolling 运行时 | ⛔ 待首个实时页联验（P34/P05/P39/P40）：无消费者可挂载，机制交付轮不冒充现场验收 |

无现场副作用操作；机制验证通过浏览器控制台以**真实模块/真实 reducer/真实管理器 API** 派发动作完成（无 mock、无测试代码入库），测试会话（root 注入）经登出链路清除，浏览器页签已关闭。

**验证环境说明**：测试过程中曾向页面注入 `import('/src/main.tsx')` 探针导致应用双重挂载（页签出现异常数量）——属验证环境污染，刷新后消失；后续全部验证在干净文档执行。LRU 首次复测因测试时序错误（脏标记置于淘汰之后）与失效模块回路出现假阴性，经全新模块实例 + 重载后主 store 双重复测确认代码正确。

## 上一轮（T00.5）交付物与证据

### 1. 五语言 ApexLocale 包（`src/i18n/locales/apexTable/` + `src/hooks/useApexLocale.ts`）

- 包 `apex-table-react` 内置仅 zhCN；en-US/zh-TW/ja-JP/ko-KR 完整文案（58 key + 6 格式化函数）随 T00.5 交付，`ApexLocale` 类型强制全 key 覆盖——首轮 typecheck 实际捕获 `resetLayout`/`clearSelection` 漏译，类型护栏有效。
- 简中不复制第二份：`resolveApexLocale` 对 zh-CN 直接返回包内 zhCN（包升级文案自动同步）；繁中按 zh-tw/zh-hk/zh-hant 前缀匹配；未知语言回退简中（与 i18next fallbackLng 一致）。
- `useApexLocale()` 订阅 i18next 语言事件：切语言表格控件文案即时重算，不刷新页面（DoD 11）。不包裹表格，页面 `<ApexTableReact locale={useApexLocale()}>` 直接消费。

### 2. 主题变量映射（`src/styles/globals.css` 追加节）

- `:root .apex-table` 把 `--apex-table-*` 全量接到 `--app-*` 令牌（bg=弹层实底、表头=品牌蓝 5% 混色、文本/静音/强调/hover/选中/边框/圆角=control 7px、字体/字号/内边距=全局节奏），亮暗随 data-theme 同帧切换；暗色另覆盖 pinned-shadow 深色投影。
- 选择器特异性高于包默认值：包 CSS 随页面懒加载可能晚于 globals.css 注入，同特异性会被反超，故加 `:root` 前缀。
- 弹层（列设置 dialog）自带 `.apex-table` class，映射对弹层同样生效（源码核验：`.apex-table.apex-table-settings-modal`）；未列出的 icon-size/overlay-z-index 沿用包默认，不重复维护。单表微调走组件 style prop（包公开能力），不改本映射。

### 3. 分页换算与行 ID 纯函数（`src/utils/table/`）

- `tablePaging.ts`：`toBackendPage(pageIndex, pageSize)` 零基→一基换算（`toBackendPageNo`/`toBackendPageSize` 单独可用），非整数/负值直接抛中文错误，宁可在开发期暴露配置错误；未知总数不填 0 的责任留给页面（JSDoc 明示，DoD 5）。
- `rowId.ts`：`stringFieldRowId(field)` 工厂返回 getRowId 回调；字段空值抛错（提前定位「哪页哪字段没配好」，避免运行期 duplicateRowId 模糊报错）；不安全整数（>2^53−1）抛错提示 int64 必须字符串承载（G10），不静默产出错乱选择/展开。

### 4. 列偏好官方适配器接入约定（`src/utils/table/columnPreferences.ts` + `src/hooks/useColumnPreferences.ts`）

- `createTableColumnPreferences({tableId, userId, schemaVersion?})`：统一 namespace='apex-admin'、tenantId='default'（后端无租户概念，占位满足官方必填）、schemaVersion 默认 '1'（列结构变更须提升）；空身份中文报错前置。返回的就是官方适配器实例，形状与行为零改动——是参数约定，不是二次封装。
- `useColumnPreferences(tableId)`：会话取 username 做 userId（无假身份，未登录返回 null 诚实降级）；effect 创建/cleanup flush+dispose（StrictMode 安全）；适配器 300ms 防抖写 localStorage。
- 页面接线四步（load 需运行时列实例 → 晚一帧应用属预期）写入文件头 JSDoc：创建 → `prefs.load({columns: tableRef.current.getAllLeafColumns()})` 受控初值 → onChange setState+save → `prefs.clear()` 重置。

### 5. 统一状态块（`src/components/StateBlock/`）

- 三语义变体（DoD 3/6 状态区分）：`noPermission`（ShieldOff）/`gap`（CircleSlash，缺口禁用+原因）/`offline`（CloudOff，失败清区+显式重试按钮，仅在提供 onRetry 时出现——不自动重发）；与「真实空数据」严格区分（表格空态仍由包内建）。
- 独立组件不包裹表格；文案 common 命名空间（zh-CN key 即文案，en-US 7 键已交付）；zh-TW/ja/ko 回退简中已登记 `i18n-missing.md`（T00.8 建基座时补齐）。

### 6. 公开 API 核验（类型探针全量组装实证后删除）

临时探针以「页面任务将采用的直接组装方式」一次性通过 `tsc -b`：request 模式 + 分页换算 + getRowId 工厂 + locale hook + `virtualization="auto"` + `expandable.expandedRowRender` + 列设置/调宽/分页配置 + 双 ref（`ref`=ApexTableRef：focus/scrollToRow/reload；`tableRef`=ApexTableInstance：getAllLeafColumns 等实例方法）+ 列偏好 `load({columns})` 类型兼容。核验结论已登记 contracts.md 第 3 节；探针验证后删除，未提交。核验中确认的包行为：`editable`/`onDataChange` 仅 data 模式（类型约束）；弹层默认渲染在表格 DOM 内（主题变量继承安全）；适配器 load 对未知列 id 自动过滤、schemaVersion 不匹配走 migrate 或回退默认。

### 验证证据（T00.5，2026-09-17）

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 167 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过（含五语言 ApexLocale 全 key 强制、探针全量组装） |
| `pnpm check:structure` | ✅ 165 源文件通过 |
| `pnpm build` | ✅ 通过（tsc -b + vite build） |
| 类型探针（页面对接方式预演） | ✅ 通过后删除；捕获并修复语言包缺 key、ref/tableRef 双 prop 误用两处认知偏差 |
| 真实只读列表 Apex request 联验 | ⛔ 受阻：登录成功路径需测试凭据（G03）；且放行证据要求落在 P03/P05 既有入口（页面任务），已在下方「未完成」列明衔接方式 |

无现场副作用操作；本轮无真实后端请求（机制交付轮），无 mock、无演示页。

## 上一轮（T00.4）交付物与证据

### 1. 权限码契约（`src/constants/auth/permission.constants.ts`，新增）

- `PERM`（菜单码 44 项）/ `PERM_BUTTON`（按钮码全量）/ `ROOT_ONLY_CODES`（auth:manage、auth:user:view、auth:role:view），码值与旧项目 `constants/permission.ts` 逐一核实对齐，不在迁移期改写任何码值。
- 不再维护旧式前端菜单结构镜像（MENU_TREE）：菜单层级唯一真相源是 `definitions.tsx`，菜单码以 `meta.perm` 挂接，祖先填充基于定义树推导。
- 历史码值交叉显式保留并注释：任务工艺页（mission-create）挂 `mission-flow:view`，工艺管理页（mission-flow）挂 `mission-template:view`；不凭页面名称"纠正"后端真实码。

### 2. 权限判定（`src/utils/auth/permission.ts` + `src/router/routeAccess.ts`，新增）

- 纯函数：`isRootUser`（root/administrator 集中管理，规格 5.8/D19）、`flattenMenuCodes`（仅收 MENU 码，对齐旧 flattenPermissionCodes 默认行为）、`hasButtonCode`。
- 路由访问核心：`buildAccessContext`（超管短路 + 祖先填充 − ROOT_ONLY）、`hasMenuAccess`（未声明 perm = 登录即可达，对齐旧 Umi「未写 access」语义，规格 5.12）、`isLeafAvailable`、`resolveLandingPath`（activated=false → 授权页 → 首页可用优先 → 首个可用业务页 → 无权限页，D29）、`resolveDirectoryLandingPath`（目录索引不指向无权限/未迁移页）、`resolveSafeRedirectPath`（深链接回跳仅限站内+存在+可用，规格 5.11）。
- 按钮码 hook `src/hooks/usePermission.ts`：hasPerm(code) 超管短路；PERM_BUTTON 字面量类型约束，页面任务消费。

### 3. 路由树与守卫（definitions.tsx / guard.ts / projections.tsx / router.types.ts / redirect.ts）

- 定义树全量挂 `meta.perm`（含分组目录码）；`migrationPending` 标记全部未完成页面任务的业务叶子；`public` 仅登录页与显式 404。
- 守卫三类：认证+权限守卫（perm 校验失败 → /no-permission，目录持码整树拦截）、index 动态落点 loader（受保护根按 D29 / 目录取首个可用子叶）、别名 redirect 守卫（先验权限再跳转，server-resource 别名与全屏页同码）。
- **独立页不再公开**：order-info（order-record:view）/ vehicle-info（vehicle-list:view）/ server-resource-monitor（同码）/ authorize-ingress / no-permission 全部挂认证+权限守卫（规格 5.6 / baseline 特殊权限点 2/4/5）。
- 目录 index 的静态 redirect 移除，改为动态解析（accessRoutes loader 与 renderRoutes 兜底组件同规则）。
- renderRoutes/accessRoutes 对 pending 页渲染统一 `MigrationPending` 占位（`src/components/MigrationPending/`，新增）：**不加载页面代码**（order.mock/dashboard.mock 页面组件退出运行路径，T00.4 退出检查"过渡未迁移页无 mock 请求"落实；mock 文件本体仍归 P03/P34 移除）。
- `buildMenuRoutes(ctx)` 签名变更为按访问上下文剪枝；DockMenu 按 auth 重算菜单；affix 播种（BasicLayout）按权限+迁移状态过滤（迁移期合并首页不播种常驻页签）。
- LoginForm 登录成功改用 `resolveSafeRedirectPath` + `resolveLandingPath`（替换模板 FALLBACK_PATH 直跳）。

### 4. 连带修复的 T00.3 缺陷（本轮浏览器联调发现）

1. **持久化恢复失效**：store.ts 自定义 `migrate` 无条件重置会话——redux-persist v6 的自定义 migrate 在每次 rehydrate 都执行（非仅版本不匹配时），导致刷新后恢复的会话被重置为未登录。修复为按存储版本分流（<v2 重置、v2 原样恢复）。**该缺陷此前未暴露因登录成功路径缺凭据，持久化恢复从未真实发生**。
2. **多窗口退出同步失效**：authBridge 监听键 `apex-admin:auth` 缺 redux-persist 默认 keyPrefix `persist:`，实际写盘键为 `persist:apex-admin:auth`，监听从未命中。已修正键名。
3. 落点候选未排除公开/辅助页（T00.4 自身缺陷，联调中发现即修复）：root 落点被 DFS 解析到 /login 形成回跳循环；已增加 hideInMenu/public 排除（isLandingCandidate）。

### 真实浏览器验证证据（2026-09-17，Vite dev server + 内置浏览器，未注入任何 mock）

**守卫/权限（17 场景全部通过）**：

| 场景 | 结果 |
| --- | --- |
| 未登录直访 /order-record、/order-info、/vehicle-info、/no-permission | ✅ 重定向 /login?redirect=<原路径>（独立详情页此前无守卫，本轮闭合） |
| 未登录访问 /404 | ✅ 公开可达 |
| 注入真实格式的普通用户会话（无权限）访问 root 专属页、任务管理、载具类型（父码拦截）、服务器资源别名 | ✅ 一律 /no-permission |
| 普通用户持 order-record:view 权限树访问 /order-record | ✅ 停留并渲染迁移占位（OrderRecord 页面代码未加载——mock 无请求） |
| root 访问用户管理、车辆管理目录、服务器资源别名 | ✅ 全部放行；别名 redirect 全屏页渲染迁移占位 |
| 落点：activated=false 访问 / | ✅ /authorize-ingress（D29） |
| 落点：root 与普通用户访问 / | ✅ 全部业务页迁移中 → /no-permission 明确反馈（修复落点解析缺陷后） |
| 会话持久化恢复 | ✅ 注入 v2 会话刷新后恢复（migrate 修复实证）；redux-persist 存储格式（键前缀 persist: + 值二次序列化）经源码与运行时双重确认 |

**说明**：权限语义与守卫行为已可复核；带真实后端账号的登录-权限端到端联验（菜单树真实形状、root/administrator 真实身份、1001000 行为）仍待测试凭据（G03，B1 放行前补验）。

## 上一轮（T00.3）交付物与证据

### 1. 请求层重写（`src/services/request/`，owner T00）

- **Result 解包**：HTTP 成功且业务 `code=200` 才进入成功分支，返回 `data`；不依赖 `message === 'success'`。非 Result 形状的 JSON 按 `CLIENT.MALFORMED_RESPONSE` 报错，不猜数据。
- **业务码常量**（`RESULT_CODES`）：`200` 成功、`1000000` 会话失效、`1001000` 未授权（旧代码证据，待真实复核）、`1000010` 登录失败。
- **会话失效收敛（单飞）**：1000000（或未预期的 HTTP 401）→ 清令牌 + 一次性 `message.warning` + 派发 `sessionExpired`，跳转由 BasicLayout 既有守卫统一处理；并发失效只处理一次，无重定向风暴；换新令牌自动复位。
- **取消静默**：AbortSignal 主动取消不报错、不判离线、不参与健康统计。
- **健康反馈**：有真实后端响应（含业务拒绝）= ok；无响应/HTML/网关级错误 = error；不得把业务拒绝当离线（规格 4.2）。
- **文件通道**：非 JSON content-type 响应原样透传 Blob，绝不套 Result 解包。
- **移除刷新重放**：`/auth/refresh` 单飞、`_retriedAfterRefresh`、401 重放逻辑全部删除（G01/G02）——后端无刷新协议，过期即重新登录。
- **认证与语言头**：`Authorization: Bearer <token>`（请求层单点）+ `Accept-Language`（i18n 初始化/切换时经 `setRequestLanguage` 同步）；请求层文案全部经 i18next 翻译（直接依赖 i18next 包，避免与 `@/i18n/i18n` 循环导入）。
- **类型**：`ResultDto<T>`、`BackendPageQuery`（pageNo/pageSize）、`BackendPageResult`（records/current/size/total/pages）、`ApiError.businessCode`；模板遗留类型（EntityStatus/PageQuery/PageResult）保留并标注，随 P03/P31/P32/P34 替换后删除。

### 2. 真实登录/登出（`src/services/auth/`）

- `login`：`POST /auth/authorize/login`，密码 MD5（spark-md5@3.0.2，auth.service 单点，G03 旧代码证据）；返回 `UserAuth` 一次性构建会话（token/activated/user/roles/permissions/permissionsTree），空字段回退诚实默认值。
- `logout`：`POST /auth/authorize/logout`（OpenAPI 已核实存在），尽力通知，失败继续本地登出。
- **删除**：直通登录（伪造 super_admin）、`loadSession`（`/users/me`）、旧 `/auth/logout` 路径；`profile.service`（`/users/me` 读写）整目录删除。
- `authBridge`（新增）：store 订阅把 `auth.token` 单向同步到请求层（登录/登出/过期/rehydrate 统一经此）；`storage` 事件监听 `apex-admin:auth` 键实现多窗口同步退出（登录不同步，规格只要求退出）；接入 `main.tsx`。

### 3. 会话持久化（`authSlice`/`store`/`auth.types`）

- `AuthState` 扩展为 token/activated/user/roles/permissions/permissionsTree，全量持久化（whitelist）；**auth schema v1→v2**：模板直通登录时代的旧会话迁移为未登录态，强制真实登录重建。
- `AuthUser` 对齐真实 `UserSummary`：id（int64 字符串承载，G10）、username、state（ENABLED/DISABLED）、level、initials；显示名取用户名（后端无独立字段）；删除虚构的 email/roleNames 字段，角色码提升到会话级。
- `AuthSession` 含 token/activated/roles/permissions/permissionsTree；`AuthPermissionDto` 为权限树权威定义（T00.4 消费）。

### 4. profile 域诚实化（虚构接口移除的落点）

- Profile 页展示真实会话信息（用户名/状态/等级/角色码）；ProfileForm 改为「资料编辑暂不可用」说明（G12 缺口，P43 收口），不再发起任何请求；en-US profile/common 分片同步更新。

### 真实接口联调证据（2026-09-17，`http://10.11.2.67:8888`）

| 请求 | 结果 | 证实内容 |
| --- | --- | --- |
| `POST /fms/v1/auth/authorize/login`（错误凭据，明文与 MD5 两种形态） | HTTP 200 + `{code:1000010, message:"用户名或密码错误", data:null}` | 端点路径、Result 包装、业务码 1000010、HTTP 200 + 业务码非 200 的错误表达 |
| `GET /fms/v1/auth/user/pageUsers`（无令牌） | HTTP 200 + `{code:1000000, message:"用户未登录"}` | 业务码 1000000 = 会话失效（与旧代码证据一致），1000000 处理策略落地依据 |
| `GET /fms/v1/systemLogos`（无令牌） | HTTP 200 + `{code:200,...,data:[...]}`；`createTime` 为 `yyyy-MM-dd HH:mm:ss` 字符串 | code=200 成功判定；响应内时间格式（非 ISO date-time） |
| `POST /fms/v1/auth/authorize/logout`（无令牌） | HTTP 200 + `{code:200}` | 登出端点真实存在且路径正确 |
| 经 Vite 代理（localhost:5173 → 10.11.2.67:8888）+ `Accept-Language: en-US` 登录 | 业务码 1000010，message 为英文 | T00.2 代理链路端到端可用；**后端按 Accept-Language 返回对应语言文案（I07 真实证据）** |

**受阻范围（外部条件）**：~~真实登录**成功**路径、MD5 摘要与 Bearer 头的最终确认、授权只读接口（带令牌）联验、业务码 1001000 真实行为~~ → **G03 已于 2026-09-18 全部补验关闭**（见下表）；带令牌业务只读转受 G17（系统未激活）阻塞。

### 真实接口联调证据（2026-09-18，G03 补验轮，`http://10.11.2.67:8888`，凭据来源 `.env.local`）

| 请求 | 结果 | 证实内容 |
| --- | --- | --- |
| `POST /auth/authorize/login`（root + **MD5 密码**） | HTTP 200 + `{code:200, data:{token, activated:false, user, roles, permissions, permissionsTree}}` | **登录成功路径实证**；UserAuth 六字段与前端 DTO 完全一致；user={id:2, username:root, state:ENABLED, level:2}；roles=[test,user,test001]；permissions 155 个；permissionsTree 树形带 childPermissions（root 真实身份确认，A05 证据） |
| `POST /auth/authorize/login`（root + **明文密码**） | HTTP 200 + `{code:1000010, message:"用户名或密码错误"}` | **MD5 确认为必需形态**（同账号明文失败、MD5 成功）——G03 密码摘要疑点关闭 |
| `GET /auth/user/pageUsers`（带 Bearer 令牌） | HTTP 200 + `{code:1001000, message:"系统没有被激活"}` | **Bearer 令牌被识别**（对照无令牌返回 1000000）；**1001000 实证=系统未激活**（软件授权状态，非登录态/权限问题）——G03 语义疑点关闭；`request.constants.ts` 注释同步实证结论 |
| `GET /auth/user/pageUsers`（无令牌，复测） | HTTP 200 + `{code:1000000, message:"用户未登录"}` | 与带令牌形成对照，Bearer 识别证据成立 |
| `GET /fms/v1/systemLogos`（带令牌） | `{code:200, data:[...]}` | 公开接口不受激活限制，带令牌同样可用 |
| `GET /auth/license/getHardwareInfo`（带/无令牌各一次） | 均 `{code:200, data:<Base64 硬件串>}` | **GET 方法确认**（OpenAPI 声明 GET，POST 实测 500）；该接口不需认证（激活入口的合理设计），不能作为授权只读证据；P02 可用性证据 |

**G03 关闭后新增阻塞 G17**：联调服务器 `activated:false`，带令牌业务接口全部返回 1001000——T00 放行证据「授权只读可用」未取得。softwareActivation 属副作用操作不自动执行；解除条件：用户激活联调环境或提供已激活后端（登记 `gaps.md` G17）。

## 上一轮（T00.2）交付物与证据

1. **npm latest 重核与安装**：antd latest = 6.6.4（项目 `^6.6.1` 兼容）；`apex-table-react@0.1.0` 安装成功并提交 pnpm 锁文件（peerDeps react ^18‖^19 兼容 React 19）。类型兼容性以临时探针验证（引用 `ApexTableReact`/`zhCN` 过 `tsc -b` 全量），验证后探针已删除，不留演示代码。
2. **dev 代理与前缀契约**：`vite.config.ts` 默认目标 `http://localhost:8000` → `http://10.11.2.67:8888`（`APEX_DEV_PROXY_TARGET` 可覆盖）；代理键 `/api` → `/fms`，**不做 rewrite**，上游最终路径准确为 `/fms/v1/...`；`request.constants.ts` 的 `DEFAULT_API_BASE_URL` `/api/v1` → `/fms/v1`（`VITE_API_BASE_URL` 覆盖机制保留）。生产由同源反向代理转发 `/fms`。
3. **部署时区**：`src/vite-env.d.ts` 登记 `VITE_DEPLOY_TIMEZONE`；新增 `src/constants/datetime.ts` 定义 `DEPLOY_TIMEZONE`（缺省 `Asia/Shanghai`），为全站唯一定义点；dayjs 展示工具 T00.7 接入消费。
4. **check:structure 修复（G16）**：新增 `scripts/check-structure.mjs`（规则依据 CLAUDE.md）：禁 `index.tsx`；`@/` 唯一别名（禁多级相对导入与伪绝对导入）；导入方向门禁（services→组件层、components|hooks→pages|features、跨 features 业务域穿透）；业务源码禁硬编码 `http(s)/ws(s)` URL 与 IPv4。**违规注入自测**：5 类违规样本（深层相对/伪绝对/services→features/跨 feature/硬编码 IP URL/index.tsx）逐一被抓后清理；期间修复两处脚本缺陷（同目录相对导入误报伪绝对、跨 feature 因一级目录判定漏报）。
5. **README/CLAUDE.md 失效依据修正**：全仓库确认 `docs/macos_ui_ux_design_guide_v3.md` **不存在**（勘误：T00.1 曾登记「文件现存」，不实）；README/CLAUDE.md 引用改为「以现有布局/token 为视觉基线」，代理与基础路径描述同步更新。

## 上一轮（T00.1）交付物与证据

1. **OpenAPI 核验**：`C:\yangwenhua\ApexTableReact\default_OpenAPI.json` SHA-256 = `A82E9B5F6EF5E564DEEE16E4E74713B28FCCB35CD0A651E3631CFD9D63D49C7C`（与规格一致）；238 operation / 408 schema 脚本实测一致。endpoint 全表 → `docs/migration/openapi-inventory.md`。
2. **45 个旧叶子清点与目标映射** → `docs/migration/baseline.md` 第 2 节（含目录/重定向结构、目标树现状核对：全部叶子已有路由节点）。特殊权限点（载具类型/数据库备份无 access、order-info/vehicle-info 复用码、服务器资源别名）→ baseline 第 5 节。
3. **历史实现排除登记** → baseline 第 6 节（Elevator/AutoDoor 历史、ConveyorLine、sysAction、重复用户角色页等）。
4. **检查与依赖故障登记** → baseline 第 7/10 节。
5. **台账初始化**：`gaps.md`（G01–G16）→ `contracts.md` → `operations.md` → `i18n-map.md` / `i18n-missing.md` / `terminology.md` → `RUN_STATE.json`（48 项串行队列）。
6. **旧五语言资源规模实测**：zh-CN/en-US 2092 顶层 key、zh-TW/ja-JP/ko-KR 1478，与规格 18.1 一致（baseline 第 8 节）。
7. **基线检查实测**：`pnpm lint` ✅、`pnpm typecheck` ✅、`pnpm check:structure` ❌（T00.2 已修复）。

## 文件归属（实际修改）

**T00.9（本轮，台账冻结，无代码改动）**：

- `docs/migration/contracts.md` — 第 7 节 i18n 契约更新为 T00.8 落地实况；变更日志补 T00.8 行与全表冻结行；新增第 9 节冻结登记（范围/变更规则/放行状态）
- `docs/migration/i18n-map.md` — 新增「各页命名空间/文件归属终版（T00.9 冻结）」45 入口表（命名空间规则与三处业务命名例外、沿用目录、H01–H03 复用 common）
- 本文件 — T00.9 交付物与证据、A/I 退出检查汇总、状态与里程碑更新；修正 commandConfirm 扩展名记录笔误

**T00.8（上一轮）**：

- `src/i18n/i18n.ts` — 重写：SUPPORTED_LANGUAGES 五语言、normalizeLanguage 精确归一化（zh-TW/ja-JP/ko-KR 不误映射）、语言×命名空间懒加载表（zh-TW/ja-JP/ko-KR 基座五分片 + en-US 九分片；查无 loader 返回空资源回退简中）、dayjs 五 locale 映射、umi_locale 一次迁移（readStoredLanguage 单一读取入口）
- `src/i18n/locales/zh-TW/{common,menu,auth,error,map}.ts` — 新增：繁体基座五分片（台湾惯用语；旧真译沿用）
- `src/i18n/locales/ja-JP/{common,menu,auth,error,map}.ts` — 新增：日文基座五分片
- `src/i18n/locales/ko-KR/{common,menu,auth,error,map}.ts` — 新增：韩文基座五分片
- `src/store/store.ts` — settings 持久化补 migrate：恢复 locale 以独立语言 key 为单一真相源（修复镜像覆盖迁移结果的双来源冲突），theme 异常回退亮色
- `src/store/slices/settingsSlice.ts` — 导出 SettingsState 接口（migrate 构造完整恢复状态用）
- `src/App/App.tsx` — ANTD_LOCALES 五语言映射（ConfigProvider）；语言切换预加载失败 catch 回滚到实际语言
- `src/layouts/BasicLayout/components/Header/Header.tsx` — 语言菜单扩五项（语言名各语言书写）
- `docs/migration/i18n-map.md`（命名空间归属与基座映射结论重写、umi_locale 迁移登记）/ `docs/migration/i18n-missing.md`（基座五分片补齐后移除对应登记，页面私有分片回退保留）/ `docs/migration/terminology.md`（已定术语表填充）/ `RUN_STATE.json` / 本文件 — 台账更新

**T00.7（上一轮）**：

- `src/utils/datetime/deployDayjs.ts` + `datetimeDisplay.ts` — 新增：部署时区 dayjs 装配与时间解析/展示纯函数
- `src/utils/stats/metricFormat.ts` — 新增：统计空值/单位纯函数（0 与缺失区分、除零保护）
- `src/utils/command/command.types.ts` + `commandConfirm.tsx` — 新增：命令三态类型、统一确认、批量真实归纳
- `src/utils/options/optionFallback.ts` — 新增：关联选项失效匹配与保留原值标签
- `src/hooks/useStaticOptions.ts` — 新增：全量只读选项统一加载 hook（取消/防乱序/失败清空）
- `src/services/map/map.service.ts` + `map.service.types.ts` — 新增：getMapInfo/getSimpleMaps 只读服务与地图 DTO
- `src/components/ReadOnlyMap/`（ReadOnlyMap.tsx/.types.ts、MapNodesLayer/MapEdgesLayer/MapEdgeHitLayer.tsx、mapGraphGeometry/mapGraphMount/mapGraphStyles.ts、useMapSelection/useMapStageInteraction/useMapBlink/useMapGraph.ts） — 新增：只读地图/选点/回填共享组件族
- `src/i18n/locales/en-US/map.ts` — 新增：共享地图文案（6 键）；`src/i18n/locales/en-US/common.ts` — 追加命令确认/批量/选项失效 8 键；`src/i18n/i18n.ts` — 懒加载表注册 map 命名空间
- `vite.config.ts` — optimizeDeps.include 预声明 react-konva（公共构建配置，owner T00）
- `package.json` / `pnpm-lock.yaml` — react 19.3.0、react-dom 19.3.0、@types/react 19.3.0、konva ^9.3.20、react-konva ^19.3.0
- `docs/migration/contracts.md`（第 5 节重写+5.1 新增、第 7 节时区行、变更日志、修复第 2 节重复行）/ `docs/migration/i18n-map.md` / `docs/migration/i18n-missing.md` / `RUN_STATE.json` / 本文件 — 台账更新

**T00.6（上一轮）**：

- `src/store/slices/tabsSlice.ts` — TabEntry 增加 dirty/dirtyLabel、`tabDirtyMarked` reducer、LRU 过滤跳过脏页签；顺带移除重复 `TabsState` 接口声明（owner 文件内死代码）
- `src/hooks/useTabDirtyGuard.ts` — 新增：页面草稿脏状态登记 hook（卸载自动清除）
- `src/hooks/useTabActionGuard.tsx` — 新增：关闭/刷新/批量关闭/退出登录统一确认（三档传输提示策略）
- `src/constants/polling.constants.ts` — 新增：轮询间隔/退避倍率/上限集中定义（全站唯一调整点）
- `src/hooks/useVisiblePolling.ts` — 新增：可见串行轮询 hook（激活+可见、完成后约 5 秒串行、退避、恢复即查、取消静默）
- `src/services/transfer/transfer.types.ts` — 新增：传输任务/阶段/进度/句柄类型（aborted=仅本机终止语义）
- `src/services/transfer/transferManager.ts` — 新增：模块级传输生命周期管理器（独立 AbortController、真实进度、processing 阶段、孤儿判定、快照订阅）
- `src/hooks/useTransfers.ts` — 新增：传输任务订阅 hook（useSyncExternalStore）
- `src/layouts/BasicLayout/components/TransferWatcher/TransferWatcher.tsx` — 新增：孤儿传输终态一次性消息 + 会话结束传输清理（零 UI 桥组件）
- `src/utils/window/standaloneWindow.ts` — 新增：独立窗口打开工具（站内路径校验、实体窗口命名、opener 切断）
- `src/components/RequestScopeProvider/RequestScopeContext.ts` + `RequestScopeProvider.tsx` — RequestScopeValue 增加 scopeKey 字段（脏状态登记用；现有消费者不受影响）
- `src/layouts/BasicLayout/BasicLayout.tsx` — 挂载 TransferWatcher + 脏页签 beforeunload 离开提示
- `src/layouts/BasicLayout/components/TabsBar/TabsBar.tsx` — 关闭/刷新/批量关闭全部走统一确认（批量动作按真实移除范围计算受影响集合）
- `src/layouts/BasicLayout/components/Header/Header.tsx` — 退出登录接入统一确认（脏页签 + 传输终止提示，保留既有文案）
- `src/i18n/locales/en-US/common.ts` — 追加 T00.6 文案 11 键（含补齐此前遗漏的「关闭当前页签」）
- `docs/migration/contracts.md` 第 4/6 节重写 + 变更日志 / `docs/migration/i18n-missing.md` / `RUN_STATE.json` / 本文件 — 台账更新

**T00.5（上一轮）**：

- `src/i18n/locales/apexTable/enUS.ts` / `zhTW.ts` / `jaJP.ts` / `koKR.ts` — 新增：ApexTableReact 四语言完整文案资源（58 key + 6 格式化函数，ApexLocale 类型强制全量）
- `src/i18n/locales/apexTable/apexTableLocales.ts` — 新增：`resolveApexLocale` 纯函数（zh-CN 复用包内 zhCN，前缀匹配，未知回退简中）
- `src/hooks/useApexLocale.ts` — 新增：表格文案随语言联动 hook（不包裹表格）
- `src/styles/globals.css` — 追加 ApexTable 主题变量映射节（`:root .apex-table` 亮暗成对 + 暗色 pinned-shadow；未动任何既有令牌）
- `src/utils/table/tablePaging.ts` — 新增：`toBackendPage`/`toBackendPageNo`/`toBackendPageSize` 分页换算纯函数
- `src/utils/table/rowId.ts` — 新增：`stringFieldRowId` 行 ID 取值工厂（空值/精度异常抛错，G10）
- `src/utils/table/columnPreferences.ts` — 新增：`createTableColumnPreferences` 官方适配器参数约定（身份四段隔离）
- `src/hooks/useColumnPreferences.ts` — 新增：列偏好适配器生命周期 hook（会话 userId、卸载 flush+dispose）
- `src/components/StateBlock/StateBlock.tsx` + `.module.css` — 新增：统一状态块（noPermission/gap/offline，不包裹表格）
- `src/i18n/locales/en-US/common.ts` — 追加 StateBlock 7 键英文文案
- `docs/migration/i18n-missing.md` — 登记 zh-TW/ja/ko 状态块文案简中回退
- `docs/migration/contracts.md` 第 3 节重写 + 变更日志 / `RUN_STATE.json` / 本文件 — 台账更新

**T00.4（上一轮）**：

- `src/constants/auth/permission.constants.ts` — 新增：PERM/PERM_BUTTON/ROOT_ONLY_CODES 权限码契约
- `src/utils/auth/permission.ts` — 新增：isRootUser/flattenMenuCodes/hasButtonCode 纯函数
- `src/router/routeAccess.ts` — 新增：访问上下文、可见性、落点/目录/回跳解析
- `src/router/guard.ts` — 重写：认证+权限守卫、index 动态落点、别名 redirect 守卫
- `src/router/projections.tsx` — 守卫挂载带 perm、独立页守卫、pending 占位渲染、buildMenuRoutes(ctx) 权限剪枝
- `src/router/definitions.tsx` — 全树挂 perm/migrationPending/public；目录 index 静态 redirect 移除改动态
- `src/router/router.types.ts` — meta 增加 perm/migrationPending/public
- `src/router/redirect.ts` — 删除 ROOT_REDIRECT_TARGET（落点动态化后无消费者）
- `src/components/MigrationPending/` — 新增：迁移过渡统一占位（组件+样式）
- `src/hooks/usePermission.ts` — 新增：按钮码判定 hook
- `src/layouts/BasicLayout/BasicLayout.tsx` — affix 播种按权限+迁移状态过滤
- `src/layouts/BasicLayout/components/DockMenu/DockMenu.tsx` — 菜单按访问上下文剪枝
- `src/features/auth/components/LoginForm/LoginForm.tsx` — 登录落点/回跳校验接入
- `src/store/store.ts` — 修复 migrate 无条件重置（T00.3 缺陷）
- `src/services/auth/authBridge.ts` — 修复监听键名缺 persist: 前缀（T00.3 缺陷）
- `src/i18n/locales/en-US/common.ts` — 新增迁移占位文案
- `docs/migration/contracts.md` / `RUN_STATE.json` / 本文件 — 台账更新

**T00.3（上一轮）**：

- `src/services/request/request.ts` — 全量重写：Result 解包、业务码、会话失效收敛、取消静默、健康反馈、Bearer/Accept-Language、移除刷新重放（公共文件，owner T00；影响全部 service 消费者，api 语义变化已在 contracts.md 变更日志登记）
- `src/services/request/request.constants.ts` — `RESULT_CODES` 业务码常量；删除 refresh 常量与 `API_ERROR_CODES`；保留 `DEFAULT_PAGE_SIZE`（模板 service 消费中）
- `src/services/request/request.types.ts` — `ResultDto`/`BackendPageQuery`/`BackendPageResult`、`ApiError.businessCode`；模板遗留类型标注保留
- `src/services/auth/auth.service.ts` — 真实登录/登出、MD5 单点、删除直通登录与 `/users/me`
- `src/services/auth/auth.service.types.ts` — `LoginParamDto`/`UserAuthDto`/`UserSummaryDto`/`AuthPermissionDto`（按 OpenAPI）
- `src/services/auth/authBridge.ts` — 新增：令牌同步 + 多窗口退出同步
- `src/types/auth/auth.types.ts` — `AuthUser` 对齐 UserSummary、`AuthSession` 扩展（消费 auth.service.types 的权威 DTO）
- `src/store/slices/authSlice.ts` — 会话状态扩展、删除 `userPatched`
- `src/store/store.ts` — auth 持久化白名单扩展、schema v1→v2 迁移
- `src/main.tsx` — 接入 `initAuthBridge()`
- `src/i18n/i18n.ts` — 初始化/切换语言时同步 `setRequestLanguage`
- `src/i18n/locales/en-US/common.ts` / `en-US/profile.ts` — 请求层错误文案与 profile 新键
- `src/layouts/BasicLayout/components/Header/Header.tsx` — 角色显示改用会话级 roles（公共文件小改，已核查消费者）
- `src/pages/profile/Profile/Profile.tsx`、`src/features/profile/components/ProfileForm/ProfileForm.tsx` — 真实会话字段 + 诚实不可用说明
- `src/services/profile/`（整目录删除）— 虚构 `/users/me` 读写
- `package.json` / `pnpm-lock.yaml` — spark-md5@3.0.2 + @types/spark-md5（开发依赖）
- `docs/migration/gaps.md` / `contracts.md` / `tasks/T00.md` / `RUN_STATE.json` — 台账更新

**T00.2（上一轮）**：依赖/代理/时区/结构门禁（清单见上一节）；**T00.1**：docs/migration 台账初始化。

## 共享契约

请求协议与会话契约已落地（contracts.md 第 1/2 节 + 变更日志）；共享只读选项 operation 已逐项核对登记（第 5 节），只读地图组件契约见第 5.1 节；时间/统计/命令契约见第 7 节与变更日志 T00.7 行。**消费者注意**：`api.get/post/put/delete` 现在返回 `Result.data` 且业务码非 200 抛 `ApiRequestError`——模板 service（order-record/system/dashboard）在各自页面任务重写前，对真实后端的联调语义以本契约为准。

## 接口缺口

G01/G02 → **已确认缺失**（模板虚构接口已全部移除运行路径）；G03 → 待确认（MD5/Bearer 已按旧代码证据单点落地，Accept-Language 已实证，最终确认待登录凭据）；G04/G10 增补 T00.3 进展；其余 G 编号状态不变 → `gaps.md`。

## 验证

**T00.9（2026-09-17）**：

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` / `typecheck` / `check:structure` / `build` | ✅ 全部通过（213 文件 lint 0 警告 0 错误；211 源文件结构检查；tsc+vite build 成功）——冻结时点最终证据 |
| mock 退出运行路径复核 | ✅ pending 页不触发 lazy，order.mock/dashboard.mock 运行不可达（引用链实测） |
| 公共导出清点 vs contracts.md | ✅ 一致（修正 commandConfirm.ts 记录笔误） |

无现场副作用操作；本轮无代码改动、无真实后端请求。

**T00.8（2026-09-17）**：

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` / `typecheck` / `check:structure` / `build` | ✅ 全部通过（213 文件 lint 0 警告 0 错误；211 源文件结构检查；tsc+vite build 成功） |
| 五语言切换全链路（真实浏览器） | ✅ zh-CN/zh-TW/ja-JP/ko-KR/en-US 各轮 html lang、持久化、菜单文案、登录页资源全部正确（证据见本轮验证表） |
| umi_locale 一次迁移 | ✅ 迁移后语言落位、新 key 写入、旧 key 删除；首轮暴露镜像覆盖缺陷，修复后复验通过 |
| 非法语言值恢复 | ✅ fr-FR 残留归一化 zh-CN，不挂起 |
| antd locale 弹窗按钮 | ⛔ 映射全覆盖 + zh-CN 特征实证；控件文案随首个消费者页面联验 |

无现场副作用操作；验证会话与测试键已清除，页签已关闭。

**T00.7（2026-09-17）**：

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` / `typecheck` / `check:structure` / `build` | ✅ 全部通过（198 文件 lint 0 警告 0 错误；196 源文件结构检查；tsc+vite build 成功） |
| 纯函数机制断言（26 项，真实模块动态加载） | ✅ 全部通过（时间/统计/选项失效/命令契约/挂载转换，证据见上节） |
| 只读地图渲染与选点回填（浏览器机制实证） | ✅ 三层 canvas 绘制、选中回显回填、pointer 命中回填、hitFunc 命中区、map 命名空间 |
| 动画类（fitView/闪烁/滚轮缩放） | ⛔ IAB 环境 rAF 抑制，待 P07 真实浏览器联验 |
| getMapInfo 带令牌联验 | ⛔ 受阻 G03（凭据）；响应 schema 按旧可达实现解析，联调登记差异 |

无现场副作用操作；无 mock 入库；探针文件已删除；不发送真实写请求。

**T00.5（2026-09-17）**：

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 167 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过（五语言 ApexLocale 全 key 类型强制 + 探针全量组装） |
| `pnpm check:structure` | ✅ 165 源文件通过 |
| `pnpm build` | ✅ 通过 |
| 类型探针（页面对接方式预演：request/分页换算/行 ID/locale/虚拟化/展开/列设置/双 ref/列偏好 load） | ✅ 通过后删除；捕获语言包缺 key、ref 双 prop 误用两处认知偏差并修正 |
| 真实只读列表 Apex request 联验 | ⛔ 受阻：登录成功路径需测试凭据（G03）；且放行证据要求落在 P03/P05 既有入口——机制已就绪，联验衔接方式见「未完成」第 2 条 |

无现场副作用操作；本轮为机制交付轮，无真实后端请求、无 mock、无演示页；主题映射当前无表格消费者，对现有页面零影响（作用域限定 `.apex-table`）。

**T00.4（2026-09-17）**：

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 156 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过 |
| `pnpm check:structure` | ✅ 154 源文件通过 |
| `pnpm build` | ✅ 通过 |
| 真实浏览器场景（17 项） | ✅ 守卫/权限/落点/迁移占位/持久化恢复全部通过（证据见上节） |
| 真实登录-权限端到端（真实账号） | ⛔ 受阻：需测试账号凭据（G03；B1 放行前补验） |

无现场副作用操作；浏览器验证全部为路由/渲染行为与只读观测，未向后端发送写命令；测试会话注入数据验证后已清除。

**T00.3（2026-09-17）**：

| 命令 / 项 | 结果 |
| --- | --- |
| `pnpm lint` | ✅ 151 文件 0 警告 0 错误 |
| `pnpm typecheck` | ✅ 通过（含 store migrate/redux-persist 类型适配） |
| `pnpm check:structure` | ✅ 149 源文件通过（profile 目录删除后 -1） |
| `pnpm build` | ✅ 通过 |
| 真实接口：登录/登出/pageUsers/systemLogos/代理链路/Accept-Language | ✅ 证据见上文联调证据表 |
| 真实接口：登录成功路径、MD5/Bearer 最终确认、带令牌只读、1001000 行为 | ⛔ 受阻：需测试账号凭据（不猜口令）；登记于 G03 与上方受阻范围 |

无现场副作用操作；所有探针为只读或错误凭据/无令牌请求，无写命令试发。

**T00.2（2026-09-17）**：lint ✅（152 文件）、typecheck ✅、check:structure ✅（150 文件 + 违规注入自测）、build ✅、apex-table-react@0.1.0 安装 ✅。

**T00.1（2026-09-17）**：`pnpm lint` / `pnpm typecheck` 通过；哈希与旧语言资源规模脚本核验通过。

## 未完成 / 后续（下一轮入口）

1. **T00 勾选与 B1 放行的唯一前置（G17，2026-09-18 起）**：联调服务器软件未激活（登录返回 `activated:false`），带令牌业务只读全部返回 `1001000 系统没有被激活`——G03 的凭据与协议疑点已全部关闭，剩余为激活状态。softwareActivation 属副作用不自动执行；解除条件：用户激活联调环境或提供已激活后端。下一轮先重新登录查 `activated`：`true` → 带令牌业务只读复核（pageUsers 返回 200 即收尾）→ 勾选 TASKS.md T00 → 下一轮领取 P01；仍 `false` → 最小化复核后结束。**队列不得在 T00 未勾选时跳向 P01**（队首受阻不另选）。
2. **T00.5 联验衔接（页面任务内）**：表格主题映射、locale 切换、request 分页换算、列偏好持久化的**真实渲染与接口联验**在 P03/P05 既有入口随页面任务完成（B2a）；证据要求不变：真实只读列表、亮暗主题截图、切语言表格文案即时更新、列偏好按用户隔离且可重置。
3. **T00.6 联验衔接（页面任务内）**：`useVisiblePolling` 运行时行为随首个实时页（P34/P05/P39/P40）联验；传输真实文件通道随 P08/P09/P25 接入；`openStandaloneWindow` 弹窗拦截检测在 IAB 自动化环境不可靠，真实浏览器行为待 P38/P39/P40 独立窗口按钮联验；跨窗口退出同步待真实双窗口复验。
4. **T00.8 联验衔接（页面任务内）**：antd 控件 locale（确认框按钮、表格空态、分页文案等）随首个消费者页面（P03/P13 等）真实联验；切换失败保留原语言的真实网络失败场景随页面任务联调覆盖；页面私有命名空间四语言分片由对应页面任务交付（交付后从 i18n-missing.md 移除回退登记）。
5. **页面任务消费提示（P01 之后全部页面）**：语言切换统一走 settings.locale → changeAppLanguage（禁止绕过 normalizeLanguage 直调 i18next.changeLanguage）；页面私有语言分片命名空间与文件域按 i18n-map.md「T00.9 冻结表」，分片放 `src/i18n/locales/<语言>/<命名空间>.ts` 并在 `src/i18n/i18n.ts` 对应语言的懒加载表登记；地图消费按 `React.lazy(() => import('@/components/ReadOnlyMap/ReadOnlyMap'))` + `'map'` 命名空间；写操作确认经 `confirmCommand`，批量结果经 `summarizeBatchOutcomes`，报表空值/单位经 `metricFormat`，时间展示经 `datetimeDisplay`。
6. **迁移过渡标记移交**：全部 pending 页面（meta.migrationPending，definitions.tsx 内联标记）随对应页面任务完成由统筹移除；页面任务按钮权限统一消费 `usePermission()` + `PERM_BUTTON`；表格页组装方式按 contracts.md 第 3 节；实时页轮询按第 4 节消费 `useVisiblePolling`，文件传输按第 4 节消费 `beginTransfer`，草稿页消费 `useTabDirtyGuard`。
7. 遗留观察：`1001000` 语义已于 2026-09-18 实证为「系统没有被激活」（跳软件授权入口），授权页跳转 UI 在 P02 落地；模板遗留分页类型（PageQuery 等）随 P03/P31/P32/P34 替换；实体详情工作区页签形态的受保护根内路由随 P38/P39/P40 接入；order.mock/dashboard.mock 文件本体分别随 P03/P34 移除（T00.9 已复核运行不可达）。
