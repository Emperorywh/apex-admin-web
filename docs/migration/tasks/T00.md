# T00 基础设施 — 任务记录

## 任务 / 负责人 / 基线

| 字段 | 内容 |
| --- | --- |
| 任务 | T00 基线、请求/会话/权限、表格、页签与五语言公共基础设施（B0，无实施前置，可跨多轮执行） |
| 负责人 | 定时调度运行 run-20260917-121624-7468（ZCode 串行调度） |
| 分支 / 提交 | `apple-rxx` / 基线 `b09d285`（工作树干净起步） |
| 开始日期 | 2026-09-17 |
| 最近更新 | 2026-09-17 |

## 状态

| 维度 | 状态 |
| --- | --- |
| 实施状态 | **进行中**（T00.1 已完成；T00.2–T00.9 未开始） |
| 验证状态 | 未验证（T00 整体；T00.1 属文档清点，其证据本文件及台账即为交付） |
| 阻塞原因 | 无 |

## 里程碑进度

| 里程碑 | 状态 | 说明 |
| --- | --- | --- |
| T00.1 基线及分工 | ✅ 完成（2026-09-17） | 交付物见下节 |
| T00.2 依赖与环境 | ⬜ 未开始 | 下一步：重核 npm latest（antd/Apex）、安装 apex-table-react、代理与部署时区、修复 check:structure |
| T00.3 请求与认证 | ⬜ 未开始 | |
| T00.4 权限与路由 | ⬜ 未开始 | |
| T00.5 Apex 与状态 | ⬜ 未开始 | |
| T00.6 页签与传输 | ⬜ 未开始 | |
| T00.7 共享业务能力 | ⬜ 未开始 | |
| T00.8 五语言基座 | ⬜ 未开始 | |
| T00.9 冻结交接 | ⬜ 未开始 | |

## 本轮（T00.1）交付物与证据

1. **OpenAPI 核验**：`C:\yangwenhua\ApexTableReact\default_OpenAPI.json` SHA-256 = `A82E9B5F6EF5E564DEEE16E4E74713B28FCCB35CD0A651E3631CFD9D63D49C7C`（与规格一致）；238 operation / 408 schema 脚本实测一致。endpoint 全表 → `docs/migration/openapi-inventory.md`。
2. **45 个旧叶子清点与目标映射** → `docs/migration/baseline.md` 第 2 节（含目录/重定向结构、目标树现状核对：全部叶子已有路由节点）。特殊权限点（载具类型/数据库备份无 access、order-info/vehicle-info 复用码、服务器资源别名）→ baseline 第 5 节。
3. **历史实现排除登记** → baseline 第 6 节（Elevator/AutoDoor 历史、ConveyorLine、sysAction、重复用户角色页等）。
4. **检查与依赖故障登记** → baseline 第 7/10 节：`check:structure` 脚本缺失（实测 `pnpm check` 失败）、`apex-table-react` 未安装、i18n 仅两语言、auth.service 假登录（实测读文件确认：任意凭据放行 + 本地 super_admin + `/users/me`、`/auth/logout`）、请求层协议不匹配、代理指向 localhost:8000。
5. **台账初始化**：`gaps.md`（G01–G16 全部「待确认」）→ `contracts.md`（协议/会话/表格/页签/选项框架）→ `operations.md`（汇总格式）→ `i18n-map.md` / `i18n-missing.md` / `terminology.md`（格式与规则）→ `RUN_STATE.json`（48 项串行队列）。
6. **旧五语言资源规模实测**：zh-CN/en-US 2092 顶层 key、zh-TW/ja-JP/ko-KR 1478，与规格 18.1 一致（脚本证据，见 baseline 第 8 节）。
7. **基线检查实测**：`pnpm lint` ✅（150 文件 0 警告 0 错误）、`pnpm typecheck` ✅、`pnpm check:structure` ❌（脚本缺失）。

## 文件归属（本轮实际修改）

全部为新增台账文档，未触碰应用代码：

- `docs/migration/RUN_STATE.json`（新建）
- `docs/migration/baseline.md`、`gaps.md`、`contracts.md`、`operations.md`、`i18n-map.md`、`i18n-missing.md`、`terminology.md`、`openapi-inventory.md`（新建）
- `docs/migration/tasks/T00.md`（本文件）

## 共享契约

本轮只登记未实现：协议契约框架见 `contracts.md` 第 1–2 节；共享只读选项 owner 归 T00（第 5 节，operation 待 T00.7 逐项核对登记）。

## 接口缺口

G01–G16 已按规格第 13 章初始化为「待确认」→ `gaps.md`。G16 的脚本缺失部分已在目标仓库实测复现。

## 验证

- 命令证据（2026-09-17）：`pnpm lint` 通过；`pnpm typecheck` 通过；`pnpm check` 失败于 check:structure（预期，待 T00.2 修复）；哈希与规模脚本核验通过。
- 无真实接口验证（T00.1 无网络请求交付；联调目标 `10.11.2.67:8888` 的可达性/登录验证属 T00.3，且须记录环境证据）。

## 未完成 / 后续（下一轮入口）

1. **T00.2 依赖与环境**（下一轮起点）：重核 npm 正式 latest（antd、apex-table-react）兼容性并提交锁文件；代理默认目标改为 `http://10.11.2.67:8888` 并明确 `/fms` 前缀处理；确定部署时区配置；修复 `scripts/check-structure.mjs` 使 `pnpm check` 真实可执行；README 视觉指南依据复核（G16）。
2. 之后按 T00.3 → T00.9 顺序推进；每个里程碑完成即在 RUN_STATE.json 与本文件记录证据。
3. 注意：`docs/macos_ui_ux_design_guide_v3.md` 现存且被 CLAUDE.md 引用为视觉基准——T00.2 复核规格 G16 中「README 失效依据」表述是否仍成立，按现状登记。
