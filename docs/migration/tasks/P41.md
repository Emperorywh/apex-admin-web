# P41 无权限页 · 交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
**状态**：✅ 已完成（2026-09-18 定时轮交付并勾选）
**批次/前置**：B1 / T00（已完成）
**入口/目标**：`/no-permission` → `src/pages/un-access/UnAccess/UnAccess.tsx`
**私有模块/语言分片**：`access-denied`（覆盖 i18n-map 冻结表原登记「复用 common/error」，任务卡指定独立分片，已完成更正）
**旧来源**：`C:\code\dd\src\pages\UnAccess\index.tsx`（单文件 63 行，无其他可达子组件）

## 交付内容

1. **页面**（UnAccess.tsx + .module.css）：antd Result 403 等价迁移（旧同款 status/title）；主说明「无权限访问当前页面」（旧 key 沿用）+ 补充说明（联系管理员）；布局外独立全屏页（standaloneMeta，受认证守卫、非公开、无菜单码）。
2. **返回路径（防循环核心）**：返回动作不指向固定地址，按真实会话经 `resolveLandingPath`（T00 导航判断复用）计算合法落点；落点仍为本页（无任何可用业务页）时**不渲染返回入口**——页面提供的返回永不构成 /no-permission 重定向循环，也不提供绕过权限的入口（落点解析只产出当前会话有权到达的地址，A05/A22）。
3. **退出登录**：真实接口 `POST /fms/v1/auth/authorize/logout` + `sessionExpired()` 清本地会话 + replace 到登录页；接口失败不阻塞本地登出（客户端登出语义，不冒充服务端已登出），写操作不自动重试。独立页无页签草稿/传输，不涉及 guardTabAction（与旧系统直接执行一致，无确认弹窗）。
4. **状态区分**：本页只表达「无权限」一种语义；服务离线/接口缺口/本期暂缓由各业务页 StateBlock 呈现，不在本页混用。会话驻留期间被其他窗口登出时（user=null 真实状态）呈现「去登录」而非假装已登录（DoD 3）。
5. **视觉**：中央玻璃反馈面板——全局壁纸为固定照片不随主题变化，深色主题下 antd 反白文字直叠浅壁纸不可读（联验截图实证），故以 `--app-glass-window` + `--app-glass-filter` + `--app-radius-window` + `--app-window-shadow`（全部既有令牌，无第二份配色）提供两主题衬底；hint 用 `--app-text-3` + `--app-fs-secondary`。
6. **五语言**：`access-denied` 命名空间四语言分片（4 键）。en-US「无权限访问当前页面」「去登录」沿用旧系统真译；zh-TW/ja-JP/ko-KR 旧资源为简中回退，按 B1 基线交付真实译文（台湾用语：存取/登入；「退出登录」复用 common 常驻命名空间既有四语言术语，不重复定义）。路由 meta 声明 `i18nNamespaces: ['access-denied']`（common 为常驻基座无需声明）。

## 操作映射

| 旧操作 | 新实现 | Method+Path | 权限 | 状态/证据 |
| --- | --- | --- | --- | --- |
| 403 无权限反馈 | Result 403 + 说明 | — | 登录（守卫） | ✅ 浏览器实测 |
| 退出登录 | logout 服务 + sessionExpired | POST /fms/v1/auth/authorize/logout | 登录 | ✅ performance entries 实证请求发出 + auth 持久化清空 |
| 去登录（未登录态） | navigate /login（replace） | — | — | ✅ 防御分支保留（正常流程守卫先拦截） |
| （新）返回可用页面 | resolveLandingPath 落点导航（replace） | — | 登录 | ✅ 全不可用时按钮不渲染（防循环）；有可用页分支随 B2a 验证 |

## 真实联验证据（2026-09-18，dev 代理 192.168.0.158:8888）

| 场景 | 结果 |
| --- | --- |
| 未登录直访 /no-permission | 守卫送回 `/login?redirect=%2Fno-permission` |
| 登录 root（全部业务页迁移中=无可用业务页场景） | 落点 `/no-permission`；页面仅「退出登录」按钮，**返回按钮不渲染**（落点=本页防循环） |
| 退出登录 | 真实请求 `/fms/v1/auth/authorize/logout`（performance entries）+ `apex-admin:auth` 清空 + `/login`（replace 无回跳参数残留） |
| 退出后直访 | 再次被守卫送回登录页；重新登录会话可重建、落点正确 |
| 五语言 | zh-CN/en-US/zh-TW/ja-JP/ko-KR 全页文案+html lang 逐语言实测（en 复用 common「Sign out」） |
| 深浅主题 | 深色下反白文字叠固定浅壁纸不可读 → 玻璃面板修复后两主题截图确认全部可读（1366×768） |
| 历史往返 | back→占位页、forward→无权限页，无重定向风暴 |
| 工程门禁 | pnpm check（229 文件 lint 0 警告 0 错误 / typecheck / 结构检查 227 源文件）+ pnpm build 全过；零调试残留 |

## 登记的限制（非缺口）

1. **「返回可用页面」出现分支**：当前无已完成迁移业务页（全部 migrationPending），任何会话落点均为本页；canReturn=true 路径待 B2a 首个业务页（P03/P05）交付后随落点联验复核。逻辑本身为 T00.4 已实证的 resolveLandingPath 复用。
2. **「去登录」防御分支**：守卫在 loader 层保证页面渲染时恒已登录，该分支仅覆盖跨窗口登出同步瞬间；未做运行时构造（代码审查结论）。
3. 无权限 redirect 的浏览器后退会再次触发来源页 loader 弹回（guard 权限语义，非本页行为）；页面自身动作（落点导航/登出/去登录）均 replace，不在历史中堆积无权限页。

## DoD 适用性

- 不适用：表格（4/5）、页签草稿（7，独立页 noCache）、地图（10）、统计（14）、文件（9）。
- 适用已满足：可达业务完整（1，旧页仅 403 反馈+登出/去登录，全量迁移）、真实服务（2，真实 logout）、权限与状态区分（3）、取消与失败（6，登出失败不阻塞本地清会话）、写入防重（8，不自动重试）、五语言（11–12）、外观终端（13，两主题×1366×768 截图）、验证证据（15）、交接收尾（16，无 mock/无残留）。
- 追踪项：A05（无权限有明确落点、返回不循环、无绕过入口）、A20（五语言+深浅色+1366×768）、A22（无权限合法落点）；I01/I02（旧 key 译文去向 access-denied 分片）、I06（四语言无缺译，无回退登记）。

## 对后续任务的影响

- **B2a（P03/P05）**：首个业务页交付后，直访无权限路由场景可真实构造（无码用户），顺带复核「返回可用页面」分支。
- **公共文件改动记录**：definitions.tsx（no-permission meta 增 i18nNamespaces 声明，单点改动）、i18n.ts（4 表注册 access-denied）。均经 typecheck/结构检查/全量构建证实无消费者破坏。
