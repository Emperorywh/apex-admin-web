# P29 软件信息 · 交接记录

**状态**：✅ 已完成（2026-09-20 定时轮交付并勾选；激活真实成功链路留专用环境验收项，不阻塞勾选——读链路/失败分支/弹窗交互已实证，写副作用零尝试）
**批次/前置**：B3 / B2 样板门禁、P02（均已完成）
**入口/目标**：`/system-involve/software-information` → `src/pages/system-involve/SoftwareInformation/SoftwareInformation.tsx`
**私有模块/语言分片**：`software-license`（页面私有；激活弹窗复用 P02 `license-activation` 组件与分片，路由 meta 双命名空间声明）
**旧来源**：`C:\code\dd\src\pages\SystemInvolve\SoftwareInformation\index.tsx`（+index.less，无其他可达子组件）

## 交付内容

1. **服务层**（P02 建、P29 增，`src/services/license-activation/`）：
   - `fetchLicense()`：POST `/auth/license/getLicense`（无请求体=旧实现同形态；查询性质 POST 只读）。响应 `data: LicenseProofDto`（hardwareInfo/agvNumber/issueDate/expirationDate/activationCode）。
   - DTO 定义于 `license.service.types.ts`（旧 `LicenseProofRecord` 等价迁移）。
2. **状态派生纯函数**（`features/software-license/utils/licenseStatus.ts`）：旧 `getLicenseStatus` 等价迁移——过期日当日零点差整天数，<0 已过期 / ≤30 即将到期 / >30 已激活 / 缺失不可解析=未知；**时区口径升级为部署时区**（旧版浏览器本地时区取日界，跨时区访问可能漂移一天；新版经 `parseBackendDateTime`/`DEPLOY_TIMEZONE` 单点解析，与全站时间展示同源）。
3. **授权信息加载 Hook**（`features/software-license/hooks/useLicenseInfo.ts`）：三态（加载/失败/成功，成功可为 null=后端无授权数据）；在途请求随工作区页签 scope 信号中止 + 每次加载独立 AbortController 防乱序；**页签重新激活自动重查一次**（AGENTS 4：非表格数据块不设手动刷新按钮，失败自愈依赖可见性重查；本页无轮询=旧版同语义）。
4. **整页重写**（SoftwareInformation.tsx + .module.css）：
   - 授权横幅：图标+标题/副标题 + 状态 Tag（未知/已过期/即将到期/已激活）+「激活软件」入口（`system:software:activate` 按钮码条件渲染隐藏，横幅与空态两处共用；root 短路全开）。
   - 两项统计：最大 AGV 数量（后端原值，缺失留白）；剩余授权天数（已过期钳制 0，**缺失/未知留白**——旧版未知显示 0，按空值纪律收敛登记；antd Statistic 默认把空串数值化成 0（`Number('')`），经 formatter 直出修复）。
   - 四张详情卡：激活日期/过期日期（`displayDateTime` 部署时区单点格式化，缺失留白——旧版「-」按留白纪律收敛）；激活码/硬件信息原文卡片（定高滚动，复制按钮空值禁用）。
   - 激活弹窗：复用 P02 `ActivationForm`（失败保留输入/真实 code=200 才成功/不自动重试）；footer 仅「取消」，关闭即销毁表单草稿（destroyOnHidden=旧 destroyOnClose 同语义）；成功后 `activationConfirmed` 同步会话激活状态（真实业务结果）+ 原地重查授权信息（不跳转，P02 落点导航仅授权页形态）。
5. **三态分界（P29 专项：读取失败不显示已授权）**：加载中 Spin；真实失败→失败文案（`获取激活信息出错：+ 后端/网络 message`），不渲染授权卡片、不并列激活入口（避免未知状态下诱导写操作，收敛登记）；成功无数据→「暂无激活信息」空态+激活入口；成功有数据→卡片。登录过期（1000000）由请求层会话守卫跳登录、未激活引导（1001000）由请求层事件跳授权页（全局既有接线），授权失效（业务码）由本页失败态呈现后端 message——**登录过期与授权失效分别处理**。
6. **路由**（definitions.tsx 单点）：`system-involve-software-information` 摘除 `pending`，`i18nNamespaces: ['software-license','license-activation']`；守卫码 `system:software:view` 既有。
7. **五语言**：`software-license` 命名空间四语言分片交付（22 键）。en-US 旧真译全部沿用（`{label}已复制`插值改 `{{label}}` 形态，P25 沉淀）；zh-TW/ja-JP/ko-KR 按 B1 基线真实译文（旧资源本页为简中回退且状态词缺失），术语与 P02 license-activation 对齐（繁 啟用/日 有効化/韩 활성화），繁中与 menu（軟體資訊）一致。zh-CN 中文 key 即文案无需分片。

## 操作映射

| 旧操作 | 新实现 | Method+Path | operationId | 权限 | 状态/证据 |
| --- | --- | --- | --- | --- | --- |
| 授权信息查询（useRequest→getLicense） | useLicenseInfo → fetchLicense | POST /fms/v1/auth/license/getLicense（无 body） | getLicense | 登录 | ✅ 带令牌联验通过（见下） |
| 状态 Tag（未知/已过期/即将到期/已激活） | deriveLicenseStatus 纯函数（部署时区口径） | — | — | — | ✅ 未知态真实环境实证；其余态为纯函数等价迁移 |
| 最大 AGV 数量 / 剩余授权天数统计 | Statistic（原值/留白纪律） | — | — | — | ✅ 0 台+留白天数实证 |
| 激活日期/过期日期卡片 | displayDateTime（缺失留白） | — | — | — | ✅ 空记录留白实证；非空形态留待有授权数据环境 |
| 激活码/硬件信息卡片+复制 | 卡片+copyText 复用（空值禁用） | — | — | 登录 | ✅ 禁用态实证；复制成功路径同源 P02 已验工具 |
| 激活弹窗（Modal+TextArea） | Modal+复用 ActivationForm | — | — | system:software:activate | ✅ 空输入禁用/失败分支实证；真实成功链路待专用环境 |
| 激活提交 | ActivationForm → activateSoftware（P02 服务复用） | POST /fms/v1/auth/license/softwareActivation | softwareActivation | 登录 | ⚠️ 副作用仅专用环境（共享已激活环境零写尝试，P02 同纪律） |

## 真实联验证据（2026-09-20，run-20260920-p29-010557，环境 192.168.0.158:8888）

**API 层（python 脚本 9/9 通过，验证后删除）**：

- 登录 code=200 + activated=true；权限两码 `system:software:view`/`system:software:activate` 真实下发。
- `getLicense` 带令牌 code=200；**契约实证：本环境返回「有激活、无授权详情」形态**——`data` 为非 null 对象但五字段全空（hardwareInfo/issueDate/expirationDate/activationCode=null，agvNumber=0），如实记录不臆造；页面据此走「未知 Tag+留白」路径。
- `expirationDate=null → 状态=unknown、剩余天数=null（留白）` 派生对照。
- 无令牌 getLicense → 1000000 用户未登录。
- getHardwareInfo 基线 B1E3252E8C273C40（P02 既有）。
- softwareActivation 零写尝试（专用环境纪律）；只读页终态零残留。

**浏览器整页（dev 代理，真实登录 root）**：

- 守卫：未登录直访 → `/login?redirect=%2Fsystem-involve%2Fsoftware-information`；登录后 redirect 回跳目标页闭环。
- 整页渲染：横幅+「未知」Tag+激活按钮（root 权限）；统计 0 台（真实 0）/剩余天数**留白**（发现并修复 antd Statistic 空串数值化为 0 缺陷，formatter 直出）；四卡标题齐、复制按钮空值禁用；无错误态。
- 激活弹窗：空输入提交禁用→输入后启用；**失败分支**（XHR 客户端拦截至不可达端口，零后端触达）：「激活出错：网络不可达…」如实呈现+输入保留+按钮恢复；取消关闭弹窗；performance 审计 softwareActivation 网络条目=0。
- **读取失败态**（getLicense 客户端拦截）：菜单切走→切回（页签重激活自动重查命中拦截）→「获取激活信息出错：网络不可达…」+ 零卡片零横幅（**不显示已授权、不以空态冒充**）→ 整页恢复后正常态回归。
- 菜单入口：Dock 系统管理→软件信息菜单项真实可达。
- 五语言逐语言：html lang 同步（en-US/zh-TW/ja-JP/ko-KR/zh-CN），横幅标题/状态 Tag/激活按钮逐语言正确（Unknown/未知/不明/알 수 없음 等）。
- 双主题双宽度四组截图：`.run-lock/shots-p29/`（light-1920/dark-1920/dark-1366/light-1366，布局无溢出无遮挡；暗色帧曾现合成层旧帧伪象，视口切换强制重合成后干净帧，AGENTS ⑱ 同款）。
- 真实登出 → 回 `/login?redirect=...`；页签关闭+dev server 停止+联验脚本删除+调试残留 0。

## 差异登记（收敛项，非缺陷）

1. **读取失败态独立呈现**：旧版失败仅 toast+复用空态模板；新版按 DoD 6/AGENTS 3 失败文案独立呈现（不与空态混同），且失败态不并列激活入口。
2. **剩余天数缺失留白**：旧版 expirationDate 缺失时显示 0 天；新版留白（DoD 14 0≠缺失 + AGENTS 3 派生值缺失留白）。已过期钳制 0 与旧版一致（真实语义）。
3. **到期计算部署时区口径**：旧版浏览器本地时区日界；新版部署时区（规格 11.3/DoD 14 不随浏览器漂移），日期原文展示不变。
4. **激活弹窗复用组件**：提交按钮文案「激活授权」（P02 组件既定）替代旧「确认激活」；取消入口由 footer「取消」承担；关闭销毁草稿同旧。
5. **图标 lucide 化**：旧 @ant-design/icons → lucide（全项目既定收敛）。
6. **日期展示缺失留白**：旧版「-」占位 → 留白（AGENTS 3）。

## 登记的限制与待验收（非缺口）

1. **激活成功链路（写副作用）**：专用环境验收——提交真实激活码 → 成功提示 + 会话 activated 更新 + 授权信息重查呈现。当前环境已激活，零写尝试（P02 同纪律）。
2. **非空授权记录形态**：本环境 getLicense 返回空记录（字段全 null），非空记录的到期日字符串口径/已激活/即将到期/已过期三态展示留待有授权数据的环境复核（派生逻辑为旧版等价纯函数，风险低）。
3. **1001000 真实触发**：未激活环境的 getLicense 若返回 1001000 将走全局引导跳授权页（P02 既有接线）；本环境 activated=true 不可复现，随专用环境验收。
4. **复制成功路径**：本环境授权字段全空→复制按钮禁用态已实证；成功路径消费 P02 `copyTextToClipboard`（剪贴板读回已在 P02 联验），不重复验证。

## DoD 适用性

- 不适用：表格（4/5——本页无表格无列偏好）、文件（9——无上传下载）、地图（10）、轮询（7 部分——旧版同语义单次加载，自愈由页签重激活重查承担）、统计条（14 部分——无统计接口，展示纪律同源）。
- 适用已满足：可达业务完整（1，操作映射表）、真实服务协议（2，严格 /fms/v1 + api.post）、权限与状态区分（3，view 守卫+activate 按钮码+四态分界）、取消与失败（6，scope 中止+乱序防护+失败清区域）、写入防重（8，submitting 防连点+不自动重试+成功以业务码判定）、五语言（11–12，en 真译沿用+插值形态修正+B1 基线）、外观终端（13，双主题双宽度四组截图）、时间（14，部署时区+留白纪律）、验证证据（15，API 9/9+浏览器全场景）、交接收尾（16，零 mock 零残留）。
- 追踪项：A03–A06（真实激活反馈失败分支实证+成功待专用环境）、A11（授权信息展示）、A14（读取失败不显示已授权实证）、A20（五语言+截图）；G03（认证契约 T00 已关闭）、G15（时间口径=部署时区单点）；I01/I04（语言随页+持久化，逐语言 html lang 实证）。

## 对后续任务的影响

- **P30+**：无新增公共文件改动；services/license-activation 为 P02 建 P29 增（fetchLicense/DTO），消费者仅本页。
- **专用环境验收清单**（P02+P29 合并执行）：真实激活码提交成功链路（含 activated=false 环境的登录落点授权页 + 1001000 引导 + 授权信息从空记录到有记录的刷新呈现）。
