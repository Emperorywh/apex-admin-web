# P12 地图推送记录 交接记录

- 任务卡：TASKS.md「P12 地图推送记录」（批次 B3 / 前置 B2 样板门禁）
- 执行轮次：run-20260919-p12-090534（2026-09-19）
- 入口：`/map-through/map-push-records`；目标：`src/pages/map-through/MapPushNotificationRecords/MapPushNotificationRecords.tsx`
- 私有模块/语言分片：`map-push-record`；接口依据：dispatcher/mapPushRecord（三 operation）

## 1. 交付范围（全部完成）

### 列表页整页重写（替换迁移过渡占位）
- `pages/map-through/MapPushNotificationRecords/MapPushNotificationRecords.tsx` + `.module.css`：
  - ApexTableReact **request 模式**：服务端分页（GET+query 平铺 `pageNo` 从 1 计数，G04 同 P09 形态，联验实证）、稳定行 ID=id（int64 经 stringFieldRowId 收敛字符串）、内建取消/错误重试、列偏好 `map-push-record:main`（四切片整体替换持久化 + P03 铁律接线）；接口仅有分页参数，**排序不启用（G09）、无筛选栏**（旧实现同边界：接口无任何筛选参数）；
  - 列结构逐列核对旧版：地图名称 170/地图版本 150/推送SLAM底图 120（boolean 明确 false 显示「否」、缺失留白——false ≠ 缺失）/推送结果（子记录状态计数 Tag 汇总，弹性列，未知枚举显示协议原值灰 Tag）/创建时间 170（displayDateTime 秒级）/操作 250（**默认钉右** defaultPinning，旧版 fixed:right 同语义）；空值一律留白（旧「-」占位废弃）；
  - **轮询接线（P05 样板）**：useVisiblePolling 约 5 秒可见串行轮询（联验实测稳定 6000ms 节拍与 P05 实证 5991–6011ms 一致）——推送状态由后端异步推进（P09 推送命令受理 ≠ 子记录终态），本页为命令状态追踪页，状态核实依赖持续重查（A12/D14）；旧版手动刷新按钮按按钮纪律废弃；
  - 命令操作（记录级整批 / 子记录级单台 × 重推 / 取消推送）升级 confirmCommand 命令确认（列明对象车辆清单（>5 台自动合并计数）+影响说明+「提交≠完成」固定附注；旧版仅 Popconfirm 一问）；行级防连点（pendingRecordKey/pendingSubKey 两集合分离）；受理成功按「受理」语义提示（命令提交 ≠ 推送完成）；**失败/超时不自动补发**，状态核实完全依赖本页轮询的真实记录，无本地伪造状态；
  - 取消入口边界（旧实现同款）：仅 WAITING/RUNNING 子记录可取消；整批取消仅在该批存在可取消子记录时开放；重推与取消共用按钮码 `map-push-record:re-push`（后端无取消专用码，登录响应实证两码下发）。
- `features/map-push-record/components/MapPushSubRecordTable.tsx` + `.module.css`：
  - 展开行子表 **Apex data 模式**（一个批次的车辆子记录全量随主行下发，真实完整小集合，禁 antd Table）；列逐列核对旧版：车辆名称 250/推送状态 110（Tag 五色映射）/完成时间 170（displayDateTime）/失败原因/等待原因（长文案截断+Tooltip）/操作 200（重推+取消推送）；空值留白；
  - **前端筛选+受控分页**（接口无子级查询/分页参数，旧实现同为纯前端）：车辆名称关键字（含匹配）+推送状态精确匹配；分页用 Apex 受控 PaginationState（TanStack 0 基 pageIndex）——筛选变化回第一页、数据变少页码兜底回退最后一页（旧实现同语义），替代旧版自绘 antd 分页器。
- `features/map-push-record/utils/pushState.ts`：`isSubRecordCancellable` 纯函数（主表/子表共用入口显隐判断；独立模块避免组件文件混出非组件导出）。

### 服务层（owner=P12）
- `services/map-push-record/map-push-record.service.ts` + `.types.ts`（三 operation，逐项核对 OpenAPI MapPushRecord/MapPushSubRecord/PageMapPushRecord/MapPushRecordPageParamMapPushRecord/MapRePushParam）：
  - `GET /dispatcher/mapPushRecord/pageMapPushRecords`（query 平铺 G04 同款）；`POST /dispatcher/mapPushRecord/rePushMap`；`POST /dispatcher/mapPushRecord/cancelPushMap`（body=MapRePushParam：mapPushRecordId + 可选 mapPushSubRecordIds）；
  - 协议纪律：重推/取消为影响现场车辆的命令类写操作，本层不做自动重试；cancelReason（OpenAPI 保留字段）与 mapDownloadLink 原样承接不丢。

### 路由与权限
- 路由 `map-through-map-push-records` 解除 `pending`，`meta.i18nNamespaces: ['mapPushRecord']`；菜单码挂既有 `PERM.MAP_PUSH_RECORD_VIEW`；按钮码 `PERM_BUTTON.MAP_PUSH_RECORD_RE_PUSH`（重推/取消共用）；**登录响应实证两码真实下发**（155 码权限树含 map-push-record:view + map-push-record:re-push），不凭空造码。

### i18n（四语言分片 + 登记）
- `mapPushRecord` 命名空间四分片 30 key 同构（en-US/zh-TW/ja-JP/ko-KR 逐 key 比对一致）：en-US 旧真译逐条沿用（Map Name/Push SLAM Base Map/Re-push initiated/Failure Reason 等，出错拼接插值改 `{{msg}}` 双花括号）；zh-TW/ja-JP/ko-KR 按 B1 基线补译；「推送SLAM底图/推送」公共词与 P09 mapList 分片同形（プッシュ/푸시）、「车辆名称/操作/创建时间」与既有分片对齐（車両名/작업/작성日時 등）；确认框影响说明按旧真译风格补译；`i18n.ts` 四表各增 1 行。

## 2. 门禁（交付终态全绿）

| 门禁 | 结果 |
| --- | --- |
| `pnpm lint` | 通过（333 文件 0 警告 0 错误；曾抓出 fast-refresh 混出导出一处，纯函数移独立模块后复跑通过） |
| `pnpm typecheck` | 通过（tsc -b --noEmit；曾抓出 Apex cell getValue() 返回 {} 需显式断言三处，修复后通过） |
| `pnpm check:structure` | 通过（331 源文件；无违规导入方向/硬编码主机） |
| `pnpm build` | 通过（tsc -b + vite build；chunk 警告为既有 apex-table-react 体积） |

## 3. 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local root/MD5；curl API 层先行登出后再开浏览器会话）

### API 层（curl）
1. 登录 code=200 + `map-push-record:view`、`map-push-record:re-push` 两码真实下发（permissions 平铺 155 码）；
2. 分页 `GET pageMapPushRecords?pageNo=1&pageSize=5` → code=200，**GET 平铺 query 实证**（G04 同款：OpenAPI 声明 pageParam 对象参数，实际后端接受平铺 pageNo/pageSize），My-Plus 响应形态（records/total/size/current）；**当前环境 total=0 真实空态**（P09 联验以来推送命令从未执行，与本页空态渲染互证）；
3. 越界页码 `pageNo=99` → code=200 空记录（无异常）；无令牌对照 → `1000000 用户未登录`（Bearer 识别）；登出 code=200。

### 浏览器整页
- 守卫回跳：清残留会话后未登录直访 → `/login?redirect=%2Fmap-through%2Fmap-push-records`；登录后**落点=redirect 目标页**（闭环）；
- 空态渲染：「暂无数据」+ 分页器「共 0 条」+ 翻页/跳页禁用 + 表头 7 列（行展开/序号齿轮/六业务列）逐列与代码定义一致；
- 列偏好闭环：齿轮面板（操作列「固定在右侧」默认选中=defaultPinning 生效）→ 隐藏「地图版本」→ 表头 6 列 + `apex-table:columns:["apex-admin","root","default","map-push-record:main"]` 落盘 → 刷新持久 → 恢复默认还原 7 列；
- 轮询节奏：整页重载干净环境下连续 5 轮请求间隔 5955/5998/6000/6016ms（5 秒静默+请求往返，P05 同款）；**联验中途出现的 6s→10s→60s 退避为验证脚本注入的 XHR 包装干扰（诊断伪象，AGENTS 第 5 节同类），干净环境不复现**；
- 五语言：en-US/zh-TW/ja-JP/ko-KR/zh-CN 逐语言切换，html lang 同步、六列表头逐语言正确（en 旧真译/Push SLAM Base Map、繁 推送SLAM底圖、日 SLAM ベースマップをプッシュ、韩 SLAM 베이스맵 푸시）、Apex 空态/分页文案随语言（No data/暫無資料/データがありません/데이터 없음）；
- 双主题双宽度四组截图：`.run-lock/shots-p12/p12-{1366,1920}-{light,dark}.png`（主题经 settings 直写+reload 落位；1366-dark 首帧黑帧为旧帧伪象，重截正常）；
- 收尾：真实登出（logout code=200）+ localStorage 清理 + 页签关闭 + dev server 停止 + 凭据临时文件清除。

## 4. 限制与待验项（写副作用纪律，同 P05/P09 先例）
- **重推（rePushMap）/取消推送（cancelPushMap）执行留专用环境**：两者为影响现场车辆的命令（产生/变更真实推送任务），按规格不授权默认试发。确认层已交付：confirmCommand 对象/影响/提交≠完成、防连点、取消零请求（代码路径）；但**子表展开、四操作按钮、确认框交互在真实数据下的运行时验证未能执行**（联验环境 total=0，产生推送记录本身需要执行被禁止的推送命令）——与 P09「推送执行留专用环境」同条件，登记待专用环境复验：展开行子表筛选/分页、记录级/子记录级重推与取消的确认→受理→列表状态演进全链路、P09→P12 推送→记录往返联验（任务卡允许排在两者合并后）；
- 无权限按钮隐藏形态待普通用户账号复验（root 具全部权限无法现场复现；机制与 P03–P11 同源已验证）；
- IAB 伪象延续既往登记（渲染管线冻结需垫片/截图黑帧/主题切换需 reload），非应用缺陷。

## 5. 契约观察（无新增 G 编号）
- pageMapPushRecords GET 平铺 query（G04 同款第 N 例，随 G04 既有台账口径，不新立缺口）；接口无筛选/排序参数（G09 既有口径）；重推/取消对不存在 id 的行为未探测（写命令纪律），如后续专用环境发现异常再登记。
