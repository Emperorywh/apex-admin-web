# P39 完整车辆详情——任务交接记录

| 字段 | 内容 |
| --- | --- |
| 任务 / 负责人 / 基线 | P39 完整车辆详情；ZCode 串行调度（run-20260918-p39-4d17f0fe）；分支 apple-rxx，基线 b51b403（P38 交付后） |
| 开始 / 更新日期 | 2026-09-18 / 2026-09-18 |
| 实施状态 | **已完成**（前端交付 + 带令牌真实联验通过；工作区页签/独立窗口双形态、实体页签隔离、可见轮询、五语言） |
| 验证状态 | **已通过**（带令牌只读全链路 + 双主题双宽度截图 + 五语言）；登记例外：① P05 列表抽屉→完整详情**点击链路**受 IAB rAF 冻结阻塞未走通（两端点已分别实证，见「未完成/后续」）；② 令牌过期收敛随 P03/P05/P38 三轮已实证，本轮未复现 |

## 依据

- 规格：第 3.3（车辆详情行）、7（详情默认工作区页签+独立窗口入口、快速预览不替代完整详情路由）、8.1/8.2（页签/可见轮询 A12/D14）、18（国际化）；D08。
- 旧路由：`C:\code\dd\.umirc.ts` `/vehicle-info`（layout:false，access=vehicle-list:view）。
- 旧可达组件：`VehicleInfo/index.tsx`（裸值 query 取 vehicleKey → GET getVehicleState → transformVehicleInfo 递归展平三列 Descriptions；失败 message；无轮询——P39 按任务卡升级为可见轮询实时页）。
- **参数形状核对结论（TASKS「核对旧调用点参数」）**：旧源码中**没有任何导航到 /vehicle-info 的调用点**（grep 全仓仅 .umirc.ts）——直访 URL 形态；旧页面 `location.search.substring(1)` 取整段 query 作 vehicleKey，历史链接为裸值 `?KEY`（与 P38 order-info 同构）。
- API 哈希：A82E9B5F…49C7C（开工复核与规格一致；`getVehicleState`：GET + query vehicleKey 必填 string → ResultVehicleStateRecord；契约比旧页面类型多 `load` 载货子对象，递归展平语义自然覆盖）。
- 前置交接：P05.md（车辆管理服务/控制样板/详情抽屉/P39 往返登记）、P38.md（实体页签形态/参数契约/i18n 纪律）。

## 形态与参数契约（登记 contracts 第 4、6 节）

1. **工作区页签形态（默认）**：`vehicle-info` 路由从布局外独立页移入受保护根（相对段 `vehicle-info`，完整路径 `/vehicle-info` 不变；perm=vehicle-list:view；hideInMenu 不进菜单/落点候选）。不同 `vehicleKey` 自动获得独立页签、独立缓存实例与独立请求 scope（实体页签第二消费者，P40 接入时同构迁移）。
2. **独立窗口形态**：工具栏「独立窗口」按钮经 `openStandaloneWindow('/vehicle-info', { vehicleKey })` 打开同一路径（同守卫同会话，同实体复用窗口）。
3. **参数单一真相**：命名参数 `?vehicleKey=<车辆唯一标识>`（`buildVehicleInfoPath`）；历史裸值 `?KEY` 与页签规范化变体 `?KEY=` 均由 `parseVehicleInfoSearch` 兼容解析（A22 历史链接可用）。
4. **轮询（相对旧实现的升级，任务卡明示）**：`useVisiblePolling` 约 5 秒可见串行；**失败清空对应远端区域**（detail=null + 真实错误文本，不保留旧快照误导）并退避重查；**车辆不存在（data=null）为稳定态停止轮询**。旧实现单次查询无轮询。

## 操作映射（来源 → 新实现 → operation → 权限 → 状态）

| 旧操作 | 新实现 | operation（已核对 OpenAPI） | 权限 | 状态 |
| --- | --- | --- | --- | --- |
| 直访 /vehicle-info 查询车辆状态 | `pages/vehicle-info/VehicleInfo`（工作区页签/独立窗口双形态；参数解析见 vehicleDetailNavigation） | GET /fms/v1/dispatcher/vehicle/getVehicleState（{vehicleKey}，G04 平铺 query） | vehicle-list:view（守卫按 meta.perm） | **带令牌联验通过**（真实车辆 2094324701569642496 完整渲染） |
| transformVehicleInfo 递归展平三列 Descriptions（33 叶子字段，orderRefer 标签） | VehicleDetailPanel + `vehicleDetailOptions.flattenVehicleState`（递归展平/标签映射不到显示原字段名/未知枚举原值/布尔既定语义/createTime→displayDateTime；空值留白——旧实现 null.toString() 会崩，按空值纪律修正） | 同一响应喂数据 | 同上 | **联验通过**（全部字段渲染：类型=小车/连接状态=在线/安全域=正常/紧急停车=正常/执行状态=空闲/调度状态=启用/已开启/未暂停/载货=否；订单编号/名称/状态与 load 空值留白；x/y 原字段名；创建时间 2026-08-31 15:21:37） |
| getFixedFieldText 枚举/布尔转中文 | connectionState/estop/dispatchState 复用提升后的共享映射（constants/vehicle）；orderState 复用 constants/order（P03/P38 同一真相源）；vehicleProcStatus 新增八值映射（旧 RobotStatus 同口径，features/vehicle-detail 私有单一消费者）；type 未知值显示原值（差异登记：旧缺省「小车」，按规格 18.3 不臆造） | — | — | 已按契约处理 |
| 失败 message.warning | StateBlock offline 呈现真实错误（按钮纪律：无重试按钮，恢复靠轮询退避自动重查） | 同上 | 同上 | 代码路径审查+数据实况（联验窗口 rAF 冻结未自然触发后端失败分支，请求层 1000000 收敛为公共机制） |
| （旧无）实时性 | useVisiblePolling（A12/D14：可见串行/后台快照/恢复即查/失败退避） | 复用 fetchVehicleState | 同上 | 已接线；轮询请求实况经 pageVehicles 同机制页面交叉实证 |
| 缺参数/车辆不存在/无权限/会话过期 | 缺参数：StateBlock gap「缺少车辆编号参数」+**零请求**（后端对空 vehicleKey 返回 HTTP 500，实证防御必要）；不存在：code=200+data=null（带令牌实证）→ StateBlock gap「车辆不存在或已被删除」+停轮询；无权限：路由守卫；会话过期：请求层 1000000 收敛（公共机制，P03/P05/P38 已三轮实证） | — | — | **联验通过**（缺参数/不存在分别实证） |
| 独立窗口（D08 新增形态，旧系统无） | 工具栏按钮 → openStandaloneWindow | — | 同守卫 | **联验通过**（新窗口「车辆详情 · 调度系统」完整外壳，同路径同参数） |
| （P05 联动）列表详情抽屉 | VehicleDetailDrawer footer 新增「完整详情」按钮（关抽屉 + 宿主 VehicleDisplay 组装 navigate(buildVehicleInfoPath(key))——跨 feature 禁导入，导航由 pages 层组装） | — | — | 按钮与导航代码门禁覆盖；**点击链路见「未完成/后续」①** |

## 文件归属

新增（features/vehicle-detail 为 P39 私有域）：
- `src/features/vehicle-detail/components/VehicleDetailPanel.tsx` / `.module.css`（详情业务组件：getVehicleState + useVisiblePolling 喂 Descriptions）
- `src/features/vehicle-detail/vehicleDetailNavigation.ts`（参数名/构造/解析单一真相，含裸值双变体兼容）
- `src/features/vehicle-detail/vehicleDetailOptions.ts`（flattenVehicleState 递归展平 + 标签/枚举/布尔映射 + VEHICLE_PROC_STATUS_LABEL）
- `src/constants/vehicle/vehicleDisplayOptions.ts`（P39 提升自 features/vehicle-list：VEHICLE_TYPE_LABEL/CONNECTION_STATE_LABEL/CONNECTION_STATE_TAG_COLOR/DISPATCH_STATE_LABEL/ESTOP_LABEL/formatComponent——P05/P39 两域单一真相）
- `src/services/vehicle/vehicle-state.service.ts` / `.types.ts`（fetchVehicleState，owner=P39；VehicleStateRecordDto 及 SafetyState/BatteryState/AGVDimension/AgvPosition/Load 子 DTO 按契约全字段）
- `src/pages/vehicle-info/VehicleInfo/VehicleInfo.tsx` / `VehicleInfo.module.css`（整页重写）
- 五语言分片：`src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/vehicleInfo.ts`（67 key 四语言同构）

修改（交接登记）：
- `src/router/definitions.tsx`：vehicle-info 移入受保护根（摘除 standaloneMeta/migrationPending）+ meta（hideInMenu/vehicleInfo 命名空间）；vehicle-deploy-vehicle-display meta 增声明 vehicleInfo（抽屉「完整详情」文案预载）。
- `src/features/vehicle-list/vehicleListOptions.ts`：枚举映射与 formatComponent 改为共享常量 re-export（P05 三处消费者导入路径不变）；运行模式/指令菜单/表单转换仍为该域私有。
- `src/features/vehicle-list/components/VehicleDetailDrawer.tsx`：新增 onOpenFullDetail 回调 prop + footer「完整详情」按钮 + useTranslation 改双命名空间 fallback。
- `src/pages/vehicle-deploy/VehicleDisplay/VehicleDisplay.tsx`：新增 useNavigate 导入 + 传 onOpenFullDetail 回调（pages 层组装跨域导航）。
- `src/i18n/i18n.ts`：vehicleInfo 四语言懒加载登记。
- `src/features/order-detail/components/OrderDetailPanel.tsx`：**顺带修复 P38 缺陷**——`t('orderInfo:任务不存在或已被删除')` 带 ns: 前缀在 nsSeparator=false 下显示字面量（违反 AGENTS 第 6 节），改 useTranslation(['orderRecord','orderInfo'], nsMode fallback) + 无前缀 key；复验通过（order-info 不存在任务分支显示正确文案无前缀泄漏）。

## 共享契约

- 消费：请求协议/Result 解包/1000000 收敛（第 1 节）；路由守卫/实体页签/独立窗口/登录回跳（第 2、4 节）；StateBlock（第 3 节）；useVisiblePolling（第 4 节）；vehicleList 既有文案（i18n-map）。
- 新增/变更（已登记 contracts 变更日志）：车辆详情参数契约（vehicleKey 命名+裸值兼容）；实体页签第二消费者；车辆域常量提升 constants/vehicle（vehicleListOptions 转 re-export）；P05 抽屉「完整详情」宿主组装模式。
- 轮询：本页为实时状态页，消费 useVisiblePolling；失败清空远端区域（任务卡专项验收）；不存在态停轮询。
- 无写操作、无文件传输——A13/A14/A16 不适用。

## 接口缺口

| 编号 | 本页具体化 | 状态 |
| --- | --- | --- |
| G04 | getVehicleState 参数平铺 query 后端接受实证（带令牌 code=200） | 已验证 |
| G09/G10 | 本页无表格无分页；createTime int64 毫秒时间戳 JSON number 承载（真实样本 1788160897564 无精度异常，displayDateTime 消费） | 不适用/已验证 |
| 新增登记 | 车辆不存在时后端返回 code=200 且 data=null（2026-09-18 带令牌实证，与任务详情同族）；**vehicleKey 为空时后端 HTTP 500「失败」**——页面缺参数分支不发空参请求（防御实证必要） | 已验证可用 |
| 契约超集 | VehicleStateRecord 含 load 载货子对象（旧页面类型无此字段）：递归展平语义自然覆盖，label 无映射显示原字段名（与旧 orderRefer||key 口径一致）；真实车辆样本 load=null 留白 | 已验证 |

## 国际化

- vehicleInfo 命名空间四语言分片 67 key 完全同构（脚本审计 key 集合零差异）：详情标签 31 + 枚举/布尔值文案 29 + 页面文案 7。
- en-US：旧真译全部沿用（orderRefer 标签与 RobotStatus/eStops/DispatchState/OrderStatus 枚举资源）；zh-TW/ja-JP/ko-KR：旧资源对本页多为简中复制，按 B1 基线补译并对齐既有分片术语（線上/連線中斷、調度、佇列/掛起/建立時間、ディスパッチ/マップ/キュー内/一時停止、디스패치/맵/대기열/일시 정지）；「已暂停」日韩旧真译（一時停止中/일시정지됨）沿用。
- **自查缺陷（已修复）**：zh-TW 分片初版误把 key 写成繁体——本项目 key 即简中文案（value 才是译文），对照既有 zh-TW 分片（`'调度状态': '調度狀態'`）全部改回简中 key，浏览器复验繁中生效（車輛唯一標識/線上/空閒/建立時間/獨立視窗）。教训：繁日韩分片 key 必须与页面 t() 的简中 key 一致。
- 语言边界：后端原文（车辆名称/地图名称）不翻译；未知枚举显示原值；x/y 无映射显示原字段名；html lang 随切换同步（五语言实测）。

## 验证

工程门禁（2026-09-18，联验后复跑）：
- `pnpm lint`：0 警告 0 错误（272 文件）
- `pnpm typecheck`：通过
- `pnpm check:structure`：通过（270 源文件；曾抓出 features 跨域导入一处，改为宿主页组装导航后通过）
- `pnpm build`：通过（tsc -b + vite build）
- 调试残留 grep：0

运行时验证（dev 代理 192.168.0.158:8888，凭据 .env.local root/MD5）：
- **API 层**：登录 code=200 activated=true；pageVehicles 真实 56 辆取 vehicleKey；getVehicleState 真实车辆 code=200 完整 17 顶层字段（orderState/load=null 触发空值留白分支；createTime 毫秒时间戳）；**不存在车辆 code=200+data=null 实证**；**缺 vehicleKey 后端 HTTP 500 实证**（页面零空参请求防御正确）。
- **浏览器**（1366×768 与 1920×945）：
  - 未登录直访 `/vehicle-info?vehicleKey=…` → 守卫回跳 `/login?redirect=%2Fvehicle-info%3FvehicleKey%3D…` 参数完整；登录成功按 redirect 回跳，标题「车辆详情 · 调度系统」✓
  - 工作区页签形态：顶栏「车辆详情」页签生成、Dock/顶栏外壳完整 ✓
  - 整页渲染：工具栏（车辆详情+编号复制+独立窗口按钮）+ 三列 Descriptions 33 叶子字段（枚举映射/布尔语义/空值留白/秒级时间/x-y 原字段名）✓
  - 实体页签隔离：客户端导航第二辆车 → 两个「车辆详情」页签并存，各自内容与页签 key 一致互不串 ✓
  - 独立窗口：按钮 → 新窗口同路径同参数完整外壳 ✓
  - 缺参数 `/vehicle-info` → StateBlock「缺少车辆编号参数」+ 零 getVehicleState 请求 ✓；不存在车辆 → StateBlock「车辆不存在或已被删除」✓；裸值 `?2093172593386483717` → 正常解析渲染 ✓
  - 五语言：en-US/zh-TW/ja-JP/ko-KR/zh-CN 逐语言文案 + html lang 同步 ✓（zh-TW 缺陷修复后复验）
  - 双主题双宽度四组截图（`.run-lock/shots-p39/`）：1920 深/浅干净通过；1366 两组内容可判正确但画面带主题下拉**旧帧伪象**（IAB rAF 冻结不合成新帧，验证操作残留非页面缺陷，登记见下）△
  - P38 缺陷修复复验：/order-info 不存在任务 → 「任务不存在或已被删除」正确显示无前缀泄漏 ✓
- 验证残留清理：浏览器验证页签全部关闭、dev server 已停止、临时凭据文件已删除。

## 联验环境教训（沉淀给后续轮）

- **IAB rAF 冻结的波及面比 P05 登记的更大**：本次整个会话 rAF 持续被抑制（桌面窗口失焦期），后果有三：① Apex 虚拟表格骨架不消除/行不绘制（数据真实到达：network 响应 7–25KB、分页 total 正常——P03 页同样复现，交叉实证与被验页面代码无关）；② `tab.screenshot()` 返回旧帧（页面不合成新帧，截图含历史时刻的悬浮元素伪象）；③ 关闭重开标签页无法恢复（渲染进程级）。判别手法：`requestAnimationFrame` 探针 400ms 计时；交叉打开无关页面对照。**等待窗口可见性恢复后补验**，不要误改代码。
- antd Dropdown 受控 open 的关闭无法靠合成 mousedown/click 触发（需真实交互）；验证脚本切换主题后应再点一次按钮收起菜单再截图。
- 非简中语言下顶栏按钮 title 随语言变化（AGENTS 第 6 节既有教训的验证侧实践）：语言循环用 `header button` 结构索引定位，不按 title 文案定位。

## 未完成 / 后续

1. **P05 列表抽屉→完整详情点击链路**：IAB rAF 冻结导致列表行不渲染（数据实况正常），行内「详情」按钮无法点击，抽屉与「完整详情」按钮的**运行时点击**未走通。两端点已分别实证：详情页全链路（直接导航路径）✓、页签生成机制（客户端导航）✓；按钮/回调代码经门禁+审查。**下轮联验窗口 rAF 恢复时优先补验**：列表行详情 → 抽屉「完整详情」→ 新页签 → 返回列表快照保留。
2. 令牌过期 1000000 收敛：P03/P05/P38 三轮已实证，本轮 root 会话存活超预期未复现；不再登记。
3. 页签标题不带实体标识为既有 TabsBar 机制（同 P38 登记，留 V01 评估公共扩展）。
4. P40 接入时按本页同构迁移 /analyze-visual/server-resource-monitor 到受保护根。
