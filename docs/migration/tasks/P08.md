# P08 告警码管理 交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
- 任务卡：TASKS.md「P08 告警码管理」（批次 B3 / 前置 B2 样板门禁）
- 执行轮次：run-20260919-auto-030525（2026-09-19，单任务串行轮）
- 入口：`/vehicle-deploy/alarm-code-management`；目标：`src/pages/system-involve/AlarmCodeManagement/AlarmCodeManagement.tsx`
- 私有模块/语言分片：`vehicle-alarm-code`；接口依据：dispatcher/vehicleAlarmCode（六 operation）

## 1. 交付范围（全部完成）

### 列表页整页重写（替换迁移过渡占位）
- `pages/system-involve/AlarmCodeManagement/AlarmCodeManagement.tsx` + `.module.css`：
  - ApexTableReact **request 模式**：服务端分页（GET+query 平铺，`pageNo` 从 1 计数、`toBackendPage` 换算）、稳定行 ID（`stringFieldRowId('id')`）、内建取消/错误重试、列偏好 `alarm-code:main`（四切片整体替换持久化，P03 实证铁律接线）；
  - 列结构逐列核对旧版：告警码 300 / 告警描述（跟随界面语言展示 desc + 金色处理建议行，匹配不到回退 zh_CN 再回退第一条；空值留白不写「—」）/ 创建时间 180（`displayDateTime` 秒级，缺失留白）/ 操作 180（编辑/删除按按钮码显隐）；排序不启用（G09：旧页面无排序）；
  - 工具行：告警码搜索框（旧 Search 同形态）+ 查询按钮；右侧 新增/上传/下载 三按钮（均按钮码无权限隐藏）；无轮询、无手动刷新按钮（普通 CRUD 配置数据，新鲜度由写后自动刷新保证）；
  - 删除升级 confirmCommand 破坏性确认（danger 红键 + 删除影响说明 + 对象清单 + 「提交≠完成」附注；旧版仅 Popconfirm 一问），行级防连点；
  - 上传（全量覆盖）：`beforeUpload` 校验 .xlsx/.xls（其余 LIST_IGNORE + 提示）→ confirmCommand（danger +「继续上传」红键）→ 经 **传输管理器 beginTransfer** 登记（切页继续、关页提示、孤儿完成提示）；`onUploadProgress` 真实字节进度上报（total 缺失即不确定进度，不伪造百分比，G11）；按钮 loading + 页内进度行；**G05 媒体类型按旧实现 multipart/form-data、字段名 `file` 适配**（联验实证，见第 3 节）；
  - 下载：POST Blob 通道（`api.downloadPost`）经传输管理器登记；JSON/HTML 响应不保存为伪文件（DoD 9 防线 + 拦截器统一解包双保险）；文件名 content-disposition 优先（RFC 5987 经 `resolveDownloadFilename`）回退本地默认名；objectURL 释放；在途防连点。

### 新增/编辑弹窗（features/vehicle-alarm-code）
- `features/vehicle-alarm-code/components/AlarmCodeFormModal.tsx`：
  - 双模式（editTarget null=新增）；**草稿保留**（A13/DoD 7：关闭不清输入、常挂 `destroyOnHidden=false`、失败留稿、显式「清空」入口）+ `useTabDirtyGuard`（输入/行增删登记页签脏标记，关页/刷新统一确认）；
  - 多语言行 Form.List：语言（必填，`ALARM_LOCALE_OPTIONS` 五语言，locale 原样提交协议）/ 告警描述（必填）/ 处理建议（选填），与旧实现同边界；提交过滤 locale 为空的残行（旧兜底同款），空行集提交空数组；
  - 告警码 maxLength=128（旧同边界）；提交防重复（submitting 早退）；成功才清稿并刷新列表；
  - 表单标签横排居左（外层 6/18 分栏既定参数；嵌套行固定 96px 标签宽 + wrapper `min-width:0`，AGENTS 第 3 节纪律）。
- `features/vehicle-alarm-code/constants.ts`：`ALARM_LOCALE_OPTIONS`（zh_CN/en_US/ja_JP/ko_KR/zh_TW，标签为语言自称不译）+ `getAlarmLocaleLabel`。

### 服务层（owner=P08）
- `services/vehicle/vehicle-alarm-code.service.ts` + `.types.ts`（六 operation，逐项核对 OpenAPI 与旧实现 method/path/请求体形态）：
  - `GET /dispatcher/vehicleAlarmCode/pageVehicleAlarmCodes`（query 平铺，G04 同形态）
  - `POST addVehicleAlarmCode` / `POST updateVehicleAlarmCode`（id 定位+全量）/ `POST deleteVehicleAlarmCode`（**按 alarmCode 字符串定位**，协议原样非 id）
  - `POST uploadVehicleAlarmCodeFile`（multipart/form-data 字段 file，带 onUploadProgress）
  - `POST downVehicleAlarmCodeFile`（空请求体 {} 原样 + Blob + 完整响应）

### 公共文件改动（记录影响并验证消费者）
- `services/request/request.ts`：新增 **`api.downloadPost`**（POST + Blob + `apexRawResponse` 完整响应，语义与 downloadGet 一致：JSON 错误仍走统一 Result 解包、signal 透传、主动取消静默）。既有消费者不受影响（纯新增；typecheck/build 全绿；P03 导出 downloadGet 未动）。

### 路由与权限
- 路由 `vehicle-deploy-alarm-code` 解除 `pending`，`meta.i18nNamespaces: ['vehicleAlarmCode']`；perm 挂后端已有码 `PERM.VEHICLE_ALARM_CODE_VIEW`（后端权限树真实下发 MENU id=27；权限树 path=/vehicle-deploy/vehicle-alarm-code 与任务卡入口拼写不同——沿用任务卡路径，新路由树以 meta.perm 判权，不公开访问不凭空造码）；按钮码 add/upload/download/update/delete 与旧 §7 一致，无权限隐藏。

### i18n（四语言分片 + 登记）
- `vehicleAlarmCode` 命名空间四分片 49 key 同构：en-US 旧真译逐条沿用（插值改 `{{var}}` 双花括号），zh-TW/ja-JP/ko-KR 按 B1 基线补译（告警碼/アラームコード/알람 코드 按 terminology.md；公共词与 P07 nodeMapping 分片对齐）；新增文案（传输进度行/删除确认影响）按旧真译风格补译。
- `i18n.ts` 四表登记（en-US/zh-TW/ja-JP/ko-KR）；菜单标题「告警码管理」T00.8 已有四语言真译，无需新增。

## 2. 门禁（交付终态全绿）

| 门禁 | 结果 |
| --- | --- |
| `pnpm lint` | 通过（301 文件 0 警告 0 错误） |
| `pnpm typecheck` | 通过（tsc -b --noEmit） |
| `pnpm check:structure` | 通过（299 源文件；无违规导入方向/硬编码主机） |
| `pnpm build` | 通过（tsc -b + vite build；chunk 大小警告为既有 apex-table-react 体积） |

## 3. 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local [登录凭据已脱敏]；IAB rAF+修正版 RO 双垫片）

### API 层（curl，单会话纪律：curl 先行全部完成后登出，再开浏览器会话）
1. 分页 `GET pageVehicleAlarmCodes?pageNo=1&pageSize=10` → code=200，total=193（真实历史数据）；
2. 模糊筛选 `alarmCode=4217` → 命中 1 条；`alarmCode=APEXTEST` → 0 条（空态如实）；
3. 新增 en_US 记录 → 200 落库一致；最小体（无 records）→ `2000450 车辆告警描述不能为空`（records 实际必填，OpenAPI 标可选——前端表单按实际契约必填行，行为兼容）；空数组 records → 同 2000450；
4. 编辑 `updateVehicleAlarmCode`（id + 全量）→ 200，改名与记录真实落库（无 G18 类缺陷）；
5. 下载 `downVehicleAlarmCodeFile`（POST 空 body）→ 22092 字节 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` + `content-disposition: attachment; filename=AGV_Alarm_Code.xlsx`；文件三列（告警ID/告警名称/建议处理措施）= 中文主表内容；
6. **上传（G05 实证）**：`curl -F "file=@…xlsx"`（multipart/form-data 字段 file）→ `code=200`；上传后总数与逐码不变（下载→回传零净变化）；伪 xlsx（文本内容）→ `code=500 失败` 且数据不变（错误不被静默吞掉）；
7. 删除（按 alarmCode 定位）→ 200，后端自清理恢复 193 条；logout 200。

### 契约差异与观察（详见 gaps.md）
- **G05 更新**（uploadVehicleAlarmCodeFile 实证）：OpenAPI 声明 `application/json` 内 binary 字段，真实后端只接受 **multipart/form-data、字段名 `file`**（curl multipart 200 / 与文档形态不符），本层按旧实现适配并联验实证。
- **2000450**：add/update 的 `alarmCodeRecords` 后端实际必填（OpenAPI 标可选）——前端多语言行语言/描述必填校验已覆盖，不构成行为差异，登记于本记录不单开缺口。
- **G20 新增**（后端暂态异常观察）：联验初期对同一告警码连续 4 次 add 返回 `code=500 失败`（约 3 分钟窗口）；随后以完全相同请求体及 zh_CN/en_US/双记录/重码增删循环/改名→删除→重加等 10+ 变体全部 200 且落库一致，未能复现；业务级重复码为 `2000430 车辆告警码已经存在`（有独立业务码，与 500 无关）。判定为后端暂态异常（疑似事务残留），客户端影响=错误提示可重试，登记观察待后端核查，非前端缺陷。

### 浏览器整页（IAB，修正版 RO 垫片 + 探针自测通过后断言）
- 守卫回跳：未登录直访 → `/login?redirect=%2Fvehicle-deploy%2Falarm-code-management`；登录后**落点= redirect 目标页**（闭环）；
- 列表真实渲染：193 条真实数据、10 行/页、分页「共 193 条」、告警描述单元格 desc + 金色建议行（无建议留白）；
- 筛选：命中（请求参数 `alarmCode=ZZZNOTEXIST` 抓包实证）→ 空态「暂无数据」→ 清空恢复全量；
- 新增弹窗：结构（告警码必填/多语言行/清空/取消/确定）；多语言行添加/语言选择（五选项）/校验拦截（「请选择语言」+ 输入保留）；**服务端失败留稿**（重复码 100000 → toast「创建车辆告警码出错：车辆告警码已经存在」+ 弹窗不关 + 输入完整保留）；成功路径 → toast + 列表刷新（194）；
- 编辑弹窗：按行回填（告警码 + 双行 desc/hint）真实预填；取消不改数据；
- 删除：confirmCommand 五要素（danger 红键实证）；取消路径行保留；确认路径 → toast「删除车辆告警码成功」+ 后端 193 自清理；
- 上传：文件注入 → 确认框（「上传将全量覆盖」+ 覆盖说明 + danger「继续上传」+ 提交≠完成附注）→ 取消路径零请求 → 确认路径 → 真实请求（performance entry）+ toast「上传车辆告警码文件成功」+ 总数 193 零净变化；
- 下载：点击 → 真实请求（performance entry +1）+ toast「下载车辆告警码文件成功」；
- 五语言：en-US/zh-TW/ja-JP/ko-KR 逐语言切换，html lang 同步（en-US/zh-TW/ja-JP/ko-KR），页面文案（列头/按钮）逐语言正确；
- 双主题双宽度四组截图：`.run-lock/shots-p08/p08-{1366,1920}-{light,dark}.png`（新页签整页载入方式规避浮层/黑帧伪象；主题经 persist:apex-admin:settings 预置）；
- 收尾：真实登出（auth persist 清空、落 `/login?redirect=…`）、页签关闭、dev server 停止；测试数据自清理（最终 total=193 还原）。

## 4. 环境教训（沉淀 AGENTS 第 5 节者已单独登记；此处备查）
- IAB 高层 locator 点击/Enter 在部分页面状态超时（元素被旧帧遮挡或焦点态异常），页面内 `evaluate` 原生事件派发可靠——与既往「旧帧伪象」同源；
- antd Modal wrap 为 fixed 定位，`offsetParent` 可见性判定失效（恒 null），应用 `getComputedStyle(w).display !== 'none'`；按钮双字文案 antd 插入空格，匹配前 `replace(/\s+/g,'')` 归一化；
- 主题切换经 UI 下拉易留浮层/黑帧伪象；联验截图改「localStorage 预置 `persist:apex-admin:settings`（双编码 JSON）+ `apex-admin:theme` 镜像 → 整页 reload」路径，天然无浮层。

## 5. 限制与待验项
- 无权限按钮隐藏形态待普通用户账号复验（root 具全部权限无法现场复现；机制与 P03–P07 同源已验证）；
- 「切页继续传输、关页提示」为传输管理器框架级语义（T00.6 已验证 + P03 导出同源消费），本页接线一致；告警码文件约 22KB 传输瞬时完成，页内进度行仅瞬时可观，未单独构造长传输场景；
- IAB 伪象（Modal 关闭动画不派发/主题切换 css-in-js 滞留 reload 正常/旧帧截图）延续既往登记，非应用缺陷。
