# P06 载具类型 · 交接记录

- **日期**：2026-09-19（run-20260919-p06-auto 单轮交付）
- **入口**：`/vehicle-deploy/vehicle-type`；**旧来源**：`C:\code\dd\src\pages\VehicleDeploy\VehicleType`（index.tsx 299 行 + CarrierModal 172 行）；**目标**：`src/pages/vehicle-deploy/VehicleType/VehicleType.tsx`。
- **接口依据**：dispatcher/carrier（OpenAPI 清单四 operation 全 POST）。

## 交付内容

### 服务层（owner 归 P06，contracts.md 第 5 节）

- `src/services/vehicle/carrier.service.ts` + `.types.ts`：
  - `POST /dispatcher/carrier/pageCarriers`（body 平铺 pageNo/pageSize/carrierName?/carrierCode?）
  - `POST /dispatcher/carrier/createCarrier`（carrierName/carrierCode/carrierLength/carrierWidth）
  - `POST /dispatcher/carrier/updateCarrier`（同上 + id 定位）
  - `POST /dispatcher/carrier/deleteCarrier`（{id}）
- 与 vehicleGroup（GET+query）不同，carrier 四接口旧实现与 OpenAPI 清单一致均为 POST+JSON body，服务层保持该形态不做归一化改写。

### 页面（整页重写，替换 pending 占位）

- ApexTableReact request 模式：服务端分页（toBackendPage 换算）、稳定行 ID `stringFieldRowId('id')`、列偏好 `carrier-type:main`（四切片铁律接线同 P03/P04）、排序不开放（G09：旧页面无排序）。
- 列结构逐列核对旧版：载具名称/编码左对齐（220/220）、载具长度(mm)/宽度(mm) 居中（140/140）、创建时间（170，displayDateTime 秒级、缺失留白）、操作列（170，按钮码控制显隐）。
- 筛选：名称/编码双输入 + 查询/重置（旧实现同形态；重置非刷新按钮，允许保留）；无手动刷新按钮、无轮询（普通 CRUD 配置数据，新鲜度由写后自动刷新保证）。
- 删除升级 confirmCommand 破坏性确认（danger；对象=名称(编码)、影响说明、提交≠完成附注），旧版仅 Popconfirm 一问；行级防连点（pendingRowKey）。
- 按钮权限：`PERM_BUTTON.CARRIER_ADD/UPDATE/DELETE`（后端权限树 id=24/25/26），无权限隐藏，与旧 §7 一致。

### CarrierFormModal（features/carrier-type/components/）

- 新增/编辑双模式；草稿保留（destroyOnHidden=false + 显式「清空」+ useTabDirtyGuard）；编辑回填按行记录，编辑目标清除时复位干净基线；失败留稿（提交失败保留全部输入，写操作不自动重试）。
- 校验：名称/编码必填 + maxLength 64 + showCount；长度/宽度必填 InputNumber min=1（旧实现同边界，协议单位 mm 原样展示，不添加旧实现没有的 precision/max）。
- 联验实证尺寸边界行为：输入 0（低于 min）时 antd InputNumber 不上抛合法值，表单校验拦截并提示「请输入载具长度」——低于下界的值无法提交。

### 路由与权限（专项验收核心）

- **旧路由核对结论**：旧前端 `/vehicle-deploy/vehicle-type` 路由未写 access（兄弟路由均有），实际权限来源=后端权限树真实下发 `carrier:view`（MENU 码，permission 数据 id=23，**path 字段为空**）；旧前端 MENU_TREE 兜底将 carrier:view 映射到本路径（permission.ts:294）。即：菜单入口受 carrier:view 控制、路由直访旧系统不拦（登录即可达）。
- **新实现决策**：PERM 登记后端已有码 `CARRIER_VIEW: 'carrier:view'`（非凭空造码），路由 meta.perm 挂载——未登录守卫拦截 + 无码用户菜单隐藏/直访拦截，满足「不得公开访问」；权限码全部来自后端真实下发，满足「不凭空创造权限码」。contracts.md 第 3 节原「P06 核对后再定」悬置已收敛。
- 路由解除 pending，进入登录落点候选；i18nNamespaces=['carrierType']。

### i18n

- carrierType 四语言 27 key 同构：en 旧真译沿用（Carrier Name/Carrier Length (mm)/Add Carrier Type 等来自旧 en-US.json），繁日韩 B1 基线补译（「载具」沿用旧译实绩词：キャリア/캐리어/載具）；i18n.ts 四语言懒加载表登记。

## 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local root/MD5，IAB rAF/RO 双垫片）

- **守卫**：清登录态直访 `/vehicle-deploy/vehicle-type` → 回跳 `/login?redirect=%2Fvehicle-deploy%2Fvehicle-type`；登录成功落点=redirect 指定的载具类型页（闭环）。
- **API 层**：pageCarriers 200（1 条历史数据 sadfasdf/code0001）；createCarrier 请求体 XHR 抓包 `{"carrierName":"P06联验载具","carrierCode":"P06-CAR-001","carrierLength":1250,"carrierWidth":800}` 平铺正确；updateCarrier `{"id":2,...全量}` 正确且**名称与尺寸真实落库**（列表回读一致）——carrier 无 G18 类缺陷（G18 仅 vehicleGroup updateVehicleGroup 独有）；deleteCarrier `{"id":2}` 真实删除后端自清理（共 1 条恢复原状）。
- **浏览器**：必填校验四条齐出；尺寸边界（0 拦截）；筛选 code0001 命中 1 条 / 不存在编码空态（共 0 条 + 暂无数据）；新增→关闭→重开草稿保留；删除确认框五要素（标题/对象/影响/提交≠完成附注/danger）+ 取消路径（行保留）+ 确认路径；按钮 root 全可见。
- **五语言**：en/zh-TW/ja/ko 逐语言切换，html lang 同步（en-US/zh-TW/ja-JP/ko-KR），列头/按钮文案逐语言正确（Carrier Name/載具名稱/キャリア名称/캐리어 이름）。
- **双主题双宽度四组截图**：`.run-lock/shots-p06/{light,dark}-{1920,1366}.png`，数据行渲染、无溢出。
- **环境伪象登记（非应用缺陷）**：① IAB 下 antd Modal 关闭动画（transitionend）不派发，leave 残留遮挡后续定位——真实浏览器无此问题，联验中以强制隐藏残留继续；② 主题切换后已挂载组件的 antd css-in-js 样式滞留（筛选框白底），深色下 reload 重挂载即正常（bg 透明+浅色字）——真实浏览器主题切换全组件重渲染无此问题。
- 验证后：真实登出（回 /login、auth 持久化 token=null）、页签关闭、dev server 停止；测试数据自清理（后端恢复 1 条历史数据）。

## 门禁

- lint 284 文件 0 警告 0 错误；typecheck 通过；check:structure 282 源文件通过；vite build 成功（chunk 大小警告为既有 apex-table-react 体积，历史存在）。

## 未完成 / 后续

- 无权限按钮隐藏形态待普通用户账号复验（root 具全部权限无法现场复现；机制与 P03/P04/P05 同源已验证）。
- IAB 动画伪象两条（见上）已在 AGENTS 第 5 节渲染管线冻结条目语境内，不另立 gap；若后续真实浏览器验收发现差异再登记。
