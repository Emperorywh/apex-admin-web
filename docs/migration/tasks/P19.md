# P19 三方交管 · 交接记录

- **运行标识**：run-20260920-p19-050544（2026-09-20）
- **任务**：TASKS.md B4a「P19 三方交管」——入口 `/tri-resource/tri-traffic`；旧来源 `TriTraffic`；目标 `src/pages/tri-traffic/TriTraffic/TriTraffic.tsx`；私有模块/语言分片 `tripartite-traffic`；接口依据 `dispatcher/tripartiteTraffic`。
- **结论**：已交付并勾选。门禁四项全绿 + API 层联验 24/24 + 补验 2 项（模拟测试双业务码契约实证）+ 浏览器整页全场景通过。

## 1. 交付内容

### 服务层 `services/tripartite-traffic`（owner=P19，contracts 第 5 节）
六 operation 逐项核对最新 OpenAPI（method/path/请求体形态一致）：
- `GET /fms/v1/dispatcher/tripartiteTraffic/pageTripartiteTraffics`（GET+query 平铺 pageParam：pageNo/pageSize/areaCode；响应 records/total/size/current）
- `POST addTripartiteTraffic` / `POST updateTripartiteTraffic` / `POST deleteTripartiteTraffic`（**三者均 POST+JSON body 完整实体**——删除与 P31/P32 的「POST+?id=」族不同，旧实现 `deleteTripartiteTraffic(record)` 整行提交同形态，等价迁移）
- `GET /fms/v1/dispatcher/map/getSimpleTripartiteTrafficEdgeGroups`（无参；路径挂 dispatcher/map 控制器但消费者仅本页，owner=P19；**不复用 P11 的 getAllSimpleNodeEdgeGroups**——不同 operation，本接口响应多 mapId/mapName）
- `POST testCommunication`（body `{applyType: APPLY|RELEASE, areaCode, systemCode}`；systemCode 固定 `rxx` = 旧实现同款常量）

### 整页重写 `pages/tri-traffic/TriTraffic`
- Apex request 模式（服务端分页/取消/错误重试内建）；行 ID=`id`（stringFieldRowId 雪花守卫）；列偏好 `tripartite-traffic:main`；搜索 trim 回首页；G09 不开放排序；无轮询无手动刷新（写操作成功自动 reload）。
- 5 列逐列核对旧版默认左对齐：区域编号弹性省略悬浮 / 点边组合弹性（**选项集合映射名称，无映射显示原值=已删除组合不可静默替换**，旧实现 `edgeGroups.find(...)?.name || value` 同语义）/ 当前占用系统弹性省略悬浮 / 是否外部系统作为仲裁方 180（是/否布尔映射、缺失留白）/ 操作 300 默认钉右。
- 权限：守卫 `traffic:tripartite:view`；按钮码 add/update/delete/check 五码下发实证（检测为粗粒度码、子项不再细分=旧 §7.4）。

### 弹窗 `features/tri-traffic/components/TrafficEditModal`
- 单弹窗双模式；四块表单：区域编号（必填 64）/ 点边组合（必填下拉 showSearch+allowClear 按名称过滤）/ 是否外部仲裁（Switch）/ 扩展参数（Form.List key-value 行+行删除+「必须添加一个URL地址」整组校验=旧实现同文案同语义）。
- 转换：提交 `listToJson`（[{key,value}]→{key:value}，同 key 后行覆盖=旧 transformListJson 同语义）；回显 `jsonToList`（旧 transformJsonList 同语义）；value 非字符串只转显示、未改动保留原值随表单提交（P23 ActionFormModal 同款守恒）。
- 编辑提交=原记录快照+表单四字段+展开后的 extendParam（旧实现 `getFieldsValue(true)` 携带回显塞入的原记录字段同边界）。
- 关闭销毁草稿（destroyOnHidden=旧 resetFields 同语义）；弹窗短生命周期不接页签脏保护（旧同边界）。
- **等价性差异收敛登记**：旧版 Form.Item 包 Switch 未写 `valuePropName="checked"`，编辑回显时开关不亮（值在表单状态里正确、仅显示不受控）——重写按 antd 标准补 checked 绑定（P29 Statistic 修复同性质：展示层缺陷修复，提交值不受影响），联验实证 aria-checked="true" 回显正常。

### 检测（通信模拟测试）= 专项验收核心
- 菜单两项 申请(APPLY)/释放(RELEASE)（旧 testControls 同源）；点击先经 **confirmCommand 确认层**（标题=动作类型、目标=区域编号、影响=「通信模拟测试说明：将向三方仲裁系统发送{action}仿真请求（系统编号 rxx），验证协同链路；以真实返回结果为准」——标识目标/仿真语义），确认后真实调用 testCommunication，**按真实结果反馈**（成功/失败+原因插值，不用动画假装成功）。
- **契约分类**（专项验收「测试是否影响现场先按契约分类」）：向三方仲裁系统发起的通信模拟，非车辆动、非本系统数据变更；A21 合法真实仿真接口保留并标识。A14 防连点：行级 pending 禁用（检测/删除共用行键）。

### 删除升级
Popconfirm 一问 → confirmCommand danger 破坏性确认（列明对象与影响+「提交≠完成」附注；P04/P31/P32 同款升级登记）。

## 2. 专项验收：模板遗留链清理
本页旧仓库来源 `TriTraffic` 为旧系统自研页（非模板页），无模板遗留 REST 链需要清理；新仓库 `src/pages/tri-traffic` 仅有占位页（已整页替换），`grep tripartite` 全库核对：仅 `services/tripartite-traffic`、`features/tri-traffic`、`pages/tri-traffic`、i18n 分片、路由定义五处新链，零死键零死代码。

## 3. 契约实证（API 层 24/24 + 补验 2 项）
- 登录五码；权限五码（traffic:tripartite:view/add/update/delete/check）真实下发。
- 分页 GET 平铺形态+分页字段齐备（records/total/size/current/pages）；基线 total=0 空态起步。
- query 筛选：未命中空态（areaCode 语义=区域编号或占用系统）。
- 选项接口 code=200 **本环境空集合**（n=0；对照 P11 getAllSimpleNodeEdgeGroups 同时刻 19 条=两者数据源不同，见 §6）。
- 无令牌双通道（分页/模拟测试）1000000 对照。
- add 落库复核（areaCode/nodeEdgeGroupId/isExternalArbitrator/extendParam 四字段一致，id=1 int64 主键）；update 落库复核（isExternalArbitrator 翻转+extendParam 多键）。
- **模拟测试业务码双形态实证（本环境真实反馈）**：
  - `isExternalArbitrator=false` → `3200150「当前不是外部系统作为仲裁方」`（后端前置校验拦截，不触达外部系统）；
  - `isExternalArbitrator=true` + extendParam.url=回环拒绝端口 → `3200120「三方交管外部请求失败,msg(ConnectException: 连接被拒绝)」`（**后端真实按 extendParam.url 向外部地址发起 HTTP 请求**；副作用限定测试后端本机回环拒绝端口，零外部影响=P17/P18 测试命令同纪律）。
- delete POST body 整行实体受理；终态审计 total=0 零残留。

## 4. 浏览器整页联验
- 守卫回跳带 redirect（`/login?redirect=%2Ftri-resource%2Ftri-traffic`）+ 登录落点=redirect 目标（闭环）。
- 空态起步（暂无数据+共 0 条）+ 5 列头逐列。
- 新增：必填三条齐出（区域编号/点边组合/必须添加一个URL地址）**零请求**。
- 编辑：回填五项（区域编号/点边组合原值裸显/开关 aria-checked=true 修复项/扩展参数 url 行）+改名 body 抓包（完整实体含 id/createTime/updateUser 等+areaCode 已改）+实时更新。
- 检测：确认层五要素（标题=申请/目标 APEX_P19_UI_B/仿真说明/取消+确定/提交≠完成附注）+取消零请求+确认后真实 POST（body `{applyType:"APPLY",areaCode:"APEX_P19_UI_B",systemCode:"rxx"}` 抓包）→ 后端 3200120 真实失败诊断**如实呈现**「三方交管测试失败：三方交管外部请求失败,msg(ConnectException: 连接被拒绝)」。
- 删除：danger 确认五要素+取消零请求+确认后 delete body=整行实体抓包+成功反馈+终态回空态。
- 搜索：命中（造行后）与空态、URL 无查询参数、清空恢复。
- 列偏好四步：隐藏仲裁列→面板「确定(Enter)」应用落盘→reload 持久→恢复默认出厂态（5 列全回）。
- 五语言逐语言 html lang：en（Area Code/Point-Edge Combination/Add Third-party Traffic）/繁（三方交通管制系）/日（サードパーティ交通管制系）/韩（서드파티 교통 관제系）/简中恢复——「三方交管」与 menu 对齐、「点边组合」与 nodeEdgeGroup（P11）对齐。
- 双主题双宽度四组截图 `.run-lock/shots-p19/`（light/dark × 1920/1366；暗色 settings 直写二次序列化+DOM data-theme 核验+viewport 重合成=AGENTS ⑥⑱）。
- 收尾：演示行删除自清理（终态 total=0）+恢复亮色+真实登出（logout code=200）+dev server 停止+联验脚本自毁。

## 5. 环境受限项（如实登记，不阻塞勾选）
- **「新增提交→落库→列表刷新」的 UI 完整闭环未走通**：本环境 `getSimpleTripartiteTrafficEdgeGroups` 返回空集合，弹窗点边组合下拉无可选项，required 校验如实拦截（该拦截路径已实证零请求）。新增提交链路的业务语义由以下组合覆盖：API 层 add 受理+落库复核（24/24 内）+编辑 UI 链路（回填/提交/抓包/实时更新完整走通）。选项接口空为后端数据形态（P11 getAllSimple 同时刻 19 条=数据源不同），非产品缺陷；有组合数据的环境（现场/专用环境）回归时按本记录 §4 同序列复验即可。
- 组合创建数据链造数尝试（P11 createSystemNodeEdgeGroup 引用 getAllSimple id）返回 500「失败」——P11/P10 数据链问题，不属 P19 范围，未深挖。

## 6. 契约发现（登记 contracts）
- `getSimpleTripartiteTrafficEdgeGroups`（返回 {id,name,mapId,mapName}）与 `getAllSimpleNodeEdgeGroups`（返回 {id,name}）**数据源不同**：同一时刻前者 0 条、后者 19 条（systemNodeEdgeGroup 分页 total=0 而 getAllSimple 19 条，进一步佐证 getAllSimple 读的是 P10 mapNodeEdgeGroup 表）。前端按契约消费不受影响；后端两接口的语义边界建议与后端确认（观察项）。
- testCommunication 双业务码：3200150（非外部仲裁拦截）/3200120（外部请求失败诊断）——前端均如实透传反馈。

## 7. 门禁与文档联动
- lint 444 文件 0 警告 0 错误 / typecheck 通过 / check:structure 442 源文件通过 / build 890ms 成功（chunk 警告为既有 apex-table-react 体积）。
- 文档联动：TASKS 勾选、operations 两行、contracts 第 5 节三方交管行+变更日志、i18n-map P19 行修正（triTraffic→tripartite-traffic）、RUN_STATE。
