# P21 工艺管理 — 中断交接

- 实施状态：进行中。原运行 run-20260920-p21-080524 已由用户明确确认停止；定时任务现已启用，按当前仓库提示词恢复。
- 代码基线：3734a6992f2d6429722f9903146ca3562c6a4671（工艺配置），已实时核实 origin/apple-rxx 包含该提交。
- 已存在：order-flow 服务、工艺列表、OrderFlowModal、Cron 编辑组件、四语言分片和路由接线。仅确认代码存在，不宣称实现或联验完整通过。
- 验收状态：待复核；实施放行：未满足。RUN_STATE.currentTaskId 已恢复为 P21，尚无 P21 完整验收证据；旧临时结果不能直接视为通过。
- 本轮仅修复自动任务工作流，不修改 P21 业务代码，不调用后端写接口。
- 恢复：按 AUTOMATION_PROMPT 的独立 Bash 命令执行 prepare，通过实时工具记录绑定 session/turn，无需等待 turn_usage 落库。完成锁与交付对账后继续 P21；历史复核顺延。逐项核对 TASKS 的主/子工艺、Cron、草稿和权限要求，不得因为已有提交就跳到 P43。
- 流程修复的公共影响：AGENTS、TASKS、SPEC、RUN_STATE 和统一控制脚本；影响后续所有自动轮次，静态与命令验证结果见对应 evidence/runs 的工作流修复记录。
