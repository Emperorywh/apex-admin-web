# P21 工艺管理 — 交付与验收记录

- **任务**：TASKS.md P21 工艺管理（B5 / B2 样板门禁、P20；入口 `/mission-cluster/mission-flow`；旧来源 `MissionCluster/MissionFlow`；目标 `src/pages/mission-cluster/MissionFlow/MissionFlow.tsx`；私有分片 `order-flow`；接口依据 dispatcher/orderFlow）。
- **本轮运行**：run-6a494b98-6cb3-4f57-9649-169e4b5ebda1（2026-09-20，implement 恢复轮；代码基线 3734a69 已含整页实现，本轮补齐差距并完成全链路验证）。
- **状态**：实现完整 + 静态门禁全绿 + 真实只读联调 8/8 + 浏览器整页验收通过；verification=partial（写操作归专用环境，见第 6 节），不勾选 TASKS。历史中断交接（原运行 run-20260920-p21-080524 用户确认停止）已被本记录取代。

## 1. 开工核对结论

- **接口族**（OpenAPI 逐 operation 核对）：`pageOrderFlows`（GET，query 平铺 {pageNo,pageSize,query}，G04 口径本轮真实实证）；`createOrderFlow`（POST body=OrderFlowParam：orderFlowName/orderTemplateKeys[]/cronExpression/triggerTimes/triggerType）；`orderFlowOperation`/`subOrderFlowOperation`（POST body=OperationParam {id int64, operation=PAUSE|CONTINUE|CANCEL}，主/子工艺同形、id 分别为工艺/子工艺数据库 id）。路径 `/fms/v1/dispatcher/orderFlow/*` 与旧 api.ts 逐一一致；API 哈希开工重核一致（A82E…49C7C）。
- **不落地 operation**：无（orderFlow 族全部 4 个 operation 均有旧页可达消费者）。
- **权限码历史交叉实证**（P20 已实证同族，本轮复核旧源码确认）：任务工艺页（P20）挂 `mission-flow:*` 族码；**本页为「工艺管理」，视图/按钮码均为 `mission-template:*` 族**（旧 .umirc.ts access=PERM.MISSION_TEMPLATE_VIEW；旧 index.tsx 三码 CREATE/RESEND/OPERATE + SubFlowTable SUB_OPERATE），按实证码值接线。
- **旧页面结构**：列表 6 列（工艺名称/工艺标识/时间表达式/循环次数/执行方式/操作 fixed right 220：重发+操作 Dropdown 三键 PAUSE/CONTINUE/CANCEL）+展开行 SubFlowTable 4 列（订单模版名称/订单模版标识/工艺状态 FlowState 六枚举映射/操作 fixed right 150）+FlowModal（创建/重发双态：名称/Cron 文本框+表达式构建器 Popover/模板集合 Transfer 300×360 双栏/循环次数 -1 无限/触发方式单选）+CronExpress 六 Tab（秒分时日月周，旧 text 约定小时字段「小时」）。
- **Cron 语义契约**：6 段空格分隔（秒 分 时 日 月 周），周位 `?` 不指定；cronRegex 仅校验「6 段非空白」形态（旧同款），越界值由后端调度器拒绝；秒位切「每秒」整表重置 `* * * * * ?`；非秒字段切换前置 `*` 收敛为 0（旧 replaceToZero）；周位扩展 `N#M`/`NL`；部署时区语义在后端解释，不推算下一次执行时间（P21 专项纪律，页面与构建器均无任何推算逻辑）。

## 2. 交付内容（3734a69 主体 + 本轮 OrderFlowModal 补丁）

### 2.1 服务层（owner=P21）

- `services/order-flow/order-flow.service.ts`：`pageOrderFlows`（GET 平铺）/`createOrderFlow`/`orderFlowOperation`/`subOrderFlowOperation`；控制命令为调度写命令，页面负责确认与防连点，服务层不自动重试（G13）。
- `order-flow.service.types.ts`：`OrderFlowDto`/`SubOrderFlowDto`（subOrderFlowState 六枚举+未知原值纪律）/`OrderFlowPageParam`/`OrderFlowPageDto`/`OrderFlowCreateParam`/`OrderFlowOperationParam`，与 OpenAPI schema 逐字段一致。
- 模板选项不复制请求：直接消费 P03 代建共享选项 `fetchOrderTemplates`（contracts 第 5 节）。

### 2.2 列表页（`pages/mission-cluster/MissionFlow/`）

- Apex request 模式：稳定行 ID=id（stringFieldRowId）、列偏好 `order-flow:main`、操作列默认钉右、G09 不开放排序、pageSizeOptions [10,20,50,100,200]。
- 列结构逐列核对旧版（6 列）：工艺名称/工艺标识/时间表达式（原样展示保留秒位与 `?`）/循环次数/执行方式（0=并行触发 1=串行触发，OpenAPI 既定语义；未知原值展示，旧实现未知返回空串为收敛差异登记）/操作（220 钉右）。
- 展开行子表 Apex data 模式（真实完整小集合、关闭虚拟化=P20/P22 同款、行 ID=子工艺 id、空集合不可展开）：4 列同旧；工艺状态六枚举映射+未知原值。
- 控制命令：主/子工艺统一 fireFlowOperation——confirmCommand 确认（标题/对象名称+标识/影响说明/「命令提交后不代表操作已完成」附注；CANCEL danger 红键）→ 防连点（pendingKey 按 flow-<id>/sub-<id> 隔离）→ 成功刷新列表以服务端状态为准；主/子分别校验 operate/sub-operate 按钮码；取消不发任何请求（本轮浏览器零网络实证）。
- 工具行：搜索框+查询按钮（trim 回首页）+创建工艺按钮（create 码显隐）；无轮询（DoD 7 配置数据）、无手动刷新按钮、空值一律留白。

### 2.3 弹窗（`features/mission-cluster/components/OrderFlowModal.tsx`）

- 双模式 create/resend（重发=按行回显后走创建接口，不带源 id，旧同语义）；表单字段与旧逐项一致；labelCol 6/18（AGENTS 第 3 节全局标准，旧 4/20 差异登记）；Modal 宽 1200（Transfer 双栏 300×360 等价保留）。
- Cron 输入=文本框+CronExpress 构建器（六 Tab，见第 1 节契约）；cronRegex onBlur 校验同旧。
- Transfer：受控 targetKeys（顺序=添加顺序原样提交）+selectedKeys 分离；打开时加载 getOrderTemplates（关闭重开自然重查）；**失效模板识别**（A17/P11 同款）：重发回显中已不在选项集合的 key 合成占位条目（名称取自源行子工艺）附「（已不可用）」并阻止提交（本轮实测：源行引用 0f84… 已删模板，已选栏正确合成、点确定弹「模板集合中 1 个模板已失效…」且零网络）。
- 提交显式按 OrderFlowParam 组装；防重复（submitting）；失败保留输入不关弹窗；关闭销毁草稿（destroyOnHidden 旧同语义）。
- **本轮补丁**：模板选项加载失败时 Transfer `locale.notFoundContent` 显示 common「加载失败」（无重试按钮=按钮纪律，P10/P20 同款；重新打开弹窗自动重查即恢复路径）。

### 2.4 Cron 构建器（`features/mission-cluster/components/cron/`，10 文件）

- 旧 CronExpress/CronTabs/五个字段 Tab/三个 FromTo 组件的配置化等价迁移：CRON_FIELD_CONFIGS 逐字段取值域与默认片段对齐（秒 1-59/0-59 整表重置、分同秒不重置、小时 0-23、日 1-31、月 1-12、周 1-7 无范围+N#M+NL+指定 1-7）；buildCronCheckboxOptions 一日起字段（日/月/周）与零起两位补零（秒/分/小时）同旧；toSpliced→map 等价改写。
- 不推算「下一次执行时间」；Popover hover 触发保持旧默认。

### 2.5 路由与权限

- 路由 `mission-cluster-mission-flow` 解除 pending，`i18nNamespaces: ['orderFlow']`；菜单码 `mission-template:view` 挂守卫（码值交叉注释已写入路由定义）。
- 按钮码 `mission-template:create/resend/operate/sub-operate` 控制创建/重发/主操作/子操作入口显隐（后端五码本轮登录返回真实下发证实）。

### 2.6 i18n

- `src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/orderFlow.ts` + i18n.ts 四表注册；zh-CN 中文 key 即文案。
- en 旧真译沿用+confirmCommand 升级补译（确认框标题/影响/插值 {{operation}}/{{msg}}/{{count}} 形态修正）；工艺/模板术语与 menu、orderTemplate 分片对齐；繁日韩真实译文（エラー类别与 P20 同款基线）。

### 2.7 门禁（本轮终态）

- `pnpm check`：lint 481 文件 0 警告 0 错误；typecheck 通过；check:structure 474 源文件通过。
- `pnpm build`：1.14s 成功（chunk 警告为既有 apex-table-react 体积）。
- 调试残留 grep：0（本轮无新增调试代码）。

## 3. 带令牌真实只读联调（2026-09-20，8/8 全过）

脚本 `evidence/P21/run-6a494b98-…/p21_readonly_verify.py`（凭据从本机 .env.local 读取、MD5 摘要提交=G03 已证实契约；结果 readonly_verify_result.json）：

1. 登录 code=200 activated=true，**mission-template 五码真实下发**（view/create/resend/operate/sub-operate）。
2. pageOrderFlows GET 平铺 {pageNo=1,pageSize=10} → code=200 total=1 records=1，分页包装 current/pages/size/total（G04 平铺口径本页实证；本环境真实数据 1 条）。
3. OrderFlow/SubOrderFlow 首行字段形态抽样与 DTO 一致（id int/subOrderFlowState str/appointVehicle* null 等）。
4. query='P2' 命中 total=1；5. query 未命中 total=0 空态；6. 无令牌 code=1000000 守卫生效；7. getOrderTemplates code=200 count=1；8. Result 包装 code/message/timestamp 形态。

## 4. 浏览器整页验收（dev 代理 192.168.0.158:8888，1920/1366，IAB Chromium）

- **守卫与落点**：未登录直访 `/mission-cluster/mission-flow` → 回跳 `/login?redirect=…`；登录成功落回 redirect 目标（页签标题「工艺管理 · 调度系统」）。
- **列表渲染**：7 列表头（行展开/序号默认放出+齿轮/6 业务列）+真实数据行 7 值逐列核对（名称/32 位 hex 标识/`59 59 23 28 2 ?` 原样/-1/并行触发映射/重发+操作）+分页「共 1 条」。
- **展开行子表**：4 列（订单模版名称/标识/工艺状态「已取消」=CANCELLED 映射/操作 Dropdown 三键）。
- **控制命令确认框**：暂停路径五要素实测（标题「暂停工艺：P21联验演示工艺」/影响说明/对象清单名称+标识/提交≠完成附注/取消+确定）；**取消路径零网络**（performance 资源条目数恒定）；未发送任何真实命令（现场保护）。
- **创建弹窗**：打开即发 getOrderTemplates（+1 资源条目）；Transfer「1 项待选工艺/0 项已选工艺」真实选项；空表单确定→4 条必填拦截（名称/表达式/任务集合/循环次数）弹窗保留零网络；Cron 构建器六 Tab 实测（秒 Tab 每秒/范围/步长/指定两位补零；周 Tab 每周/不指定/步长/第 N 个星期 M/NL/指定 1-7）；「不指定」写回 `0 0 0 0 0 ?`（前置通配收敛 0+周位 ?=旧 replaceToZero 同语义）；取消零网络。
- **重发回显**：名称/Cron/循环次数/触发方式四字段回显正确；源行引用已删模板 0f84… → 已选栏合成「P21临时模板自清理（已不可用）」→ 点确定弹失效拦截消息且零网络（A17 实证）。
- **五语言**：en-US/zh-TW/ja-JP/ko-KR 逐语言实测（html lang 同步、列头/按钮/占位/页签标题全翻译、无整页刷新；en Process Name/Create Process；繁 流程管理·調度系統；日 工程管理·ディスパッチシステム；韓 공정 관리·디스패치 시스템+列头按钮占位全韓訳）。
- **双主题双视口**：浅/深 × 1920/1366 截图 5 张（`screenshots/`，本地留存）；布局无溢出、操作列钉右可达、Dock/页签/分页完整。
- **登出清理**：token/user/roles/permissions/permissionsTree 全清，语言偏好保留，回 /login。

## 5. IAB 环境现象与垫片边界（不影响验收结论的登记）

- ResizeObserver 不触发导致表格视口测量 1px：注入 RO 诊断垫片（constructor 捕获回调+四类 entry+observe 即派发+rAF 持续 fire，仅页面运行时注入、不进产品代码）；垫片后 mount 的组件正常渲染（关页签+菜单导航重 mount 路径）。
- IAB Playwright click/CUA 输入通道本轮整体故障（document 级 click 监听零到达）：交互经 evaluate 派发完整指针事件序列完成（登录/菜单/展开/弹窗/构建器均真实触发 React 处理）；该垫片不能证明真实指针链路，OS 级指针未覆盖项保留。
- 确认框/弹窗「取消」点击实际立即生效（业务 promise resolve、零网络），但 CSS 关闭动画被 IAB 冻结致 DOM 残留（ant-zoom-leave 常驻）；按 AGENTS 以请求与真实状态核验、reload 文档清理，未 remove DOM。
- 首帧旧合成帧一例（1366 浅色）：强制 reflow+重绘重截排除，DOM 核实正常。
- fill("") 不触发 input 事件一例：React 原生 setter 清空成功，证明页面 onChange 链路正常，属 IAB fill 通道限制。

## 6. 未验收 / deferred（verification=partial）

| 编号 | 内容 | category | basis | owner | resumeCondition |
| --- | --- | --- | --- | --- | --- |
| A14(发) | createOrderFlow 创建工艺（调度器按 cron 真实消费）；orderFlowOperation/subOrderFlowOperation 暂停/继续/取消现场命令 | site_acceptance | 规格 15.1/D32：调度写命令影响现场任务执行，本环境 192.168.0.158:8888 为共享现场环境；前端机制级已实证（确认框五要素/防连点/取消零网络/失败留稿/不自动重放），API 层未发送任何写命令 | V01（专用测试调度环境或现场人员监督） | 专用环境可用或现场授权执行一轮真实创建+控制命令并核实列表状态回流 |

- **观察项（归 T00/V01，非 P21 引入）**：快速连续切换语言（<1s 间隔连切四语言）可触发 App.tsx 语言同步竞态（changeAppLanguage promise 按完成序覆盖 settings.locale，造成菜单高亮与实际语言脱钩；点当前高亮项被幂等守卫挡住）。单次切换五语言全部正常（本轮实测）；根因在 T00.8 公共语言基座，登记待样板级复核。
- 无其他缺口：本页无文件操作、无统计、无批量；G13（无事务/幂等键）按纪律以确认+不重放+状态回流应对；G15 时区语义后端解释已落实。

## 7. 消费者影响

- 本轮唯一代码改动 `OrderFlowModal.tsx`（Transfer 失败呈现）为 P21 私有组件，无跨页消费者。
- 共享依赖（cron 组件 10 文件/useStaticOptions/confirmCommand/请求层/路由/i18n 基座）为 T00/P20 既有交付，本轮零修改。
