# P14 电梯 — 交付与联验记录

- 运行标识：run-20260919-p14-110535（2026-09-19，单轮交付完成并勾选）
- 分支：apple-rxx；工作目录：C:\code\apex-admin-web
- 任务卡：TASKS.md「P14 电梯」；入口 `/tri-resource/tri-device/elevator`；旧来源 `C:\code\dd\src\pages\TriDevice\Elevator_back`（当前路由唯一来源，历史 `Elevator` 实现未复制任何能力）；目标 `src/pages/tri-device/Elevator/Elevator.tsx`

## 1. 交付内容

### 服务层（owner=P14，contracts.md 第 5 节）
- `services/device-elevator/device-elevator.service.types.ts`：ElevatorDto（DeviceElevator，旧记录类型中 OpenAPI 未声明的 agvKeys 不迁）/ ElevatorPageDto / ElevatorPageParam / ElevatorSaveParam（ElevatorParam）/ DeviceParam（deleteElevator/openDoor/closeDoor/clearElevatorOccupy 共用，doorWay 仅开关门携带）/ ElevatorOuterCallParam（ElevatorOperationParam，targetFloor 内呼语义字段外呼不传）/ ElevatorDriverDto（DeviceDriver）/ ElevatorStateDto（ElevatorDetail + SimpleAGV）。
- `services/device-elevator/device-elevator.service.ts`，10 个 operation 逐项核对 OpenAPI：
  - GET pageElevators（GET+query 平铺，G04）/ GET getElevatorDrivers（全量）/ GET getElevatorState（deviceKey query）；
  - POST addElevator / updateElevator（原 deviceKey 定位+全量提交，旧实现同语义：表单无 deviceKey 输入框）/ deleteElevator（DeviceParam）；
  - POST outerCall（ElevatorOperationParam 仅 deviceKey+currentFloor）/ openDoor / closeDoor（doorWay=FRONT/BACK）/ clearElevatorOccupy。
  - **G06：innerCall 未出现在 OpenAPI，服务层不提供**；页面禁用入口说明，不用 outerCall 的 targetFloor 字段猜替。
  - getElevators（电梯下拉选项）共享服务归 P10（services/cross-map fetchElevatorOptions），本页列表走分页接口不重复落地。
  - A06 纪律：不依赖英文 success 文案判断成败，统一由请求层业务码判定。

### 弹窗组件（features/device-elevator/components/）
- `ElevatorFormModal`（新增/编辑双模式）：
  - 草稿保留（destroyOnHidden=false + useTabDirtyGuard + 失败留稿 + 显式「清空」；P06 同形态）；编辑模式按行重填，设备配置以 4 空格缩进 JSON 回显（旧实现同形态），编辑提交回带原 deviceKey 定位。
  - **驱动切换校验（P14 专项验收）**：选中驱动即以其 driverProtocol 覆写设备配置 JSON（旧实现同语义）；设备配置提交前 JSON.parse 真实解析校验（非对象/解析失败报错并保留输入不关弹窗）。
  - 数值边界旧实现同边界：楼层 -200..200 整数、端口 0..65535 整数；名称/ip 64 字 showCount；deviceStatus Radio 启用/禁用（默认启用）。
  - 与旧实现的差异（有意）：驱动 Select 取消选择不清空已有配置（旧 onDeselect 置 undefined 会丢弃用户已编辑值）。
- `ElevatorControlModal`（外呼/开门/关门）：参数录入（外呼=当前楼层 -200..200 整数；开关门=doorWay 前门/后门，协议枚举）→ 页面层 confirmCommand 命令确认（对象=电梯名（deviceKey），影响含具体参数，附「提交≠完成」固定附注）→ 发送；**无内呼模式（G06）**。

### 宿主页（pages/tri-device/Elevator/Elevator.tsx）
- ApexTableReact request 模式：服务端分页（toBackendPage 换算）、稳定行 ID=deviceKey（stringFieldRowId）、列偏好 device-elevator:main、G09 不开放排序、普通 CRUD 配置数据不轮询（DoD 7，新鲜度由写后自动刷新保证）、无手动刷新按钮（按钮纪律）。
- 列结构逐列核对旧版（10 列）：设备名称 160/设备标识 160/楼层 90/设备状态 100（boolean 两态：false=禁用 不是缺失，缺失留白）/设备驱动 140（driverKey 经驱动集合映射名称，未命中显示原 key 保留原值）/IP地址 140/端口 90/设备配置 220（紧凑 JSON + Popover 内格式化预览，旧实现同形态）/网络状态 100（ONLINE=在线绿 Tag、OFFLINE=离线红 Tag、未知枚举原值灰 Tag、缺失留白）/操作 360（默认钉右）。
- **电梯状态（详情状态）**：「状态」按钮按需查询（旧实现同交互，本页非实时轮询页，任务卡无轮询要求），状态按行缓存（修复旧版全部行共享一份状态的串扰）；Popover 内容=Descriptions 六项（在线状态/当前楼层/运行状态/前电梯门状态/后电梯门状态/占用电梯的车辆），**枚举显示协议原值**（联验实测 UNKNOWN/ONLINE 原样展示）、缺失留白；查询失败清空该行区域+错误如实透传。
- **控制命令升级**（旧版清占用直接执行、开关门/外呼仅弹窗一问）：外呼/开门/关门=弹窗参数 → confirmCommand → 发送；清占用=confirmCommand danger（打断占用语义）→ 发送；受理 ≠ 设备动作完成，提示明确「执行结果以『状态』查询为准」；失败不自动补发。
- 删除升级 confirmCommand danger（对象=设备名（deviceKey）+影响说明；旧版仅 Popconfirm 一问）。
- **G06 内呼呈现**：操作菜单保留「电梯上楼（内呼）」入口——灰色标记（opacity 0.45）+ Tooltip + 点击 message.warning「内呼接口暂不可用：后端尚未提供该接口契约，开放时间待后端确认」，**零请求**；不用 antd disabled（禁用项会抑制 Tooltip/点击，导致缺口说明对用户不可达——联验实测 antd v6 禁用菜单项 Tooltip 不触发后改为本方案），符合 gaps.md G06「保留有权限入口并标记接口暂不可用，不用外呼接口猜测替代」。
- 权限：device:elevator:view 挂路由守卫；add/update/delete/operate 四按钮码分别控制新增设备/编辑/删除/操作项入口（无权限隐藏，与旧实现一致）；「状态」为查看行为不控权（旧实现同边界）。
- 驱动集合经 useStaticOptions 加载（scope 取消/防乱序/失败清空+error 标记，无重试按钮——按钮纪律）；驱动加载失败不影响列表（驱动列回退原 key），仅弹窗下拉失去数据源。

### 路由与 i18n
- `/tri-resource/tri-device/elevator` 解除 pending，meta.i18nNamespaces=['deviceElevator']。
- deviceElevator 四语言分片 63 key 同构：en-US 旧真译逐条沿用（Device Name/Device ID/IP Address/Call Elevator/Clear Occupying Elevator Vehicle/Elevator go up successfully 等全量），插值改 {{var}} 双花括号，命令确认影响说明与 G06 禁用说明按旧真译风格补译；繁日韩旧资源对本页 key 均为简中复制（非有效译文），按 B1 基线补译（繁中 IP位址/連接埠/網路狀態 台湾惯用语；日文 車内呼び 内呼业界用语、開扉/閉扉；韩文 엘리베이터 호출/내부 호출）；启用/禁用/在线/离线/编辑/删除/操作等公共词与既有分片对齐（ja 有効/無効、ko 활성화/비활성화）。
- i18n.ts 四语言 loader 表各增 1 行（deviceElevator）。

## 2. 带令牌真实联验（2026-09-19）

### API 层（python，凭据 .env.local APEX_TEST_*/MD5，独立会话先行）
- 登录 code=200 activated:true；**device:elevator 五码（view/add/update/delete/operate）真实下发**。
- pageElevators GET+query 平铺 code=200（基线 total=0 干净环境）；getElevatorDrivers code=200 共 2 条（虚拟电梯/Modbus-tcp电梯，DeviceDriver 三字段与 OpenAPI 一致）；无令牌对照 code=1000000「用户未登录」。
- addElevator code=200 落库一致（deviceKey 后端生成、中文文件体、deviceConfig 嵌套对象原样、deviceStatus=false 落库）；筛选 query 命中 1 条。
- **updateElevator code=200 全字段落库**（改名+楼层 3→5+状态翻转+配置替换，无 G18 类缺陷）。
- getElevatorState code=200 真实响应形态：onlineState=ONLINE、runningState/frontDoorState/backDoorState=**UNKNOWN**、currentFloor=null、occupyAgv=null——未知枚举原值展示与缺失留白的界面纪律据此实证。
- deleteElevator code=200 后端自清理；**终态审计 total=0 零残留**。

### 浏览器整页（dev 代理 192.168.0.158:8888，IAB rAF/RO 双垫片+页签/菜单导航重挂载定版手法）
- 守卫：未登录直访 → /login?redirect=%2Ftri-resource%2Ftri-device%2Felevator；登录后落点=redirect 目标（闭环，两轮复验）。
- 整页渲染：页签/工具行（搜索框 300px enterButton+新增设备）/10 列表头/列设置齿轮落位序号列表头/真实空态（共 0 条）/分页器视口内完整可达。
- 新增全链路：必填拦截 6 错齐出（空表单提交零请求）→ 选驱动（下拉 2 项真实加载，选中即填充 driverProtocol JSON）→ 提交 → toast「新增电梯成功」+ 行渲染与 API 数据逐列一致（驱动列显示名称非裸 key、在线绿 Tag）。
- 编辑全链路：回显完整（名称/楼层/ip/端口/驱动名称/启用态/JSON 配置）→ 改名+楼层提交 → toast「更新电梯成功」+ 列表实时更新。
- 状态 Popover：点击即展开+查询（行级 loading），内容六项**枚举原值**（ONLINE/UNKNOWN）与缺失留白；**受控 open 修复实证**（见第 3 节缺陷修复）。
- 操作项菜单：呼叫电梯/电梯上楼（内呼）/电梯开门/电梯关门/清除占用电梯车辆五项+分隔线；内呼灰色标记+Tooltip+点击 warning「内呼接口暂不可用…」**零请求实证**（XHR 计数不变）。
- 外呼确认层：弹窗填当前楼层 2 → 确定 → confirmCommand 五要素完整（对象=P14联验电梯C（deviceKey）/影响含具体参数/提交≠完成附注）→ 取消 → **outerCall 请求数 0**；命令执行留专用环境（现场设备副作用，与 P05/P09/P12 同条件）。
- 删除：confirmCommand danger 红键 → 确认 → toast + 行消失回真实空态；后端终态审计 total=0。
- 列偏好闭环：隐藏「端口」→ 面板确定(Enter) 应用 → 表头 9 列 → reload+重挂载后仍隐藏（持久化）→ 恢复默认+应用 → 端口列回归（10 列）；**钉右闭环**：操作列 data-pinned='end' + savedPinning end:['actions']。
- 五语言：en-US（Device Name/IP Address…）/zh-TW（設備名稱/IP位址…）/ja-JP（デバイス名/IP アドレス…）/ko-KR（장치 이름/IP 주소…）表头逐语言正确+html lang 同步；zh-CN 默认态全轮通过。
- 双主题双宽度四组截图：.run-lock/shots-p14/{1920,1366}-{light,dark}.png（1920×945/1366×768；**1366 暗色实证钉右**：操作列贴边完整+钉列阴影，中段列横向滚动消化）；暗色经 persist:apex-admin:settings 直写 theme（二次序列化）+reload 落位。
- 收尾：测试电梯删除（code=200）+ 后端终态审计 total=0 零残留；列偏好 localStorage 残留键清除；真实登出（POST logout code=200）+本地会话清理；页签关闭、dev server 停止、凭据临时文件（p14-env/p14-token）删除。

## 3. 联验中发现并修复的应用缺陷（本任务引入，已修复复验）
- **状态 Popover 不显示**：statePendingKey/stateByDevice 任一 setState 重建 columns 数组 → Apex 单元格重挂载 → 非受控 Popover 内部 open 被重置。修复=Popover 受控 open（页面级 openStateKey 持有，重挂载不丢展开态）；点击已展开按钮（收起动作）不重复发查询。修复后点击即展开+内容随加载/结果联动复验通过。
- **G06 禁用项说明不可达**：antd v6 禁用菜单项抑制 Tooltip（合成事件与真实悬停均不触发，pointer-events 正常仍不弹）——改为灰色标记+点击 warning 方案（零请求，说明始终可达）。

## 4. 环境教训与边界登记（沉淀 AGENTS 第 5 节）
- **antd v6 列设置面板钉位置控件是原生 select**（取消固定/固定在左侧/固定在右侧），显示状态是 td[3] 内 [role=switch]（aria-label「显示列 X」）；行内首个 button 是拖拽手柄——自动化误点会改钉位置。本轮误点把操作列置为「取消固定」并随偏好保存（**用户偏好覆盖代码默认的语义正确**：defaultPinning 仅在无已保存偏好时生效），重拍截图前经面板 select 还原 end 并实证 data-pinned='end'。
- **残留 confirm 最后实例纪律再实证**：已取消的外呼确认实例残留在 DOM，querySelector 命中旧实例（点击其确定无效不发货）；confirmCommand 的确定一律在「最后一个 .ant-modal-confirm 中含目标标题的实例」内定位。
- **后端会话短 TTL**：联验中途令牌过期（1000000×2，页面 toast 如实提示、请求失败区域如实清空、驱动列回退原 key）——与 P38 实证一致；应用侧收敛行为正确，重新登录即可继续，不构成缺陷。
- 控制命令（outerCall/openDoor/closeDoor/clearElevatorOccupy）执行与现场设备副作用留专用环境；确认层/参数校验/零请求取消路径已实证。

## 5. 未完成 / 后续
- 外呼/开关门/清占用命令的现场执行与设备真实响应核验：留专用环境（登记同 P05/P09/P12 条件）；getElevatorState 真实设备在线数据（非 UNKNOWN）的字段核验同条件。
- 无其余缺口：G06 按任务卡要求以禁用说明呈现闭合于本页；无新增 G 编号。
