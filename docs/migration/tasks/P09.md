# P09 地图列表 交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
- 任务卡：TASKS.md「P09 地图列表」（批次 B3 / 前置 B2 样板门禁）
- 执行轮次：run-20260919-p09-050637（2026-09-19，续作轮，接管 041037 遗留锁——并发会话事件见第 5 节）
- 入口：`/map-through/map-list`；目标：`src/pages/map-through/MapList/MapList.tsx`
- 私有模块/语言分片：`map-list`；接口依据：dispatcher/map、mapVersion（十 operation）

## 1. 交付范围（全部完成）

### 列表页整页重写（替换迁移过渡占位）
- `pages/map-through/MapList/MapList.tsx` + `.module.css`：
  - ApexTableReact **request 模式**：服务端分页（GET+query 平铺，`pageNo` 从 1 计数）、稳定行 ID=mapId、内建取消/错误重试、列偏好 `map-list:main`（四切片整体替换持久化，P03 实证铁律接线）；排序不启用（G09）；
  - 列结构逐列核对旧版：地图名称（弹性）/地图标识 300/地图状态 120（仅 ENABLED 显示「启用」其余「禁用」，旧实现同款）/当前版本/楼层 80/更新人 120/更新时间 170（`displayDateTime` 秒级）/操作 350；空值一律留白；
  - 工具行：名称/标识搜索框（旧 Search 同形态 300px）+ 查询按钮；右侧 创建地图/导入调度地图/导入车载地图/拉取地图（禁用，G07）按按钮码显隐；无轮询、无手动刷新按钮（元数据配置数据，新鲜度由写后自动刷新保证）；
  - 删除升级 confirmCommand 破坏性确认（danger 红键+影响说明+对象清单+提交≠完成附注；旧版仅 Popconfirm 一问），行级防连点；**联验实证后端业务规则：删除前必须先禁用地图（3000210），错误如实透传——与旧实现直接删+透传错误行为等价**；
- `features/map-list/components/`：
  - `MapFormModal`：双模式（editTarget null=新增）；草稿保留（关闭不清输入、常挂 `destroyOnHidden=false`、失败留稿、显式「清空」）+ `useTabDirtyGuard`；编辑模式名称禁用、提交不含 mapName（updateMap 按 mapKey 定位只提交 mapState/floor，旧实现同边界）；楼层 -200~200 整数、名称 64 字上限（旧同边界）；6/18 横排标签分栏；
  - `MapUploadButton`：导入调度地图（.zip）/导入车载地图（.bin）共用；beforeUpload 扩展名校验（旧仅 accept 过滤）；**旧伪进度条（定时器逼近 90%）废弃**，改经传输管理器 beginTransfer 登记真实字节进度（total 缺失即不确定进度，G11），进度行由宿主页渲染（切页继续/关页提示，A16）；导入为新增性操作不做破坏性确认（旧同）；
  - `VersionModal`：版本表 Apex request 模式（服务端分页按 mapId 隔离/内建取消错误重试；G09 不开放排序）；列结构逐列核对：版本号 260/是否发布 100（currentMapInfoVersion truthy→绿 Tag）/备注/来源版本/创建人 100/创建时间 170/操作 280；搜索版本号/备注防抖 300ms 自动查询、清空立即重置（旧同节奏）；**发布按钮禁用依据适配：currentMapInfoVersion（OpenAPI boolean，联验实证下发）并旧字段 published（新后端不下发，保留前向兼容）——联验实证仅依赖 published 会漏禁已发布版本**；发布升级 confirmCommand（列明目标版本与「替换当前线上地图数据」影响）；版本「编辑」先关弹窗再导航 H02（`/map-through/map-nest-modify?mapId=…&mapVersionId=…`，不携入完整编辑器，A02/A17）；下载经 beginTransfer 登记（DoD 9：JSON/HTML 响应不存伪文件；文件名 content-disposition 优先、`地图名_版本号.zip` 兜底——**两分支均有真实实证：DEV 版本下发 content-disposition、6车间 V15 不下发**）；
  - `PushVersionModal`：二层弹窗；Transfer 复用共享契约 getSimpleVehicles（56 项真实加载）；缺 key 条目过滤；推送目标至少一辆；SLAM 底图开关默认关；vehicleKeys 提交选项 key 原样；成功提示按「后端受理」语义（命令受理≠车辆已生效，A14/A15，最终状态归 P12 推送记录核实）；
- **G07 拉取缺口**：旧 PullModal 的 downloadMapInfo 未在 OpenAPI，入口保留禁用按钮+Tooltip 说明原因，不猜替 downloadMap/vehicleDownloadMap 语义（联验实证 disabled 态+Tooltip 文案）。

### 服务层（owner=P09）
- `services/map/map-admin.service.ts` + `.types.ts`（十 operation，逐项核对 OpenAPI 与旧实现 method/path/请求体形态）：
  - `GET /dispatcher/map/pageMapInfos`（query 平铺 G04 同形态）；`POST createMap`/`POST updateMap`（mapKey 定位）/`POST deleteMap`（mapId 定位）；
  - `POST uploadMap`/`POST uploadVehicleMap`（multipart/form-data 字段 file，G05 同 P08 已实证形态，带 onUploadProgress）；
  - `POST downloadMap`（JSON body {mapInfoVersionId} + api.downloadPost Blob 通道）；
  - `GET /dispatcher/mapVersion/pageMapInfoVersions`（按 mapId 隔离）；`POST publishMapInfoVersion`；`POST pushMapInfoVersion`；
  - 协议纪律：downloadMapInfo（G07）不提供；upLoadMap/updateMapResource（G08 未声明路径）不迁移；发布/推送命令类写操作不做自动重试。

### 路由与权限
- 路由 `map-through-map-list` 解除 `pending`，`meta.i18nNamespaces: ['mapList']`；菜单码挂既有 `PERM.MAP_LIST_VIEW`；按钮码 map-list:add/upload-dispatcher-map/upload-vehicle-map/update/delete/version + map-version:update/publish/download 共十码与旧 §7 一致，无权限隐藏（前序轮登录响应实证十码真实下发）。

### i18n（四语言分片 + 登记）
- `mapList` 命名空间四分片 77 key 同构（逐 key 比对一致）：en-US 旧真译逐条沿用（插值改 `{{var}}` 双花括号），zh-TW/ja-JP/ko-KR 按 B1 基线补译（公共词与 P08 vehicleAlarmCode 分片对齐）；新增文案（删除确认影响/传输进度行/G07 禁用说明）按旧真译风格补译；`i18n.ts` 四表登记。

## 2. 门禁（交付终态全绿）

| 门禁 | 结果 |
| --- | --- |
| `pnpm lint` | 通过（311 文件 0 警告 0 错误） |
| `pnpm typecheck` | 通过（tsc -b --noEmit） |
| `pnpm check:structure` | 通过（309 源文件；无违规导入方向/硬编码主机） |
| `pnpm build` | 通过（tsc -b + vite build；chunk 警告为既有 apex-table-react 体积） |

## 3. 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local [登录凭据已脱敏]；IAB rAF+修正版 RO 双垫片探针自测通过后断言）

### API 层（curl，单会话纪律：curl 全部完成后登出，再开浏览器会话）
1. 分页 `GET pageMapInfos?pageNo=1&pageSize=5` → code=200，6 条真实历史地图；
2. 模糊筛选 `query=车间` → 命中 1 条；`query=ZZZNOTEXIST` → 0 条（空态如实）；**query 命中语义=名称模糊（标识不命中），与旧实现同参数透传，属后端行为**；
3. 版本分页 `GET pageMapInfoVersions?mapId=…` → code=200（6车间 1 条/ceshi 8 条版本链、parentMapVersion 链真实）；`currentMapInfoVersion` boolean true/false 真实下发；**`published` 字段新后端不下发（联验实证）**；版本筛选 `query=V1788` 命中；
4. 下载 `downloadMap {mapInfoVersionId:15}` → 465,808 字节 `application/zip` 真 zip 魔数，**无 content-disposition**（前端本地默认名兜底分支实证）；DEV 版本 16 下载 → `content-disposition: attachment; filename=DEV-V1789363781939.zip; filename*=…`（优先分支实证，前序轮同证）；
5. 创建 createMap → 200 落库一致（ASCII 与中文 UTF-8 文件体各一；**Git Bash curl 行内中文 -d 会编码损坏致后端 500，浏览器/axios 规范 UTF-8 不受影响**——验证脚本陷阱非后端缺陷）；
6. 编辑 updateMap（mapKey+mapState+floor）→ 200 真实落库（floor -5→7、DISABLED→ENABLED 回读一致，无 G18 类缺陷）；updateUser 被后端记为 `system`（不记录操作者，后端行为备查）；
7. 删除 deleteMap：ENABLED 地图 → `3000210 删除地图之前请先禁用地图`（业务规则如实）；不存在目标 → `3000100 地图不存在`；**禁用后删除 → 200 后端自清理**；
8. 上传 uploadMap（multipart 字段 file，真实 zip 回传）→ 200；**语义实证：zip 数据成为源地图的新「未发布」版本（parent=当前版本、current=false），线上当前版本不动=零净影响**；伪 zip → 500 拒绝零净变化（前序轮同证：伪 bin → 4000060 上传的文件格式已经失败）；
9. 发布 publishMapInfoVersion → 对自有测试地图版本（已是 current）执行 `code=200`（安全对象全链路实证）；**对不存在版本 id 999999999 后端亦返回 `code=200 success`（不校验存在性）→ 登记 G21 观察**；发布/推送对生产地图的执行仍留专用环境；
10. 推送 pushMapInfoVersion **未执行**（车辆副作用，纪律同 P05/P12）；logout 200。

### 浏览器整页（IAB，修正版 RO 垫片 + 探针自测通过后断言）
- 守卫回跳：未登录直访 → `/login?redirect=%2Fmap-through%2Fmap-list`；登录后**落点= redirect 目标页**（闭环）；
- 列表真实渲染：6 条真实数据、分页「共 6 条」、列结构逐列对齐、齿轮落位序号列表头；
- 筛选：命中（车间→1）/空态「暂无数据」/清空恢复全量；
- 创建弹窗：必填三条齐出（弹窗不关）；填写提交 → toast「创建地图成功」+ 列表刷新（6→8，两轮各自测试图）+ 落库一致；草稿保留（取消重开值保留）+ 清空复位；
- 编辑弹窗：按行回填、名称禁用、改状态提交 → 「更新地图信息成功」+ 行内状态翻转 + 落库一致；
- 删除：confirmCommand 五要素（danger 红键实证）；ENABLED 确认删除 → 后端 3000210 错误如实透传+行保留；禁用后确认 → 「删除地图成功」+ 后端自清理；两轮测试数据全部 UI 自清理（终态审计 total=6 还原）；
- 版本弹窗（6车间）：版本列表真实渲染（是否发布 Tag 是/否）；**发布按钮状态实证：current=true 的 V15 禁用、current=false 的未发布版本可用**（适配在真实数据下工作正确）；发布确认框五要素+取消零请求（performance 实证 publishRequests=0）；搜索防抖命中；
- 下载：真实请求（performance entry 466,108 字节 application/zip）+ objectURL 释放 + 无伪文件；
- 推送弹窗：56 真实车辆选项加载/空选校验「请至少选择一辆车辆」/取消零请求（pushRequests=0，**推送执行留专用环境**）；
- 版本「编辑」导航 H02：URL=`/map-through/map-nest-modify?mapId=…&mapVersionId=22` 带参闭环 + 暂缓说明页渲染；
- 上传：.txt → 「仅支持 .zip 格式的文件」+零请求；伪 zip → 真实 multipart 请求（performance entry）→ 后端 500 → 「上传地图出错：失败」如实透传+零净变化；
- G07：拉取地图 disabled + Tooltip「地图拉取接口暂未在服务契约中提供，入口保留待后端确认后开放」（真实指针悬停实证）；
- 五语言：en-US/zh-TW/ja-JP/ko-KR 逐语言切换 html lang 同步、列头/按钮文案逐语言正确（zh-CN 复归）；
- 列偏好闭环：列设置面板（草稿式：开关→确定传播）隐藏楼层列 → visibility.floor=false 落盘 → 刷新持久 → 恢复默认还原 8 列；
- 双主题双宽度四组截图：`.run-lock/shots-p09/p09-{1366,1920}-{light,dark}.png`（新页签整页载入规避黑帧伪象；主题经应用内三态菜单切换+reload 落位）；
- 收尾：真实登出（回 `/login?redirect=…`、auth persist 字段置空）、页签关闭、dev server 停止；后端终态审计还原 6 条真实地图。

## 4. 契约发现与观察（详见 gaps.md）
- **deleteMap 业务规则**：后端要求先禁用（3000210）——旧实现同样直接删并透传错误，行为等价保持，未做前端强约束（用户按旧流程：编辑禁用→删除）；契约登记 contracts 第 5 节。
- **published 字段不下发**：发布按钮禁用依据适配为 currentMapInfoVersion 并 published（见 VersionModal 注释）。
- **uploadMap 语义**：真实 zip 回传→追加为源地图未发布版本（parent=当前版本，current=false），非覆盖式；**遗留观察：对 6车间-货架区 产生的未发布版本 id=22（内容与当前版本 15 同源）无版本删除接口可清理，留待后端核查**（线上数据零影响）。
- **content-disposition 按文件而异**：DEV 版本下发（filename+filename* 双写）、6车间 V15 不下发——前端「优先+兜底」两分支均实证。
- **G21 新增**：publishMapInfoVersion 对不存在版本 id 返回 `code=200 success`（不校验存在性）——前端成功文案已按「受理」语义，登记观察待后端核查。

## 5. 并发会话事件（透明登记）
- 本轮 05:05 以「进程核查」判定前序轮（041037）已结束并接管遗留锁；前序轮事后在 `.run-lock/HANDOFF-041037.md` 说明其当时仍存活（同宿主进程内会话，进程检测无法区分空闲与结束），发现接管后即停写避免冲突，docs 联动留由本轮完成。
- 冲突影响评估：前序轮停写（~05:18）后，本轮对 VersionModal/types 的发布禁用适配、门禁复跑、整页浏览器联验、终态审计均独立完成于最终代码；两轮各自创建的测试数据均自行清理（终态 total=6 还原）；无代码冲突。前序轮的门禁/联验结论与本轮独立复核一致处采信为双重复证（权限十码下发、伪文件拒绝、守卫/五语言等），分歧处以本轮实证为准（如 6车间 V15 下载无 content-disposition）。
- 教训沉淀 AGENTS 第 5 节：锁接管判据缺陷与多点探活要求。

## 6. 限制与待验项
- **推送 pushMapInfoVersion 与发布对生产地图的执行留专用环境**（确认层已全验：确认框/校验/取消零请求；版本/目标车辆核验已在页面实现）；
- uploadVehicleMap 仅伪文件拒绝实证，真实 .bin 导入留专用环境（无安全测试数据来源）；
- 无权限按钮隐藏形态待普通用户账号复验（root 具全部权限无法现场复现；机制与 P03–P08 同源已验证）；
- 「切页继续传输、关页提示」为传输管理器框架级语义（T00.6 已验证+P08 同源消费），本页接线一致；地图 zip 较大传输的页内进度行可观，未单独构造长传输场景；
- IAB 伪象延续既往登记（Modal 关闭动画不派发/主题切换需 reload 落位/截图黑帧与旧帧），非应用缺陷。
