# P18 风淋门 交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
- **批次 / 前置**：B3 / B2 样板门禁。**入口**：`/tri-resource/tri-device/air-shower-door`。
- **旧来源**：`C:\code\dd\src\pages\TriDevice\AirShowerDoor_back`（index.tsx + AirDoorModal + ControlModal；任务卡明示旧来源带 `_back` 后缀，历史 `TriDevice/AirShowerDoor` 目录已不存在，当前路由唯一来源即 `_back`）。
- **目标**：`src/pages/tri-device/AirShowerDoor/AirShowerDoor.tsx`（整页重写，替换迁移占位）。
- **接口依据**：device/airShowerDoor（default_OpenAPI.json 逐 operation 核对）。
- **本轮运行标识**：run-20260919-p18-150546（锁：docs/migration/.lock）。

## 1. 交付物

1. `services/device-air-shower`（owner=P18，contracts.md 第 5 节）——8 operation 落地：
   - `pageAirShowerDoors`（GET+query 平铺，G04 同款；pageNo 从 1 计数）
   - `fetchAirShowerDoorDrivers`（GET getAirShowerDoorDrivers 无参全量）
   - `fetchAirShowerDoorState`（GET+query deviceKey，「状态」按钮按需查询）
   - `addAirShowerDoor` / `updateAirShowerDoor`（原 deviceKey 定位+整体提交）/ `deleteAirShowerDoor`（POST JSON body）
   - `openDoor` / `closeDoor`（POST DeviceParam，doorWay=FRONT/BACK 双门语义必填）
   - **不落地 3 项**（纪律同 P15/P16/P17：OpenAPI 有但旧仓库无活跃 UI 消费者）：`shower`（旧 items 无此菜单项，handleMenuClick 残留 case 不可达）、`clearAirShowerDoorOccupy`（旧 items 中显式注释）、`getAirShowerDoors`（旧仓库唯一消费者为 H02 暂缓页地图编辑器 ThirdDevice 属性面板，与 P16 getAllChargePiles 同例；后续消费者出现时按选项 owner 纪律落地）。
2. `features/device-air-shower/components/AirShowerFormModal`——双模式弹窗（草稿保留 + useTabDirtyGuard + 失败留稿 + 显式清空 + 提交防重复；驱动切换以 driverProtocol 覆写设备配置 JSON；设备配置 JSON.parse 真实解析校验失败留稿；**无「是否启用」字段——旧表单无此输入、OpenAPI AirShowerDoorParam 亦无 deviceStatus/showerStatus，不发明 UI**）。
3. `features/device-air-shower/components/AirShowerControlModal`——开门/关门参数弹窗（门类型 FRONT 前门/BACK 后门必选，取值域=协议枚举；控制命令即时性输入不保留草稿；弹窗标题/label/占位按旧版原样「风淋门开门/开门类型/请选择开门类型」，校验文案「请选择(开/关)门类型」旧版原样）。
4. `pages/tri-device/AirShowerDoor` 整页重写——Apex request 模式 + 列偏好 `device-air-shower:main` + 操作列默认钉右 + 行 ID=deviceKey；G09 不开放排序；普通 CRUD 不轮询；无手动刷新按钮。
5. deviceAirShower 四语言分片（en/zh-TW/ja-JP/ko-KR；zh-CN key 即文案）+ i18n.ts 四表注册。
6. 路由解除 pending，meta.i18nNamespaces=['deviceAirShower']，air-shower-door 地址保留。

## 2. 等价迁移关键决策（逐项核对旧实现）

- **列表 9 列与旧版逐列一致**（列序同旧版设备名称在前）：设备名称/设备标识/设备状态（启用/禁用，boolean 两态缺失留白）/风淋状态（showerStatus 同形态）/IP地址/端口/设备配置（紧凑 JSON + Popover 格式化预览，旧 JsonViewer 同形态）/网络状态（ONLINE 在线绿/OFFLINE 离线红/未知枚举原值灰 Tag，缺失留白）/操作（钉右 360，旧 fixed:'right' 同语义；**按钮顺序同旧版：状态/编辑/删除/操作项**——与 P15 的「状态/操作项/编辑/删除」为各自旧版差异，不统一改写）。
- **风淋门状态升级为行级缓存+受控 Popover**：旧实现全部行共享一份 airDoorState（串扰缺陷）+行内 setState 重挂载丢展开态——按 P14/P15 定版修复：状态按行缓存（undefined=未查询/null=已查询无数据）+受控 open 由页面持有；内容=在线状态（与列表列共用映射）/故障（是/否，boolean 两态缺失留白）/风淋状态（UNKNOWN/AIRING/AIRED 协议原值）/前门状态/后门状态（UNKNOWN/ERROR/DOOR_OPENED/DOOR_OPENING/DOOR_CLOSED/DOOR_CLOSING 六枚举原值，不臆造中文），AirShowerDoorState 五字段完整呈现，缺失留白；旧实现 t() 翻译枚举原值（语言包无 key 显示原值）与「协议原值展示」等价。
- **控制命令升级（DoD8）**：旧实现开门/关门=弹窗选门类型后直接发送（零确认）；按 P14 电梯控制样板升级为 弹窗选门类型 → confirmCommand 命令确认（标题「风淋门开门/风淋门关门」+影响说明携带门类型+对象清单+提交≠完成附注，非 danger）→ 发送；受理 ≠ 设备动作完成，成功提示「已发送风淋门开门命令，执行结果以「状态」查询为准」（旧「风淋门开门成功」按受理语义改写，不假装完成）；失败经 apiErrorMessage 透传后端诊断；不自动重试。
- **删除升级（DoD8）**：旧版 Popconfirm 仅一问 → confirmCommand（danger，对象+引用检查影响）。
- **只迁当前来源可达控制**：任务卡「迁移……风淋和清占用；只迁当前来源的可达控制」——当前来源 items 仅开门/关门（风淋在 items 无入口、清占用被注释），二者不迁 UI 不落地服务层（本章第 1 条），未通过提前开发把不可达能力变成可达。
- **表单字段与旧版逐项一致**：设备名称（必填 64 字 showCount）/设备驱动（必填 Select；P15 起定版 allowClear=false 不提供反选清空配置入口，旧版 allowClear+onDeselect 清空配置易误触丢稿，不沿用）/ip地址（label 小写旧版原样，必填 64 字 showCount）/端口（必填 0..65535 整数旧同边界）/设备配置（必填 TextArea，选中驱动自动填充 driverProtocol 模板 4 空格缩进，编辑回显 4 空格缩进旧版同）；JSON 解析失败报错留稿。
- **提交体形态**：AirShowerDoorParam 六字段（deviceKey 编辑回带定位/deviceName/driverKey/ip/port/deviceConfig），不含 deviceStatus/showerStatus（后端默认 true 落库，联验实证）；deviceName/ip 提交前 trim。
- 权限：device:air-shower-door:view/add/update/delete/operate 五码（登录响应实证下发），view 挂守卫、四按钮码控入口；「状态」查看行为不控权（旧实现同边界）。

## 3. 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local [登录凭据已脱敏]；python API 层先行、浏览器会话独立）

### API 层（python，全部通过）
- 登录 code=200；permissions 五码 `device:air-shower-door:view/add/update/delete/operate` 全部真实下发。
- 无令牌对照：code=1000000「用户未登录」。
- pageAirShowerDoors GET 平铺基线 total=0 真实空态。
- getAirShowerDoorDrivers：真实驱动「Modbus-tcp风淋门」MODBUS_AIR_SHOWER_DOOR，driverProtocol 含 slaveId/faultSignal.readPoints/frontDoorFullyOpenSignal 等真实协议结构。
- addAirShowerDoor code=200 落库一致：deviceKey 后端生成（32 hex）、deviceConfig 原样、deviceStatus/showerStatus=true（后端默认）、onlineState=OFFLINE 真实下发。
- updateAirShowerDoor code=200 全字段落库（改名+改端口+改 deviceConfig），无 G18 类缺陷；编辑后设备探测 onlineState 翻转为 ONLINE（后端行为，非前端问题）。
- getAirShowerDoorState 真实形态实证：`{onlineState:OFFLINE, showerState:UNKNOWN, failed:false, frontDoorState:UNKNOWN, backDoorState:UNKNOWN}` 五字段枚举原值。
- openDoor（对象为自建记录 ip=127.0.0.1:18200 回环拒绝端口，副作用限定测试后端本机）：code=8010040「设备驱动模式配置异常」——真实命令失败诊断形态实证；openDoor 缺 doorWay：code=500「失败」（后端必需语义实证，页面表单 required 正确）。
- deleteAirShowerDoor 自清理；终态审计 total=0 零残留。

### 浏览器（IAB，rAF+RO 双垫片；全部通过）
- 守卫回跳 /login?redirect=%2Ftri-resource%2Ftri-device%2Fair-shower-door；登录落点=redirect 目标（闭环）。
- 必填拦截 5 项齐出（名称/驱动/ip地址/端口/设备配置）零请求（请求计数实证）。
- 驱动下拉真实数据加载，选中后设备配置自动填充真实协议模板（4 空格 JSON 3495 字符）零请求。
- 状态按钮按需查询：受控 Popover 五项「在线状态=在线（Tag）/故障=否/风淋状态=UNKNOWN/前门状态=UNKNOWN/后门状态=UNKNOWN」（协议原值+缺失留白），单次 getAirShowerDoorState 请求。
- 开门命令全链路：操作项菜单（开门/关门两项同旧 items）→ 控制弹窗（标题「风淋门开门」/label「开门类型」旧版原样）→ 门类型必填拦截「请选择(开/关)门类型」零请求 → 选前门确定 → confirmCommand 五要素（标题/影响含门类型「（前门）」/对象清单/提交≠完成附注/非 danger）→ 取消零请求 → 确认后真实 POST openDoor，失败诊断「风淋门开门出错设备驱动模式配置异常」（8010040）如实呈现，不用动画假装成功。
- 编辑回显完整（4 空格 JSON、驱动名称非裸 key）；改名提交 updateAirShowerDoor 实时更新列表。
- 删除确认 danger 红键；取消路径零请求；确认后「删除风淋门成功」回空态 共 0 条。
- 列偏好闭环：隐藏端口列 → 面板「确定(Enter)」应用 → 落盘（apex-table:columns:[…,"device-air-shower:main"]）→ 整页 reload 持久 → 面板恢复默认 10 列回归（出厂空态落盘，钉右清空=P14 沉淀⑧既定行为）→ 清除偏好键重挂载后代码默认钉右 TH[data-pinned=end] 回归。
- 五语言 html lang 逐语言（en Device Name/Air Shower Status；zh-TW 設備名稱/風淋狀態/IP位址/連接埠；ja デバイス名/エアシャワー状態/開扉·閉扉；ko 장치 이름/에어샤워 상태）。
- 双主题双宽度四组截图 `.run-lock/shots-p18/`（light/dark × 1920/1366；dark-1366 实证操作列钉右+表格内部横向滚动）。
- 恢复亮色主题+真实登出（落点 /login?redirect=…）+页签关闭+dev server 停止+令牌临时文件清除。

## 4. 环境记录（IAB）

- 本轮复验 P17 沉淀两条：关闭动画伪象（判定以 React 状态路径为准）、reload 后垫片在重挂载时序中需经真实菜单导航往返（同路由点击不重挂载组件）。无新增沉淀。
- 开门命令确认层确定后请求计数出现两条 openDoor 记录（均失败诊断、无现场副作用）；怀疑为冻结管线中确认按钮事件与浮层多实例并存的脚本侧重复触发，非业务代码连点缺陷（fireCommand 有 commandPendingKey 防连点），登记观察不阻塞。

## 5. 未完成 / 留专用环境

- 无前端缺口。开门命令已按受控对象（自建记录+回环拒绝端口）真实执行并实证失败诊断路径；对真实现场风淋门的开/关门成功路径与状态翻转（DOOR_OPENED 等）留现场环境，不阻塞本页验收（与本轮 API/浏览器证据链一致）。风淋/清占用为当前来源不可达能力，不属于本页范围。
