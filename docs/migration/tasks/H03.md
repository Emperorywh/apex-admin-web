# H03 录制回放暂缓说明 · 交接记录

**状态**：✅ 已完成（2026-09-18 定时轮交付并勾选）
**批次/前置**：B1 / T00（已完成）
**入口/目标**：`/analyze-visual/record-playback` → `src/pages/record-playback/RecordPlayback/RecordPlayback.tsx`
**私有模块/语言分片**：无新增（复用 common/menu 基座命名空间，与 i18n-map 冻结表「H03 复用 common+menu」一致）
**旧来源**：`C:\code\dd\src\pages\RecordPlayback`（index.tsx + Konva 回放画布 KonvaRender + ControlPanel 控制面板 + TopBar + 录制文件导入/导出 ImportModal/ExportModal + 历史录制 HistoryModal + 车辆信息面板 VehicleInformation/VehicleInfoModal，依赖 getPlaybackFrames 回放帧轮询 / getSimpleMaps / getSystemStatusRanges，本期整体不迁移）

## 交付内容

1. **页面**（RecordPlayback.tsx 重写）：清除模板期 PagePlaceholder 占位，改为复用 T00 统一暂缓组件 `MigrationPending`（title=录制回放）的页面级呈现；多行简体中文注释登记 D07 暂缓边界。页面无业务请求、无轮询/连接、无录制文件导入导出、无时间轴、无回放轮询、无全局进度、无任何完整回放模块代码。
2. **路由标记**：record-playback 从 `pending: true` 转为 `migrationDeferred: true`（H01 建立语义：本期确定不迁移、页面正常加载渲染统一说明），icon: CirclePlay 保留。
3. **落点与回跳**：复用 routeAccess.isLeafAvailable 既有 deferred 排除——本页不作为登录落点候选、登录回跳不放行（root 登录落点保持 /no-permission）。菜单可见性与直访守卫不受影响。
4. **权限核对**：旧 `.umirc.ts` `/analyze-visual/record-playback` access=PERM.RECORD_PLAYBACK_VIEW（record-playback:view）= 新 permission.constants.ts 同值常量，两侧一致未改；有权限用户菜单/直访一致可达，无权限用户守卫拦截。
5. **查询上下文保留**：直访 `?mapId=42&record=abc`（旧入口地图/录制上下文形态）参数原样保留在地址栏，页面不消费、不清洗、不跳转——既不死链也不自动跳旧系统。
6. **资源归属隔离**（验收要求）：任务统计归 P03、实时看板归 P34、统计报表类归 P33/P34/P36/P37，均不受本页影响——本页仅是路由叶子呈现，未触碰任何共享统计/回放组件与 service；MigrationPending 为 H01 已交付公共组件，本轮零改动。

## 操作映射

| 旧操作 | 新实现 | Method + Path | 权限 | 状态/证据 |
| --- | --- | --- | --- | --- |
| /analyze-visual/record-playback 入口（Dock 数据统计子菜单/直访） | RecordPlayback.tsx 渲染统一暂缓说明 | — | record-playback:view（守卫） | ✅ 浏览器实测 |
| 旧页回放业务（Konva 画布/控制面板/时间轴/回放轮询） | 不迁移（D07 本期暂缓） | — | — | ✅ 页内零 /fms 请求、无回放代码加载 |
| 录制文件导入/导出（ImportModal/ExportModal） | 不迁移（验收明确排除） | — | — | ✅ 页面代码路径无导入导出 |
| 历史录制/车辆信息面板 | 不迁移（随回放模块整体暂缓） | — | — | ✅ 页面代码路径无相关组件 |
| 旧深链/查询上下文（地图/录制参数） | URL 参数保留不清洗 | — | — | ✅ ?mapId=42&record=abc 实测保留 |

## 真实联验证据（2026-09-18，dev 代理 192.168.0.158:8888，1366×768）

| 场景 | 结果 |
| --- | --- |
| 未登录直访 /analyze-visual/record-playback?mapId=42&record=abc | 守卫送回 `/login?redirect=%2Fanalyze-visual%2Frecord-playback%3FmapId%3D42%26record%3Dabc`（含查询参数） |
| root 登录（请求带该 redirect） | 落点 `/no-permission`——deferred 页不抢占落点、回跳被拒 |
| 已登录直访 | 统一暂缓说明渲染（标题+「该功能正在迁移中，迁移完成后开放使用」）；页签「录制回放」正常创建；Dock「数据统计」子菜单含「录制回放」入口且点击导航正常 |
| 业务隔离 | 页面加载后 performance 资源条目中 /fms 请求 0 条；页面代码路径仅 MigrationPending，无网络/轮询/回放代码 |
| 五语言 | zh-CN（初始态）/en-US「Record & Playback」/zh-TW「錄影回放」/ja-JP「録画再生」/ko-KR「녹화 재생」标题+说明逐语言实测；html lang 随语言同步 |
| 深浅主题 | 玻璃面板两主题截图：标题/说明均落面板内可读（MigrationPending H01 修复直接受益） |
| 历史往返 | back/forward 稳定在本页 URL，无重定向风暴 |
| 收尾 | 清测试会话（persist:apex-admin:auth 移除）后刷新直访再次被守卫拦回登录页；语言/主题已复位（zh-CN/light）；页签已关；dev server 已停 |
| 工程门禁 | pnpm check（228 文件 lint 0 警告 0 错误 / typecheck / 结构检查 226 源文件）+ pnpm build 全过 |

## 公共文件改动记录（影响消费者）

| 文件 | 改动 | 消费者影响 |
| --- | --- | --- |
| `src/router/definitions.tsx` | record-playback 标记 pending→migrationDeferred | 仅该节点变化；routeAccess/菜单/守卫逻辑零改动（复用 H01 既有 deferred 语义） |
| `src/pages/record-playback/RecordPlayback/RecordPlayback.tsx` | 整页重写 | 无其他消费者 |

## DoD 适用性

- 不适用：表格（4/5）、草稿（7）、写入（8）、文件（9）、地图（10）、统计（14）。
- 适用已满足：可达业务完整（1，旧页业务整体不迁移=暂缓语义，无死链）、权限与状态区分（3，RECORD_PLAYBACK_VIEW 守卫；暂缓≠无权限）、五语言（11，menu/common 既有五语言复用，无新增 key、无缺译）、外观终端（13，两主题×1366×768 截图）、验证证据（15）、交接收尾（16，模板占位清除、无 mock）。
- 追踪项：A01/A02（/analyze-visual/record-playback 为暂缓说明呈现）、A05（原权限保留）、A20（五语言+两主题+最低视口）、A22（菜单别名一致、回跳拒绝）；I02（旧资源去向：菜单 key 走 menu 分片真译、页内回放业务文案随业务不迁移、说明文案复用 common 五语言）、I06（无回退、无缺译）。

## 对后续任务的影响

- H01–H03 三个暂缓入口全部交付完成；B1 批次收口。
- **P03/P34 等 B2a/B3 统计类页面**：本页未触碰任何共享统计组件/service；「数据统计」分组其余入口交付时不受影响。
- 无未完成项、无接口缺口（本页无业务接口）。
