# P20 任务工艺 — 交付与联验记录

- **任务**：TASKS.md P20 任务工艺（B4b / B2 样板门禁、P23、P24；入口 `/mission-cluster/mission-create`；旧来源 `MissionCluster/MissionCreate`；目标 `src/pages/mission-cluster/MissionCreate/MissionCreate.tsx`；私有分片 `orderTemplate`；接口依据 dispatcher/orderTemplate、地图/站点/车辆/分组选项）。
- **本轮运行**：run-20260920-p20-070519（2026-09-20）。
- **状态**：**已交付并带令牌真实联验通过，任务勾选**（API 层 27/27 + 浏览器整页全场景，见第 4 节）。

## 1. 开工核对结论

- **接口族**（OpenAPI 逐 operation 核对）：`pageOrderTemplates`（GET，query 对象平铺 `{pageNo,pageSize,query}`，G04 口径，query=模板名称或编号）；`createOrderTemplate`（POST body=OrderTemplateParam：orderTemplateName/appointVehicleKey/appointVehicleGroupKey/orderMissions[]）；`updateOrderTemplate`（POST body=OrderTemplateUpdateParam：int64 id+同创建字段，整模板替换）；`deleteOrderTemplate`（POST body=`{orderTemplateKey}`——**按调度 key 定位，与 P23/P24 按 int64 id 定位不同族，勿混淆**）。
- **不落地 operation**：`getOrderTemplates`（无参全集）旧任务工艺页零 UI 消费者（新仓库已由 P03 代建共享选项服务 fetchOrderTemplates 供 P03/P21）；`createOrderRecords`（订单执行链路）旧页零消费者。按「只迁当前来源可达控制」纪律不落地。
- **权限码历史交叉实证**（任务卡警告逐条核对）：旧 `.umirc.ts` 中 `/mission-cluster/mission-create` 的 access=`PERM.MISSION_FLOW_VIEW`（=**mission-flow:view**，视图码）；按钮码 `PERM_BUTTON.MISSION_FLOW_ADD/COPY/UPDATE/DELETE`（=mission-flow:add/copy/update/delete）。**任务工艺页挂的是 mission-flow:* 族码，工艺管理页（P21）才是 mission-template:* 族**——旧仓库命名与页面称谓交叉，按实证码值接线不按名称猜。
- **旧页面结构**：列表 4 列（任务名称/任务标识/指定车辆（组）=appointVehicleName 优先回退 appointVehicleGroupName/操作 fixed right 260：复制 primary+编辑 primary+删除 danger Popconfirm）+双层展开行子表（MissionTable：地图名称/地图ID/站点名称/站点ID；ActionsTable：动作类型/动作描述/阻塞类型/动作参数 Descriptions column=1）+MissionModal 三态弹窗（创建/编辑/复制：isModify+modifyRow 组合；三层嵌套 Form.List：子任务→动作→参数）。
- **旧语言资源**：en-US 对本页 key 有 58 键全量真译逐条沿用；zh-TW/ja-JP/ko-KR 全部为简体复制词条，按 B1 基线补译（任务/模板术语与 orderRecord 分片对齐「任務/建立任務/範本」，动作术语与 P23 agvAction 分片对齐「アクション種別/説明/ブロック種別/액션 유형/설명/블록 유형」，地图/站点术语与 crossMap 分片对齐）。

## 2. 交付内容

### 2.1 服务层（owner=P20）

- `services/order-template/order-template-manage.service.ts`：`pageOrderTemplates`（GET 平铺）/`createOrderTemplate`/`updateOrderTemplate`（id 定位整模板替换）/`deleteOrderTemplate`（body `{orderTemplateKey}` 旧同形态）。
- `order-template-manage.service.types.ts`：`TemplateRowDto`（OrderTemplate 全量+orderMissions 带后端回填 mapName/stationName）/`TemplateMissionRowDto`/`TemplateActionDto`（含 conditionStr 保留字段）/`TemplateActionParameterDto`（value 宽松 unknown）/四个 Param DTO（G04 平铺口径与 body 形态注释）。
- 不动 P03 代建的 `order-template.service.ts`（只读选项服务边界保持）。

### 2.2 列表页（`pages/mission-cluster/MissionCreate/`）

- Apex request 模式：稳定行 ID=id（stringFieldRowId 雪花守卫）、列偏好 `order-template:main`、操作列默认钉右（defaultPinning end:['actions']）、G09 不开放排序（全列 enableSorting:false）、pageSizeOptions [10,20,50,100,200]。
- **列结构逐列核对旧版**（4 列）：任务名称（弹性，缺失留白）/任务标识（弹性）/指定车辆（组）（弹性；appointVehicleName 优先、缺失回退 appointVehicleGroupName=旧 render 同语义）/操作（260 Apex 钉右：复制+编辑 primary、删除 danger；按钮带 lucide 图标=样板升级形态）。
- **删除升级 confirmCommand 破坏性确认**（标题「删除任务」/对象=模板名/影响说明/附注「命令提交后不代表操作已完成」/danger 红键；旧 Popconfirm 仅一问，DoD 8 升级 P04–P24 同款登记）；行级防连点。
- **展开行双层子表均 Apex data 模式**（真实完整小集合、只读、关闭虚拟化=P22 同款）：第一层子任务 4 列（行 ID=数据库 id）；第二层动作 4 列（动作参数列 260px，单元格 Descriptions column=1 旧同形态；**行 ID=传入前注入「动作类型+卡内序号」复合标识**——动作无独立主键，旧 rowKey=actionType 的唯一化增强）；`rowExpandable` 空集合不可展开。
- 工具行：搜索框 256px（旧 Search enterButton 同宽）+查询按钮+回车提交+trim 回首页（旧 onSearch 同语义）；创建任务按钮按 `mission-flow:add` 显隐。无轮询（配置数据 DoD 7）、无手动刷新按钮、空值一律留白。

### 2.3 弹窗（`features/mission-cluster/components/MissionTemplateModal.tsx`）

- **单弹窗三态**（mode=create/edit/copy，旧 isModify+modifyRow 组合同语义）：标题「创建任务/编辑任务/复制任务」；复制=整表单回显+提交走 create 不带 id（旧实现同语义）。
- 表单结构与旧版逐项一致：模板名称（必填「请输入模板名称」）/指定车辆与指定车辆分组互斥显示（旧 shouldUpdate 条件渲染同形态；**切换时互斥清理对方字段值**——收敛旧实现「隐藏字段值残留提交」的模糊边界，OpenAPI 声明两字段语义互斥；提交兜底分组优先）。
- **子任务 Form.List**（至少 1 条整组校验「子任务不能为空」+Form.ErrorList 呈现）：地图（必填，切换清空本卡站点=旧 onMissionMapIdChange 同语义）/站点（必填，**下拉展开时按地图 id 拉取 getCrossMapStations 幂等重查**（P10 CrossMapModal 同模式=旧 onNodeIdOpenChange 同语义），**按 mapId 缓存共享**（旧 stationsMap 同款：同地图多子任务复用选项；编辑回显时按回显数据合成缓存条目=旧 initialStations 同语义））/动作 Form.List。
- **动作卡**：动作类型/动作描述（必填文本）/阻塞类型（必填，NONE/SOFT/HARD；**编辑回显未知原值并入选项=P23 同款纪律**，经 Form.useWatch 整树订阅逐卡计算）/动作参数 Form.List（动作名+动作值 Row 11/11/2+行删除图标+「添加子任务动作参数」虚线按钮=旧同布局；**value 非字符串 getValueProps 仅转显示、未改动行提交原始值=P23 守恒纪律**）。
- 提交：显式按 OpenAPI 组装（后端只消费声明字段，行为等价旧 getFieldsValue(true) 全量）；编辑带 id（从 target 显式携带）；提交防重复（confirmLoading+submitting 门）；失败保留输入不关弹窗（旧失败路径同语义）；关闭销毁草稿（destroyOnHidden，旧 handleCancel resetFields 同语义）。打开按模式回填/清空依赖 open 触发（P32 教训：复制与编辑可能指向同一行引用）。
- Modal 宽度 720（三层嵌套卡片在旧默认 520 中不可读，展示差异登记）。
- 选项加载失败下拉 notFoundContent 显示 common「加载失败」文本（无重试按钮=按钮纪律，P10 同款）；站点加载失败 message.warning 如实反馈（旧同语义）。

### 2.4 路由与权限

- 路由 `mission-cluster-mission-create` 解除 pending，`i18nNamespaces: ['orderTemplate']`；菜单码 `mission-flow:view` 挂守卫（**历史码值交叉注释已写入路由定义**）。
- 按钮码 `mission-flow:add/copy/update/delete`（PERM_BUTTON 既有常量）控制创建/复制/编辑/删除入口显隐（旧 §7/§8.2 同边界）。

### 2.5 i18n

- `src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/orderTemplate.ts` + `i18n.ts` 四表注册。
- en 旧真译 58 键逐条沿用+样板升级补译（删除确认标题/影响、错误 msg 插值形态、查询按钮）；插值形态修正登记：旧「创建任务出错」无插值 → 新「创建任务出错：{{msg}}」（P19/P24 同款）；旧键「确定删除当前任务?」随 confirmCommand 升级退役、「查询任务组合列表出错」随 Apex 内建错误态退役（i18n-map 登记映射去向）；繁日韩 B1 基线补译真实译文（术语对齐见第 1 节）。

### 2.6 门禁（交付终态）

- `pnpm lint`：459 文件 0 警告 0 错误（曾抓出弹窗未用 import 一处、Form.Item 泛型路径类型两处、Apex cell getValue 数组列 {} 推断一处（P12 同款），修复后复跑通过）；
- `pnpm typecheck`：通过；
- `pnpm check:structure`：457 源文件通过；
- `pnpm build`：968ms 成功（chunk 警告为既有 apex-table-react 体积）；
- 调试残留 grep：0。

## 3. 契约发现与观察项（联验实证）

1. **后端允许模板重名**（同名两次 create 均 200 落库，计数=2）：模板名称无唯一性约束——与 P24 动作组重名 5000000 拒绝相反；UI 层复制/重名场景同样受理（浏览器实证列表 2 行并存）。
2. **update 按 int64 id 定位正常**（id 改名+子任务/动作/参数整模板替换落库，无 G18 类缺陷）；首轮 API 脚本 update 3001000「任务模版不存在」经对照实验定性为**脚本自身缺陷**（重名清理时分页倒序误删 update 目标，dups[1:] 陷阱），非后端问题——登记为脚本陷阱教训：**分页清理不假设顺序，按业务 id 排除**。
3. **后端按 appointVehicleGroupKey/appointVehicleKey 互斥语义受理**（浏览器抓包：选车辆时 body 仅含 appointVehicleKey，groupKey 不提交；API 层创建带 groupKey 时 appointVehicleKey 缺省落库正常）。
4. **pageOrderTemplates 分页倒序**（新行在前）——首轮脚本清理残留的根因之一，已登记脚本纪律。
5. **【观察项·跨页样板级】清列偏好+reload 后代码 defaultPinning 未在 DOM 激活**（data-pinned 缺失、position=relative）：传导链已对照实验隔离——显式偏好 {end:['actions']} 落盘 reload 后钉右正常激活（data-pinned=end+sticky/0px），面板手动钉右同样正常；仅「无存储→首帧 defaultPinning」场景未激活，且 prefs.save 300ms 防抖会在 removeItem 后写回（清理须等防抖窗口）。P14–P24 同样板页面均登记过「默认钉右」，P24「清偏好后默认钉右回归」与本轮「清偏好+reload」场景存在差异（疑似未 reload 或时序不同）。非本页引入（样板代码逐字相同），登记待样板级统一复核（建议 V01 收口）。

## 4. 带令牌真实联验（2026-09-20 run-20260920-p20-070519，全通过）

### 4.1 API 层（.run-lock/p20_api_verify.py，27/27 全过，脚本验证后删除）

登录 code=200 → 权限五码真实下发（mission-flow:view/add/update/delete/copy）→ 首轮遗留清理兜底 → 分页 GET 平铺形态（records/total/size/current；本环境 total=0 真实空态）→ 无令牌读写双通道 1000000 对照 → query 未命中空态 → 选项接口四件核对（getSimpleVehicles 56 辆/getVehicleGroups 0 条空集合如实/getSimpleMaps 6 张/getCrossMapStations 1691 站点）→ create code=200 落库复核（名称命中+两子任务结构+地图站点回填名+动作顺序保持 MOVE/WAIT+阻塞 NONE/SOFT+参数 speed=1.5 顺序保持）→ 重名 200 落库（契约发现 1）→ update 改名+子任务变更落库复核（无 G18 类缺陷）→ 复制（create 不带 id）生成独立行（新 key 新 id）→ delete body {orderTemplateKey} 自清理 → 主模板删除 → **终态零残留（P20E2E 前缀审计 0 条 final_total=0 base_total=0）**。
- 首轮 3 FAIL（update 3001000+残留 1 条）经诊断定性为脚本倒序清理误删链（契约发现 2/4），修正重跑全过；首轮残留 id=2 由遗留清理兜底清除。

### 4.2 浏览器整页（dev 代理 192.168.0.158:8888，IAB rAF+RO 永续 fire 垫片）

- **守卫与落点**：未登录直访回跳 `/login?redirect=%2Fmission-cluster%2Fmission-create`；登录成功落回 redirect 目标（闭环；页签标题「任务工艺 · 调度系统」）。
- **空态起步**：后端 total=0 真实空态（暂无数据/共 0 条）。
- **必填拦截**：空表单确定→「请输入模板名称」+「子任务不能为空」两条齐出，零请求（XHR 计数 18→18）。
- **创建全链路（三层嵌套）**：填模板名称→指定车辆下拉选中（**分组下拉互斥消失=联动实证**）→添加子任务→地图选中「6车间-货架区」→站点下拉展开（**getCrossMapStations 请求真实发出=onOpenChange 按需加载实证**）选中「126」→添加动作卡→类型 MOVE/描述/阻塞 NONE→参数行 speed=1.5→提交→**抓包 body 完整正确**（模板名/appointVehicleKey/orderMissions[{mapId,stationId,actions[{actionType,actionDescription,blockingType,actionParameters}]}]，互斥字段收敛生效）→自动刷新 1 行。
- **展开行双层子表**：第一层 4 列（地图名称/地图ID/站点名称/站点ID 与提交一致）→第二层动作 4 列（MOVE/P20E2E浏览器动作一/NONE/speed 1.5）。
- **编辑**：回填全字段（标题「编辑任务」+名称+车辆+地图站点（**站点缓存合成回显**）+动作四字段+参数值）→改名提交→抓包 update body 含 id=8+全量结构（未改动值守恒）→列表行实时更新。
- **复制**：标题「复制任务」+完整回显→直接提交→抓包走 createOrderTemplate 无 id→列表 2 行并存（后端允许重名 UI 实证）。
- **删除 confirmCommand 五要素**：标题「删除任务」/对象「P20E2E浏览器演示模板改名」/影响「该任务模板及其子任务配置将被永久删除，不可恢复」/附注「命令提交后不代表操作已完成」/danger 红键；取消路径零请求（31→31）；确认逐行删除→**终态回空态（暂无数据/共 0 条）**。
- **搜索三态**：命中「P20E2E搜索演示模板」1 条/未命中空态/清空恢复 1 条。
- **列偏好四步**：隐藏「任务标识」+面板确定（原生 dialog 点确定=P30 沉淀㉒）→表头消失+偏好落盘（order-template:main 键 columnVisibility）→reload+垫片+菜单导航回页仍隐藏（持久）→恢复默认+确定→全列回归→清偏好（**等 300ms 防抖窗口后 removeItem**）→显式偏好对照实验（落盘 {end:['actions']} reload 后 data-pinned=end+sticky/0px 激活=传导链实证）；**无存储首帧 defaultPinning 未激活登记观察项（契约发现 5，跨页样板级）**。
- **五语言**：zh-CN/zh-TW/en-US/ja-JP/ko-KR 逐语言切换（apex-admin:lang 真相源直写+reload），html lang 同步（en Task Name/Create Task；繁 任務名稱/建立任務；日 タスク名/タスク作成；韓 작업명/작업 생성），表头/按钮/占位逐语言正确。
- **双主题双宽度四组截图**：`.run-lock/shots-p20/{light,dark}-{1920,1366}.png`（垫片+菜单导航路径重截，全部带真实数据行）；布局无溢出、深浅色可读、Dock/页签/分页齐（截图抽查确认）。
- **多页签容器限定教训再现并处置**：文档级 querySelector 曾命中 PageCache 缓存页签（order-record）的展开按钮与 0 尺寸幽灵按钮——以「占位输入框向上定位 _page_ 容器」限定后操作（P24 沉淀延续，容器定位脚本化）。
- **清理**：演示行 UI 删除自清理（终态空态）、恢复亮色+简中、真实登出（用户菜单→退出登录→token 清空+回跳登录页）、dev server 停止（PID 35972 终止）、脚本自毁（p20_api_verify.py 删除）、终态审计零残留。

## 5. 向 P21 提供的契约（已实证版）

- 模板实体：`TemplateRowDto`（id int64/orderTemplateKey 调度 key/orderTemplateName/appointVehicleKey·Name/appointVehicleGroupKey·GroupName/orderMissions[] 带后端回填 mapName/stationName）。
- 选项消费：P21 关联模板选择经 P03 代建共享选项服务 `fetchOrderTemplates`（getOrderTemplates 全量，orderTemplateKey 为选项值）。
- 后端允许模板重名（无唯一性约束）；删除按 orderTemplateKey 定位（body {orderTemplateKey}）；update 为 int64 id 整模板替换。
- 动作/参数值域契约见 P23.md 第 5 节（P20 模板内嵌动作与 P23 动作库同构：blockingType 受控枚举、参数 value 宽松保形）。
