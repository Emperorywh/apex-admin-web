# P23 车辆动作 — 交付与联验记录

- **任务**：TASKS.md P23 车辆动作（B3 / B2 样板门禁；入口 `/mission-cluster/action-control/agv-action`；旧来源 `ActionControl/AGVAction`；目标 `src/pages/action-control/AGVAction/AGVAction.tsx`；私有分片 `agvAction`；接口依据 action/agvAction）。
- **本轮运行**：run-20260919-p23（交付）+ run-20260919-p23-recheck2（补验，2026-09-19）。
- **状态**：**已交付并带令牌真实联验通过，任务勾选**（API 层 17 项 + 浏览器整页全场景，见第 4 节）。

## 1. 开工核对结论

- **接口族**（OpenAPI 逐 operation 核对）：`pageAGVActions`（GET，query 参数对象 `actionPageParam` → 平铺 `{pageNo,pageSize,query}`，G04 口径，已实证）；`addAGVAction`/`updateAGVAction`/`deleteAGVAction`（POST，body 均为 `AGVActionParam`——**delete 以 body `{id}` 定位**，与 P22 避障的 POST+?id= 形态不同，后端接受性已实证）；`blockingType` 枚举 NONE/SOFT/HARD（**后端受控：未知值 500 拒绝，联验实证**）；`ActionParameter.value` OpenAPI 声明 object（**实际形态已实证：字符串/数字/数组/对象混提落库回读全保形**）。
- **递归检查原可达文件操作**：`importExcel`/`exportExcel` 在旧仓库（含车辆动作页在内全仓）**零 UI 消费者**——旧页面只有搜索+新增+行内编辑/删除，无导入导出入口；按「只迁当前来源可达控制」纪律不落地（P17 getTrafficLights / P22 getObstacleAvoidanceList 同口径）。
- **getAGVActions**：旧仓库零 UI 消费者；新仓库已由 P03 代建共享选项服务（`services/action/agv-action.service`，P24/P20 消费），P23 不重复落地。
- **旧语言资源**：en-US 对本页 key 有全量真译逐条沿用；zh-TW/ja-JP/ko-KR 全部为简体复制词条，按 B1 基线补译（「アクション/액션」与 menu 分片「車両アクション/차량 액션」对齐）。

## 2. 交付内容

### 2.1 服务层（owner=P23）

- `services/action/agv-action-manage.service.ts`：`pageAgvActions`（GET 平铺）/`addAgvAction`/`updateAgvAction`（id 定位整体提交）/`deleteAgvAction`（body `{id}`，旧实现同形态）。
- `services/action/agv-action-manage.service.types.ts`：`AgvActionRowDto`（AGVAction 全量字段；**blockingType 放宽为 string 承载未知枚举原值**——与 P03 共享 `AGVActionDto` 的差异点，文件头注明）；`AgvActionParameterDto`（value: unknown）；`AgvActionPageParam`（G04 平铺口径注释）。
- 不动 P03 代建的 `agv-action.service.ts`（文件头「本服务不扩展」边界保持）。

### 2.2 列表页（`pages/action-control/AGVAction/`）

- Apex request 模式：稳定行 ID=id（int64 经 stringFieldRowId）、列偏好 `agv-action:main`、操作列默认钉右（`defaultPinning end:['actions']`）、G09 不开放排序（全列 enableSorting:false）、pageSizeOptions [10,20,50,100,200]。
- **列结构逐列核对旧版**（5 列）：
  | 旧版 | 新版 | 等价说明 |
  | --- | --- | --- |
  | 动作类型（弹性） | actionType | 原文/缺失留白 |
  | 动作描述（弹性） | actionDescription | 原文/缺失留白 |
  | 阻塞类型（弹性） | blockingType | 协议原值（NONE/SOFT/HARD 不译），未知枚举显示原值不臆造中文 |
  | 动作参数（Descriptions column=1 size=small，label=key/children=value） | 同形态 antd Descriptions | 非字符串 value 以 JSON 文本展示（展示层转换 `toValueDisplayText`，不改数据）；无参数留白 |
  | 操作（width 200 fixed right，编辑 primary+删除 danger Popconfirm） | actions size=200 Apex 钉右 | 删除升级 confirmCommand danger（旧 Popconfirm 仅一问，DoD 8 升级）；行级防连点 |
- 工具行：Input 360px（占位「查询车辆动作」）+查询按钮+回车提交（旧 Search enterButton 形态按 B3 收敛）；新增按钮按 `action:vehicle:add` 显隐。
- 无轮询（配置数据 DoD 7）、无手动刷新按钮（按钮纪律）、空值一律留白。

### 2.3 弹窗（`features/action-control/components/ActionFormModal.tsx`）

- 双模式（editTarget=null 新增）；footer 清空/取消/确定（B3 样板升级形态）；草稿保留+useTabDirtyGuard+失败留稿+提交防重复。
- 四字段与旧版逐项一致：动作类型（必填「请输入动作类型!」）/动作描述（必填）/阻塞类型（Select allowClear 必填 NONE/SOFT/HARD）/动作参数 Form.List（Row 11/11/2 全宽、行内动作名+动作值必填、CircleMinus 行删除、底部「添加动作参数」虚线按钮；**Form.List 直接置于 Form 下无标签列，与旧版结构逐像素一致**——首轮自查曾误加外层 label Form.Item 会把参数行挤进 wrapperCol，已修正）。
- **P23 专项「已有动作参数不因未知枚举丢失」两处实现**：
  1. 编辑回显的 blockingType 不在枚举集内时，原值动态并入下拉 options（label=value 原样），保存原值回传不静默改写（**联验实证：后端 blockingType 为受控枚举，未知值 500 拒绝——该路径为防御性实现，自然路径不触发，保留**）；
  2. 参数 value 非字符串（OpenAPI object 域）回显经 `getValueProps` 仅转显示（`toValueDisplayText`），表单状态保留原值——**联验实证：未改动行直接提交，请求体 value={"n":5,"keep":true} 对象保形回传**（旧实现 Input 受控同语义，仅修复非字符串的显示损坏）。
- 提交契约：新增 body={actionType, actionDescription, blockingType, actionParameters[]}；编辑另带 `id`（int64，从 editTarget 显式携带）；表单值不 trim（旧版原样提交同语义）。

### 2.4 路由与权限

- 路由 `mission-cluster-action-control-agv-action` 解除 pending，`i18nNamespaces: ['agvAction']`；菜单码 `action:vehicle:view` 挂守卫（路由既有 `PERM.ACTION_VEHICLE_VIEW`）。
- 按钮码 `action:vehicle:add/update/delete`（`PERM_BUTTON` 既有常量）控制新增/编辑/删除入口显隐（旧 §7/§8.2 同边界）。
- i18n：`src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/agvAction.ts` + `i18n.ts` 四表注册。

### 2.5 门禁（交付终态）

- `pnpm lint`：394 文件 0 警告 0 错误；
- `pnpm typecheck`：通过；
- `pnpm check:structure`：392 源文件通过；
- `pnpm build`：成功（chunk 警告为既有 apex-table-react 体积）；
- 调试残留 grep：0。

## 3. 静态审计（交付轮完成项，补验轮已全部闭环）

- **格式化代码路径审计**：第 2.2 节列对照表即审计结果——逐列/逐字段与旧版一致；空值路径（缺失/空数组）全部留白；阻塞类型未知枚举原值路径直达。
- **静态布局算术**：表格容器 `flex:1; min-height:0` + `height="100%"`（AGENTS 第 1 节标准写法，无 calc 硬编码）；操作列 200 固定+其余弹性，1366/1920 视口均无容器 blowup 风险；弹窗默认 520px，参数行 11/11/2 全宽两输入框各约 216px，与旧版一致。
- ~~待视觉验收风险点~~（交付轮登记 5 项，补验轮全部实证）：
  1. ✅ pageAGVActions GET 平铺口径（G04 实证：`?pageNo=1&pageSize=10&query=…`）；
  2. ✅ 权限四码 `action:vehicle:*` 真实下发（登录响应 permissions 平铺含四码）；
  3. ✅ 参数 value 非字符串形态的落库与回读（字符串 '1.5'/数字 3/数组 ['A','B']/对象 {mode:'auto',n:1} 混提全保形；未改动行提交原始对象实证）；
  4. ✅ deleteAGVAction body `{id}` 形态后端接受（code=200 自清理）；
  5. ✅ 页面渲染/增改删/列偏好/守卫回跳/五语言/双主题双宽度全链路（第 4 节）。

## 4. 带令牌真实联验（2026-09-19 补验轮 run-20260919-p23-recheck2，全通过）

### 4.0 G25 定性修正（补验轮首要结论）

- **root 凭据从未失效**。交付轮「连续 4 次 1000010」的真因：联验脚本 `.run-lock/p23_api_verify.py` 登录请求体误写 `userName`（大写 N），而后端与前端 `auth.service` 实际字段名为 `username`（全小写）——后端收到 null 用户名返回「用户名或密码错误」，被误判为「密码被外部修改」。
- 复查轮以 `username` 字段名单次探测即 code=200；修正脚本字段名后 17 项全过。备用环境 10.11.2.67 仍超时（与本案无关）。
- 教训已沉淀 gaps G25（关闭）与 AGENTS 第 5 节：联验受阻先核对脚本请求体与前端 service 实际字段名，不先入为主归因外部变更。

### 4.1 API 层（.run-lock/p23_api_verify.py，17 项全过）

登录 code=200（activated=True）→ 权限四码真实下发 → 分页 GET 平铺基线（total=1 真实数据 drop）→ 无令牌 1000000 对照 → 创建 code=200（参数含字符串 '1.5'/数字 3/数组 ['A','B']/对象 {mode:'auto',n:1}）→ 落库复核全字段一致+四形态 value 保形 → 编辑全字段落库（无 G18 类缺陷：desc/blockingType/参数整体替换全部生效）→ 筛选命中与未命中空集 → 删除 body {id} code=200 自清理 → 终态审计零残留（base=1 final=1）。

### 4.2 浏览器整页（dev 代理 192.168.0.158:8888，IAB rAF+修正版 RO 双垫片）

- **守卫与落点**：未登录直访回跳 `/login?redirect=%2Fmission-cluster%2Faction-control%2Fagv-action`；登录成功落回 redirect 目标（闭环）。
- **列表**：5 列+序号逐列与旧版一致；真实数据 drop/fh/HARD 无参数留白；分页「共 N 条」。
- **必填拦截**：空表单确定→「请输入动作类型!/请输入动作描述!/请选择阻塞类型!」三项齐出，零请求（XHR 计数不变）。
- **新增**：填类型/描述/SOFT/两行参数提交→「添加动作成功」toast→列表刷新新行（Descriptions 渲染 speed=1.5/mode=auto）。
- **编辑**：回显四字段+SOFT Select+两行参数；改描述提交→行实时更新+「编辑动作成功」。
- **删除**：confirmCommand 五要素（标题「删除车辆动作」/对象·P23E2E/影响「永久删除不可恢复」/附注「提交≠完成」/取消+danger 红键）；取消零请求；确认→行消失+「删除动作成功」。残留 confirm 旧实例点击无效属 IAB 动画伪象（P17 ⑪/⑦ 复验，最后实例纪律有效）。
- **列偏好闭环四步**：隐藏「动作描述」→应用→表头消失；整页 reload→保持隐藏（持久化）；恢复默认→全列回归（出厂空态无钉）；清偏好→reload→代码默认钉右回归（`position:sticky; right:0`；**data-pinned 属性仅在偏好保存时写入，代码 defaultPinning 以 sticky 样式呈现**——登记 Apex 呈现差异观察）。
- **P23 专项（浏览器层）**：API 直发 P23UNK（SOFT+对象 value {keep:true,n:5}）→列表参数列显示 JSON 文本→编辑回显 input 为 JSON 文本、Select 显示 SOFT→**不改动直接提交→XHR 抓包请求体 value 为原始对象**（getValueProps 仅转显示、表单状态保留原值实证）。
- **blockingType 后端受控枚举实证**：直发 blockingType='P23UNKNOWN' 后端 500 拒绝——前端未知枚举 options 并入为防御性实现，自然路径不触发。
- **五语言**：zh-CN/zh-TW/en-US/ja-JP/ko-KR 逐语言切换，html lang 同步（zh-CN/en-US/ja-JP/ko-KR 实测快照+zh-TW 全页），页签/按钮/表头/分页逐语言正确（en 旧真译 Action Type/Description/Block Type/Parameters 沿用）。
- **双主题双宽度四组截图**：`.run-lock/shots-p23/{light,dark}-{1920,1366}.png`，布局无溢出、深浅色可读、Dock/页签/分页齐。
- **清理**：P23UNK 删除自清理（audit total=0）、恢复亮色+简中、真实登出 code=200、页签关闭、dev server 停止。

## 5. 向 P24/P20 提供的动作契约（已实证版）

- 动作实体：`AgvActionRowDto`（= OpenAPI AGVAction；id int64 主键/actionType/actionDescription/blockingType NONE|SOFT|HARD（放宽 string；**后端受控枚举，未知值 500**）/actionParameters[{key,value:unknown}]）。
- 参数值域：**字符串/数字/数组/对象混提落库回读全保形（实证）**；页面表单输入产出字符串，非字符串值经 API 直发产生；编辑回显仅转显示、未改动行提交原始值（实证）。
- P24 动作分组（组内动作整组提交）与 P20 工艺模板（引用动作）按此契约消费；共享选项服务 `fetchAGVActions`（P03 代建）不变。
