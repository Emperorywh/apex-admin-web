# P28 操作日志 — 交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
> run-20260920-p28-000549（2026-09-20 交付并勾选）。旧来源 `C:\code\dd\src\pages\SystemInvolve\OperationLog`（index.tsx + SearchForm）。

## 1. 交付范围

- **服务层**：`services/system/sys-log`（owner=P28，contracts.md 第 5 节）。
  - `POST /fms/v1/common/sysLog/pageSysLogs`（POST + JSON body，OpenAPI SysLogPageParamSysLog；与 P06 carrier 同形态，不归一化改写为 GET query）。
- **页面**：`pages/system-involve/OperationLog/OperationLog.tsx` 整页重写（替换迁移过渡占位）：
  - 列表 Apex request 模式（POST 服务端分页/取消/错误重试内建）；行 ID=id（int64 主键，联验实证安全整数范围内）；列偏好 `operation-log:main`；**代码默认钉位** `{start:['title'], end:['requestTime']}`（旧版 fixed left/right 等价迁移；用户保存偏好覆盖代码默认）。
  - **11 列逐列核对旧版**（全 center）：日志标题 180（钉左省略悬浮）/日志类型 100（ERROR 红「异常」/NORMAL 绿「正常」/未知枚举原值 Tag）/操作用户 120/请求IP 140/目标名称 200/模块 120（14 枚举映射蓝 Tag，未知原值）/请求参数 220/响应参数 220（JSON 详情单元格）/异常详情 220/请求耗时(ms) 120/请求时间 250（钉右，displayDateTime 秒级展示转换）。合计 1890 与旧版 scroll.x 1800 同密度。
  - 筛选栏五条件（旧版同布局：首行四条件+次行时间范围与按钮组）：日志标题/目标名称（trim 空裁剪）/日志类型（NORMAL|ERROR）/模块（14 枚举常量等价迁移 `features/operation-log/moduleOptions.ts`）/请求时间范围（RangePicker showTime HH:mm + allowEmpty 两端独立）。
  - G09 不开放排序（旧页面无排序、接口未声明）；普通审计日志不轮询（DoD 7）；无手动刷新按钮（按钮纪律）。
- **JSON 详情单元格**（`features/operation-log/JsonPreviewCell.tsx`，旧 JsonViewer 等价迁移）：Popover 格式化 JSON（30vw×40vh pre-wrap 可滚动）+ 标题复制按钮（复制格式化文本）。
- **路由**：`system-involve/operation-log` 解除 pending，挂 `operationLog` 命名空间，菜单码守卫。
- **i18n**：operationLog 四语言分片同构（en 旧真译逐条沿用；繁日韩 B1 基线补译，模块 14 枚举沿用旧真译，公共词与 P26 对齐：查詢/検索/조회、清空/クリア/지우기、ログ種別/로그 유형；「点击查看详情」为本页新增 key）。

## 2. 专项验收对照

| 验收项 | 结果 |
| --- | --- |
| 时间筛选边界和序列化有证据 | **API 层实证**：`startRequestTime/endRequestTime` 秒级字符串（`yyyy-MM-dd HH:mm:ss`）受理；2026 全年范围命中（total≈基线+当日增量）/未来范围（2030）空态 total=0/仅起始单端边界受理。代码层 `format('YYYY-MM-DD HH:mm:ss')` 与旧版 dayjs 逐字等价 |
| 敏感筛选不自动写 URL | 实证：提交 title=「登录」筛选后 `location.href` 保持 `/system-involve/operation-log` 无任何 query；本页无 URL 同步逻辑 |
| 日志详情区分缺失、空值和查询失败 | 缺失（null/undefined/空串）→ 留白不可点击；JSON 空值（`'null'` 字面量/`'{}'`）→ 显示原文形态可点开（与缺失可区分）；非 JSON 文本 → 保留服务器原文；查询失败 → Apex 内建错误态+表格内重试 |
| 字段展示遵守部署时区（G15） | requestTime 后端返回恒定 23 字符毫秒串 `yyyy-MM-dd HH:mm:ss.SSS`（联验抽样 20 条长度集合={23}）；displayDateTime 既有正则前缀匹配截断为秒级展示（P26 同款既有能力覆盖）；时间条件按部署时区语义提交（缺省 Asia/Shanghai） |

## 3. 契约实证与差异登记

1. **POST body 形态**：OpenAPI requestBody required（application/json）与旧实现一致；联验实证 body `{pageNo,pageSize,...}` 受理。
2. **id 主键形态**：int64 声明；联验实测 number 且在安全整数范围（首条 513254 量级，自增语义）。`stringFieldRowId` 的不安全整数守卫保留（若后端未来切雪花 ID 会在联调期显性报错）。
3. **targetName 为精确匹配**（与旧源码注释「模糊查询」不符）：`targetName=root` 命中 205 条、`roo` 命中 0 条。页面 placeholder「请输入目标名称」不承诺模糊语义，代码不改；差异在此登记。
4. **logType 未知枚举防御行为**：`{logType:"FOO",module:"BAR"}` 后端 HTTP 200 + bizCode=500「失败」（如实记录；前端选项只发 NORMAL/ERROR 与 14 模块枚举，不会触发）。
5. **module 值域实证**：本页数据实际出现 ORDER/USER，与 14 枚举常量兼容；未知枚举原值呈现防御保留。
6. **越界页码**：真实越界页（pageNo=999999）返回 200 + records=[] + total 真实值。
7. **无令牌对照**：POST pageSysLogs 无令牌 code=1000000「用户未登录」（会话失效收敛）。
8. **等价收敛差异（旧版行为修正，不属缺陷）**：① 旧版 logType 缺失显示字符串 `"NULL"`，按空值纪律收敛为留白；② 旧 JsonViewer 解析失败/空对象吞成「暂无数据」，按「保留服务器业务原文」改进为原文呈现。
9. **默认 pageSize**：旧版 15（pageSizeOptions [10,15,20,50,100,200]）；Apex 基座 pagination 仅支持 pageSizeOptions，初始 pageSize 为基座内定，档位清单已等价保留（登记差异）。
10. **权限**：仅菜单码 `system:operation-log:view`（联验实证下发）；本页只读无写操作、无下载，**无按钮码**（旧 §7 同边界）。

## 4. 验证证据

- **门禁**：lint 422 文件 0 警告 0 错误；typecheck 通过；check:structure 420 源文件；build 成功（chunk 警告为既有 apex-table-react 体积）。
- **API 层**：`.run-lock/p28_api_verify.py`（18/18 通过，脚本验证后删除）——登录/权限码/POST 分页形态/记录形态（id 精度/requestTime 毫秒串/module 值域）/无令牌对照/筛选（NORMAL 命中 512,965 与 ERROR 287 互补/.title 模糊 245/未命中空态 0/module 精确/targetName 精确-语义差异登记）/时间边界三态/未知枚举 500 记录/越界页码/只读零残留。
- **浏览器整页**（dev 代理 192.168.0.158:8888，IAB rAF+RO 双垫片——垫片注入时机竞态「时好时坏」再现，goto 返回瞬间注入+两轮手动 fire 恢复，AGENTS 5-⑫⑬⑭⑰ 全套纪律）：
  - 守卫回跳带 redirect + 登录落点闭环（`/login?redirect=%2Fsystem-involve%2Foperation-log` → 登录 → 原路落点）。
  - 列表 10 行真实渲染、12 格逐列核对（含异常详情缺失留白/requestTime 毫秒串→秒级展示）。
  - JSON 详情：点击请求参数单元格 → Popover 标题「请求参数」+复制按钮+4 空格格式化 JSON 多行正文（真实业务参数 extendParameters/orderMissions 原文）。
  - 筛选：title=登录 命中 251 条（XHR 抓包 body `{"pageNo":1,"pageSize":10,"title":"登录"}` = 空条件裁剪实证+序列化证据）；未命中 total=0 +「暂无数据」「共 0 条」；清空按钮重置表单**零请求**（旧版语义保持）。
  - URL 全程无筛选参数（敏感筛选不写 URL 实证）。
  - 列偏好四步闭环：隐藏异常详情→Enter 应用（表头 11→10）→偏好落盘（`columnVisibility.exceptionDetail:false`）→reload 持久恢复→恢复默认→Enter（表头回 11 列、偏好 exceptionDetail:true、pinning 重置出厂空态 ⑲）。
  - 钉位：偏好切片 `{start:['title'], end:['requestTime']}` 随保存落盘；data-pinned DOM 属性为已知呈现差异（P23 沉淀⑧登记）。
  - 五语言逐语言真实菜单切换：en（Log Title/Operator/Duration (ms) 全表头真译）/繁（操作使用者/響應參數）/日（ログ種別/処理時間(ms)）/韩（작업 사용자/요청 소요 시간(ms)）/简中回切，html lang 逐语言同步。
  - 双主题双宽度四组截图 `.run-lock/shots-p28/`（dark-1920/dark-1366/light-1920/light-1366）。
- **清理**：真实登出（logout → /login?redirect=...，persist auth token=null/permissions=[]）+页签关闭+dev server 停止+联验脚本删除；只读页终态零残留。

## 5. 已知限制

- RangePicker 浏览器层「面板选择→提交请求体」的直接抓包证据未取得：IAB 合成事件/DOM 注入/注入键盘均无法驱动 antd v6 RangePicker 内部 Dayjs 状态（Input 同法注入成功证明提交流程本身正常；非产品缺陷）。时间序列化与边界证据以 API 层为准（第 2 节），面板结构（showTime HH:mm/allowEmpty/五语言 placeholder）已实证。
- 重启/删除等副作用操作不存在（只读页），无专用环境遗留项。
