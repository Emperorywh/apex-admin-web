# P32 角色管理（合并模板实现）— 任务交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
| 字段 | 内容 |
| --- | --- |
| 任务 / 负责人 / 基线 | P32 角色管理（合并模板实现）；AI 执行者（run-20260920-p32-040544，定时轮）；分支 apple-rxx，基线 8c842d9（P31 交付后） |
| 状态 | 实施状态：已合并（本地提交）；验证状态：已通过（API 层 26/26 + 回显契约对照实验 3 项 + 浏览器整页全场景，2026-09-20）；阻塞：无（出口推送阻塞为外部环境，见 RUN_STATE） |
| 依据 | TASKS.md §6 P32；规格第 5/10/11/13/15 章；旧路由 `.umirc.ts` `/access-management/role-management` → `./AccessManagement/RoleManagement`；旧 SPEC_permission_modal_tri_state.md（三态勾选 D1–D12）/SPEC_menu_permission.md/SPEC_button_permission.md（B15 权限时效=下次登录生效）；OpenAPI「角色管理」6 operation + 「权限资源管理」getPermissions |

## 来源 → 目标操作映射

| 旧实现 | 新实现 | method/path | 权限 | 状态与证据 |
| --- | --- | --- | --- | --- |
| 分页表格 antd Table | ApexTableReact request 模式（服务端分页/内建取消/错误重试） | GET /fms/v1/auth/role/pageRoles?pageNo=&pageSize=&query=（query 平铺，旧 get(url,data) 同形态；OpenAPI param 占位对象，P31 pageUsers 同款先例） | auth:role:view（守卫；**root 专属码 ROOT_ONLY_CODES**，T00 routeAccess 同语义拦截） | 已联验：GET 平铺抓包、total/records/size/current 形态、query 筛选命中与空态、越界由内建分页消化；G09 不开放排序（旧页面无排序） |
| 关键字搜索（重置第一页） | Input.Search 360px enterButton allowClear + Apex resetPageIndex；trim 裁剪 | 同上 | — | 已联验：URL 编码 query 参数抓包、未命中空态「暂无数据 共 0 条」、清空恢复无参数重查 |
| 状态列（ENABLED→启用 else 禁用） | 纯文本映射；**纪律收敛差异登记**：旧版把非 ENABLED 一律渲染「禁用」，重写后按 AGENTS 第 3 节 ENABLED/DISABLED 映射文案、未知枚举原值呈现、缺失留白 | — | — | 已联验：ENABLED→启用/DISABLED→禁用渲染；空态环境（total=0 起步）留白路径天然实证 |
| 新增/编辑共用弹窗（RoleModal） | RoleEditModal：单弹窗双模式（modifyRow null=新增）；三字段校验等价（编码/名称必填 maxLength 64 showCount+状态必选）；打开按模式回填/清空（旧 useEffect 依赖 open 教训注释等价保留——仅依赖 modifyRow 在「编辑→取消→再编辑同行」引用不变时跳过回填）；关闭销毁草稿（destroyOnHidden=旧 destroyOnClose 同语义）；表单参数按 AGENTS 3 统一 labelCol 6/wrapper 18（旧 5/17 为统一前形态，行为等价） | POST /auth/role/addRole body {code,name,state}；POST /auth/role/updateRole body {id,code,name,state} | auth:role:add / auth:role:update（弹窗入口控权，弹窗内无需重复控权=旧 §7.7） | 已联验：空提交必填三条齐出零请求；addRole body 抓包 `{code:"P32_UI_R",name,state:"ENABLED"}`+落库复核；updateRole body `{...,id:4}` 抓包+改名实时更新列表；三字段回填实证（含状态下拉选中值） |
| 删除 Popconfirm | confirmCommand 破坏性确认升级（P04/P31 B3 先例同款登记）：标题/对象（name 或 code 回退）/影响（永久删除+关联用户失去权限）/danger 红键/提交≠完成附注五要素 | POST /auth/role/delete?id=（**POST + query 参数 id，body 空对象**——旧实现 post(url?id=) 同形态，与 POST body 族不同） | auth:role:delete | 已联验：五要素齐备、取消零请求、确认 body `{}`+query id=4 抓包、toast+列表回空态；**前端不按行收敛内置角色**（旧实现同边界，内置角色保护由后端业务码拒绝如实反馈） |
| 分配权限弹窗（PermissionModal 三态持久半选） | PermissionAssignModal：SPEC D1–D12 完整等价迁移——唯一数据源 authorizedSet(S)（父与子 id 混存，key String 化 G10 守卫）；deriveChecked 派生 checkStrictly 受控值 {checked,halfChecked}（父全选=自身+全部后代∈S/半选=自身∈S 后代未全/空半选自然呈现）；toggleNode 三态循环（非全选→全选=子树+祖先入 S 向上联动 D4；全选→未选=子树移出祖先保留粘性 D6）；onCheck/onSelect 同一 toggleNode（D10 点标题等同 checkbox）；全选/取消全选按钮（D9 全树灌入/清空） | GET /fms/v1/auth/permission/getPermissions（不带 roleId=全部资源树渲染；带 roleId=该角色已分配树回显）双查并行+独立 controller 防乱序（P31 RoleAssignModal 同款收敛） | auth:role:assign-permission | 已联验（SPEC §10 验收用例逐条）：树渲染 9 顶层 144 节点；用例 1 点父级→父+5 子全实勾（规则①）；用例 2 取消 1 子→父半选（规则②）；**用例 4 全部子级取消→父级保持半选（规则③持久半选核心）**；用例 6 空半选点父级→全选；用例 10 全选点父级→未选；用例 11/12 全选 48/48+取消全选 0/48（按钮文案随 isAllChecked 翻转） |
| 提交 assignPermissions | 提交=「分配权限成功」toast+关闭；下次打开重新双查即「保存后重新读取」；**不伪造即时权限刷新**（旧 SPEC B15：权限时效=下次登录生效） | POST /auth/role/assignPermissions body {roleId,permissionIds}（S 直接展开数值化；整组提交=整体替换语义） | 同上 | 已联验：完整子树提交 body `{roleId:4,permissionIds:[1,2,3,4,5,6]}`（D8+D4 祖先不变量实证）；**空半选提交 body `{roleId:4,permissionIds:[1]}`（D1 独立授权：仅父级 id 上送）**；重开回显全选/空半选两形态均一致 |

## 回显契约对照实验（API 层专项，2026-09-20）

后端 getPermissions({roleId}) 按树结构组装回显，授权集合须含祖先节点（与前端 toggleNode D4 不变量「后代∈S⟹祖先∈S」天然吻合，正常交互恒满足）：

| 实验 | 提交集合 | 回显 |
| --- | --- | --- |
| A 完整子树 | [1,2,3,4,5,6] | 顶层 1 全树 6（父+5 子全选形态） |
| B 仅父级（空半选语义） | [7] | 顶层 7 单节点 childPermissions=[] ——**SPEC R3/R5 后端契约实证：被独立授权的父节点原样返回，空半选可回显** |
| C 父级+部分子级 | [7,8] | 顶层 7 全树 2（半选形态） |
| 反例（脚本构造） | [2,3,4]（纯叶子无祖先） | 空——**前端交互不可能产生的非法形态**（三态算法保证祖先恒在集合），首轮脚本 FAIL 即此，修正口径后 26/26 |

## 文件归属

- 新增：`src/services/access-role/access-role.service.ts` + `.types.ts`（owner=P32；六 operation：pageRoles/addRole/updateRole/deleteRole/getPermissions/assignPermissions；getRoles 不重建——本页无角色选项场景，P31 owner 关系不变）；`src/features/access-role/components/RoleEditModal.tsx`、`PermissionAssignModal.tsx`；`src/pages/access-management/RoleManagement/RoleManagement.tsx` + `.module.css`（整页重写替换占位）；`src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/access-role.ts` 四语言分片
- 修改：`src/router/definitions.tsx`（解除 pending + i18nNamespaces ['access-role']；公共文件，影响=单一消费者本页）；`src/i18n/i18n.ts`（四表各 +1 行注册）；`src/i18n/locales/en-US/system.ts`（role 死键 19 个清理：角色族 14 键+状态筛选/创建时间/状态已更新/详情，menu 消费的 状态/启用/停用/排序+全部菜单键保留归 P43；文件头注释同步收敛）
- 删除（专项验收「整合 src/pages/system/role」）：模板遗留链 `src/pages/system/role/`、`src/services/system/role/`、`src/features/system/role/`、`src/types/system/role/` 全链（对接虚构 REST `/roles` 端点，与 P31 删除 `/users` 同款死代码；消费者核查=仅链内互引，无外部消费者；未挂路由；`src/types/system/menu/` 被 menu 链消费保留归 P43）

## 共享契约

- `services/access-role` 第 5 节登记（owner=P32）；与 access-user 的 AuthRoleDto 同源 schema 但类型独立映射（用户侧=选项视角/本页=管理主体视角，避免跨服务耦合）
- 权限码：auth:role:view/add/update/assign-permission/delete 五码登录响应下发实证（root 会话 155 码含全部）；view 属 ROOT_ONLY_CODES（root 专属），T00 routeAccess 已消费
- 列偏好 tableId：`access-role:main`（行 ID=stringFieldRowId('id')，int64 雪花守卫）；代码默认钉位：操作列 end（旧 fixed right 等价；用户偏好覆盖）
- 权限树资源（getPermissions 全量 9 顶层 144 节点）owner=P32 本页消费；P43 无权限树场景

## 接口缺口 / 契约发现

- **回显树组装要求祖先在集合**（对照实验反例）：纯叶子集合回显空树——非产品缺陷（前端不变量保证），登记为后端契约形态认知
- **空半选回显契约确认成立**（SPEC R3 未决项关闭）：后端对仅父级 id 的授权返回 childPermissions=[] 的父节点，D7 空半选回显可用
- 视觉缺陷修复（联验中发现并修复）：时间列 Apex 默认 150 宽截断秒级时间串（AGENTS 1「时间列宽 ≥160 起步」），改 170 后完整显示
- 纪律收敛差异：旧状态列非 ENABLED 一律显示「禁用」→ 新实现未知枚举原值呈现（AGENTS 3 强制），缺失留白
- 本环境角色列表空态起步（total=0）：增改删与权限分配全链路以自建记录验证（创建→授权→回收→删除自清理），终态审计 total=0 零残留

## 国际化

- access-role 四语言分片：en 旧真译逐条沿用（列头/按钮/校验/反馈/三态全选词 Select All/Unselect All，公共词与 access-user 同源：Enabled/Disabled/Created/Updated/Confirm）；失败反馈统一「……失败：{{msg}}」插值（旧版裸拼接语义等价）；「删除影响」confirmCommand 升级文案按旧真译风格补译（P31 同款）
- 繁日韩 B1 基线真实译文：角色=角色/ロール/역할、分配權限/権限割り当て/권한 할당、啟用/停用·有効/無効·사용/중지，与 menu/access-user 对齐
- 五语言实测：zh-CN/zh-TW/en-US/ja-JP/ko-KR 逐语言 html lang + placeholder + 新增按钮 + 列头全绿（本轮语言菜单按语言名称匹配修正脚本后逐语言通过）

## 验证

- 门禁四项全绿（交付后复跑，含时间列宽修复）：pnpm lint（437 文件 0 警告 0 错误）/ typecheck / check:structure（435 源文件）/ build（861ms 成功；chunk 警告为既有 apex-table-react 体积）
- API 层 26/26（脚本验证后删除）：登录/权限五码/pageRoles GET 平铺形态+分页字段+id 安全/query 筛选命中与空态/getPermissions 全量树 9 顶层 144 节点/addRole 受理+落库复核/updateRole name+state 双字段落库复核/assignPermissions 受理+已分配树回显含祖先/空数组整体替换受理+回收后回显空/deleteRole 自清理终态 total 还原/无令牌三通道（pageRoles/getPermissions/assignPermissions）1000000
- 回显契约对照实验 3+1 项（见上表）
- 浏览器整页（IAB rAF+RO 永续 fire 垫片）：未登录直访回跳带 redirect → 登录落点=redirect 目标（页签「角色管理」）/整页渲染空态起步/新增必填拦截零请求→提交 1 行/编辑回填三字段+改名提交 body 抓包/分配权限三态九用例（含规则③持久半选）+双查回显+提交 body 两形态+重开回显两形态/搜索 URL 编码 query+空态+清空恢复/删除 confirmCommand 五要素+取消零请求+确认终态回空态/列偏好四步（隐藏状态列-落盘 access-role:main 键-reload 持久-恢复默认出厂态）/五语言逐语言/双主题双宽度四组截图 `.run-lock/shots-p32/`（暗色经 settings 直写二次序列化+viewport 切换重合成 AGENTS ⑥⑪⑱）
- 真实登出：logout 后回 /login、auth 持久化 token:null；演示角色页面删除自清理、终态审计 total=0 还原；dev server 已停止；验证脚本已删除
- 环境伪象登记（非产品缺陷，AGENTS 已有沉淀）：①IAB 冻结动画 Modal/confirm 关闭 DOM 残留（React 状态路径正确）——confirmCommand 确定一律**最后实例定位**（AGENTS ⑦，本轮再次实证：querySelector 首实例误点致删除请求未发出，最后实例重放成功）；②语言菜单项按语言名称（简体中文/English/日本語/한국어）而非 lang 代码匹配；③暗色截图合成层旧帧全黑伪象经 viewport 切换重合成解决（⑱）

## 未完成 / 后续

- P31 RoleAssignModal 勾选分配回归：本环境已具备角色数据创建路径，P31 轮登记的「assignRoles 勾选分配目标角色回归待 P32 联验环境」可随 V01 或后续轮回归（本页已实证角色 CRUD 全链路，用户侧勾选分配的前端受控交互+接口写链路 P31 各自实证）
- 权限树键盘可访问性（旧 SPEC §13 后续项）：antd Tree Space 切换在 checkStrictly 下的三态循环未单独验证（鼠标全链路已验，键盘为 antd 原生能力），登记不阻塞
- P43 公共入口清理时复核 profile 对角色选项的消费（getRoles 归属 P31 服务不变）
