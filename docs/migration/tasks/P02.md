# P02 软件授权 · 交接记录

> 当前状态（2026-09-21 audit 第二次复核轮）：实现保留；复核通过为 verification=partial、gate=ready。A14 激活副作用与 A04/I06（同 T00/P01 口径）deferred，不勾选 TASKS。最新验收记录：`docs/migration/evidence/P02/run-9fc8d28e-f5a1-4c48-8d7f-cbb16250663e/acceptance.json`（49 项矩阵+24 文件哈希重绑 26c94cc）；上轮记录：`docs/migration/evidence/P02/audit-20260920-p02-408144b/acceptance.json`（408144b）。
**批次/前置**：B1 / P01（已放行）
**入口/目标**：`/authorize-ingress` → `src/pages/authorize-ingress/AuthorizeIngress/AuthorizeIngress.tsx`
**私有模块/语言分片**：`license-activation`（业务命名，覆盖 P02+P29 共用激活业务；i18n-map 冻结表已按此登记）
**旧来源**：`C:\code\dd\src\pages\AuthorizeIngress\index.tsx`（audit 递归复核确认仅 index.tsx+index.less 两文件，无其他可达子组件）
**历史交付**：2026-09-18 定时轮交付；旧「已勾选」按 2026-09-20 复核纪律不再作为验收凭证

## 交付内容（audit 复核与交接一致）

1. **服务层**（`src/services/license-activation/license.service.ts` + `.types.ts`，P29 共用）：`fetchHardwareInfo()`（GET `/auth/license/getHardwareInfo`，ResultString，无需认证实证）；`activateSoftware()`（POST `/auth/license/softwareActivation`，写不自动重试，业务 code=200 判定）；`fetchLicense()` 为 P29 增补。
2. **可复用激活业务组件**（P29 已消费并联验）：`ActivationForm`（输入+提交+loading 防连点+失败保留输入+code=200 唯一成功判定，`onActivated` 归调用方）、`useHardwareInfo`（三态+卸载取消+防乱序+失败不以空串冒充）、`copyText`（安全上下文 Clipboard API + 内网 http execCommand 兜底=旧系统已证实行为）。
3. **页面**：旧深蓝科技风等价移植（独立页固定深色与旧系统一致，静态审查证实不读外壳 data-theme；含 prefers-reduced-motion）；硬件码展示+复制；「SOFTWARE LICENSE ACTIVATION」为固定装饰文案（旧系统同款）。
4. **激活成功导航**：`activationConfirmed`（authSlice）更新会话 activated → `resolveLandingPath` 按真实会话与权限导航；不复刻旧硬编码跳 `/over-look`（规格优先级，行为差异已登记）。
5. **1001000 全局引导接线**：request.ts 单飞（`activationGuidanceHandled` 新令牌复位）→ 一次性提示 → `apex:activation-required` 事件 → `ActivationRedirectListener`（App 根常驻）SPA 导航；授权页自身路径抑制防循环。事件常量唯一定义 request.constants.ts。
6. **路由**：standaloneMeta 签名扩展 i18nNamespaces；页面非公开、受认证守卫（规格 5.6 授权入口≠免鉴权）。
7. **五语言**：license-activation 四语言分片 12 键（旧 10 键全保留含 en 真译+「激活成功/重试」新增登记；旧系统该页繁/日/韩为简中回退，B1 基线补真实译文非译文丢失）；common 四语言各增 1 键（1001000 提示）。

## 2026-09-21 audit 第二次复核证据（run run-9fc8d28e，代码基线 26c94cc）

基线推进背景：上轮复核在 408144b，其后 P21/P33/P34/P35/P36/P37/P40/P43 八个代码交付推进基线至 26c94cc；本轮从 P02 视角重新验证。

- **受检文件**：24 个中 22 个哈希与上轮完全一致（license-activation 全域 13 文件+request/authSlice/routeAccess/App/common×4 零变更）；2 个变更公共文件 definitions.tsx/i18n.ts 逐 hunk 审查均为 T00 audit 已确认的授权扩展（dashboard pending 解除+dashboard-realtime→/dashboard 重定向、report-*/server-resource/profile 注册、system 死链移除），authorize-ingress 节点与 license-activation 注册零改动（索引：本轮 `code-files.json`）。
- **静态门禁**：复用 T00 audit @ 392e831 全量复绿结果（lint 543 文件 0/0+structure 529+build 1.24s；git diff 392e831..26c94cc 证实其后仅 docs 提交）。
- **真实只读**（`p02-audit2-readonly-result.json`+`p02-audit2-probe-result.json`）：getHardwareInfo 无令牌经 dev 代理+直连对照一致 2/2（72 字符，值不入档仅记长度+哈希前 8 位）；**10.11.2.67 本轮恢复可达**（上轮连接超时），认证交换 3/3（MD5 登录 activated=true/155 权限码→带令牌 getHardwareInfo 72 字符→登出清理）——该 OpenAPI 默认联调目标实测为已激活环境，未激活环境依旧不可得，A14 与 activated=false 落点两项 deferred 依据维持、表述更新（「网络不可达」→「可达但已激活」）。
- **浏览器针对性复测 8 项**（`browser-ui-result.json`，IAB 1280×720）：未登录守卫拦截（→/login?redirect=%2Fauthorize-ingress）；登录合法回跳授权页+硬件码真实渲染（performance 证实页面服务 9ms，与直连一致）；复制成功提示路径复现（剪贴板读回受合成事件无 transient activation 限制不可核验——环境限制，组件代码零变更且上轮已实测读回一致）；「返回登录」语义核验（replace 保留会话，源码注释证实非登出）；**落点规则随 P34 交付由 /order-record 兑现为 /dashboard**（已登录直访+无 redirect en-US 登录两路径实测，D29）；顶栏登出清理（persist 解析核验 token/user/permissions 全清+语言保留）；en-US 授权页全元素渲染+html lang 同步+截图。
- **边界登记**：IAB 输入管线冻结复现（与 P01 二次复核同现象），提交类点击经 evaluate 派发 pointer 序列垫片，不能证明真实指针交互（归 V01）；1366/1920 双视口与繁日韩全页由上轮覆盖（布局/分片代码零变更不重复）。

## 2026-09-20 audit 复核证据（run audit-20260920-p02-408144b，代码基线 408144b，src 与 a9a3cc0 一致）

- **静态门禁**：复用同日 T00 audit 在 a9a3cc0 的 pnpm check/build 复绿结果（git diff a9a3cc0..HEAD 证实 src/package.json/pnpm-lock/vite.config 零变化）。
- **真实只读 2/2**（`run-p02-audit-readonly.mjs`）：getHardwareInfo 无令牌经 dev 代理 code=200（72 字符真实硬件码，值不入档）；直连后端对照一致。10.11.2.67:8888 激活态探测网络不可达（连接超时）——未激活环境依旧不可得，探测结果即 defer 依据。
- **浏览器 UI**（`browser-ui-result.json`，IAB Chromium，1366/1920）：未登录直访守卫拦截保留站内回跳参数；登录后合法回跳授权页（redirect 参数放行）；无 redirect 落点=/order-record（首个可用业务页）；en/繁/日/韩/简中五语言授权页全页证据（标题/副标题/硬件码/复制/占位符/按钮+html lang+持久化逐语言核对，超出上轮 en/繁全页+日韩标题范围）；复制剪贴板读回一致+Copied 提示；激活失败分支客户端注入实测（错误文案+输入保留+按钮恢复，performance 资源条目证实零网络接触——未对共享环境提交写尝试）；授权页 reload 会话恢复；登出清理（persist auth 全清、语言偏好保留）；1920 首帧旧合成帧伪影经 DOM 核实（卡片 480×607 居中）+重绘重截排除。截图仅本地留存（`screenshots/`，git 排除）。
- **复核结论**：零新缺陷。硬件码失败态「重试」按钮判定为 StateBlock offline+onRetry 既定模式（ReadOnlyMap/DispatchHub 同款：无轮询独立数据块唯一自愈路径），不属「手动刷新」禁令；激活码 maxLength=2000/trim 为输入卫生增强（已登记，专用环境发现更长合法码再放宽）。

### 历史联验对照（2026-09-18，仅作对照不作新凭证）

当时实测：守卫回跳、硬件码渲染与复制、失败分支（同注入技术）、1001000 接线（生产构建事件→导航、授权页抑制）、五语言（en/繁全页+日韩标题）、1366 截图、静态门禁全绿。原始截图未持久化，本轮新鲜证据已覆盖同等及更大范围。

## 登记的限制与 deferred（归 V01）

1. **A14 激活成功链路（写副作用）**：专用未激活环境或现场执行——提交有效激活码 → 成功提示+activated 更新+落点导航（P29 复用同组件随 getLicense 到期语义一并复核）。当前可用后端均已激活，不对共享环境提交写尝试（含无效激活码写尝试）。
2. **activated=false → 登录落点授权页 / 1001000 真实业务触发**：需未激活后端环境；两轮探测 10.11.2.67——上轮网络不可达，2026-09-21 复核轮恢复可达但认证交换实测 activated=true（已激活联调后端）；G17 有真实业务码观察；机制两端各自实证（落点分支 T00.4、事件引导生产构建接线；resolveLandingPath 落点已随 P34 交付实测 /dashboard），归 V01 未激活环境复核。
3. **A04 长会话 token 过期/多窗口退出**：机制级实证（本轮补授权页 reload 恢复+登出清理），真实场景与 T00/P01 同口径归 V01。
4. **I06 真实资源加载失败不白屏**：IAB 无法构造网络故障；四语言分片完整无缺失回退，同口径归 V01。
5. **授权失效（到期）业务码**：仅实证 1001000=未激活；到期如以其他码表达走通用业务错误分支，随 P29 getLicense 联调复核。

## DoD 适用性

- 不适用：表格（DoD4/5）、文件传输（DoD9）、地图关联（DoD10）、统计（DoD14）。
- 适用已满足：真实服务协议（2）、权限状态区分（3，四态分流+守卫实测）、取消与失败（6，三态+防乱序）、草稿（7，失败留稿+刷新恢复）、写入防重（8，loading+不自动重放）、五语言（11–12，全页实测）、外观终端（13，1366/1920 双视口截图）、验证证据（15，静态复用+新鲜只读/UI）、交接收尾（16，零 mock 残留）。
- 追踪项：A03–A06、A10–A11、A13、A20–A23 passed；A04/A14/I06 deferred（见上）。

## 对后续任务的影响

- **P29 软件信息**：已复用 `services/license-activation` + `ActivationForm`（P29 轮带令牌联验通过 2026-09-20）；getLicense/到期语义归 P29。
- **公共文件改动记录**：request.ts/request.constants.ts（1001000 收敛+事件常量）、authSlice（activationConfirmed）、definitions.tsx（standaloneMeta 签名+本页 meta）、i18n.ts（4 表注册）、common×4（新增 1 键）、App.tsx（挂载监听）。全部经 typecheck/结构检查/全量构建证实无消费者破坏；T00 audit 同基线已复核公共文件，此后零变化。
