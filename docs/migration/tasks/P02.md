# P02 软件授权 · 交接记录

> 当前状态（2026-09-21 audit 第三次复核轮）：实现保留；复核通过为 verification=partial、gate=ready。A14 激活副作用与 A04/I06（同 T00/P01 口径）deferred，不勾选 TASKS。最新验收记录：`docs/migration/evidence/P02/run-c7d53b59-d130-42e0-81df-7766f8c5b242/acceptance.json`（49 项矩阵+24 文件哈希重绑 59b5434）。
**批次/前置**：B1 / P01（已放行）
**入口/目标**：`/authorize-ingress` → `src/pages/authorize-ingress/AuthorizeIngress/AuthorizeIngress.tsx`
**私有模块/语言分片**：`license-activation`（业务命名，覆盖 P02+P29 共用激活业务；i18n-map 冻结表已按此登记）
**旧来源**：`C:\code\dd\src\pages\AuthorizeIngress\index.tsx`（audit 递归复核确认仅 index.tsx+index.less 两文件，无其他可达子组件）
**历史交付**：2026-09-18 定时轮交付；旧「已勾选」按 2026-09-20 复核纪律不再作为验收凭证

## 交付内容（audit 复核与交接一致）

1. **服务层**（`src/services/license-activation/license.service.ts` + `.types.ts`，P29 共用）：`fetchHardwareInfo()`（GET `/auth/license/getHardwareInfo`，ResultString，无需认证实证）；`activateSoftware()`（POST `/auth/license/softwareActivation`，写不自动重试，业务 code=200 判定）；`fetchLicense()` 为 P29 增补。
2. **可复用激活业务组件**（P29 已消费并联验）：`ActivationForm`（输入+提交+loading 防连点+失败保留输入+code=200 唯一成功判定，`onActivated` 归调用方）、`useHardwareInfo`（三态+卸载取消+防乱序+失败不以空串冒充）、`copyText`（安全上下文 Clipboard API + 内网 http execCommand 兜底=旧系统已证实行为）。
3. **页面**：旧深蓝科技风等价移植（独立页固定深色与旧系统一致，静态审查证实不读外壳 data-theme；含 prefers-reduced-motion）；硬件码展示+复制；「SOFTWARE LICENSE ACTIVATION」为固定装饰文案（旧系统同款）。
4. **激活成功导航**：`activationConfirmed`（authSlice）更新会话 activated → `resolveLandingPath` 按真实会话与权限导航；不复刻旧硬编码跳 `/over-look`（规格优先级，行为差异已登记）。
5. **1001000 全局引导接线**：request.ts 单飞（`activationGuidanceHandled` 新令牌复位）→ 一次性提示 → `apex:activation-required` 事件 → `ActivationRedirectListener`（App 根常驻）SPA 导航；授权页自身路径抑制防循环。事件常量唯一定义 request.constants.ts。
6. **路由**：standaloneMeta 签名扩展 i18nNamespaces；页面非公开、受认证守卫（规格 5.6 授权入口≠免鉴权）。
7. **五语言**：license-activation 四语言分片 12 键（旧 10 键全保留含 en 真译+「激活成功/重试」新增登记；旧系统该页繁/日/韩为简中回退，B1 基线补真实译文非译文丢失）；common 四语言各增 1 键（1001000 提示）。

## 2026-09-21 audit 第三次复核证据（run run-c7d53b59，代码基线 59b5434）

基线推进背景：上轮复核在 26c94cc，其后唯一代码变更为 57ce7ba（P41-audit-1 修复 routeAccess.ts +5 行），daeb4be/a0a1071/59b5434 均为 docs 提交；本轮从 P02 视角重新验证。

- **受检文件**：24 个中 23 个哈希与上轮完全一致（license-activation 全域 13 文件+request/authSlice/App/common×4/definitions.tsx/i18n.ts 零变更）；唯一变更 routeAccess.ts 逐 hunk 从 P02 视角审查成立——`resolveSafeRedirectPath` 新增显式拒绝仅精确匹配 `/no-permission`（ROUTE_PATHS 常量），授权页 perm 未声明经 `hasMenuAccess` 登录即可达、回跳链路（`findLeafByPath`→`isLeafAvailable`）零改动、`resolveLandingPath` activated=false→授权页落点分支（规格 5.6/D29）未触及（索引：本轮 `code-files.json`）。
- **静态门禁**：复用 P41 audit @ 57ce7ba 全量复绿结果（lint 547 文件 0/0+structure 529+build 1.39s；git diff 57ce7ba..59b5434 证实其后仅 docs 提交）。
- **真实只读**（`p02-audit3-readonly-result.json`）：getHardwareInfo 无令牌经 dev 代理+直连对照一致 2/2（72 字符，sha256_8=e5c6aacc，值不入档）；10.11.2.67 连续第二轮可达，认证交换 3/3（MD5 登录 activated=true/155 权限码→带令牌 getHardwareInfo→登出清理）——未激活环境依旧不可得，A14 与 activated=false 落点两项 deferred 依据维持。
- **浏览器针对性复测 8 项**（`browser-ui-result.json`，IAB 1280×720）：未登录守卫拦截（→/login?redirect=%2Fauthorize-ingress）；**routeAccess 修复后授权页回跳复现**（redirect=%2Fauthorize-ingress 登录落 /authorize-ingress，修复未破坏合法回跳）；硬件码真实渲染且与只读脚本 SHA-256 一致；复制成功提示按组件设计不出现（合成环境 Clipboard API NotAllowedError+execCommand false 双路失败，诊断垫片实测；组件零变更且上轮已实测读回一致——环境限制维持）；授权页 reload 会话恢复+「返回登录」replace 保留会话；五语言菜单齐全+en-US 切换全链路（html lang=en-US）+授权页全元素 en 渲染（截图本地留存）；顶栏登出清理 persist 二次解析核验（token/user=null、permissions/permissionsTree=[]、语言偏好保留）；落点回归（无参数 en 登录落 /dashboard）。
- **方法学修正登记**：persist:apex-admin:auth 字段为双层 JSON 序列化，token 清空判定须二次 parse——直接 `Boolean(auth.token)` 会把字符串 "null" 误判为存在（假阳性），本轮已修正并以二次解析结论为准。
- **边界登记**：IAB 输入管线冻结复现（与前两轮同现象），提交类点击经 evaluate 派发 pointer 序列垫片，不能证明真实指针交互（归 V01）；1366/1920 双视口与繁日韩全页由第二轮覆盖（布局/分片代码零变更不重复）。

## 历史复核结论（压缩存档）

- **第二轮（run-9fc8d28e @ 26c94cc，2026-09-21）**：基线 408144b→26c94cc（八个代码交付）后 24 受检文件 22 个零变更、definitions.tsx/i18n.ts 两变更公共文件逐 hunk 确认授权页节点与分片注册零改动；getHardwareInfo 代理+直连一致、10.11.2.67 恢复可达实测 activated=true（上轮网络不可达表述更新）；浏览器 8 项（守卫/回跳/硬件码 9ms 渲染/返回登录语义/落点 /dashboard 两路径（P34 兑现 D29）/登出清理/en-US 渲染）。完整记录：`docs/migration/evidence/P02/run-9fc8d28e-f5a1-4c48-8d7f-cbb16250663e/acceptance.json`。
- **第一轮（audit-20260920-p02-408144b @ 408144b）**：静态复用 T00 audit 复绿；getHardwareInfo 双通道一致；浏览器五语言全页+激活失败注入（零网络接触）+双视口；硬件码失败态「重试」按钮判定为 StateBlock offline+onRetry 既定模式非手动刷新违纪；激活码 maxLength=2000/trim 输入卫生增强登记。完整记录：`docs/migration/evidence/P02/audit-20260920-p02-408144b/acceptance.json`。
- 三轮零新缺陷；上两轮 1366/1920 双视口、繁/日/韩全页、激活失败注入证据因受检文件零变更继续有效。

## 登记的限制与 deferred（归 V01）

1. **A14 激活成功链路（写副作用）**：专用未激活环境或现场执行——提交有效激活码 → 成功提示+activated 更新+落点导航（P29 复用同组件随 getLicense 到期语义一并复核）。当前可用后端均已激活（10.11.2.67 连续两轮实测 activated=true），不对共享环境提交写尝试（含无效激活码写尝试）。
2. **activated=false → 登录落点授权页 / 1001000 真实业务触发**：需未激活后端环境；机制两端各自实证（落点分支 T00.4、事件引导生产构建接线；resolveLandingPath 落点已随 P34 交付实测 /dashboard），归 V01 未激活环境复核。
3. **A04 长会话 token 过期/多窗口退出**：机制级实证（授权页 reload 恢复+登出清理 persist 解析核验），真实场景与 T00/P01 同口径归 V01。
4. **I06 真实资源加载失败不白屏**：IAB 无法构造网络故障；四语言分片完整无缺失回退，同口径归 V01。
5. **授权失效（到期）业务码**：仅实证 1001000=未激活；到期如以其他码表达走通用业务错误分支，随 P29 getLicense 联调复核。
6. **真实指针交互**：IAB 输入管线冻结（三轮同现象），登录/复制/语言切换/返回登录/登出经合成 pointer 序列垫片完成，归 V01 自然浏览器复核。

## DoD 适用性

- 不适用：表格（DoD4/5）、文件传输（DoD9）、地图关联（DoD10）、统计（DoD14）。
- 适用已满足：真实服务协议（2）、权限状态区分（3，四态分流+守卫实测）、取消与失败（6，三态+防乱序）、草稿（7，失败留稿+刷新恢复）、写入防重（8，loading+不自动重放）、五语言（11–12，全页实测）、外观终端（13，1366/1920 双视口截图）、验证证据（15，静态复用+新鲜只读/UI）、交接收尾（16，零 mock 残留）。
- 追踪项：A03–A06、A10–A11、A13、A20–A23 passed；A04/A14/I06 deferred（见上）。

## 对后续任务的影响

- **P29 软件信息**：已复用 `services/license-activation` + `ActivationForm`（P29 轮带令牌联验通过 2026-09-20）；getLicense/到期语义归 P29。
- **公共文件改动记录**：request.ts/request.constants.ts（1001000 收敛+事件常量）、authSlice（activationConfirmed）、definitions.tsx（standaloneMeta 签名+本页 meta）、i18n.ts（4 表注册）、common×4（新增 1 键）、App.tsx（挂载监听）。全部经 typecheck/结构检查/全量构建证实无消费者破坏；此后唯一公共文件变更 routeAccess.ts（57ce7ba P41-audit-1 修复）经本轮逐 hunk 审查不影响授权页。
