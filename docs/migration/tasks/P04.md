# P04 车辆分组 · 任务交接记录

> 当前状态（2026-09-20 校正）：实现已交付；最终验收与实施放行均待证据复核。下文保留原轮次实测及未验收项，历史“已通过/已勾选”不是新的最终验收结论；以 RUN_STATE 独立状态为准。
| 字段 | 内容 |
| --- | --- |
| 任务 / 负责人 / 基线 | P04 车辆分组 / run-20260918-p04-220542-20870 / 分支 apple-rxx，起点提交 9453d65（P39 补验对账后），完成于 2026-09-18 |
| 实施状态 | 已合并（整页重写交付，pending 解除） |
| 验证状态 | 只读与写链路已通过（带令牌真实联验）；一处后端缺陷登记缺口（updateVehicleGroup groupName 不落库，非前端问题）；无权限按钮隐藏以机制复用登记（root 账号无普通用户环境） |

## 依据

- 规格：调度系统迁移规格第 6 章车辆分组卡、DoD 1–16、G04/G09/G10。
- 旧来源：`C:\code\dd\src\pages\VehicleDeploy\VehicleGroup`（index.tsx + GroupModal/index.tsx；`.umirc.ts` access=PERM.VEHICLE_GROUP_VIEW）。
- API：`C:\yangwenhua\ApexTableReact\default_OpenAPI.json` SHA-256 `A82E…49C7C` 与规格一致（开工已重核）；涉及 operation：pageVehicleGroups / addVehicleGroup / updateVehicleGroup / deleteVehicleGroup / getVehicleGroups（P03 代建共享选项）/ getSimpleVehicles（共享车辆选项）。

## 操作映射（来源 → 目标 → method/path → 权限 → 状态）

| 旧实现 | 新实现 | method/path | operationId | 权限 | 状态与证据 |
| --- | --- | --- | --- | --- | --- |
| 列表分页 + 搜索（名称/标识） | `pages/vehicle-deploy/VehicleGroup` ApexTableReact request 模式（服务端分页/内建取消/错误重试；G09 不开放排序） | GET /fms/v1/dispatcher/vehicleGroup/pageVehicleGroups（pageNo/pageSize/query 平铺，G04 口径） | pageVehicleGroups | vehicle-group:view（守卫） | 已验证（带令牌：真实空集合→新增后 1 条；筛选命中/0 条；XHR 抓包 URL 平铺形态实证） |
| 展开行组内车辆 Tag | Apex `expandable.expandedRowRender` + `rowExpandable`（组内无车不可展开；名称缺失退回原 key） | —（行内数据） | — | — | 已验证（展开显示组内车辆 Tag，收起/展开状态正确） |
| GroupModal 新增（名称+Transfer 选车） | `features/vehicle-group/components/GroupFormModal`（草稿保留/清空入口/useTabDirtyGuard；Transfer 复用共享选项 getSimpleVehicles） | POST /fms/v1/dispatcher/vehicleGroup/addVehicleGroup | addVehicleGroup | vehicle-group:add（无权限隐藏） | 已验证（真实新增成功：「添加车辆组成功」+列表刷新+后端落库 name/simpleAGVs 正确；Transfer 56 项真实加载） |
| GroupModal 编辑（预填+全量提交） | GroupFormModal 编辑模式（按行重填，simpleAGVs key 集合回填、缺失回退 agvKeys 契约超集） | POST /fms/v1/dispatcher/vehicleGroup/updateVehicleGroup | updateVehicleGroup | vehicle-group:update（无权限隐藏） | 前端已验证（XHR 抓包请求体正确：groupKey+groupName+vehicleKeys；响应 code=200；提交后自动刷新）；**后端缺陷**：groupName 落库不生效（见缺口节） |
| Popconfirm 删除（「确认删除此项?」） | confirmCommand 破坏性确认（danger，列明对象+影响+「提交≠完成」附注）→ deleteVehicleGroup → 刷新 | POST /fms/v1/dispatcher/vehicleGroup/deleteVehicleGroup | deleteVehicleGroup | vehicle-group:delete（无权限隐藏） | 已验证（取消路径分组保留；确认路径真实删除：「删除车辆分组成功」+列表清空+后端 total=0 自清理实证） |
| Transfer 选项 getSimpleVehicles | 复用 `services/vehicle/vehicle.service` fetchSimpleVehicles（contracts 第 5 节唯一 operation，零新增请求） | GET /fms/v1/dispatcher/vehicle/getSimpleVehicles | getSimpleVehicles | — | 已验证（56 项真实车辆加载；int64 key 字符串无损） |

## 文件归属

| 文件 | 变更 |
| --- | --- |
| `src/services/vehicle/vehicle-group.service.ts` | 扩展：pageVehicleGroups/addVehicleGroup/updateVehicleGroup/deleteVehicleGroup；保留 fetchVehicleGroups（P03/P20 消费不动）。owner 自 P04 起持有 |
| `src/services/vehicle/vehicle-group.service.types.ts` | 扩展：SimpleAGVDto/VehicleGroupRecordDto/VehicleGroupPageParam/VehicleGroupPage/VehicleGroupAddParam/VehicleGroupUpdateParam/VehicleGroupDeleteParam；VehicleGroupDto 选项契约不变 |
| `src/features/vehicle-group/components/GroupFormModal.tsx` | 新建（新增/编辑弹窗） |
| `src/pages/vehicle-deploy/VehicleGroup/VehicleGroup.tsx` | 整页重写（替换占位） |
| `src/pages/vehicle-deploy/VehicleGroup/VehicleGroup.module.css` | 新建（工具栏+表格容器样式） |
| `src/router/definitions.tsx` | vehicle-group 解除 pending，meta.i18nNamespaces=['vehicleGroup'] |
| `src/i18n/i18n.ts` | 四语言懒加载表登记 vehicleGroup |
| `src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/vehicleGroup.ts` | 新建四语言分片（33 key 同构） |

公共文件改动仅 definitions.tsx（本页 meta 行）与 i18n.ts（懒加载登记），均为页面任务常规差异；无其他共享文件变更。

## 共享契约

- 消费：`useColumnPreferences`（tableId `vehicle-group:main`）、`stringFieldRowId('agvGroupKey')`、`toBackendPage`、`useApexLocale`、`confirmCommand`、`usePermission`（PERM_BUTTON.VEHICLE_GROUP_ADD/UPDATE/DELETE）、`useStaticOptions`、`useTabDirtyGuard`、`displayDateTime`、`fetchSimpleVehicles`。
- 新增：无新公共契约；vehicle-group 服务 owner 自 P03 交接至 P04（contracts.md 第 5 节已同步）。
- 下游消费者：fetchVehicleGroups 现有消费者 P03（CreateOrderModal）/未来 P20 不受影响（函数签名与行为未动，typecheck 实证）。

## 接口缺口

| 编号 | 内容 | 状态 | 证据 | 影响 |
| --- | --- | --- | --- | --- |
| P04-G1（新增登记 gaps.md） | 后端 updateVehicleGroup 的 **groupName 更新不落库**：前端请求体正确（XHR 抓包 `{"groupKey":"…","groupName":"P04联验分组B","vehicleKeys":[…]}`）、响应 code=200，但复查数据 agvGroupName 保持旧值，仅 updateTime 更新；vehicleKeys 更新生效（simpleAGVs 随提交变化）。curl 直连复现（UTF-8 文件体，排除编码因素）同症状——纯后端缺陷 | 已确认（后端缺陷，待后端修复） | 2026-09-18 联验 XHR 抓包 + 前后端数据对照 + curl 复现 | 编辑弹窗保存名称后界面恢复旧名（列表刷新如实反映后端）；前端行为正确不放宽。后端修复后需复验编辑名称链路 |
| — | 无权限按钮隐藏（ADD/UPDATE/DELETE）以机制复用登记：与 P03/P05 同一 usePermission 按钮码机制（已验证），联验账号 root 具全部权限无法现场复现无权限形态 | 机制已验证 / 形态待普通用户账号复验（V01） | 代码路径审查 | 无阻塞 |
| — | 失效关联合成条目（组内车辆已删除/不可见时保留原标识+「已不可用」标注+保存原样提交提示）为专项验收实现；真实失效场景需删除车辆才能复现，以代码审查登记 | 代码审查（场景未现场触发） | GroupFormModal transferDataSource/missingCount 逻辑 | 无阻塞 |

## 国际化

- 命名空间 `vehicleGroup`（33 key，四语言同构；zh-CN key 即文案无资源文件）。
- en-US：旧真译全部沿用（列表列/搜索/新增编辑弹窗/穿梭框/操作反馈），删除确认/影响说明/失效标注/清空等为补译。
- zh-TW/ja-JP/ko-KR：旧资源对该页 key 全部为简中复制（无有效译文），按 B1 基线补译（繁体转换/日文真译/韩文真译）；「撤回」韩语取「회수」避免与「取消」重复。
- 插值：`组内 {{count}} 辆车…`（i18next 双花括号）；顶栏/菜单文案属 common/menu 命名空间。
- 路由 meta.i18nNamespaces=['vehicleGroup'] 已声明（切语言预载）。

## 验证

- 门禁（2026-09-18，交付终态）：`pnpm lint` 277 文件 0 警告 0 错误；`pnpm typecheck` 通过；`pnpm check:structure` 275 源文件通过（无 index.tsx/无违规导入方向/无硬编码主机）；`pnpm build` 成功；调试残留 grep 为零。
- 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local [登录凭据已脱敏]，IAB + rAF/RO 双垫片）：
  - 未登录直访守卫回跳 `/login?redirect=%2Fvehicle-deploy%2Fvehicle-group`；登录后正确回落本页；
  - 列表：真实空集合（共 0 条）如实呈现；新增后行渲染正确（名称/标识省略+Tooltip/创建时间秒级 2026-09-18 22:34:16/操作按钮）；
  - 新增：Transfer 56 项真实加载、勾选→添加→已列表 1 项、源列表计数 55、提交成功反馈+自动刷新+后端落库一致；
  - 编辑：预填（标题/名称/右侧已添加/源 55 项）、提交请求体 XHR 实证正确、code=200、自动刷新（groupName 不落库为后端缺陷，见缺口）；
  - 展开行：展开显示组内车辆 Tag、按钮 +/− 与 expanded 状态正确；
  - 删除：确认框五要素（标题/对象含标识/影响说明/提交≠完成附注/danger 红键）、取消路径不删除、确认路径真实删除+列表清空+后端 total=0；
  - 筛选：命中（query=P04联验→1 条）与空结果（不存在词→0 条空态）；
  - 草稿：新增弹窗输入→取消→重开保留（清空入口复位）；
  - 五语言：zh-CN/en-US/zh-TW/ja-JP/ko-KR html lang 逐语言正确 + 特征文案（Add Group/新增分組/グループ追加/그룹 추가）；
  - 双主题双宽度四组截图：亮/暗 × 1366/1920 全部布局完整（工具栏/表格/分页器/Dock 无溢出无遮挡）。
- 联验后清理：测试分组已删除（后端 total=0）；验证页签关闭；真实登出；dev server 停止。

## 未完成 / 后续

1. **P04-G1 后端缺陷**：updateVehicleGroup groupName 不落库——待后端修复后复验编辑名称链路（复验入口：编辑弹窗改名→确定→列表与后端复查）；不阻塞后续任务（P19/P20 等仅消费分组选项，不依赖更新名称）。
2. 无权限按钮隐藏形态待普通用户账号复验（V01 统一回归）。
3. 失效关联合成条目的现场触发验证（需真实删除一辆已被分组的车辆）留 V01。
