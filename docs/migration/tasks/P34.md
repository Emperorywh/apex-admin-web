# P34 合并业务首页 / 实时看板（/dashboard）交接记录

| 字段 | 内容 |
| --- | --- |
| 任务 / 负责人 / 基线 | P34；ZCode 自动轮（sess_8a62c249，run-72990389-0d3f-40a6-958d-a9693ed3ce96）；分支 apple-rxx，基线 d14ee88（P33 交付 HEAD），实现提交见验收记录 codeCommit |
| 状态 | implementation=implemented；verification=partial（deferred 归 V01/跨页口径）；gate=ready |
| 开始 / 更新日期 | 2026-09-20 / 2026-09-20 |

## 依据

- 规格 `docs/SPEC_dispatch_migration_20260917.md` §3.1（首页合并 D29/落点）、§3.3（实时看板行）、§7（图表与交互容器）、§8.2（实时轮询/失败清理）、§11.2（指标口径/空值纪律）、§11.3（时间口径）、§12（零 mock）、§18（五语言）；API 基线 `default_OpenAPI.json` SHA-256 `A82E9B5F...49C7C`（未变化，本轮复核）。
- 旧来源 `C:\code\dd\src\pages\AnalyzeVisual\RealtimeDashboard`（index.tsx 轮询入口 + RealtimeDashboard.tsx 展示 + model/kpiDefinitions + model/types + charts/VehicleStatusPie + charts/TodayTaskTrendLine，全量 6 文件）及可达依赖 `DashboardShared`（HttpDashboardRepository/board 适配与口径、policy、businessTime、formatters、kpiViewModel、enumLabels、KpiCard、ChartPanel）；旧参考 `C:\code\dd\docs\SPEC_dashboard_stats.md` v3.1（页面形态与公式口径）。
- 权限沿用旧 `.umirc.ts` `dashboard-realtime:view`（旧路由 access 原样，目标 `PERM.DASHBOARD_REALTIME_VIEW` 同码，无凭空造码）。

## 操作映射（逐 operation 核对 OpenAPI）

| 旧调用 | 新实现 | method/path | operationId | 权限 | 状态与证据 |
| --- | --- | --- | --- | --- | --- |
| 实时看板聚合（dashboardRepository.fetchRealtime → board days=2） | `getDashboardBoard({days:2})` + `buildRealtimeSnapshot` 纯计算（realtime.ts 单一口径源） | POST `/fms/v1/dispatcher/dashboard/board`（body=DashboardParam，orderTypes 不传=后端默认工作任务，与旧一致） | board | 路由守卫 dashboard-realtime:view（页内无按钮码，与旧一致） | 已验证（真实只读：code=200、hourlyCounts 45 行覆盖今昨两天、五状态和=56=totalVehicleCount 不变量、无令牌 1000000 拒绝；浏览器 KPI 数值随轮询实时增长） |
| 未关闭告警列表（fetchOpenAlertItems → pageSystemAlarmRecords isClosed=false pageSize=100） | `pageOpenSystemAlarmRecords()`（固定第一页 + isClosed=false，records 归一化） | POST `/fms/v1/report/systemAlarmRecord/pageSystemAlarmRecords` | pageSystemAlarmRecords | 同上 | 已验证（真实只读：code=200、0 条记录为真实空结果、全部 isClosed=false 语义、无令牌 1000000 拒绝；浏览器空态「暂无未关闭告警」） |
| 旧路由 `/analyze-visual/dashboard-realtime` | 兼容重定向到 `/dashboard` 同一实例（definitions.tsx redirect + createAliasRedirectLoader 同码守卫） | —（路由层） | — | dashboard-realtime:view | 已验证（健康会话整页访问 → URL 变 /dashboard 且 KPI 渲染） |

查询性质 POST 分类：board 与 pageSystemAlarmRecords 均为纯查询语义（看板聚合读取 + 告警列表分页读取），按 TASKS §1 归入只读查询，可安全轮询重查。

## 范围事实（D24 登记）

- 旧 `RealtimeDashboard.tsx` 中 `RecentTaskList`（最近完成任务表）**已定义但从未渲染**（旧实现注释「本期不对接，恒为空」）——不可达 UI，按 D24 不迁移；`recentCompletedTasks` 字段不进入新数据结构。
- 旧系统告警失败「静默降级为空列表」（SPEC_dashboard_stats §15）不再保留：按 D15/规格 8.2 收敛为独立失败态（StateBlock offline），空列表与查询失败必须可区分（升级登记）。
- 合并首页为 D29 新形态：模板仪表盘（mock）退役，页面布局沿用模板玻璃卡片体系（kpiRow + 12 栅格），业务内容等价旧实时看板（8 KPI + 环形图 + 双折线 + 告警列表）。

## 文件归属

- 重写 `src/services/dashboard/`（service+types，owner=P34）：board / pageSystemAlarmRecords 两 operation，DTO 逐字段对齐 OpenAPI。
- **删除** `src/services/dashboard/dashboard.mock.ts`、`src/types/dashboard/`（模板 DTO）、`src/features/dashboard/` 模板组件树（StatCard/TrendAreaChart/DailyBarChart/StatusDonutChart/RankBars/RecentAlarmList/ChartTooltip/useDashboardOverview/useElementWidth/chartGeometry，共 19 文件，git rm）；消费者清点：仅模板 Dashboard.tsx 自身（已重写），无其他引用（grep 实证）。
- 新建 `src/features/dashboard/`：`realtime.ts`（纯计算：小时键归一化/今日昨日汇总/完成率/平均耗时/平均每小时/利用率除零纪律/五状态快照/趋势桶/告警映射与译文回退链/formatDurationMs）、`kpiViewModel.ts`（格式化与环比语义纯函数）、`hooks/useRealtimeDashboard.ts`（useVisiblePolling 单循环双区域并行、独立失败清空、页面级退避）、`components/`（RealtimeKpiCard 含 KpiHint、VehicleStatusDonut、TodayTrendLine、OpenAlertList + css）。
- 重写 `src/pages/dashboard/Dashboard/Dashboard.tsx` + module.css（整页）。
- **删除** `src/pages/analyze-visual/RealtimeDashboard/`（占位页，路由改 redirect 后无消费者）。
- 公共文件修改（消费者影响已核）：`src/router/definitions.tsx` 两处——/dashboard 解除 pending（进入登录落点候选，`resolveLandingPath` 即时生效）+ dashboard-realtime 改 redirect（菜单/直访同码守卫）；`src/i18n/i18n.ts` 三语言懒加载表各增 1 行 dashboard。
- echarts 依赖复用 P33 已安装 `echarts@6.1.0`（按需注册 PieChart/LineChart，无新增依赖）。

## 共享契约

- 消费：`useVisiblePolling`（T00.6 唯一轮询入口，5s 串行+退避集中配置）、`usePageRequest`/RequestScope（取消级联）、`StateBlock`（offline 态，无重试按钮=按钮纪律）、`buildAccessContext`/`hasMenuAccess`（导航入口权限过滤）、`buildOrderInfoPath`/`buildVehicleInfoPath`（P38/P39 实体导航单一真相）、`parseBackendDateTime`（T00.7 时间解析单点）、`DEPLOY_TIMEZONE`（11.3）、`useTheme`（主题解析）。
- 新增（owner=P34）：**看板 KPI 计算口径单一定义点** `features/dashboard/realtime.ts`——今日/昨日按部署时区墙钟小时键（`yyyy-MM-dd HH`，兼容 `T` 分隔）汇总；今日任务总数=Σcreated（创建口径）、完成率=ΣcreatedSucceededCount/Σcreated、平均耗时=ΣcreatedSucceededDurationSeconds/ΣcreatedSucceededCount×1000、平均每小时完成=Σsucceeded/已过小时数（完成口径）、利用率=runningCount/onlineVehicleCount（瞬时近似）；昨日基线取相同小时数对齐；快照类指标 baseline 恒 null 不伪造环比。P35/P36/P37 不得另写同义计算（接口族不同则按各自契约实现）。
- **合并首页导航契约（D29，旧系统无此交互，用户决策规格 3.1/V01 9.1 派生）**：今日任务总数 KPI 卡 → `/order-record`（P03）；告警来源车辆（sourceType=VEHICLE）→ `/vehicle-info?vehicleKey=`（P39）；告警关联订单 → `/order-info?orderKey=`（P38）；全部按目标菜单码权限过滤（无权限不渲染入口）。**P36 故障告警页导航：仅登记契约不实现入口**（P36 未交付，避免死链），P36 交付后补验（TASKS 既定）。
- 图表技术决策：echarts 6.1.0 按需注册（P33 同款模式），主题 hex 色板双套（canvas 不能消费 CSS 变量），语言切换经 props 重建 option（notMerge）。

## 接口缺口

- 无新增 G 编号。G15（统计口径/时区）页面侧落实：小时桶与汇总全部按部署时区墙钟；除零/缺失 → null 展示「--」，不补 0；公式口径已在 KPI 提示（11 条口径文案四语言）显式说明，真实后端语义最终核对留 V01（同 P33 口径）。
- G10（int64）：计数字段 JSON number 承载（P21/P22/P33 同口径），告警 id String 化作行 key。

## 国际化

- `dashboard` 命名空间四语言分片交付（zh-CN key 即文案无资源文件）：en-US 重写（模板遗留 key 随 mock 退役；旧实时看板 en 真译逐条沿用，11 条口径说明按 P34 单段 key 重组沿用旧译语义）；zh-TW/ja-JP/ko-KR 新建（旧资源有真译者逐条沿用——级别/空态/标题等；状态词旧资源为简体复制词条按 B1 基线补真实译并与 vehicleInfo 分片术语对齐：空闲=空閒/アイドル/대기、离线=離線/オフライン/오프라인；口径说明为本页新增 key 补译）。i18n-missing.md 原「dashboard 全键回退简中」登记随本轮交付移除。
- 无新增 i18n-missing 条目（本页全部 key 四语言交付）。
- 语言切换行为：看板数据为数值+服务端多语言译文（客户端 `resolveAlertDescription` 回退链当前语言→zh_CN→en_US→原文），语言切换无需重查服务端；图表/枚举经 t() 重建（浏览器实测五语言动态重建、html lang 同步、无整页刷新）。

## 验证

- 静态门禁（两轮修复后复跑）：`pnpm check` lint 0/0（490 文件）/ typecheck / structure 481 文件；`pnpm build` 1.15s。代码提交前 lint 曾发现 0 缺陷，typecheck 首轮 5 处（未用导入/DTO 导入遗漏/KPI useMemo 类型/KPI 卡 accent 属性）已修。
- 真实只读 8/8（`readonly-result.json` + `run-p34-readonly.mjs`，经 Vite 同源代理 10.11.2.67 后端）：登录 activated=true；board days=2 code=200（hourlyCounts 45 行、覆盖 2026-09-19/20、五状态和 56=totalVehicleCount）；小时键 45/45 可解析；今日创建口径 22090/完成口径 22088/积压 1（供 UI 对照基准）；告警分页 0 条全部未关闭语义；无令牌 1000000 用户未登录；登出清理。
- 浏览器整页验收（真实登录、真实数据、IAB Chromium 1920×945 与 1366×768、深/浅主题、简中/EN/繁/日/韩；**交互经合成事件路径**——本会话 IAB 真实指针/键盘注入管线故障，事件日志垫片实证零事件到达，登记于下方边界说明）：登录落点 /dashboard（解除 pending 后 D29 生效）；8 KPI 真实值+环比箭头（与只读基准同口径，22,276→22,492 随轮询增长）；环图五状态图例+中心在线数 56；趋势双折线截至当前小时；告警真实空态；轮询 5s 串行持续（board+告警并行同拍，多轮计数 +1/周期）；**会话被踢真实失败场景**：board 区与告警面板独立 StateBlock（错误呈现、无重试按钮、互不冒充）；深浅主题图表重建；1366 KPI 2 列换行无溢出；五语言动态重建；KPI 卡导航 → /order-record；旧地址重定向同实例；登出清理（回 /login?redirect=…、auth 持久化 token/user 清空）。截图 9 张 `screenshots/`（本地留存，local_only）。
- 本轮发现并修复缺陷 2 处（截图实证）：① 图表面板高度塌陷（panelBody `flex:1` basis 0 覆盖内联 height，grid 行收缩到标题高）→ 去除 flex:1 让 bodyHeight 生效；② 环图图例泄漏中心占位系列名 `center` → legend.data 显式列五状态（旧实现同款排除）。

### 验证边界（如实登记）

- **IAB 输入管线故障**：本会话真实指针/键盘事件无法注入页面（locator click 常规与 force、cua 坐标、keyboard press 全部无效；按钮事件日志垫片记录为空数组）——所有交互（登录提交、主题/语言菜单、KPI 卡导航、登出）经 `dispatchEvent` 合成事件序列完成。合成事件可验证组件逻辑与路由行为，**不能证明真实用户指针交互**；登录凭据写入用原生 setter+input 事件（值经 antd Form 校验后真实提交，登录请求真实发出）。真实指针交互复核留 V01 自然浏览器场景（同 P39 RO 垫片登记口径，环境类限制非页面缺陷）。
- **告警行车辆/订单链接未点击验证**：本环境未关闭告警为 0 条（真实空结果），链接渲染分支（sourceType=VEHICLE+权限过滤+实体参数构造）为代码审查证据；P38/P39 目标页面与参数契约已在各自任务联验。留 V01 有告警环境复核。
- 验证期间一次「curl 重新登录互踢浏览器会话」（AGENTS §5 单会话纪律的验证操作失误，非产品缺陷）：意外获得双区域独立失败态的真实截图证据（`session-kicked-error-state-1920.png`）；随后浏览器重新登录恢复，整页持久化恢复复验通过。

## 未完成 / 后续

| 项 | 类别 | 说明 | 负责人 / 解除条件 |
| --- | --- | --- | --- |
| A04/I06（真实 token 过期收敛、资源加载失败不白屏） | cross_page | 与 T00/P01/P02/P21/P33/P43 同口径 deferred 归 V01 长会话复核（本轮轮询失败退避与失败态呈现机制级已实证） | V01 |
| A24（长时多页签/真实规模稳定性、图表重建资源回收） | cross_page | 图表 dispose/重建机制级已实现（Activity 隐藏卸载即释放）；长时观测归 V01 统一收口 | V01 |
| 告警行车辆/订单链接点击 + P36 首页告警导航 | cross_page | 本环境 0 未关闭告警无法点击；P36 交付后按 TASKS 补验首页→告警导航并更新两项验收记录 | P36 / V01 |
| 真实指针交互复核 | cross_page | 本轮 IAB 输入管线故障，交互经合成事件完成（边界见上）；真实指针自然浏览器复核归 V01 | V01 |
| G15 公式真实数据复核 | site_acceptance | KPI 公式口径已按契约+旧实现等价交付并四语言显式说明；后端语义最终确认（与 P33 同口径） | V01 |

## P36 交付后的补验更新（2026-09-20，run-09f6e01f）

- **首页→告警导航契约已落地并实测**：P36 轮在告警面板标题行实现「查看全部告警」入口（`dashboard-fault:view` 权限过滤，无权限不渲染），浏览器实测点击导航 `/dashboard` → `/analyze-visual/dashboard-fault` 成功。上表「告警行车辆/订单链接点击 + P36 首页告警导航」项的导航半项已关闭；告警行行内链接点击仍留 V01（本环境 0 未关闭告警）。
- **共享文件变更登记（消费者影响已核）**：本轮修改 `Dashboard.tsx`（DashboardPanel 增可选 action prop+入口按钮）、`Dashboard.module.css`（+panelAction 样式）、`dashboard` 四语言分片（+「查看全部告警」key）。首页行为回归：登录落点、8 KPI、告警面板、KPI 卡导航、登出清理均实测正常；因此 P34 原 acceptance.json 中 Dashboard.tsx/Dashboard.module.css 哈希失效，P34 状态保持 partial 待其 audit 复核轮按新哈希重建（本轮变更均为增量、无行为破坏）。
- 本轮 IAB 环境较 P34 轮进一步劣化（RO 冻结致表格行不渲染、screenshot 超时、输入管线故障加剧），详见 evidence/P36/run-09f6e01f…/checkpoint-2.json。
