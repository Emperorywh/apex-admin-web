# P11 多地图点边组合 交接记录

- 任务卡：TASKS.md「P11 多地图点边组合」（批次 B3 / 前置 B2 样板门禁）
- 执行轮次：run-20260919-next-0720（2026-09-19）
- 入口：`/map-through/point-edge-combination`；目标：`src/pages/map-through/PointEdgeCombination/PointEdgeCombination.tsx`
- 私有模块/语言分片：`node-edge-group`；接口依据：dispatcher/systemNodeEdgeGroup 五 operation（OpenAPI 逐项核对：pageSystemNodeEdgeGroups / createSystemNodeEdgeGroup / updateSystemNodeEdgeGroup / deleteSystemNodeEdgeGroup / getAllSimpleNodeEdgeGroups）

## 1. 交付范围（全部完成）

### 列表页整页重写（替换迁移过渡占位）
- `pages/map-through/PointEdgeCombination/PointEdgeCombination.tsx` + `.module.css`：
  - ApexTableReact **request 模式**：服务端分页（GET+query 平铺）、稳定行 ID=id（int64 主键经 `stringFieldRowId('id')` 收敛字符串）、内建取消/错误重试、列偏好 `node-edge-group:main`（四切片整体替换持久化）；排序不启用（G09：pageSystemNodeEdgeGroups 无排序参数）；
  - 列结构逐列核对旧版：组合名称（220）/组合Key（弹性）/包含组合（Tag 列表 Space wrap，Tag key=点边组合 id+地图 id 复合同旧 rowKey）/更新时间（180）+ 操作（180 钉右）；空值一律留白；
  - **展开行子表**（组合内的点边组合-所属地图明细）同样使用 ApexTableReact **data 模式**（真实完整小集合；禁 antd Table）：点边组合名称/所属地图/节点数量/边数量四列逐列核对旧版 expandedRowRender；计数列协议缺失留白（留白 ≠ 0）；行 ID=点边组合 id+地图 id 复合（旧 rowKey 同语义）；无明细行不可展开；
  - 工具行：组合名称搜索框（旧 Search 同形态 320px）+ 查询按钮（回首页重查）+ 新增组合按钮（按钮码显隐）；配置数据不轮询、无手动刷新按钮（写后自动刷新）；
  - 删除升级 confirmCommand 破坏性确认（标题「删除多地图点边组合」+对象+影响+提交≠完成附注；旧版仅「删除之后不可恢复」一问），行级防连点。
- `features/node-edge-group/components/NodeEdgeGroupModal.tsx`（新增/编辑双模式弹窗）：
  - 字段：组合名称（必填 64 字 showCount，旧同边界）+ 点边组合 Transfer（`getAllSimpleNodeEdgeGroups` 全量 19 条真实加载；搜索本地过滤；必填 + 至少一个——旧双 rule 同边界）；编辑全字段可改（updateSystemNodeEdgeGroup 按 int64 主键 systemNodeEdgeGroupId 定位、mapNodeEdgeGroupIds 整体替换——协议语义照实提交；后端生成的 nodeEdgeGroupKey 不可改不提交）；
  - **点/边/地图组合标识与顺序按真实契约提交**（专项验收）：mapNodeEdgeGroupIds 为 string id 数组（OpenAPI LittleSimpleMapNodeEdgeGroup.id 协议即 string），顺序=用户在穿梭框中的添加顺序，原样提交不做重排（协议无排序字段，顺序语义由数组顺序承载）；
  - **失效关联可识别并阻止错误保存**（P11 专项验收，较 P04 更严的差异化实现）：编辑回显中已选 id 不在当前选项集合（地图编辑侧被删除/当前不可见）时，合成条目保留原名称标识（名称取自编辑行原始明细 nodeEdgeGroups[].nodeEdgeGroup.name，缺失退回裸 id）并标注「已不可用」；存在失效项时提交校验直接拦截（toast 提示先移除再保存）并在弹窗底部常驻黄色说明——避免以失效 id 提交导致后端 500 或静默丢关联；选项加载中/失败不误判失效；真实失效场景需经地图编辑器（H02 暂缓）删除点边组合才能复现，实现以代码审查登记；
  - **选择取消不污染草稿**（专项验收）：穿梭框勾选态（selectedKeys）与已选集合（targetKeys）分离受控——勾选后取消、取消勾选均不改变已选集合；取消按钮关闭弹窗保留草稿（页签内存），丢弃路径是显式「清空」按钮；
  - 草稿保留（关闭不清输入、`destroyOnHidden=false`、失败留稿、显式「清空」）+ `useTabDirtyGuard`；提交防重复；6/18 横排标签分栏；选项 `useStaticOptions`（仅弹窗打开时加载；失败空态转「加载失败」文本，不冒充成功，不设重试按钮）。

### 服务层（owner=P11）
- `services/node-edge-group/node-edge-group.service.ts` + `.types.ts`（五 operation，逐项核对 OpenAPI）：
  - `GET /dispatcher/systemNodeEdgeGroup/pageSystemNodeEdgeGroups`（query 平铺，pageNo 一基）；
  - `POST createSystemNodeEdgeGroup` / `POST updateSystemNodeEdgeGroup`（id 定位整体替换）/ `POST deleteSystemNodeEdgeGroup`（id 定位）；
  - `GET /dispatcher/systemNodeEdgeGroup/getAllSimpleNodeEdgeGroups`（全部地图的点边组合选项扁平集合，页面私有消费 owner=P11；缺 id 条目过滤）；
  - DTO 嵌套结构：SystemNodeEdgeGroup.nodeEdgeGroups: MapNodeEdgeGroupJson[]（nodeEdgeGroup{id,name,edgeIds,nodeIds} + simpleMap{mapId,mapName}）；行主键 id int64 以 number 承载（G10 同 P09/P10），点边组合/地图 id 协议即 string 原样保持。

### 路由与权限
- 路由 `map-through-point-edge-combination` 解除 `pending`，`meta.i18nNamespaces: ['nodeEdgeGroup']`；菜单码挂既有 `PERM.POINT_EDGE_COMBINATION_VIEW`；按钮码 point-edge-combination:add/update/delete 与旧 §7/§8.2 一致，无权限隐藏；**登录响应实证四码真实下发**（permissions 含 point-edge-combination:view/add/update/delete），不凭空造码。

### i18n（四语言分片 + 登记）
- `nodeEdgeGroup` 命名空间四分片 36 key 同构：en-US 旧真译逐条沿用（Group Name/Group Key/Included Groups/Updated/Node-Edge Group Name/Belonging Map/Node Count/Edge Count/Available & Selected Node-Edge Groups 等），补译词条（删除影响说明、失效识别与阻止保存提示、出错拼接 `{{msg}}`）按旧真译风格；zh-TW（旧资源对主要 key 有真繁体沿用：刪除多地圖點邊組合/點邊組合名稱/所屬地圖等；更新時間/編輯/刪除/確定/點邊組合等简体复制词按 B1 补译）、ja-JP/ko-KR（真译沿用：グループ名/ノードエッジグループ/그룹 이름/노드-엣지 그룹 등；欠落词 B1 补译，公共词确定=確認/확인、取消=キャンセル/취소、清空=クリア/지우기 与 P10 crossMap 分片对齐）；`i18n.ts` 四表各增 1 行。

## 2. 门禁（交付终态全绿）

| 门禁 | 结果 |
| --- | --- |
| `pnpm lint` | 通过（325 文件 0 警告 0 错误） |
| `pnpm typecheck` | 通过（tsc -b --noEmit；曾抓出页面漏 import App 一处，修复后复跑通过） |
| `pnpm check:structure` | 通过（323 源文件） |
| `pnpm build` | 通过（chunk 警告为既有 apex-table-react 体积） |

## 3. 带令牌真实联验（dev 代理 192.168.0.158:8888，凭据 .env.local root/MD5；curl API 层先行全部完成后开浏览器会话——单会话纪律）

### API 层（curl，中文请求体一律 UTF-8 文件体 --data-binary）
1. 权限：登录响应 permissions 真实下发 point-edge-combination:view/add/update/delete 四码；
2. 分页 `pageSystemNodeEdgeGroups?pageNo=1&pageSize=10` → 200（初始 total=0 空集合）；
3. 选项 `getAllSimpleNodeEdgeGroups` → 200 共 19 条真实数据（独占区2–7 等，id 32 位字符串协议原样）——地图编辑侧维护的点边组合实体在新后端已有真实数据；
4. **create 空集合试探 → 后端 500「失败」拒绝**（mapNodeEdgeGroupIds=[] 不被接受，与前端「至少选择一个」校验同边界；业务拒绝形态为 500+通用「失败」而非业务码——登记观察项于 G23）；
5. 创建 `createSystemNodeEdgeGroup`（名称+独占区5/独占区7 两个 id）→ 200 落库一致：id=1（int64）、nodeEdgeGroupKey 后端生成（32 位 hex）、**明细嵌套结构完整且顺序与提交一致**（独占区5→7；nodeIds 25/32、edgeIds 196/138、simpleMap=压力测试1L）；
6. query=`__p11` 筛选命中 1 条、`__nomatch__` 空态；
7. 编辑 `updateSystemNodeEdgeGroup`（id=1+改名+集合换为独占区3/独占区2）→ 200 整体替换落库一致（key 不变、顺序保持、无 G18 类缺陷）；
8. 删除 `deleteSystemNodeEdgeGroup`（id=1）→ 200 后端自清理（total 回 0）；**对不存在 id 999999 亦 200 success（不校验存在性）→ G21 扩展登记**（幂等删除族行为同 publish/deleteCrossMap）。

### 浏览器整页（IAB，rAF+修正版 RO 垫片；垫片早于 Apex mount——文档内注入后经 Dock 菜单真实导航触发卸载重挂载，P10 手法）
1. 守卫：未登录直访 `/map-through/point-edge-combination` → 回跳 `/login?redirect=%2Fmap-through%2Fpoint-edge-combination`；登录后落点闭环回原页；
2. 列表：空态「暂无数据」共 0 条（与后端一致）→ UI 创建后行渲染、共 1 条；列序/列宽与旧版一致；
3. 新增弹窗全链路：Transfer 双栏「19 项可选点边组合 / 0 项已选」（选项真实加载）→ **必填拦截**：空表单确定 → 「请输入组合名称」+「请选择点边组合」+「至少选择一个点边组合」齐出 → 填名称+勾选独占区5/独占区7 加入 → 右栏 2 项 → **选择取消不污染草稿实证**：勾选独占区3 不加入/再取消勾选，右栏始终 [独占区5,独占区7] → **草稿保留实证**：取消关闭弹窗重开，名称与已选集合完整保留 → 提交「创建多地图点边组合成功」+ 列表刷新（key 后端生成、更新时间秒级、Tag 列表）；
4. 编辑回显：标题「编辑多地图点边组合」+ 名称回显 + 已选集合按明细嵌套 id 回显为名称（非裸 ID）→ 移除独占区7+改名提交 → 「更新多地图点边组合成功」+ 行实时更新（key 不变、Tag 只剩独占区5）；
5. 展开行子表：点边组合名称/所属地图/节点数量/边数量 → 独占区4/压力测试1L/25/194（真实明细数据）；
6. 删除确认：confirmCommand 五要素齐备（标题/对象/影响说明/danger 红色确定键/提交≠完成附注）→ 取消路径行保留 → 确认路径「删除多地图点边组合成功」+ 列表回空态；
7. 筛选：命中（「筛选」→1 条）与空态（不存在关键词→暂无数据）；
8. 列偏好闭环：齿轮落位序号列表头 → 隐藏「更新时间」列确定 → 刷新+Dock 重导航后保持隐藏（localStorage `apex-table:columns:…node-edge-group:main` 持久化）→ 恢复默认五列齐回；
9. 五语言：en-US（Group Name/Query/Add Group/…html lang=en-US）、zh-TW（組合名稱/新增組合…）、ja-JP（グループ名/グループを追加…）、ko-KR（그룹 이름/그룹 추가…）、zh-CN 回切——html lang 与持久化逐语言正确；
10. 双主题双宽度四组截图（1920/1366 × 亮/暗，展开行子表在画面中）：`.run-lock/shots-p11/`。

### 联验环境注记（IAB 伪象，非应用缺陷；均已在 AGENTS 第 5 节有登记）
- Modal 关闭动画不派发 → 已关弹窗 DOM 视觉卡留；**残留 confirm 的 DOM 点击无效**（React 已 resolve 的实例）——confirmCommand 的确定/取消一律在 `.ant-modal-confirm` 内最后实例定位（本轮实证复验该纪律：误点首个残留实例导致删除未发出，后端审计核实后按最后实例重放成功）；
- 主题切换后 reload 黑帧（css-in-js 滞留）→ 新页签整页载入规避；**settings 持久化值为二次 JSON 序列化**（`"theme":"\"dark\""`），直写时必须 `JSON.stringify('dark')` 否则 rehydrate 解析失败回退亮色（本轮踩坑并沉淀至 AGENTS 第 5 节）；
- Transfer 连续快速勾选在同一帧内会丢失前次勾选（React 未重渲染）——逐项勾选并间隔等待，属验证脚本节奏问题，人工操作不受影响。

## 4. 测试数据与环境清理
- UI 创建的 `__p11浏览器组合`（经编辑改名 `__p11浏览器组合改`）与 `__p11筛选组合` 均经 UI 删除确认路径清理；
- 后端终态审计：pageSystemNodeEdgeGroups total=0、records=[]（**零残留**）；
- 真实登出未执行（页签直接关闭），残留 localStorage 会话已被浏览器登录链路自然覆盖；settings theme 已恢复 light；
- dev server 已停止，联验页签已关闭。

## 5. 向 P19 提供的已确认组合实体契约（专项验收）
- **多地图点边组合实体**（P19 交管配置引用的「已确认组合」）：`SystemNodeEdgeGroup { id: number(int64 主键), nodeEdgeGroupName: string(唯一名称), nodeEdgeGroupKey: string(后端生成唯一 key), nodeEdgeGroups: MapNodeEdgeGroupJson[](创建时仅以 mapNodeEdgeGroupIds: string[] 提交,回读时展开为 {nodeEdgeGroup{id,name,edgeIds,nodeIds}, simpleMap{mapId,mapName}} 嵌套明细) }`；
- **引用/选项接口**：`getAllSimpleNodeEdgeGroups` → `LittleSimpleMapNodeEdgeGroup { id: string, name: string }`（全部地图扁平集合；P19 若需组合下拉应复用该 operation，服务已在 `services/node-edge-group/node-edge-group.service.ts` 落地 owner=P11，P19 可直接 import 或经 contracts 协调迁移 owner）；
- **行为要点**：删除为幂等（不存在 id 亦 200）；创建空集合被后端 500 拒绝（前端已必填拦截）；mapNodeEdgeGroupIds 顺序即用户意图顺序原样提交。

## 6. 遗留与登记
- **G21 扩展**：deleteSystemNodeEdgeGroup 对不存在 id 亦返回 200 success（幂等删除族行为第三例）；
- **G23 登记**：createSystemNodeEdgeGroup 以空 mapNodeEdgeGroupIds 提交时后端返回 HTTP 500+「失败」而非业务码（OpenAPI 未声明该校验）；前端表单已必填拦截，实际用户不可达，仅 API 直调可触发；
- 失效关联阻止保存的真实失效场景复现需地图编辑侧（H02 暂缓）删除点边组合，实现以代码审查登记，现场触发留 V01；
- P19 依赖本页契约已确认（第 5 节），无阻塞。
