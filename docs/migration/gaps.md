# 接口缺口与契约待核实台账

> 状态取值：`待确认 / 已确认缺失 / 已确认替代 / 已验证可用 / 本期暂缓`。
> 本表由 T00 按规格第 13 章初始化（2026-09-17，运行 run-20260917-121624-7468），全部为「待确认」——规格中的发现是勘察事实，不代表已完成后端验证。
> 关闭任何缺口必须附请求证据与验证日期；页面任务在自己的 `tasks/<ID>.md` 登记本页涉及的具体缺口证据，统筹者汇总到这里。

| 编号 | 发现 / 证据 | 影响页面 | 状态 | 本期处理 | 决定 / 验证日期 |
| --- | --- | --- | --- | --- | --- |
| G01 | 旧 `GET /fms/v1/auth/authorize/detail` 未出现在 OpenAPI | 会话恢复与权限更新（T00、P01；P31/P32/P43 不得调用） | 已确认缺失 | 登录返回数据持久化；权限拒绝后提示重新登录；不调用未文档化接口。T00.3 已移除模板 `/users/me` 会话恢复路径（profile.service 已删除） | 2026-09-17（OpenAPI 复核 + 模板调用点清理） |
| G02 | 文档没有模板 `/auth/refresh`、`/users/me` 协议（模板 auth.service 现存调用） | 模板认证基础设施（T00、P43） | 已确认缺失 | T00.3 已删除：请求层刷新重放/`_retriedAfterRefresh` 全部移除，`loadSession`/`getMyProfile`/`updateMyProfile` 删除；会话过期（业务码 1000000）直接清会话重新登录 | 2026-09-17 |
| G03 | 文档无 securitySchemes；`LoginParam.password` 未说明 MD5，旧登录使用 MD5 + Bearer | 登录及全局认证头（T00、P01/P02/P29） | 已验证可用 | 2026-09-18 真实凭据联验关闭：① MD5 确认为必需形态——MD5(root) 登录 `code=200` 返回完整 UserAuth，明文密码同账号返回 `1000010`；② Bearer 确认被识别——带令牌业务接口返回 `1001000`（进入激活检查），无令牌返回 `1000000`（未登录），令牌前缀形态正确；③ `1001000` 真实语义=`系统没有被激活`（软件授权状态，与登录态/权限无关），跳软件授权入口方向确认（UI 跳转归 P02）；④ 登录返回 UserAuth 六字段与前端 DTO 完全一致（token/activated/user/roles/permissions/permissionsTree，permissionsTree 树形带 childPermissions）；⑤ root 真实身份：id=2、level=2、state=ENABLED、roles=[test,user,test001]、155 个权限码。`request.constants.ts` 注释已同步实证结论 | 2026-09-18（真实登录成功/MD5-明文对照/Bearer-无令牌对照） |
| G04 | 多个 GET 参数以对象 DTO 声明（如 `OrderRecordPageParamOrderRecord`） | 全部列表筛选分页 | 部分验证（P03 抽样）；其余逐 endpoint 继续 | 逐 endpoint 验证 query 序列化，记录差异，不统一猜扁平/对象。T00.3 已定义 `BackendPageQuery`（pageNo/pageSize）与 `BackendPageResult`（records/current/size/total/pages）为常见形状基准。**2026-09-18 P03 带令牌实证：pageOrderRecords 与 exportOrderRecords 平铺 query 被后端接受且筛选真实生效**（`?pageNo&pageSize&query=…` 命中 1 条、导出单条 4362 字节）；其余 endpoint 随各自页面任务逐条核实 | 2026-09-18（P03 双 endpoint 带令牌） |
| G05 | `uploadSystemVersion` 等上传在文档表现为 `application/json` 内 binary 字段 | P08/P09/P25/P27（P23 若有可达上传同样核实） | 部分确认（P08 实证）；其余上传随各自页面核实 | **2026-09-19 P08 实证：uploadVehicleAlarmCodeFile 文档声明 `application/json`+binary，真实后端只接受 multipart/form-data、字段名 `file`**（curl multipart `code=200`；文档形态未采用）；服务层按旧实现 multipart 适配。**2026-09-19 P09 实证（uploadMap/uploadVehicleMap 同形态）：伪 zip curl → 500 拒绝、伪 bin → 4000060 上传的文件格式已经失败、真实 zip 回传 multipart `code=200`（语义=追加源地图未发布版本，零线上影响）、浏览器伪 zip 真实请求 500 如实透传**——P09 两个上传 operation 均已实证。P25/P27 的上传仍需逐个实证，不沿用本结论 | 2026-09-19（P08/P09 curl 实证 + 浏览器整页上传） |
| G06 | 旧电梯内呼 `/fms/v1/device/elevator/innerCall` 未出现在 OpenAPI，且控制弹窗可达 | P14 | 部分收敛（P14 交付保留禁用说明；接口本身待后端确认） | 2026-09-19 P14 交付：操作菜单对 operate 权限用户保留「电梯上楼（内呼）」入口——灰色标记+Tooltip+点击 warning「接口暂不可用」，零请求实证（XHR 计数不变）；不用 antd disabled（禁用项抑制 Tooltip 致说明不可达，联验实测）；不用外呼（ElevatorOperationParam.targetFloor）猜替，服务层不提供该 operation。后端若未来提供契约，开放菜单项+服务层补 operation 即可 | 2026-09-19（P14 联验） |
| G07 | 旧地图拉取 `downloadMapInfo` 未出现在 OpenAPI，MapList/PullModal 引用它 | P09 | 部分收敛（P09 交付保留禁用；接口本身待后端确认） | 2026-09-19 P09 交付：拉取入口保留 disabled 按钮+Tooltip 说明原因（浏览器实证），不猜替 `downloadMap`/`vehicleDownloadMap` 语义（后者为下载/车载下载，P09 已各自真实接入）；后端若未来提供拉取契约再开放入口 | 2026-09-19（P09 联验） |
| G08 | 旧 API 清单含 `upLoadMap/updateMapResource`、旧版本分页/更新重启等未声明路径 | P09、P25 | 部分收敛（P09 侧） | 2026-09-19 P09 结论：旧 `upLoadMap`/`updateMapResource` 未在新契约出现，本页不迁移（新契约 uploadMap 已覆盖导入语义）；旧版本分页由 `pageMapInfoVersions` 真实替代实证。其余项归 P25 | 2026-09-19（P09 核对） |
| G09 | 多数分页 DTO 未声明排序参数 | 全部表格页 | 待确认 | 不支持的列禁用排序或仅在完整本地集合明确实现；不发虚构 sort | — |
| G10 | int64 ID 与计数字段，实际规模未验证 | 行标识、关联选择、精确计数（全部相关页） | 待确认 | 联调核实精度；禁止损失精度后转字符串掩盖问题。T00.3：登录返回的 `UserSummary.id`（int64）在 DTO 层保持 JSON number、实体层以字符串无损承载，联调时需抽样核对真实 id 规模 | — |
| G11 | 普通文件接口未必有进度、取消、断点续传契约 | P03/P08/P09/P23/P25/P26/P27/P30 | 待确认 | 仅真实或不确定进度；取消不等于服务端回滚 | — |
| G12 | 模板个人资料、菜单、角色/用户 REST DTO 与调度 DTO 不同 | P31/P32/P43（P27 不虚构设置 CRUD） | 待确认 | 以调度 API 为准；无对应能力的入口注明缺口，不留假数据 | — |
| G13 | 缺少已核实的事务、版本号和幂等键支持 | 全部写操作页 | 待确认 | 预读冲突检查、结果未知态、禁止自动重放；不承诺强一致事务 | — |
| G14 | 旧全局 WebSocket 地址属于调度监控，OpenAPI 不含对应消息契约 | H01 及需实时能力的页面 | 待确认 | 优先已声明 HTTP 查询；确有依赖再登记独立契约，不复制硬编码 ws 地址 | — |
| G15 | 时区、某些统计公式/空值语义和状态集合待与真实数据核对 | P21/P28/P29/P33–P37/P40 | 待确认 | 部署配置时区（缺省 Asia/Shanghai）；缺失显示不可计算；公式验证后才能验收 | — |
| G16 | 目标 `check:structure` 脚本缺失（2026-09-17 实测 `pnpm check` 失败）；README 视觉指南 `docs/macos_ui_ux_design_guide_v3.md` 全仓库确认不存在（T00.1 曾误记「文件现存」，已勘误） | 质量门禁与设计依据（T00、V01） | 已验证可用 | T00.2 已落地 `scripts/check-structure.mjs`（禁 index.tsx、别名/相对导入、导入方向、硬编码主机门禁，违规注入自测可抓）；README/CLAUDE.md 失效引用改为「以现有布局/token 为视觉基线」 | 2026-09-17 |

## 新增缺口登记

（新缺口从 G17 起编号，由统筹者汇总；页面任务先在自己的记录中登记，避免并行改本表冲突。）

| 编号 | 发现 / 证据 | 影响页面 | 状态 | 处理 | 决定 / 验证日期 |
| --- | --- | --- | --- | --- | --- |
| G17 | 联调服务器（10.11.2.67:8888）软件未激活：2026-09-18 真实登录返回 `activated:false`，root 带有效令牌访问业务接口（pageUsers 实测）返回 `1001000 系统没有被激活`，公开接口（systemLogos、getHardwareInfo 无令牌）不受影响 | T00 放行证据残留项（带令牌业务只读）、B2 样板门禁真实只读联验、P02 激活流程、全部 B2+ 页面的真实接口联验 | 已解决（2026-09-18 用户提供已激活后端） | 用户提供已激活后端 `192.168.0.158:8888`（登记于 `.env.local` 的 `APEX_DEV_LEGACY_TARGET`，dev 代理唯一配置源）；实测登录 `activated:true`、带令牌 pageUsers 返回 `code=200` 真实用户列表——放行证据「授权只读可用」取得，T00 勾选。注：该后端 root 无角色（`roles=[]`）、权限码集与旧联调环境不同（含 `device:auto-door:*` 等），页面权限相关联验以该环境为准 | 2026-09-18（解决） |
| G18 | 后端 updateVehicleGroup 的 **groupName 更新不落库**：P04 联验中前端请求体正确（XHR 抓包 `{"groupKey":"…","groupName":"P04联验分组B","vehicleKeys":[…]}`）、后端响应 `code=200 success`，但复查数据 `agvGroupName` 保持旧值、仅 `updateTime` 更新；同请求 vehicleKeys 更新生效（simpleAGVs 随提交变化）。curl 直连复现（UTF-8 文件体排除编码因素）同症状——纯后端缺陷 | P04（编辑分组改名）；其他消费分组名称展示的页面（P03/P20 等）读到旧名属后端数据态 | 已确认（后端缺陷，待后端修复） | 前端行为正确不放宽（请求体/成功判定/自动刷新均实证）；后端修复后需复验编辑名称链路（编辑弹窗改名→确定→列表与后端复查）；不阻塞后续任务（P19/P20 仅消费分组选项） | 2026-09-18（P04 联验发现并登记） |
| G19 | 后端 saveAGVNodeMapping/updateAGVNodeMapping **强制要求 agvKeys 非空**：P07 联验中空数组提交返回 `code=2004030 AGV唯一key列表不能为空`（curl 直连复证稳定复现），非空即接受（不校验 key 真实存在性）；旧实现（dd）与旧 SPEC 均允许 agvKeys 为空可选——现行后端比旧版更严 | P07（新增/编辑节点映射）；后续消费节点映射的页面任务 | 已确认（前端已按现行契约适配：关联AGV 必填校验「请选择关联AGV」） | 前端不放宽也不猜测：按实际契约必填；若后端未来恢复可选语义，仅需移除 required rule 一处 | 2026-09-19（P07 联验发现并登记） |
| G20 | 后端 addVehicleAlarmCode 对同一告警码的**暂态 500「失败」**：P08 联验初期约 3 分钟窗口内连续 4 次 add 返回 `code=500 失败`（含 zh_CN 单记录、双记录等形态）；随后以完全相同请求体及 zh_CN/en_US/双记录/同码增删循环/改名→删除→重加等 10+ 变体全部 `code=200` 且落库一致，未能复现；业务级重复码另有独立业务码 `2000430 车辆告警码已经存在`（与该 500 无关）。疑似后端事务残留/暂态异常，非请求体形态问题 | P08（新增/编辑告警码）；用户侧影响=错误提示后可重试成功 | 观察登记（未复现，待后端核查） | 前端不做特殊处理（错误如实呈现、失败留稿可重试已实证）；若后续再现请后端核查 add/update 的事务与唯一索引残留；另登记：`alarmCodeRecords` 后端实际必填（`2000450 车辆告警描述不能为空`，OpenAPI 标可选），前端表单已按实际契约必填适配 | 2026-09-19（P08 联验发现并登记） |
| G23 | 后端 createSystemNodeEdgeGroup 对空 mapNodeEdgeGroupIds **返回 HTTP 500+「失败」而非业务码**：P11 联验实测 `{"systemNodeEdgeGroupName":"__p11探针","mapNodeEdgeGroupIds":[]}` → `code=500 失败`（OpenAPI 未声明该校验与错误语义）；同族业务拒绝均有独立业务码（如 2004030/4000540），此处行为不一致 | P11（创建组合——前端表单「至少选择一个点边组合」必填拦截，实际用户不可达，仅 API 直调可触发） | 观察登记（前端已拦截，无用户侧影响） | 前端保持必填校验不放松；不 特适配 500 形态（错误如实透传已实证）；后端如收敛为业务码无需前端改动 | 2026-09-19（P11 联验发现并登记） |
| G21 | 后端 publishMapInfoVersion **对不存在版本 id 不校验存在性**：P09 联验中 `{"mapVersionId":999999999}` 返回 `code=200 success`（deleteMap 对不存在目标则返回 `3000100 地图不存在`，同族行为不一致）；另证 **deleteMap 业务规则：ENABLED 地图删除被拒 `3000210 删除地图之前请先禁用地图`**（旧实现同样直接删并透传错误，前端行为等价保持）；uploadMap 真实回传会为源地图追加未发布版本（无版本删除接口，P09 联验在 6车间-货架区 产生一条未发布版本 id=22（内容与当前版本同源）无法 API 清理，线上数据零影响，留待后端核查） | P09（发布按钮文案已按「受理」语义）；P12 推送记录消费发布/推送结果 | 观察登记（待后端核查） | 前端不做存在性预检（避免多余请求）；发布/推送结果以推送记录与地图当前版本核实为准。**P10 扩展：deleteCrossMap 对不存在 id 亦返回 200 success（幂等删除/无存在性校验，同族行为登记）；P11 扩展：deleteSystemNodeEdgeGroup 对不存在 id 亦返回 200 success（幂等删除族第三例）**；**P25 扩展：deletePendingJar 对不存在 buildId 亦返回 200 success（幂等删除族第四例，2026-09-19 联验实证，终态审计全集与基线一致零变化）** | 2026-09-19（P09 联验发现并登记；P10/P11/P25 逐例扩展） |
| G22 | 后端 createCrossMap/updateCrossMap **强制「多地图关联节点角度不能为空」**（`4000540`）：P10 联验中按 OpenAPI CrossMapParam（mapId+nodeId）提交被拒；请求体字段名探测（angle/nodeAngle/rotation/direction/orient/heading）全部同错——**校验对象是所选站点在地图数据中的 angle 属性**（getMapInfo node.angle，弧度制），非请求体可补字段；旧实现（dd）提交体同样无角度字段；环境实证：「6车间-货架区」4444/5555、「测试大地图」KJ02/KC02（有 angle）可创建成功并落库一致，ceshi/压力测试1L/DEV/MAP_20260401_2 全站无 angle 站点不可创建 | P10（创建/编辑关联——用户选无角度站点提交时被拒，后端消息前端如实透传已实证）；P11/P19 点边组合若消费站点选择需知晓该规则 | 已确认（后端现行校验规则，OpenAPI 与文档均未声明） | 前端不做适配也无法适配（角度在地图节点数据中，不在请求协议内）；后续可选体验增强=站点选择器标注/过滤无角度站点（不在本期，需后端先确认规则走向：放开校验或补文档契约） | 2026-09-19（P10 联验发现并登记） |
| G24 | apex-table-react **「唯一业务数据列被隐藏」边界行为**：P22 列偏好联验中，把「避障名称」列隐藏（仅剩钉右操作列）并刷新后，表头/分页/total 正常但 tbody 行区与 BodyState 均不渲染（ApexTable canRender=false 且 rows.length>0 不显示 BodyState；数据模型有行）；恢复默认（两列可见）后渲染立即恢复。P03–P18 各页列偏好闭环隐藏的都是非唯一数据列，从未触发该组合 | P22（本页仅两列，用户可把唯一业务列隐藏至只剩操作列） | 观察登记（apex-table-react 库边界行为，非页面接线缺陷；页面接线四步与 P14–P18 完全一致） | 页面不特判不兜底（列偏好是用户显式操作，恢复默认即可还原，P22 交接记录登记提示）；若其他两列表（如 P06 名称+操作形态）复现，统一报统筹评估是否向包 owner 反馈；不在页面内写「至少保留一列」的私有拦截（避免发明面板没有的约束） | 2026-09-19（P22 联验发现并登记） |
| G25 | ~~root 凭据失效~~ **定性修正（2026-09-19 20:05 复查轮实证）：凭据从未失效，系联验脚本自身缺陷**——.run-lock/p23_api_verify.py 登录请求体误写 `userName`（大写 N），而后端/前端 auth.service 实际字段名为 `username`（全小写）；后端收到 null 用户名返回 `1000010 用户名或密码错误`，连续多次被误判为「密码被外部修改」。修正字段名后同凭据同 MD5 形态立即登录成功（code=200），P23 补验 17 项全过 | P23 联验（已按风险点清单补验通过并勾选）；后续任务联验脚本字段名以 `username` 为准 | **已解决（定性修正关闭）** | 教训：联验受阻先核对脚本请求体与前端 service 实际字段名（OpenAPI 文档声明与后端实现可能不一致，以前端实证为准），不要先入为主归因外部变更；后续联验脚本登录一律复用前端 auth.service 的 `username` 字段名 | 2026-09-19（P23 轮误判登记，同日复查轮修正） |
| G20（P31 扩展第二例） | 后端 addUser **暂态 500「失败」**：P31 联验首轮两种形态（OpenAPI 两字段/旧实现三字段）先后各返回一次 `code=500 失败`（无详情）；随后诊断变体与复测 10+ 次（两字段/三字段/不同密码/不同用户名）全部 `code=200` 且落库一致，首轮失败均未复现；与 P08 addVehicleAlarmCode 暂态 500 同形态（message=失败、无业务码、可重试成功） | P31（新增用户）；用户侧影响=错误提示后可重试成功 | 观察登记（复测未复现，待后端核查） | 前端不做特殊处理（错误如实呈现、输入保留可重试）；请后端核查 addUser 事务/暂态异常根因（与 P08 同族） | 2026-09-20（P31 联验发现并登记） |
| G26 | 后端 pageUsers 列表响应**每行返回 password 字段（MD5 摘要）与 level 字段**：2026-09-20 P31 联验实证 root/user/admin 行均含 password（32 位 hex）+level；OpenAPI AuthUser schema 同样声明该字段（契约与实现一致但属敏感信息下发）；旧实现同源未展示该字段 | P31（用户列表）；P32（角色管理若消费用户数据同受影响） | 观察登记（安全观察项，待后端确认是否脱敏） | 前端 DTO 类型保留完整（契约原样）、UI 不消费不展示、绝不持久化；建议后端列表响应脱敏 password（授权场景前端无该字段消费需求） | 2026-09-20（P31 联验发现并登记） |
