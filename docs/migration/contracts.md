# 共享契约台账

> T00.1 初始化框架（2026-09-17）；T00.3–T00.8 逐节填充，T00.9 冻结。
> 本表登记跨页面消费的共享契约：请求协议、DTO 约定、权限、实体导航、表格、草稿、轮询、传输、地图、i18n、时区。页面任务消费前先查本表；新增/变更契约由 owner 登记并评估全部消费者。

## 1. 请求协议契约（owner：T00，T00.3 已落地）

| 项 | 契约 | 状态 |
| --- | --- | --- |
| 基础路径 | `/fms/v1/...`，严格按 OpenAPI operation，不改写路径；`DEFAULT_API_BASE_URL='/fms/v1'`；dev 代理 `/fms` 原样转发（默认目标 `http://10.11.2.67:8888`，`APEX_DEV_PROXY_TARGET` 可覆盖）；经代理的端到端请求已实证（错误凭据登录返回业务码） | 已落地并实证（T00.2/T00.3） |
| 成功判定 | HTTP 成功且业务 `code=200`；不依赖 `message === 'success'` | 已落地并实证（GET /systemLogos 返回 `{code:200,...}`） |
| 响应包装 | `code/message/timestamp/data` 统一解包（`unwrapResult`）；文件/流（非 JSON content-type）原样透传不解包；非 Result 形状的 JSON 按 `CLIENT.MALFORMED_RESPONSE` 报错，不猜数据 | 已落地（T00.3） |
| 业务码 | `200` 成功；`1000000` 会话失效（真实实证：无凭据访问 pageUsers 返回该码，HTTP 200）→ 单飞清会话+一次性提示+外壳跳登录；`1000010` 用户名或密码错误（真实实证）；`1001000` 未激活（2026-09-18 真实环境实证，带令牌业务接口返回）→ 单飞一次性提示 + 事件引导 SPA 跳授权页（P02 已接入；授权页自身请求抑制；真实触发随未激活环境+首个业务页复核） | 已落地（含 1001000 引导） |
| 错误 | 统一 ApiError：`businessCode` 携带业务码，业务拒绝不判离线（health 不记失败）；文案经 i18next 翻译 | 已落地（T00.3） |
| 分页 | `BackendPageQuery`（pageNo/pageSize）→ `BackendPageResult`（records/current/size/total/pages）为常见形状基准，service 层规范化暴露；逐 endpoint 核实（G04） | 类型已定义；逐 endpoint 待联调 |
| 查询序列化 | GET 对象 DTO 逐 endpoint 核实 | 待联调（G04） |
| 认证 | `Authorization: Bearer <token>`（请求层单点）+ 密码 MD5（auth.service 单点，spark-md5）；均为旧代码证据，完整确认待登录凭据（G03）；`Accept-Language` 头统一发送（真实实证：后端按头返回对应语言 message，含错误文案） | 已落地（Bearer/MD5 待真实登录确认；Accept-Language 已实证） |
| 时间 | date-time/日期/统计天数按 endpoint；部署时区优先，缺省 Asia/Shanghai。已实证响应内时间为 `yyyy-MM-dd HH:mm:ss` 字符串（systemLogos createTime） | 已确认（规格 11.3、D23）；展示工具 T00.7 接入 |
| int64 | 实体层字符串承载；DTO 层保持 JSON number；联调抽样核实（G10） | 待联调（G10） |
| 取消/重试 | 主动取消（AbortSignal）静默：不报错、不判离线、不计健康；任何请求不自动重放（无刷新令牌协议，过期即重新登录） | 已落地（T00.3） |
| 健康反馈 | 有真实后端响应（含业务拒绝）= ok；无响应/HTML/网关错误 = error；取消不参与统计 | 已落地（T00.3） |

## 2. 会话与权限契约（owner：T00；T00.3 会话、T00.4 权限与路由已落地）

| 项 | 契约 | 状态 |
| --- | --- | --- |
| 登录 | `POST /auth/authorize/login`（OpenAPI 已核实）：请求 `{username, password(MD5)}`，响应 `data: UserAuth{token, activated, user, roles, permissions, permissionsTree}` 一次性构建会话 | 已落地（登录成功路径待真实凭据联验，G03） |
| 持久化 | token/activated/user/roles/permissions/permissionsTree 经 redux-persist 全量持久化（auth schema v2），刷新/重启恢复前端上下文；不存密码；模板时代旧会话（v1）迁移为未登录态。T00.4 联调修复：自定义 migrate 此前无条件重置会话，现按存储版本分流（<v2 重置、v2 原样恢复）；存储键含 redux-persist 默认前缀（`persist:apex-admin:auth`） | 已落地并经真实浏览器恢复实证（T00.4） |
| 会话失效收敛 | 业务码 1000000 → 请求层单飞处理：清令牌、一次性提示、派发 `sessionExpired`，BasicLayout 统一跳登录并携带回跳；无重定向风暴 | 已落地（T00.3） |
| 多窗口退出 | `storage` 事件监听持久化键（`persist:apex-admin:auth`，T00.4 修正键名缺前缀缺陷），其他窗口清空会话时本窗口同步清空；登录不做跨窗口同步 | 已落地（键名已实证修正，跨窗口行为待真实双窗口复验） |
| 无刷新接口 | 不实现静默刷新；过期重新登录（D05/G01/G02，模板 refresh/me 调用已删除） | 已落地（T00.3） |
| 权限码契约 | `PERM`（菜单码）/ `PERM_BUTTON`（按钮码）/ `ROOT_ONLY_CODES`（auth:manage、auth:user:view、auth:role:view）定义于 `src/constants/auth/permission.constants.ts`，码值与旧系统逐一核实对齐；历史码值交叉保留：任务工艺页挂 `mission-flow:view`、工艺管理页挂 `mission-template:view`；载具类型旧路由未写 access 已由 P06 核对收敛：后端权限树真实下发 `carrier:view`（MENU，旧 permission 数据 id=23，path 为空；旧前端 MENU_TREE 兜底映射 /vehicle-deploy/vehicle-type），PERM 已登记该已有码并挂路由（不公开访问、不凭空造码）；节点映射权限码由 P07 登录响应实证：permissions 平铺真实下发 `node-mapping:view/add/update/delete` 四码（permissionsTree 为 MENU 分组形态 path 空，与载具同构），路由挂已有码 PERM.NODE_MAPPING_VIEW（不公开访问、不凭空造码）；数据库备份路由维持登录即可达，待 P30 核对后再定；告警码权限码由 P08 联验实证：后端权限树真实下发 `vehicle-alarm-code:view`（MENU id=27，path=/vehicle-deploy/vehicle-alarm-code 与任务卡入口拼写不同，沿用任务卡路径、以 meta.perm 判权）及 add/upload/download/update/delete 五按钮码，路由挂已有码 PERM.VEHICLE_ALARM_CODE_VIEW（不公开访问、不凭空造码）；地图列表权限码由 P09 登录响应实证：permissions 平铺真实下发 map-list:view/add/update/delete/version/upload-dispatcher-map/upload-vehicle-map + map-version:update/publish/download 十码，路由挂已有码 PERM.MAP_LIST_VIEW、按钮码消费 PERM_BUTTON.MAP_*（不公开访问、不凭空造码）；地图关联权限码由 P10 登录响应实证：permissions 平铺真实下发 cross-map:view/add/update/delete 四码，路由挂已有码 PERM.CROSS_MAP_VIEW、按钮码消费 PERM_BUTTON.CROSS_MAP_*（不公开访问、不凭空造码）；多地图点边组合权限码由 P11 登录响应实证：permissions 平铺真实下发 point-edge-combination:view/add/update/delete 四码，路由挂已有码 PERM.POINT_EDGE_COMBINATION_VIEW、按钮码消费 PERM_BUTTON.POINT_EDGE_COMBINATION_*（不公开访问、不凭空造码）；地图推送记录权限码由 P12 登录响应实证：permissions 平铺真实下发 map-push-record:view + map-push-record:re-push 两码（重推与取消共用 re-push 一码，后端无取消专用码，旧实现同边界），路由挂已有码 PERM.MAP_PUSH_RECORD_VIEW、按钮码消费 PERM_BUTTON.MAP_PUSH_RECORD_RE_PUSH（不公开访问、不凭空造码）；调度中心权限码由 P13 登录响应实证：permissions 平铺真实下发 dispatch-hub:view/save/reset 三码，路由挂已有码 PERM.DISPATCH_HUB_VIEW、save 码控制保存入口与单元格可编辑（后端无独立编辑码，编辑唯一目的经保存提交，旧实现同边界）、reset 码控制重置入口（不公开访问、不凭空造码）；电梯权限码由 P14 登录响应实证：permissions 平铺真实下发 device:elevator:view/add/update/delete/operate 五码，路由挂已有码 PERM.DEVICE_ELEVATOR_VIEW、按钮码消费 PERM_BUTTON.DEVICE_ELEVATOR_ADD/UPDATE/DELETE/OPERATE（「状态」查看行为不控权，旧实现同边界；不公开访问、不凭空造码）；自动门权限码由 P15 登录响应实证：permissions 平铺真实下发 device:auto-door:view/add/update/delete/operate 五码，路由挂已有码 PERM.DEVICE_AUTO_DOOR_VIEW、按钮码消费 PERM_BUTTON.DEVICE_AUTO_DOOR_ADD/UPDATE/DELETE/OPERATE（「状态」查看行为不控权，旧实现同边界；不公开访问、不凭空造码）；充电桩权限码由 P16 登录响应实证：permissions 平铺真实下发 device:charge-pile:view/add/update/delete/operate 五码，路由挂已有码 PERM.DEVICE_CHARGE_PILE_VIEW、按钮码消费 PERM_BUTTON.DEVICE_CHARGE_PILE_ADD/UPDATE/DELETE/OPERATE（状态为列表列展示不控权；不公开访问、不凭空造码）；交通灯权限码由 P17 登录响应实证：permissions 平铺真实下发 device:traffic-light:view/add/update/delete/operate 五码，路由挂已有码 PERM.DEVICE_TRAFFIC_LIGHT_VIEW、按钮码消费 PERM_BUTTON.DEVICE_TRAFFIC_LIGHT_ADD/UPDATE/DELETE/OPERATE（连通性测试无独立 test 码归 operate 粗粒度码，旧实现同语义；不公开访问、不凭空造码）；风淋门权限码由 P18 登录响应实证：permissions 平铺真实下发 device:air-shower-door:view/add/update/delete/operate 五码，路由挂已有码 PERM.DEVICE_AIR_SHOWER_DOOR_VIEW、按钮码消费 PERM_BUTTON.DEVICE_AIR_SHOWER_DOOR_ADD/UPDATE/DELETE/OPERATE（「状态」查看行为不控权，旧实现同边界；不公开访问、不凭空造码）；避障模板权限码由 P22 登录响应实证：permissions 平铺真实下发 obstacle-avoidance:view/add/update/delete 四码，路由挂已有码 PERM.OBSTACLE_AVOIDANCE_VIEW、按钮码消费 PERM_BUTTON.OBSTACLE_AVOIDANCE_ADD/UPDATE/DELETE（无权限隐藏，旧 §7/§8.2 同边界；不公开访问、不凭空造码）；车辆动作权限码：路由挂已有码 PERM.ACTION_VEHICLE_VIEW、按钮码消费 PERM_BUTTON.ACTION_VEHICLE_ADD/UPDATE/DELETE（无权限隐藏，旧 §7/§8.2 同边界；不公开访问、不凭空造码），**四码下发已由 P23 登录响应实证：permissions 平铺真实下发 action:vehicle:view/add/update/delete 四码（2026-09-19 补验）**；版本管理权限码由 P25 登录响应实证：permissions 平铺真实下发 system:version:view/restart/upload/rollback/download/delete 六码，动作分组权限码由 P24 登录响应实证：permissions 平铺真实下发 action-group:view/add/update/delete 四码，路由挂已有码 PERM.ACTION_GROUP_VIEW、按钮码消费 PERM_BUTTON.ACTION_GROUP_ADD/UPDATE/DELETE（无权限隐藏，拖拽重排归 update 码=旧 §7.2 同边界；不公开访问、不凭空造码）；任务工艺权限码由 P20 登录响应实证：permissions 平铺真实下发 mission-flow:view/add/update/delete/copy 五码（**历史码值交叉实证：任务工艺页视图/按钮码均为 mission-flow:* 族，工艺管理页才是 mission-template:* 族，旧路由与权限清单核实**），路由挂已有码 PERM.MISSION_FLOW_VIEW、按钮码消费 PERM_BUTTON.MISSION_FLOW_ADD/COPY/UPDATE/DELETE（无权限隐藏，旧 §7/§8.2 同边界；不公开访问、不凭空造码）；版本管理权限码由 P25 登录响应实证：permissions 平铺真实下发 system:version:view/restart/upload/rollback/download/delete 六码，路由挂已有码 PERM.SYSTEM_VERSION_VIEW、按钮码消费 PERM_BUTTON.SYSTEM_VERSION_RESTART/UPLOAD/ROLLBACK/DOWNLOAD/DELETE（无权限隐藏，旧 §7 同边界；不公开访问、不凭空造码）；系统日志权限码由 P26 登录响应实证：permissions 平铺真实下发 system:log:view/download 两码，操作日志权限码由 P28 登录响应实证：permissions 平铺真实下发 system:operation-log:view 单码（本页只读无写操作无下载，无按钮码），路由挂已有码 PERM.SYSTEM_OPERATION_LOG_VIEW（不公开访问、不凭空造码）；用户管理权限码由 P31 登录响应实证：permissions 平铺真实下发 auth:user:view/add/resetPassword/assign-role/delete 五码，路由挂已有码 PERM.AUTH_USER_VIEW（**root 专属码 ROOT_ONLY_CODES：非超管即使后端下发也不可见，T00 routeAccess 统一拦截**）、按钮码消费 PERM_BUTTON.AUTH_USER_ADD/RESET_PASSWORD/ASSIGN_ROLE/DELETE（无权限隐藏+root 行三项操作按 username=root 收敛隐藏，旧实现同边界；不公开访问、不凭空造码） | 已落地（T00.4；P06 收敛载具类型行、P08 补告警码行、P09 补地图列表行、P10 补地图关联行、P12 补推送记录行、P13 补调度中心行、P15 补自动门行、P16 补充电桩行、P17 补交通灯行、P18 补风淋门行、P22 补避障模板行、P23 补车辆动作行已实证、P25 补版本管理行已实证、P31 补用户管理行已实证、P24 补动作分组行已实证、P20 补任务工艺行已实证） |
| 权限判定 | 纯函数 `@/utils/auth/permission`（isRootUser/flattenMenuCodes/hasButtonCode）+ 路由访问核心 `@/router/routeAccess`（buildAccessContext/hasMenuAccess/isLeafAvailable/resolveLandingPath/resolveDirectoryLandingPath/resolveSafeRedirectPath）；超管（[登录凭据已脱敏]）短路全放行；菜单码扁平化 + 基于定义树的祖先填充（有子菜单则父分组可见）− root 专属码；按钮码 hook `usePermission()`（超管短路） | 已落地（T00.4） |
| 路由守卫 | 定义树 meta.perm 挂码；认证+权限守卫覆盖全部非 public 入口（含布局外独立页 order-info/vehicle-info/server-resource-monitor/authorize-ingress/no-permission，不因 layout:false 公开）；目录 index 与受保护根 index 按会话动态解析落点（目录索引不指向无权限/未迁移页）；迁移过渡页（meta.migrationPending）渲染统一占位、不加载页面代码（无请求）、不作落点候选；页面任务完成后由统筹移除标记 | 已落地并经 17 项真实浏览器场景实证（T00.4） |
| 登录落点 | activated=false → `/authorize-ingress`（D29）；首页（/dashboard，P34 合并后同码 dashboard-realtime:view）可用优先；否则菜单顺序首个「有权限且已完成迁移」业务页；全部不可用 → `/no-permission`；登录回跳仅接受站内、存在且可用的目标（resolveSafeRedirectPath） | 已落地并实证（T00.4） |
| 权限拒绝 | 阻止操作 + 提示重新登录；不伪造刷新 | 已确认（规格 5.9）；页面接入随各页任务 |

## 3. 表格契约（owner：T00，T00.5 已落地）

| 项 | 契约 | 状态 |
| --- | --- | --- |
| 组件 | 官方 npm `apex-table-react@0.1.0`（T00.2 已安装并提交锁文件）；无 antd Table/自绘 table；**页面直接使用包公开 API，禁止二次封装**（用户决策 2026-09-17，规格 6.1 修订） | 已安装（A07）；公共资源与约定 T00.5 落地 |
| 五语言 locale 包 | 包内置仅 zhCN；en-US/zh-TW/ja-JP/ko-KR 完整 ApexLocale 交付于 `src/i18n/locales/apexTable/`（简中不复制第二份，直接引用包内 zhCN）；`resolveApexLocale(lang)` 纯函数（前缀匹配、未知语言回退简中）+ `useApexLocale()` hook（随 i18next 语言联动重渲染）；ApexLocale 类型强制全 key 覆盖（探针曾捕获缺 key，类型即护栏） | 已落地（T00.5）；实际渲染效果随 P03/P05 首个真实表格联验 |
| 主题变量映射 | globals.css `:root .apex-table` 把 `--apex-table-*` 全量接到 `--app-*` 令牌（bg/表头/文本/强调/hover/选中/边框/圆角/字体/字号/内边距，亮暗随 data-theme 同帧切换；暗色另有 pinned-shadow 深色投影）；特异性高于包默认值，防懒加载包 CSS 反超；单表微调走组件 style prop，不改映射 | 已落地（T00.5）；亮暗视觉待 P03/P05 联验 |
| request 模式 | 零基 pageIndex → 后端 pageNo：`toBackendPage(pageIndex, pageSize)`（`src/utils/table/tablePaging.ts`，非法入参抛错）；返回 `{data, rowCount}`；未知总数不填 0（DoD 5）；组件内建取消（request 回调 signal）/错误重试/防乱序 | 换算函数已落地（T00.5）；真实接口分页联验随 P03/P05 |
| data 模式 | 仅真实完整小集合与草稿行（规格 6.2）；`editable`/`onDataChange` 仅 data 模式可用（包类型约束） | 已确认（T00.5 核验） |
| 行 ID | `stringFieldRowId(field)`（`src/utils/table/rowId.ts`）：字段空值抛错、不安全整数（int64 精度丢失，G10）抛错提示改 DTO；禁止位置性 ID | 已落地（T00.5） |
| 列偏好 | 官方适配器 `apex-table-react/adapters/local-column-preferences` 接入约定：`createTableColumnPreferences({tableId, userId})`（`src/utils/table/columnPreferences.ts`，namespace='apex-admin'、tenantId='default'、schemaVersion 默认 '1'，列结构变更须提升）+ `useColumnPreferences(tableId)` hook（会话取 userId、卸载 flush+dispose）；存储 key 四段隔离满足 DoD 5；页面接线四步见文件头 JSDoc（setState 与 save 都不可省，save 传合并后的完整四切片）。交互约定（用户决策 2026-09-18）：开启 `columnSettingsEnabled` 的表格**序号列默认放出**，齿轮入口随序号列表头（不单独占表头上方工具条行）；序号列是辅助轨道，不进偏好切片 | 已落地（T00.5）；localStorage 持久化与交互闭环已验证（P03 带令牌联验 2026-09-18） |
| 公开 API 核验 | 虚拟化 `virtualization: 'auto'\|boolean\|{overscan}`（分页 >200 行自动开启）；行展开 `expandable`（expandedRowRender/受控 keys/兼容虚拟滚动）；单元格编辑 `meta.apex.editor`（10 种 antd 控件）+ `editorConfig`；双 ref：`ref`=ApexTableRef（focus/scrollToRow/reload）、`tableRef`=ApexTableInstance（TanStack 实例方法）；弹层默认渲染在表格 DOM 内（主题变量继承安全），可用 getPopupContainer 重定向 | 已核验（T00.5，类型探针全量组装通过后删除） |
| 统一状态块 | `src/components/StateBlock/StateBlock.tsx`：`noPermission`（无权限）/`gap`（缺口禁用+原因）/`offline`（失败清区+显式重试）三语义，与「真实空数据」区分；独立组件不包裹表格；文案走 common 命名空间（zh/en 已交付，zh-TW/ja/ko 回退简中已登记 i18n-missing.md） | 已落地（T00.5） |
| 选择 | 当前页选择；翻页/筛选/失权清理（包内建当前页语义，跨页保留由业务决定——本项目按 DoD 5 不跨页保留） | 已确认（规格 6.2；页面任务消费） |
| 排序 | 仅真实支持的能力（G09）：不发虚构 sort | 已确认（G09） |

## 4. 页签/草稿/轮询/传输契约（owner：T00，T00.6 已落地）

| 项 | 契约 | 状态 |
| --- | --- | --- |
| 草稿登记 | 页面经 `useTabDirtyGuard(isDirty, label?)`（`src/hooks/useTabDirtyGuard.ts`）把「是否存在未保存修改」同步到所属页签（`tabsSlice.tabDirtyMarked`）；草稿数据本身留在页面组件内存，标记只是元数据；组件卸载（关页/淘汰/刷新重建）自动清标记 | 已落地（T00.6）；页面任务自 P03 起消费 |
| LRU 豁免 | `tabsSlice.tabSynced` 的 LRU 淘汰跳过 dirty 页签：草稿不静默丢弃，宁可临时超出 `PAGE_CACHE_MAX_ENTRIES=10`；登出/会话失效整体重置 | 已落地并经浏览器程序化验证（T00.6） |
| 统一动作确认 | 关闭/刷新/关闭其他/左侧/右侧/全部、退出登录一律经 `useTabActionGuard`（`src/hooks/useTabActionGuard.tsx`）：列出受影响脏页签（路由标题+脏对象说明），确认后才派发 reducer；无任何旁路。传输提示三策略：`none`（刷新不影响传输）/`continue-after-close`（关页传输继续+完成消息提示）/`terminate`（登出本机终止，不保证服务端已停止） | 已落地并经真实浏览器验证（脏行/脏+传输/仅传输三形态，T00.6） |
| 浏览器离开提示 | BasicLayout：存在脏页签时挂 `beforeunload`（preventDefault + returnValue），无脏页签不挂监听；浏览器终止等非正常退出不承诺恢复草稿 | 已落地并经对照实验验证（脏→刷新被拦、无脏→正常重载） |
| 实时轮询 | `useVisiblePolling({refresh, enabled?})`（`src/hooks/useVisiblePolling.ts`）为实时页面唯一刷新调度入口：页签激活+文档可见才刷新；串行（完成后约 5 秒发起下一次，间隔自完成时刻起算）；重新可见/激活立即刷新；失败按倍率退避至上限；成功复位；主动取消（scope 信号中止）静默停止不算失败；后台保留快照。间隔与退避集中定义于 `src/constants/polling.constants.ts`（5s / ×2 / 60s），页面不得私设定时器；refresh 必须是只读查询并把收到的 signal 传给请求层 | 已落地；运行时行为随首个实时页（P34/P05/P39/P40）联验 |
| 传输生命周期 | `src/services/transfer/transferManager.ts` 模块级单例：AbortController 由管理器持有，不挂页面 scope——切页（Activity 隐藏/effect 清理）、关页、LRU 淘汰不误杀传输；普通查询取消（RequestScopeProvider）与传输互不影响。页面 `beginTransfer({tabKey, kind, name})` 取句柄：`signal` 传给请求层、`setProgress(loaded,total)` 回报真实进度（total 未知=null 不确定进度）、`markProcessing()`（字节 100% 后服务端处理中）、`succeed()/fail(reason)` 依业务结果判定；仅真实可取消的传输暴露 cancel | 已落地并经浏览器验证（关页后传输继续、完成后提示）；真实文件通道随页面任务（P08/P09/P25 等）接入 |
| 取消语义 | 本地 cancel/abort 仅代表客户端终止：阶段标 `aborted`，文案注明「仅本机终止，服务端处理不保证已撤销」（结果待确认，规格 10.4）；禁止把 Abort 当服务端回滚 | 已落地（T00.6） |
| 传输状态追踪 | `useTransfers(tabKey?)` 订阅任务列表（页面进度 UI 用）；`findActiveTransfersIn(tabKeys)` 供关闭确认一次性查询；承载页签已关闭的孤儿传输终态由 `TransferWatcher`（BasicLayout 内）以一次性 antd message 提示结果——**本期不新增用户可见全局任务中心**；终态记录保留 15 秒后自动清理 | 已落地并经浏览器验证（孤儿完成 toast，T00.6） |
| 会话结束清理 | 登出/会话失效/多窗口同步退出（isAuthenticated=false）：TransferWatcher 终止全部进行中传输并清空全部记录（含终态残留）；登出确认框提示未保存修改与传输终止影响 | 已落地并经浏览器验证（T00.6） |
| 实体页签 | tab.key = pathname + 规范化 search（参数名稳定排序）：实体详情以 `?orderKey=<实体ID>` 等定位参数打开时，不同实体自动获得独立页签、独立 Activity 缓存实例与独立请求 scope，互不串缓存；同参数复用同一页签。默认工作区页签形态要求实体路由位于受保护根内——P38 落地首个消费者（order-info 移入受保护根），P39 第二个消费者同构落地（vehicle-info 移入受保护根，完整路径 /vehicle-info 不变，hideInMenu 不进菜单/落点候选）；P40 接入时同构迁移 | 已落地并经 P38/P39 真实浏览器验证（P38 两 orderKey 三页签；P39 两 vehicleKey 两页签并存互不串） |
| 独立窗口 | `openStandaloneWindow(path, params?, options?)`（`src/utils/window/standaloneWindow.ts`）：仅接受站内绝对路径；按「路由+排序参数」命名窗口（同实体复用窗口、异实体独立）；居中 1280×800 popup；打开成功后切断 opener。独立窗口 = 打开同一路径（P38 起实体详情路由位于受保护根，窗口内为完整外壳形态），受同一认证+权限守卫，会话/语言/主题来自持久化；多窗口退出同步复用 authBridge（T00.3）。打开失败（弹窗拦截）返回 false，调用方给可读提示 | 已落地并经 P38 真实浏览器验证（新窗口渲染完整外壳+主体+守卫） |

## 5. 共享只读选项与地图能力契约（owner：T00；T00.7 已核对登记）

跨页只读选项由 T00 登记唯一 operation；查选项不等管理页完成，复用底层服务，页面不得各自复制请求。加载行为统一消费 `useStaticOptions`（`src/hooks/useStaticOptions.ts`：scope 取消、防乱序、失败清空 + error）；失效值经 `matchOptionById`/`missingOptionLabel`（`src/utils/options/optionFallback.ts`）保留原值并禁用保存，禁止静默选第一项。operation 逐项核对自 OpenAPI（2026-09-17）：

| 选项 | operation（GET /fms/v1 相对路径，method 已核对） | 参数 | 消费者 | 状态 |
| --- | --- | --- | --- | --- |
| 地图图形（节点/路径） | `GET /dispatcher/map/getMapInfo` | `mapId`(必填) | P07（已消费：选点弹窗+节点选项缓存）/P10/P11/P19/P20 等；服务已落地 `src/services/map/map.service.ts` | operation 已核对；P07 带令牌联验通过（响应 schema 按旧可达实现解析成立，节点/路径渲染与选点正常） |
| 地图下拉选项 | `GET /dispatcher/map/getSimpleMaps` | 无（全量） | P07（已消费）/P09/P10/P11；服务已落地 `fetchSimpleMaps` | operation 已核对；P07 带令牌联验 6 项真实加载 |
| 地图站点/站点(非节点) | `GET /dispatcher/map/getStations`；`GET /dispatcher/map/getSites` | `mapId`(必填)、`type`(可选) | P10 | operation 已核对，服务随 P10 |
| 车辆 | `GET /dispatcher/vehicle/getSimpleVehicles` | `mapId`(可选) | P04（GroupFormModal 已消费）等；服务已落地 `fetchSimpleVehicles` | operation 已核对；P04 带令牌联验 56 项真实加载（int64 key 字符串无损） |
| 分组 | `GET /dispatcher/vehicleGroup/getVehicleGroups` | 无（全量） | P03（已消费）/P20；P04 起服务 owner=P04（选项 fetchVehicleGroups 保持不变） | operation 已核对；P03 联验已消费 |
| 载具 | `POST /dispatcher/carrier/pageCarriers` | 分页查询（body 平铺） | P06 载具类型页（消费中）；其他关联载具的页面任务 | operation 已核对；服务已落地 `services/vehicle/carrier.service`（owner=P06，含 create/update/delete 三写操作），P06 带令牌联验通过 |
| 节点映射 | `GET /dispatcher/agvNodeMapping/pageAGVNodeMapping`；save/update/delete 为 POST | 分页 GET+query（pageNo 从 1 计数）；save/update 为 JSON body（update=mappingKey+全量分组，整体替换）；delete 为 mappingKey | P07 节点映射页（消费中）；P19 组合任务等后续按需 | operation 已核对；服务已落地 `services/vehicle/node-mapping.service`（owner=P07，五 operation 含 getSuggestionsForCollectionNodes），P07 带令牌联验通过；**agvKeys 后端强制非空（2004030）为现行契约差异，已登记 G19** |
| 车辆告警码 | `GET /dispatcher/vehicleAlarmCode/pageVehicleAlarmCodes`；add/update/delete/upload/down 为 POST | 分页 GET+query（pageNo 从 1 计数）；add/update 为 JSON body（update=id+全量，records 后端实际必填 2000450）；delete 按 alarmCode 字符串定位；upload=multipart 字段 file（G05 实证）；down=POST 空 body + Blob（文件三列：告警ID/告警名称/建议处理措施） | P08 告警码管理页（消费中）；P34/P40 看板如需告警统计按需 | operation 已核对；服务已落地 `services/vehicle/vehicle-alarm-code.service`（owner=P08，六 operation），P08 带令牌联验通过（含上传零净变化回传与下载文件头核对）；**同码暂态 500 观察 G20** |
| 地图管理（元数据+版本） | `GET /dispatcher/map/pageMapInfos`；createMap/updateMap（mapKey 定位）/deleteMap（mapId 定位）为 POST；uploadMap/uploadVehicleMap=multipart 字段 file（**deleteMap 后端规则：先禁用再删除，ENABLED 删除 3000210**）；`GET /dispatcher/mapVersion/pageMapInfoVersions`（mapId 隔离）；publishMapInfoVersion/pushMapInfoVersion 为 POST；downloadMap=POST JSON body {mapInfoVersionId}+Blob（content-disposition 按文件而异，前端本地名兜底）；downloadMapInfo（G07 拉取）不在 OpenAPI 不提供 | P09 地图列表页（消费中）；P10/P11/P19/P20 等继续复用 ReadOnlyMap/getSimpleMaps 选项行 | operation 已核对；服务已落地 `services/map/map-admin.service`（owner=P09，十 operation），P09 带令牌联验通过（增改删/上传回传语义=追加未发布版本/下载/发布安全执行；**推送与发布对生产地图执行留专用环境**；publish 对不存在 id 返回 200 观察 G21） |
| 车辆动作 | 管理 `GET /action/agvAction/pageAGVActions`（GET+query 平铺）/`POST addAGVAction`/`updateAGVAction`（int64 id 定位+四字段整体提交）/`deleteAGVAction`（**body {id}**——OpenAPI delete body=AGVActionParam $ref，与避障 POST+?id= 形态不同）；选项 `GET getAGVActions`（全量，P03 代建共享服务不变） | 分页 GET+query（pageNo 从 1 计数，query=动作搜索）；blockingType 枚举 NONE/SOFT/HARD（未知枚举原值保留不改写——P23 专项）；actionParameters[{key, value:unknown}]（OpenAPI value 声明 object，**实际形态已实证：字符串/数字/数组/对象混提落库回读全保形**；非字符串 value 编辑回显仅转显示不改数据，未改动行提交原始值已实证——update 请求体 value={"n":5,"keep":true} 对象保形；**blockingType 为后端受控枚举：未知值后端 500 拒绝（前端动态并入 options 为防御性实现，自然路径不触发）**） | **P23 管理服务已落地 `services/action/agv-action-manage.service`（owner=P23，4 operation）**；P24 动作分组（组内动作整组提交）/P20 工艺模板（引用动作）按此契约消费 | operation 逐项核对 OpenAPI；**带令牌联验已通过（2026-09-19 补验：权限四码/分页/增改删/四形态 value/删除自清理终态审计零残留；G25 定性修正=联验脚本登录字段名 userName→username 笔误，凭据从未失效，见 gaps）**；importExcel/exportExcel 旧仓库零 UI 消费者不落地 |
| 动作分组 | 管理 `GET /action/agvActionGroup/pageAGVActionGroups`（GET+query 平铺）/`POST addAGVActionGroup`/`updateAGVActionGroup`（int64 id 定位+整组提交：名称+动作 id 集合；**列表拖拽重排与弹窗编辑共用本 operation，旧实现同形态**）/`deleteAGVActionGroup`（**body {id}**，与 P23 deleteAgvAction 同款）；选项 `GET getAGVActions`（P03 代建共享服务，弹窗打开时拉取）；`GET getAGVActionGroups` 旧页零 UI 消费者不落地 | 分页 GET+query（pageNo 从 1 计数，query=组名模糊）；重名新增 **5000000「车辆动作组名称已经存在」**（唯一性约束在后端，旧实现同边界）；**删除被引用动作 5000010「车辆动作已经关联了车辆动作组」=后端动作↔组双向引用完整性，「组内失效动作」自然路径不可达（前端失效识别为防御性实现，P23 未知枚举同款处置）**；**update.agvActionIds 含不存在动作 id 后端 200 受理但静默过滤（实测提交 [2,99999] 落库后组内只剩 [2]）=前端失效拦截存在价值实证（防静默丢关联）** | **P24 管理服务已落地 `services/action/agv-action-group-manage.service`（owner=P24，4 operation）**；P20 工艺模板按组消费经 P03 代建共享选项服务 fetchAGVActionGroups（组内动作 id 集合即提交形态） | **带令牌联验已通过（2026-09-20：权限四码/分页平铺/增改删+重排落库/删除自清理终态 base=0 final=0 零残留）**；拖拽手势 IAB 合成事件无法激活=环境受限登记（listeners 挂载已实证，协议层 API 覆盖，真实指针拖拽留现场复核） |
| 任务工艺 | 管理 `GET /dispatcher/orderTemplate/pageOrderTemplates`（GET+query 平铺，query=模板名称或编号）/`POST createOrderTemplate`（body=OrderTemplateParam：orderTemplateName/appointVehicleKey/appointVehicleGroupKey（语义互斥二选一，可均空）/orderMissions[]）/`updateOrderTemplate`（body=OrderTemplateUpdateParam：int64 id+同创建，整模板替换）/`deleteOrderTemplate`（**body {orderTemplateKey} 按调度 key 定位——与 P23/P24 按 int64 id 定位不同族**）；子任务=OrderMissionTemplateParam{mapId,stationId,actions[]}，动作=ActionParam（actionType/actionDescription/blockingType NONE|SOFT|HARD/conditionStr（旧表单无此字段，回显原样保留）/actionParameters[]）；选项 getOrderTemplates（P03 代建共享服务 fetchOrderTemplates 供 P21）；createOrderRecords 旧页零 UI 消费者不落地 | 分页 GET+query（pageNo 从 1 计数）；**后端允许模板重名（同名两次 create 均 200 落库，与 P24 组名 5000000 相反）**；appointVehicleGroupKey/appointVehicleKey 互斥语义受理（前端切换互斥清理+提交兜底分组优先）；**update 整模板替换落库无 G18 类缺陷**（改名+子任务/动作/参数全生效） | **P20 管理服务已落地 `services/order-template/order-template-manage.service`（owner=P20，4 operation）**；P21 关联模板选择按第 5 节 orderTemplate 共享行消费 fetchOrderTemplates | **带令牌联验已通过（2026-09-20：权限五码/分页平铺/增改删+复制语义/删除自清理终态零残留；脚本倒序清理陷阱教训见 tasks/P20.md §3）** |
| 系统版本 | `GET /systemVersion/getSystemVersions`（全集无分页参数，客户端切片分页 rowCount=全集长度）/`POST restartSystem`/`rollback`/`deletePendingJar`/`downloadSystemVersionJar`（均 body SystemVersionParam{buildId}，下载走 Blob 通道）/`uploadSystemVersion`（multipart 字段 file）——六 operation 全落地 `services/system/system-version`（owner=P25）；G08 替代核实：旧 pageSystemVersions/updateJarRestart 新 OpenAPI 不存在，消费点由 getSystemVersions/restartSystem 替代 | 列表项 gitBuildId 定位键；type 枚举 PENDING/CURRENT/BACKUP（未知枚举原值展示）；时间字符串秒级 displayDateTime 展示；**deletePendingJar 对不存在 buildId 亦 200（G21 幂等族第四例）**；上传后端校验真实构建产物内容（自建包 7000000 拒绝=失败如实透传）；restartSystem/rollback 真实执行留专用环境 | 仅 P25 消费（页面私有服务）；P31/P32 的 services/system/user、role 不受影响 | operation 逐项核对 OpenAPI；**带令牌联验已通过（2026-09-19：权限六码/全集形态/无令牌对照/上传通道受理/94.7MB 真实包下载 PK 魔数+content-disposition/G21 第四例/终态零残留）** |
| 系统日志 | `GET /systemLog/pageSystemLogs`（GET+query 平铺 G04）/`GET getSystemLogTypes`/`POST downloadSystemLog`（body {logType, logNames[]} → zip Blob）——三 operation 全落地 `services/system/system-log`（owner=P26） | **类型集合为模块名 vehicleState/auth/device/dispatcher/driver/imap/system（旧 NORMAL/ERROR 枚举不再下发，且缺 logType 后端返回空 data=必传条件）**；列表项 name/time（time 毫秒字符串，displayDateTime 秒级展示）；下载错误形态：不存在日志名 HTTP 200+JSON 7000130「导出的系统日志文件为空」且仍带 content-disposition 附件头（错误文件识别高危形态，请求层已修复统一解包） | 仅 P26 消费（页面私有服务） | operation 逐项核对 OpenAPI；**带令牌联验已通过（2026-09-19：权限两码/分页筛选形态/无令牌三通道 1000000/31.6MB zip PK 魔数+content-disposition/终态零残留）** |
| 操作日志 | `POST /common/sysLog/pageSysLogs`（POST+JSON body，OpenAPI requestBody required，P06 carrier 同形态）——单 operation 落地 `services/system/sys-log`（owner=P28） | SysLog 14 字段契约完整（requestUri/requestMethod 旧版未展示不新增列）；id int64 联验实测安全整数自增（stringFieldRowId 雪花守卫保留）；requestTime 恒 23 字符毫秒串 yyyy-MM-dd HH:mm:ss.SSS，displayDateTime 秒级展示；**targetName 后端为精确匹配（旧源码注释「模糊查询」不符，差异登记）**；logType/module 未知枚举值后端 500 拒绝（前端仅发受控枚举） | 仅 P28 消费（页面私有服务） | operation 已核对 OpenAPI；**带令牌联验已通过（2026-09-20：权限码/POST 形态/筛选命中空态/时间边界三态/无令牌对照/终态零残留）** |
| 软件授权（license） | `POST /auth/license/getLicense`（无 body，查询性质 POST，data=LicenseProof{hardwareInfo,agvNumber,issueDate,expirationDate,activationCode}）/`GET getHardwareInfo`（P02）/`POST softwareActivation`（P02）——三 operation 全落地 `services/license-activation`（owner=P02 建 P29 增 fetchLicense+DTO）；**真实实证（2026-09-20，192.168.0.158）**：activated=true 环境下 getLicense 返回「有激活无授权详情」空记录（data 非 null 但五字段全空）——页面按缺失留白/未知状态呈现，非空记录到期字符串口径留待有授权数据环境复核 | P29（已消费） | 已带令牌联验通过（权限两码 system:software:view/activate 下发；无令牌 1000000；softwareActivation 零写尝试留专用环境） |
| 数据库备份（dataBase） | `GET /dataBase/getDataBases`（无参→库名 string[]）/`GET getDataBaseBackupFiles?database=`（全集无分页参数）/`GET downloadDataBaseBackupFile?database=&backupFileName=`（双 query 均 required→Blob）——三 operation 全 GET，落地 `services/system/database-backup`（owner=P30）；与旧页面 a 标签 GET 同形态（旧 api POST 版本为死代码不迁移）；**真实实证（2026-09-20）：下载流式响应无 Content-Length（application/octet-stream）→前端恒走不确定进度路径（G11 降级）；文件字节 PGDM 头（PostgreSQL dump）；content-disposition ASCII filename；无令牌下载返回业务码 7000140 直达业务逻辑=安全观察项（该端点不做会话校验，P27 公开族同型观察，待后端确认是否补鉴权；前端正常带令牌不受影响）** | P30（已消费） | 已带令牌联验通过（权限两码 system:database-backup:view/download 下发；fms+rcs_flow 两库 20 条五字段齐备（fileSize 字节+fileSizeReadable 可读串+createTime 毫秒串 23 字符）；缺参 500/不存在文件名 7000140 如实记录；零写副作用零残留） |
| 用户/角色（auth） | `GET /auth/user/pageUsers`（GET+query 平铺 pageNo/pageSize/query，G04 P12 先例同形态）/`POST addUser` body {username,password:MD5,confirm:MD5}（旧实现三字段等价形态；**联验形态对照：OpenAPI 两字段与三字段后端均受理，confirm 提供时后端校验一致性，不一致 500「失败」**）/`POST updateState` body {username,state}（按 username 定位）/`POST resetPassword`·`deleteUser` body {id}/`POST assignRoles` body {userId,roleIds}（整组提交=整体替换语义，空数组清空实证）/`GET /auth/role/getRoles?userId=`（不带 userId=全部角色，带=该用户已分配，双查回显语义）——七 operation 落地 `services/access-user`（owner=P31；getRoles 为角色选项族唯一 operation，P32 复用）；**真实实证（2026-09-20）**：id int 安全整数（用户量小）、pageUsers 响应含 password（MD5 摘要）+level 字段=**安全观察项登记**（前端 DTO 类型保留完整、UI 不消费不展示，建议后端脱敏）、业务码 1000140 用户已经存在/1000130 用户名格式不正确/1000000 无令牌（pageUsers+addUser 双通道） | P31（页面+写操作族）；P32（getRoles 复用）；P43（profile 用户选项核查时复核） | 已带令牌联验通过（API 层 21/21+浏览器整页，2026-09-20；addUser 首轮暂态 500 登记 G20 第二例） |
| 工艺模板 | `GET /dispatcher/orderTemplate/getOrderTemplates` | 无（全量） | P03/P21 | operation 已核对，服务随 P03 |
| 驱动（电梯/充电桩/自动门/风淋门/交通灯） | `GET /device/elevator/getElevatorDrivers`；`GET /device/chargePile/getChargePileDrivers`；`GET /device/autoDoor/getAutoDoorDrivers`；`GET /device/airShowerDoor/getAirShowerDoorDrivers`；`GET /device/trafficLight/getDrivers` | 无（全量） | P14–P18 | operation 已核对；电梯驱动服务已落地 `services/device-elevator`（owner=P14，经 useStaticOptions 消费），P14 带令牌联验 2 条真实驱动；自动门驱动服务已落地 `services/device-auto-door`（owner=P15），P15 带令牌联验 1 条真实驱动（MODBUS_AUTO_DOOR）；充电桩驱动服务已落地 `services/device-charge-pile`（owner=P16），P16 带令牌联验 1 条真实驱动（中力充电桩 ZHONG_LI_CHARGE_PILE，driverProtocol={localPort,isDebugMode}）；交通灯驱动服务已落地 `services/device-traffic-light`（owner=P17），P17 带令牌联验 1 条真实驱动（http-交通灯 HTTP_TRAFFIC_LIGHT）；风淋门驱动服务已落地 `services/device-air-shower`（owner=P18），P18 带令牌联验 1 条真实驱动（Modbus-tcp风淋门 MODBUS_AIR_SHOWER_DOOR，driverProtocol 含 slaveId/faultSignal 等真实协议结构）；其余随各自设备页 |
| 充电桩 | 管理 `GET pageChargePiles`（GET+query 平铺，query=名称模糊）/`POST addChargePile`/`updateChargePile`（原 deviceKey 定位+全量 ChargePileParam）/`deleteChargePile`/`startCharge`/`stopCharge`（均 DeviceParam，doorWay 为电梯语义可选字段充电桩不携带） | 分页 GET+query（pageNo 从 1 计数）；命令类均为 POST JSON body | **P16 管理服务已落地 `services/device-charge-pile`（owner=P16，7 operation）**；**无 getChargePileState 独立状态查询——充电桩状态随分页行记录内嵌（deviceChargePileState.chargePileState）下发，列表刷新即状态重查（旧实现同形态）**；getChargePileDetail 旧仓库无引用不落地；getAllChargePiles 旧仓库唯一消费者为 H02 暂缓页（地图编辑器第三方设备标识选择器）随其落地；命令族仅开始/停止充电（无开关门/清占用/呼叫语义）；状态枚举 ERROR/IDLE/CHARGING/FULL/FAULT/OFFLINE 沿用旧系统 ChargeState 既定映射（错误/空闲/充电中/充满/错误/离线） | operation 逐项核对；P16 带令牌联验：五权限码下发/pageChargePiles 平铺/增改删全字段落库（无 G18 类缺陷）/行内嵌状态真实响应 state=OFFLINE 形态实证/删除自清理终态零残留；开始/停止充电命令执行留专用环境（现场充电设备副作用） |
| 交通灯 | 管理 `GET pageTrafficLights`（GET+query 平铺，query=名称或唯一key）/`POST addTrafficLight`/`updateTrafficLight`（原 deviceKey 定位+全量）/`deleteTrafficLight`/`testTrafficLight`（delete/test 均DeviceTrafficLightParam 仅 deviceKey） | 分页 GET+query（pageNo 从 1 计数）；命令类均为 POST JSON body | **P17 管理服务已落地 `services/device-traffic-light`（owner=P17，6 operation）**；deviceConfig 为通用 map（additionalProperties: object），旧 UI 键集 {url, requestParam, responseSuccessExpression} 原样承载，新契约顶层 ip/port/deviceStatus 三字段旧 UI 无、不发明 UI；getTrafficLights（GET 全量）旧仓库无活跃消费者不落地；测试=向现场设备发起真实连通性请求（A21 设备控制类），code=200 即连通正常、失败透传后端诊断 | operation 逐项核对；P17 带令牌联验：五权限码下发/pageTrafficLights 平铺/增改删全字段落库（无 G18 类缺陷）/测试真实失败诊断 code=8000010「HTTP请求异常」形态实证（对象为自建记录+回环拒绝端口）/删除自清理终态零残留 |
| 风淋门 | 管理 `GET pageAirShowerDoors`（GET+query 平铺，query=名称或标识）/`GET getAirShowerDoorState`（deviceKey query，响应 AirShowerDoorState 五字段：onlineState/showerState/failed/frontDoorState/backDoorState）/`GET getAirShowerDoorDrivers`/`POST addAirShowerDoor`/`updateAirShowerDoor`（原 deviceKey 定位+全量）/`deleteAirShowerDoor`/`openDoor`/`closeDoor`（delete/open/close 均 DeviceParam，open/close 的 doorWay=FRONT/BACK 双门语义必填） | 分页 GET+query（pageNo 从 1 计数）；命令类均为 POST JSON body | **P18 管理服务已落地 `services/device-air-shower`（owner=P18，8 operation）**；提交体 AirShowerDoorParam 六字段无 deviceStatus/showerStatus（旧表单无此输入不发明 UI，后端默认 true）；shower/clearAirShowerDoorOccupy/getAirShowerDoors 旧仓库无本页可达消费者不落地（清占用与风淋在旧 items 不可达、全量选项唯一消费者为 H02 暂缓页）；开门/关门=弹窗选门类型后 confirmCommand 确认发送（DoD8 升级），受理≠完成以 getAirShowerDoorState 核验 | operation 逐项核对；P18 带令牌联验：五权限码下发/pageAirShowerDoors 平铺/增改删全字段落库（无 G18 类缺陷）/状态五字段真实形态 UNKNOWN+OFFLINE 实证/开门真实失败诊断 code=8010040「设备驱动模式配置异常」形态实证（对象为自建记录+回环拒绝端口）/openDoor 缺 doorWay 后端 500 实证/删除自清理终态零残留 |
| 自动门 | 管理 `GET pageAutoDoors`（GET+query 平铺）/`GET getAutoDoorState`（deviceKey query，响应 AutoDoorState=doorState 六枚举+onlineState）/`POST addAutoDoor`/`updateAutoDoor`（原 deviceKey 定位+全量）/`deleteAutoDoor`/`openDoor`/`closeDoor`/`clearAutoDoorOccupy`（均 DeviceParam，doorWay 为电梯语义可选字段自动门不携带） | 分页 GET+query（pageNo 从 1 计数）；命令类均为 POST JSON body | **P15 管理服务已落地 `services/device-auto-door`（owner=P15，9 operation）**；getAutoDoors 全量选项暂无活跃消费者不落地（旧仓库仅历史 AutoDoor 实现与 H02 暂缓页引用）；与电梯命令族差异：无 outerCall/innerCall（呼叫为电梯独有）、开关门无 doorWay 录入（单门） | operation 逐项核对；P15 带令牌联验：五权限码下发/pageAutoDoors 平铺/增改删全字段落库（无 G18 类缺陷）/getAutoDoorState 真实响应 doorState=UNKNOWN 枚举原值/删除自清理终态零残留；开门/关门/清占用命令执行留专用环境（现场设备副作用） |
| 电梯 | 选项 `GET /device/elevator/getElevators`；管理 `GET pageElevators`（GET+query 平铺）/`GET getElevatorState`（deviceKey query）/`POST addElevator`/`updateElevator`（原 deviceKey 定位+全量）/`deleteElevator`/`outerCall`（ElevatorOperationParam 仅 deviceKey+currentFloor）/`openDoor`/`closeDoor`（doorWay=FRONT/BACK）/`clearElevatorOccupy`（DeviceParam） | 分页 GET+query（pageNo 从 1 计数）；命令类均为 POST JSON body | P10（选项已消费，fetchElevatorOptions owner=P10）；**P14 管理服务已落地 `services/device-elevator`（owner=P14，10 operation）**；**G06：innerCall 未出现在 OpenAPI，服务层不提供**，页面以灰色标记入口+点击说明呈现（零请求）不用外呼猜替 | operation 逐项核对；P14 带令牌联验：五权限码下发/pageElevators 平铺/增改删全字段落库（无 G18 类缺陷）/getElevatorState 真实响应含 UNKNOWN 枚举原值与 null 缺失/删除自清理终态零残留；外呼/开关门/清占用命令执行留专用环境（现场设备副作用） |
| 多地图点边组合 | `GET+POST /dispatcher/systemNodeEdgeGroup/*`（pageSystemNodeEdgeGroups GET+query / createSystemNodeEdgeGroup / updateSystemNodeEdgeGroup（int64 id 定位+mapNodeEdgeGroupIds 整体替换）/ deleteSystemNodeEdgeGroup / getAllSimpleNodeEdgeGroups 全部地图点边组合选项） | 分页（pageNo 一基+query 名称模糊）；其余全量 | P11（已消费）；P19（交管边组合选择将引用 getAllSimpleNodeEdgeGroups 与组合实体契约） | operation 逐项核对 OpenAPI；服务已落地 `services/node-edge-group/node-edge-group.service`（owner=P11）；P11 带令牌联验五 operation 全链路（创建顺序保持/编辑整体替换/删除幂等 G21 扩展/选项 19 条真实数据；空集合创建后端 500 登记 G23）；**P19 引用契约已确认：LittleSimpleMapNodeEdgeGroup{id:string,name:string}，组合实体 SystemNodeEdgeGroup 嵌套明细结构见 P11.md 第 5 节** |
| 地图推送记录 | `GET+POST /dispatcher/mapPushRecord/*`（pageMapPushRecords GET+query 平铺 / rePushMap / cancelPushMap（body=MapRePushParam：mapPushRecordId+可选 mapPushSubRecordIds；记录级不传子记录 id=整批生效，子记录级传单元素数组）） | 分页（pageNo 一基，无筛选/排序参数 G09）；子记录全量随主行下发（无子级查询接口） | P12（已消费）；P09（PushVersionModal 推送受理语义的最终状态核实页） | operation 逐项核对 OpenAPI；服务已落地 `services/map-push-record/map-push-record.service`（owner=P12）；P12 带令牌联验查询链路（GET 平铺 G04 同款/My-Plus 形态/total=0 真实空态/越界页码/无令牌 1000000 对照）；重推/取消命令执行留专用环境（车辆副作用）；OpenAPI MapPushSubRecord 保留 cancelReason 字段与 waitReason 并存（旧实现已改用 waitReason，前端原样承接不丢） |
| 调度配置 | `GET+POST /dispatcher/taskConfig/*`（getTaskConfigs GET 一次全量树形（configType 分组+childTaskConfigs）/ batchEditConfigs POST body=TaskConfigEditParam 数组（configKey+configValue 字符串；select 多选 ";" 连接）；editConfig POST 单条旧实现未消费不迁移） | 无分页无筛选（G09 不开放排序）；configValueType 协议无枚举 string，前端收敛 int/bool/enum/select/double/string 六型，未知只读不猜；后端范围校验为最终防线（范围外 3000620 拒绝实证），前端 InputNumber min/max 为第一道 | P13（已消费） | operation 逐项核对 OpenAPI；服务已落地 `services/dispatch-config/dispatch-config.service`（owner=P13）；P13 带令牌联验：getTaskConfigs 6 分类 44 项（int21/bool17/double2/string4）/batchEditConfigs 真实提交×2 终态零净变化+落库复核/G13 预读冲突中止真实验证（页面内 fetch 模拟他人修改）/3000620 越界拒绝；重置=草稿回退基线（旧版恢复默认值写后端语义按 TASKS.md 验收明文废弃，见 tasks/P13.md 第 4 节） |
| 避障模板 | `GET+POST /dispatcher/obstacleAvoidance/*`（pageObstacleAvoidance GET+query 平铺 / createObstacleAvoidance / updateObstacleAvoidance（int64 id 定位+parameters 整体替换）/ deleteObstacleAvoidance（OpenAPI 声明 id 为 required query 参数，POST+?id= 实证接受）） | 分页 GET+query（pageNo 从 1 计数，query=避障策略名称模糊）；parameters 数组元素 {name（模板名简中 key 原样提交）, avoid（`${模板key}_index_N` 形态标识）, enable（boolean）}；避免破坏历史数据回显，value 规则不得变更 | P22（已消费）；getObstacleAvoidanceList（GET 全量）旧仓库唯一活跃消费者为 H02 暂缓页（地图嵌套修改安全区面板），随其落地不重复提供 | operation 逐项核对 OpenAPI；服务已落地 `services/obstacle-avoidance/obstacle-avoidance.service`（owner=P22，4 operation）；P22 带令牌联验：四权限码下发/分页平铺（G04 口径再实证）/增改删全字段落库（无 G18 类缺陷；parameters 数组顺序保持+enable boolean 正确）/筛选命中与未命中/删除自清理终态零残留 |
| 角色/权限资源（auth） | `GET /auth/role/pageRoles`（GET+query 平铺 pageNo/pageSize/query）/`POST addRole`·`updateRole` body {…,state:ENABLED\|DISABLED}/`POST delete?id=`（**POST+query 参数 body 空对象**=旧实现同形态）/`GET /auth/permission/getPermissions?roleId=`（不带=全部资源树 9 顶层 144 节点，带=该角色已分配树）/`POST assignPermissions` body {roleId,permissionIds}（授权集合 S 整组提交=整体替换）——六 operation 落地 `services/access-role`（owner=P32）；getRoles 选项族归 services/access-user（owner=P31）不变；**真实实证（2026-09-20）**：回显树按结构组装、授权集合须含祖先（前端三态 D4 不变量天然满足）；空半选父节点（仅父级 id 授权）原样返回 childPermissions=[]（**SPEC R3/R5 后端契约确认成立，空半选可回显**）；权限时效=下次登录生效（旧 SPEC B15），不伪造即时刷新 | P32（已消费）；P31 RoleAssignModal（getRoles 消费关系不变） | P32 带令牌联验通过（API 层 26/26+回显契约对照实验 3+1 项+浏览器三态九用例，2026-09-20） |
| 三方交管（dispatcher） | `GET /dispatcher/tripartiteTraffic/pageTripartiteTraffics`（GET+query 平铺 pageNo/pageSize/areaCode，areaCode 语义=区域编号或占用系统）/`POST addTripartiteTraffic`·`updateTripartiteTraffic`·`deleteTripartiteTraffic`（**三者均 body 完整实体 TripartiteTraffic；删除=旧实现整行提交同形态，区别于 ?id= query 族**）/`GET /dispatcher/map/getSimpleTripartiteTrafficEdgeGroups`（无参选项 {id,name,mapId,mapName}；路径挂 map 控制器、owner=P19；**与 P11 getAllSimpleNodeEdgeGroups 为不同 operation 数据源不同——同刻 0 vs 19 条登记观察项，建议与后端确认语义边界**）/`POST testCommunication` body {applyType:APPLY\|RELEASE, areaCode, systemCode:"rxx"}（systemCode 旧实现同款常量）——六 operation 落地 `services/tripartite-traffic`（owner=P19）；**真实实证（2026-09-20）**：模拟测试双业务码 3200150「当前不是外部系统作为仲裁方」（isExternalArbitrator=false 前置拦截）与 3200120「三方交管外部请求失败,ConnectException」（true 时后端真实按 extendParam.url 向外部发起 HTTP 请求，联验以回环拒绝端口限定副作用）——前端均如实透传反馈=专项验收「依据真实结果反馈」 | P19（页面+写操作族+模拟测试）；P43（菜单键回归时复核） | P19 带令牌联验通过（API 层 24/24+补验 2 项+浏览器整页，2026-09-20；新增提交 UI 链因本环境点边组合选项空集合受限，API 落库复核覆盖详见 tasks/P19.md §5） |
| 品牌 Logo | `GET /systemLogos`（listMeta）；二进制 `GET /systemLogos/{placementKey}/file`；上传 `PUT /systemLogos/{placementKey}`（multipart 字段 file，OpenAPI upsert）；不落地：getMeta/DELETE/fileBase64（旧 UI 零消费者，DELETE 仅联验自清理用过） | **接口族无需认证（联验实证：无令牌 PUT 受理 code=200、无令牌 GET file 无资源 404=资源状态非会话拒绝）——登录前可用装饰资源的设计语义，前端入口由菜单码+按钮码控制**；后端不校验图片内容（伪图片受理，展示层 Blob 校验+img onError 双防线不伪装有图）；展示位三键 headerLogo/loginBackground/favicon（旧 ImageSettings 同源） | P01/P27 | operation 已核对（T00.3 实证无令牌 code=200）；服务 `services/system/brand`（P01 建查询/二进制，P27 扩展 upsert+品牌变更通知）；**P27 带令牌联验通过（上传字节一致/getMeta 元数据/伪图片行为/终态零残留；浏览器三卡预览+顶栏 logo+动态 favicon+登录页背景刷新闭环；2026-09-19）** |

### 5.1 只读地图共享组件（T00.7 已落地）

| 项 | 契约 | 状态 |
| --- | --- | --- |
| 组件 | `src/components/ReadOnlyMap/ReadOnlyMap.tsx`（默认导出供 React.lazy 按需加载，konva 随组件进异步 chunk）；能力=只读渲染/平移缩放/节点与路径选点/定位高亮/回填回调；不引入地图编辑、监控、回放、车辆图层（H01/H02 范围） | 已落地（T00.7） |
| 数据供给 | `mapId`（内部 `fetchMapGraph`：加载/失败/空态闭环，失败渲染 StateBlock offline + 显式重试）或 `graph` 外部直供（跳过请求） | 已落地；getMapInfo 真实联验随 P07（G03 凭据） |
| 选中与回填 | `onChange(MapSelectedItem[])` 携带完整节点/路径 DTO；`initialSelectedIds` 回显（失效 ID 静默忽略，上层以原值标注不可用）；`ref` 暴露 fitView/zoom/clearSelection/setSelectedIds/focusNode/focusEdge/reload | 机制已实证（浏览器：回显回调携带完整 DTO、点击命中回填）；fitView/闪烁动画随 P07 真实浏览器联验 |
| 依赖 | `react@19.3.0`（minor 升级，满足 react-konva peer）+ `react-konva@19.3.0` + `konva@^9.3.20`（与旧系统实装版本一致；react-konva peer 支持 ^9；konva@10 未经本轮等价验证故不采用）；`vite.config.ts` optimizeDeps.include 预声明 react-konva（避免动态发现优化导致的二次 reload 与陈旧产物双 React 实例） | 已安装并锁定（T00.7）；组件在本轮浏览器机制验证中真实渲染（三层 canvas + 选中环像素级确认） |
| 命名空间 | 地图组件文案走共享 `map` 命名空间（en-US 分片已交付；zh-TW/ja/ko 回退简中已登记）；消费页面需在路由 meta.i18nNamespaces 声明 `'map'` | 已落地（en-US 6 键） |

## 6. 实体导航契约（owner：T00，页面任务接入）

| 项 | 契约 | 状态 |
| --- | --- | --- |
| 任务详情 | `/order-info?orderKey=<任务编号>`（命名参数，裸值 `?KEY`/`?KEY=` 历史兼容；构造/解析单一真相 `features/order-detail/orderDetailNavigation.ts`）；默认工作区页签（路由在受保护根内）+「独立窗口」按钮；快速预览弹窗复用 OrderDetailPanel 详情业务组件；参数名 orderKey 已按旧调用点核对（旧源码无导航调用点，直访裸值形态） | 已验证（P38 带令牌联验 2026-09-18） |
| 车辆详情 | `/vehicle-info?vehicleKey=<车辆唯一标识>`（命名参数，裸值 `?KEY`/`?KEY=` 历史兼容；构造/解析单一真相 `features/vehicle-detail/vehicleDetailNavigation.ts`）；默认工作区页签（路由在受保护根内）+「独立窗口」按钮；可见轮询 useVisiblePolling（实时状态页）；列表抽屉快速预览不替代完整详情路由（footer「完整详情」入口由宿主页 VehicleDisplay 组装跨域导航，features 域间禁导入）；参数名 vehicleKey 已按旧调用点核对（旧源码无导航调用点，直访裸值形态） | 已验证（P39 带令牌联验 2026-09-18；P05 往返的抽屉点击链路受 IAB rAF 冻结阻塞，两端点已分别实证，登记 P39.md） |
| 首页落点 | 登录后优先 `/dashboard`（P34 合并首页）；无权限进首个有权限且可用业务页；无可用页明确反馈 | 已确认（D29） |
| 暂缓页 | 统一「本期暂未迁移」说明组件，保留合法上下文与原权限 | 已确认（D07） |

## 7. i18n / 时区契约（owner：T00，T00.8 已落地、T00.9 冻结）

| 项 | 契约 | 状态 |
| --- | --- | --- |
| 语言 | 五语言 `SUPPORTED_LANGUAGES = ['zh-CN','zh-TW','en-US','ja-JP','ko-KR']`（`src/i18n/i18n.ts`）；中文 key 即文案（keySeparator/nsSeparator=false）；命名空间按「语言 × 命名空间」懒加载，查无 loader 返回空资源经 fallbackLng 回退简中 | 已落地并经真实浏览器五语言全链路实证（T00.8） |
| 归一化 | `normalizeLanguage(raw)`：en*→en-US、ja*→ja-JP、ko*→ko-KR；繁中变体（zh-tw/zh-hk/zh-mo/zh-hant*）精确保留 zh-TW 不并入简中；zh-cn/zh/zh-hans/未知回退 zh-CN。全部语言读取路径（存储/迁移/回滚）统一经此收敛 | 已落地（繁/日/韩不误映射经注入值实证，T00.8） |
| 语言偏好 | 单一真相源 localStorage `apex-admin:lang`，唯一读取入口 `readStoredLanguage()`；同源旧 key `umi_locale` 一次迁移（写新删旧不留双源）；settings 持久化恢复以独立语言 key 为准（持久化镜像不反向覆盖）；退出登录保留语言偏好 | 已落地并实证（含 fr-FR 非法残留归一化不挂起，T00.8） |
| 切换 | 统一走 `changeAppLanguage(lang)`：预加载 base+已开页签命名空间 → i18next/antd ConfigProvider/dayjs/`document.documentElement.lang`/请求层 `Accept-Language`（含上传通道）同帧切换；预加载失败抛错不改状态，App catch 回滚到实际语言（失败保留原语言） | 已落地（真实网络失败场景随页面任务联调） |
| 第三方 locale | antd `ANTD_LOCALES` 五语言映射（App.tsx，typecheck 强制全key）；ApexTableReact 五语言 locale 包（第 3 节）；dayjs 五 locale 静态注册；控件弹窗按钮文案随首个消费者页面联验 | 已落地；控件文案联验随 P03 等 |
| 页面命名空间 | 各页归属终版见 `i18n-map.md`「各页命名空间/文件归属终版（T00.9 冻结）」；页面在路由 meta.i18nNamespaces 声明，分片四语言随页面任务交付 | 已冻结（T00.9） |
| 时区 | 部署时区优先，缺省 Asia/Shanghai，环境配置提供；`DEPLOY_TIMEZONE` 常量唯一定义点 `src/constants/datetime.ts`，页面不得各自硬编码；dayjs utc/timezone 插件单点装配于 `src/utils/datetime/deployDayjs.ts`；解析/展示纯函数 `parseBackendDateTime`/`formatInDeployTimezone`/`displayDateTime`（`src/utils/datetime/datetimeDisplay.ts`）：墙钟字符串按部署时区、带偏移换算、纯日期=部署时区零点、无法识别返回 null 不猜 | 已落地（D23/T00.7）；带偏移时间真实样本联调随报表页 |

## 8. 契约变更日志

| 日期 | 契约 | 变更 | 操作者 | 消费者影响 |
| --- | --- | --- | --- | --- |
| 2026-09-17 | 全部 | 初始化框架 | T00 run-20260917-121624-7468 | — |
| 2026-09-17 | 请求基础路径/代理/时区/表格组件 | `DEFAULT_API_BASE_URL` `/api/v1`→`/fms/v1`；dev 代理默认目标 `10.11.2.67:8888`、`/fms` 不改写；新增 `DEPLOY_TIMEZONE`（`VITE_DEPLOY_TIMEZONE`）；安装 `apex-table-react@0.1.0` | T00（T00.2 run） | 现有模板 service 的相对请求路径随之指向 `/fms/v1`；模板旧协议（envelope/错误形状）仍待 T00.3 重写，期间模板页面联调不可用属预期 |
| 2026-09-17 | 请求协议/认证/会话 | T00.3 落地：Result 解包（code=200）、业务码常量（1000000/1001000/1000010）、ApiError 带 businessCode、文件通道透传、Bearer + Accept-Language 头、移除 refresh/me/logout(旧路径) 假定、会话持久化（auth schema v1→v2）、authBridge 令牌同步与多窗口退出、profile 虚构接口删除；`DEFAULT_PAGE_SIZE` 保留为通用默认；安装 spark-md5@3.0.2 | T00（本轮 run） | `api` 调用方语义变化：返回值为 Result.data（非原始 body）、code≠200 抛 `ApiRequestError`；auth 会话消费方（Header/Profile/useLogin/guard）已同步适配；删除 `userPatched`/`loadSession`/`MeResponseDto`/`API_ERROR_CODES` 等模板导出；`EntityStatus`/`PageQuery`/`PageResult` 保留为模板遗留类型（已标注不得在新代码引用，随 P03/P31/P32/P34 替换后删除） |
| 2026-09-17 | 权限与路由 | T00.4 落地：权限码常量（PERM/PERM_BUTTON/ROOT_ONLY_CODES，码值与旧系统逐一核实对齐）、权限纯函数与路由访问核心（超管短路/祖先填充/落点解析/回跳校验）、定义树 meta.perm 挂码 + migrationPending/public 标记、认证+权限守卫覆盖独立页、目录与首页动态落点、迁移过渡占位（MigrationPending，pending 页不加载页面代码）、DockMenu 权限剪枝、affix 播种按权限过滤、usePermission 按钮码 hook、LoginForm 落点接入；连带修复 T00.3 缺陷：store migrate 无条件重置（持久化恢复失效）与 authBridge 监听键名缺 persist: 前缀（多窗口退出同步失效） | T00（本轮 run） | 全部路由 meta 增加 perm 字段（页面任务按钮权限经 usePermission 消费 PERM_BUTTON）；页面任务完成后由统筹移除本页 migrationPending 标记（definitions.tsx 单点）；菜单消费者必须传访问上下文（buildMenuRoutes 签名变更，DockMenu 已适配） |
| 2026-09-17 | 表格接入形态 | 用户决策：**禁止对 apex-table-react 二次封装**（撤销 T00.5 首轮薄适配组件方案，相关代码已全部撤回）；页面直接使用 ApexTableReact 公开 API（组件/locale/官方列偏好适配器/插槽/ref）；公共设施仅限五语言 locale 包（包内仅内置 zhCN）、主题 `--apex-table-*`→`--app-*` 变量映射、pageIndex→pageNo 换算与行 ID 纯函数、不包裹表格的独立状态块；规格 6.1 与 TASKS DoD#4/T00.5/§2.2 已同步修订 | 用户（本轮 run） | 全部表格页面任务直接 import apex-table-react 并自行组装 props；公共资源的具体文件位置与形状在 T00.5 交付时登记 |
| 2026-09-17 | 表格公共资源落地 | T00.5 交付：五语言 ApexLocale 包（`src/i18n/locales/apexTable/` + `resolveApexLocale`/`useApexLocale`）、主题映射（globals.css `:root .apex-table`，亮暗成对）、分页换算 `toBackendPage`、行 ID `stringFieldRowId`、列偏好约定 `createTableColumnPreferences`/`useColumnPreferences`、统一状态块 `StateBlock`（noPermission/gap/offline）；公开 API 核验（虚拟化/展开/编辑/双 ref）经类型探针全量组装验证后删除探针 | T00（本轮 run） | P03/P05 起全部表格页按 contracts.md 第 3 节约定组装：`useApexLocale()` 供 locale、`toBackendPage` 供 request 分页、`stringFieldRowId` 供 getRowId、`useColumnPreferences` 供列偏好、StateBlock 供失败/无权限/缺口区域；zh-TW/ja/ko 的 StateBlock 文案回退简中已登记 i18n-missing.md，T00.8 补齐 |
| 2026-09-17 | 共享业务能力落地（T00.7） | ① 时间/时区：`deployDayjs`（utc/timezone 单点装配）+ `datetimeDisplay`（parseBackendDateTime/formatInDeployTimezone/displayDateTime，缺失/不可解析→'—' 不猜）；② 统计空值/单位：`src/utils/stats/metricFormat.ts`（0 与缺失严格区分、safeRatio 除零→null、formatPercent/formatWithUnit/secondsToHours，公式归页面 owner）；③ 控制确认/未知/批量反馈：`src/utils/command/`（CommandOutcome 三态、confirmCommand 列对象+影响+提交≠完成附注、summarizeBatchOutcomes/summarizeWholeBatch/formatBatchSummaryText，缺项计入未知、整批不编造逐项）；④ 选项失效：`src/utils/options/optionFallback.ts` + `src/hooks/useStaticOptions.ts`（scope 取消/防乱序/失败清空）；⑤ 只读地图：`src/services/map/`（getMapInfo/getSimpleMaps）+ `src/components/ReadOnlyMap/`（11 文件，能力见 5.1 节）；⑥ 依赖：react 19.2.8→19.3.0、@types/react 19.3.0、konva@^9.3.20、react-konva@^19.3.0、vite optimizeDeps 预声明 react-konva | T00（本轮 run） | 消费者：实时页/详情页轮询不变；P07 起地图页消费 ReadOnlyMap+useMapGraph+map 命名空间；选项页自 P03 起消费 useStaticOptions；写操作页消费 confirmCommand/批量归纳；报表页消费 metricFormat 与 datetimeDisplay；common 新增 8 键、新增共享 map 命名空间（消费页面 meta.i18nNamespaces 声明） |
| 2026-09-17 | 页签/草稿/轮询/传输契约落地 | T00.6 交付：`tabsSlice` 增加 dirty 标记（LRU 豁免脏页签）、`useTabDirtyGuard`（页面草稿登记）、`useTabActionGuard`（关闭/刷新/批量关闭/退出登录统一确认，三档传输提示策略）、`beforeunload` 脏页签离开提示、`useVisiblePolling` + `polling.constants`（可见串行轮询/退避集中配置）、`transferManager` + `useTransfers` + `TransferWatcher`（独立传输生命周期/关页提示/孤儿完成消息/会话结束清理）、`openStandaloneWindow`（独立窗口工具）；`RequestScopeValue` 增加 `scopeKey` 字段 | T00（本轮 run） | 实时页面（P34/P05/P39/P40）轮询一律消费 `useVisiblePolling`，禁止私设定时器；文件传输页面（P08/P09/P25/P26/P30 等）一律经 `beginTransfer` 登记并把句柄 signal 传给请求层；全部写草稿页面自 P03 起消费 `useTabDirtyGuard`；消费者注意：`RequestScopeValue` 新增 scopeKey（现有 usePageActive/usePageRequest 消费不受影响） |
| 2026-09-17 | 五语言基座落地 | T00.8 交付：`SUPPORTED_LANGUAGES` 扩五语言、`normalizeLanguage` 精确归一化（zh-TW/ja-JP/ko-KR 不误映射）、语言×命名空间懒加载表（zh-TW/ja-JP/ko-KR 基座五分片 common/menu/auth/error/map 全量，key 集合与 en-US 一致）、`readStoredLanguage` 单一读取入口 + umi_locale 一次迁移（写新删旧）、settings 持久化 migrate 以独立语言 key 为单一真相源（修复镜像覆盖迁移）、`ANTD_LOCALES` 五语言映射 + 切换失败回滚、dayjs 五 locale、html lang 同步、Header 语言菜单五项 | T00（本轮 run） | 页面任务不得绕过 `changeAppLanguage`/`normalizeLanguage` 直调 i18next.changeLanguage；页面私有分片放 `src/i18n/locales/<语言>/<命名空间>.ts` 并在懒加载表登记（命名空间归属见 i18n-map.md T00.9 冻结表）；请求层语言头已含上传通道，页面不得单独读 umi_locale |
| 2026-09-17 | 全表冻结 | T00.9 冻结交接：contracts.md 各节与实际公共导出核对一致（清点见 tasks/T00.md T00.9 节）；第 7 节 i18n 契约更新为落地实况；各页 namespace/文件归属终版冻结于 i18n-map.md；过渡期 mock 退出运行路径复核成立（pending 页不触发 lazy，order.mock/dashboard.mock 不可达，文件本体归 P03/P34 移除）。此后契约变更须在变更日志登记 owner 与全部消费者影响 | T00（本轮 run） | B1（P01）与 B2 样板（P03/P05/P38/P39）所需机制已就绪；真实登录成功路径/带令牌只读联验受阻 G03（测试凭据），B1 放行前必须补验，不以未验机制宣称放行 |
| 2026-09-18 | 业务码 1001000 引导 / 会话激活状态 / 新增 license-activation 域 | P02 交付：① request.ts 新增 1001000 单飞收敛（提示+`apex:activation-required` 事件，`activationGuidanceHandled` 随新令牌复位；授权页自身路径抑制）；② authSlice 新增 `activationConfirmed`（激活成功更新 activated，供落点解析）；③ 新增 `services/license-activation`（getHardwareInfo/softwareActivation，P29 共用）与 `features/license-activation`（ActivationForm/ActivationRedirectListener/useHardwareInfo/copyText，App 根挂载监听）；④ definitions.tsx standaloneMeta 签名扩展 i18nNamespaces，/authorize-ingress 摘除 migrationPending；⑤ i18n 新增 license-activation 命名空间（四语言分片）+ common 四语言各增 1 键 | P02（本轮 run） | 消费者影响：standaloneMeta 其余调用兼容（typecheck 证实）；请求层新增分支不影响既有业务码路径；未激活环境的业务请求（自 P03/P05 起）将触发引导跳授权页；P29 消费 ActivationForm 时路由 meta 须声明 `license-activation` |

| 2026-09-18 | 文件下载通道 + 共享选项服务 + orderRecord 五语言 | P03 交付：① request.ts 新增 `api.downloadGet`（GET+Blob+完整响应，`apexRawResponse` 配置标记；JSON 错误仍走统一解包）与 `resolveDownloadFilename`（RFC 5987），既有消费者行为不变；② P03 代建共享选项服务（contracts 第 5 节唯一 operation）：`services/vehicle`（fetchSimpleVehicles→P04）、`services/vehicle-group`（fetchVehicleGroups→P04）、`services/action`（fetchAGVActions/fetchAGVActionGroups→P24）、`services/order-template`（fetchOrderTemplates，owner=P03 供 P20/P21）、`services/map` 增 fetchMapSites（→P10）；全部经 useStaticOptions 消费，零复制请求；③ orderRecord 四语言分片交付（en-US 旧真译沿用+补译；zh-TW/ja-JP/ko-KR 按 B1 基线补译，「车辆」旧真译迁入）；④ 新增共享 `utils/clipboard`（P02 域内 copyText 未动，后续可收口） | P03（本轮 run） | 消费者影响：请求层扩展向后兼容（typecheck/build 全绿）；选项服务 owner 交接见第 5 节（P04/P10/P24 接手后维护）；order-record 路由 pending 标记移除，本页进入登录落点候选 |
| 2026-09-18 | 表格列设置接入形态（用户决策） | 业务表格开启 `columnSettingsEnabled` 时**序号列默认放出**（不传 `showRowNumber={false}`）：包内把列设置齿轮入口放进序号列表头，行内显示序号；显式关闭序号列会使齿轮退化为表头上方独立工具条行（P03 实测）。序号列是辅助轨道，不进列偏好 columnOrder/columnVisibility 切片。P03 联验同轮修复两处页面接线缺陷：受控切片须随 onChange 更新（否则改动被回弹）；适配器 save 为整体替换语义，必须保存合并后的完整四切片（面板一次确认连发四类回调，单片补丁互相覆盖） | 用户 / P03（联验轮） | 消费者影响：P05 起全部带列设置的表格页照此形态接入（序号列默认放出 + `useColumnPreferences` 四步接线见 columnPreferences.ts 文件头）；第 3 节列偏好行补此交互约定 |
| 2026-09-18 | 车辆管理服务 + 控制交互样板 | P05 交付：① `services/vehicle` 扩展 vehicle-manage 服务（pageVehicles/addVehicle/updateVehicle/deleteVehicle/vehicleOperate/allVehicleOperate/getUnRelationSimpleVehicles，owner=P05；P03 代建 getSimpleVehicles 选项服务不变）；② 控制样板（设备类页面复用）：useVisiblePolling 可见轮询 → confirmCommand 列对象与影响 → verifyVehicleFresh 执行前核验（pageVehicles+query 重查目标，missing/stale 不执行并刷新）→ 发命令；批量整批接受诚实呈现（后端无逐项契约，A15）；③ `vehicleList` 命名空间四语言分片（en 旧真译沿用+补译，繁日韩 B1 基线，「マップ/맵」真译迁入）；④ 路由 vehicle-diplay 解除 pending（历史拼写保留），进入登录落点候选 | P05（本轮 run） | 消费者影响：vehicle-manage 服务可被设备页复用（verifyVehicleFresh 样板工具在 features/vehicle-list/）；本页按钮码消费 PERM_BUTTON.VEHICLE_LIST_*（已登记）；写副作用执行与 P39 独立详情往返登记待验收 |
| 2026-09-18 | 实体页签契约落地 + 任务详情双形态 + 详情参数契约 | P38 交付：① order-info 路由移入受保护根（完整路径 /order-info 不变；hideInMenu 不进菜单/落点候选）——实体页签机制首个消费者，默认工作区页签形态达成，P39/P40 接入时同构迁移；② 详情参数单一真相 `features/order-detail/orderDetailNavigation.ts`：命名参数 `?orderKey=` + 历史裸值 `?KEY` 及页签规范化变体 `?KEY=` 兼容解析；③ 详情业务组件 `features/order-detail/components/OrderDetailPanel`（单请求喂主体+mission 表）由完整页与快速预览弹窗共用——OrderInfoModal/MissionActionsTable 迁入 features/order-detail（跨 feature 禁止导入，弹窗外壳随迁，OrderRecord.tsx 仅改导入路径）；④ 枚举展示常量提升 `src/constants/order/orderDisplayOptions.ts`（orderRecord/orderDetail 两域共同消费，orderRecordOptions 改 re-export）；⑤ 服务层 getOrderRecordDetail 返回类型如实标注可空（任务不存在 code=200+data=null 实证）；⑥ orderInfo 命名空间四语言分片 + order-record 路由 meta 增声明该命名空间（弹窗新按钮文案） | P38（本轮 run） | 消费者影响：实体页签/独立窗口行更新（第 4、6 节）；P39/P40 照此形态迁移路由并核对各自参数；OrderInfoModal 导入路径变化仅 OrderRecord.tsx 一处（已同步）；getOrderRecordDetail 可空语义消费者为 OrderDetailPanel（已判空）；P03 详情弹窗行为经回归联验不变形；i18n 纪律沉淀：nsSeparator=false 下 t() 禁带「ns:」前缀（AGENTS 第 2 节） |
| 2026-09-18 | 实体页签第二消费者 + 车辆详情双形态轮询 + 车辆域常量提升 + 修复两处 i18n 缺陷 | P39 交付：① vehicle-info 路由移入受保护根（完整路径 /vehicle-info 不变；hideInMenu；perm=vehicle-list:view）——实体页签第二消费者同构落地；② 参数单一真相 `features/vehicle-detail/vehicleDetailNavigation.ts`（命名 `?vehicleKey=` + 裸值 `?KEY`/`?KEY=` 兼容；旧源码无导航调用点直访裸值形态实证）；③ 新增 `features/vehicle-detail`（VehicleDetailPanel：getVehicleState 递归展平 Descriptions + useVisiblePolling 可见轮询；失败清空远端区域退避重查、不存在态停轮询）与 `services/vehicle/vehicle-state.service`（fetchVehicleState 返回可空，GET 平铺 query G04）；④ 车辆域枚举映射与 formatComponent 自 vehicleListOptions 提升至 `src/constants/vehicle/vehicleDisplayOptions.ts`（P05/P39 两域共同消费，vehicleListOptions 改 re-export，P05 三处消费者导入路径不变）；⑤ P05 抽屉 footer 新增「完整详情」入口（导航回调由宿主 VehicleDisplay 组装，features 域间禁导入）；⑥ 修复两处 i18n 缺陷：OrderDetailPanel `t('orderInfo:…')` 前缀 key 显示字面量（nsSeparator=false 违反 AGENTS 第 6 节，改 nsMode fallback 无前缀）；P39 自查发现 zh-TW 分片误用繁体 key（key 必须简中文案，全部改回简中 key + 繁体 value）；⑦ vehicleInfo 命名空间四语言分片（en 旧真译沿用，繁日韩 B1 基线补译，67 key 四语言同构） | P39（本轮 run） | 消费者影响：实体页签行更新为 P38/P39 双消费者（P40 照此迁移）；vehicleListOptions re-export 对 P05 三处消费者透明（typecheck/build 实证）；OrderDetailPanel 命名空间数组化对既有 orderRecord key 零影响（P38 缺陷修复复验通过）；VehicleDisplay 新增 useNavigate + onOpenFullDetail 回调（P05 交付行为不变）；抽屉「完整详情」宿主组装模式沉淀：跨域详情入口一律 pages 层组装导航 |

| 2026-09-18 | 车辆分组管理页 + vehicle-group 服务 owner 交接 | P04 交付：① `services/vehicle/vehicle-group` 扩展 pageVehicleGroups/addVehicleGroup/updateVehicleGroup/deleteVehicleGroup（owner 自 P04 起持有；P03 代建 fetchVehicleGroups 选项函数签名与行为不变，P03/P20 消费者透明）；② `features/vehicle-group/components/GroupFormModal`（草稿保留/useTabDirtyGuard/Transfer 复用 getSimpleVehicles 共享契约/失效关联合成条目：组内车辆 key 不在选项集合时保留原标识+「已不可用」标注+保存原样提交，不静默替换）；③ `pages/vehicle-deploy/VehicleGroup` 整页重写（Apex request+列偏好 tableId `vehicle-group:main`+expandable 展开行组内车辆 Tag+confirmCommand 破坏性删除确认）；④ 路由 vehicle-group 解除 pending（进入登录落点候选），meta.i18nNamespaces=['vehicleGroup']；⑤ vehicleGroup 四语言分片 33 key 同构（en 旧真译沿用+补译，繁日韩 B1 基线补译） | P04（本轮 run） | 消费者影响：fetchVehicleGroups 消费者（P03 CreateOrderModal/P20）无感（typecheck 实证）；definitions.tsx 仅本页 meta 行变化；i18n.ts 四表各增 1 行；联验发现后端缺陷 P04-G1（updateVehicleGroup groupName 不落库，vehicleKeys 生效；前端行为正确）登记 gaps.md，不阻塞后续任务（P19/P20 仅消费分组选项） |
| 2026-09-19 | 载具类型管理页 + carrier 服务落地 + 载具权限码核对收敛 | P06 交付：① 新增 `services/vehicle/carrier.service`（pageCarriers/createCarrier/updateCarrier/deleteCarrier 四 operation 均为 POST+JSON body，与 vehicleGroup 的 GET+query 分页形态不同，保持旧实现与 OpenAPI 清单原样；owner=P06）；② `features/carrier-type/components/CarrierFormModal`（草稿保留/useTabDirtyGuard/失败留稿；名称/编码 64 字上限；长度宽度 InputNumber min=1，mm 协议原样）；③ `pages/vehicle-deploy/VehicleType` 整页重写（Apex request+列偏好 `carrier-type:main`+名称编码双筛选+confirmCommand 破坏性删除确认；G09 不开放排序；无轮询）；④ 路由 vehicle-type 解除 pending，perm 挂 `PERM.CARRIER_VIEW`（后端权限树真实 MENU 码 id=23，path 为空；旧路由未写 access、旧前端 MENU_TREE 兜底映射本路径——登记已有码非凭空造码），meta.i18nNamespaces=['carrierType']；⑤ carrierType 四语言分片 27 key 同构（en 旧真译沿用，繁日韩 B1 基线补译，「载具」沿用旧译実績词キャリア/캐리어/載具） | P06（本轮 run） | 消费者影响：carrier 服务暂无其他消费者（后续关联载具页面接入时复用）；PERM 新增 CARRIER_VIEW 对既有路由 meta 无影响（typecheck 实证）；definitions.tsx 仅本页 meta 行变化；i18n.ts 四表各增 1 行；联验实证 carrier 的 updateCarrier 名称/尺寸真实落库，无 G18 类缺陷 |
| 2026-09-19 | 节点映射页 + agvNodeMapping 服务落地 | P07 交付：① 新增 `services/vehicle/node-mapping.service`（pageAGVNodeMapping GET+query / save / update（mappingKey+全量分组整体替换）/ delete / getSuggestionsForCollectionNodes 五 operation，逐项核对 OpenAPI 清单与旧实现；owner=P07）；② `features/node-mapping`（草稿模型 types.ts 行三态纯函数 + MappingFormModal + MapPickerModal）；③ `pages/vehicle-deploy/NodeMapping` 整页重写（Apex request+列偏好 node-mapping:main+映射名称筛选+confirmCommand 破坏性删除确认；G09 不开放排序）；行表格按「业务表格统一 Apex」以 data 模式承载编辑态草稿（半填高亮逐 cell 包装、antd 受控分页器）；④ 选点弹窗复用 T00 ReadOnlyMap（回显定位不闪烁+失效标注+确定禁用=无选中）；⑤ **契约差异 G19：后端 save/update 拒绝空 agvKeys（2004030），旧实现允许为空——前端加必填校验**；⑥ 路由解除 pending，meta.i18nNamespaces=['nodeMapping','map']；⑦ nodeMapping 四语言分片 54 key 同构（en 旧真译沿用+插值语法改 {{var}}，繁日韩 B1 基线） | P07（本轮 run） | 消费者影响：node-mapping 服务暂无其他消费者；ReadOnlyMap/confirmCommand/useStaticOptions 均只消费不改动；i18n.ts 四表各增 1 行；联验实证 update 整体替换真实落库（无 G18 类缺陷）、删除后端自清理 |
| 2026-09-19 | 告警码管理页 + vehicleAlarmCode 服务落地 + POST 文件下载通道 | P08 交付：① 新增 `services/vehicle/vehicle-alarm-code.service`（六 operation：pageVehicleAlarmCodes GET+query / add / update（id 定位+全量）/ delete（按 alarmCode 字符串定位，协议原样）/ uploadVehicleAlarmCodeFile（multipart 字段 file）/ downVehicleAlarmCodeFile（POST 空 body + Blob）；owner=P08）；② request.ts 新增 **`api.downloadPost`**（POST+Blob+完整响应，与 downloadGet 同语义：JSON 错误仍走统一解包、signal 透传、取消静默；纯新增向后兼容）；③ `features/vehicle-alarm-code`（AlarmCodeFormModal 多语言行 Form.List + constants LOCALE_OPTIONS）；④ `pages/system-involve/AlarmCodeManagement` 整页重写（Apex request+列偏好 alarm-code:main+告警码筛选+confirmCommand 破坏性删除确认；上传/下载经 beginTransfer 登记：切页继续/关页提示/真实字节进度；G09 不开放排序）；⑤ **G05 实证：上传文档声明 application/json+binary，真实后端仅接受 multipart/form-data 字段 file**；alarmCodeRecords 后端实际必填（2000450，OpenAPI 标可选）；**G20：同码 add 暂态 500 观察**（未复现，详 gaps）；⑥ 路由解除 pending，meta.i18nNamespaces=['vehicleAlarmCode']；⑦ vehicleAlarmCode 四语言分片 49 key 同构（en 旧真译沿用+插值改 {{var}}，繁日韩 B1 基线） | P08（本轮 run） | 消费者影响：downloadPost 暂无其他消费者（P09/P23/P25 等文件任务按需复用）；vehicle-alarm-code 服务暂无其他消费者；i18n.ts 四表各增 1 行；联验实证增改删/上传（零净变化）/下载全链路 |
| 2026-09-19 | 地图列表页 + map 管理服务落地 + 发布禁用依据适配 | P09 交付：① 新增 `services/map/map-admin.service`（十 operation：pageMapInfos GET+query / createMap / updateMap（mapKey 定位）/ deleteMap（mapId 定位）/ uploadMap / uploadVehicleMap（multipart 字段 file）/ downloadMap（api.downloadPost）/ pageMapInfoVersions（mapId 隔离）/ publishMapInfoVersion / pushMapInfoVersion；owner=P09）；② `features/map-list`（MapFormModal 草稿保留 / MapUploadButton 真实字节进度（旧伪进度条废弃）/ VersionModal / PushVersionModal）；③ `pages/map-through/MapList` 整页重写（Apex request+列偏好 map-list:main+confirmCommand 破坏性删除确认；G09 不开放排序）；④ **deleteMap 后端规则实证：先禁用再删除（3000210 如实透传，与旧实现行为等价）**；⑤ **发布按钮禁用依据适配：currentMapInfoVersion（OpenAPI boolean，联验实证下发）并旧字段 published（新后端不下发）**——仅依赖 published 会漏禁已发布版本；⑥ G07 拉取入口保留禁用+Tooltip；⑦ 路由解除 pending，meta.i18nNamespaces=['mapList']；⑧ mapList 四语言分片 77 key 同构（en 旧真译沿用，繁日韩 B1 基线） | P09（本轮 run） | 消费者影响：map-admin 服务暂无其他消费者（P10/P11 等地图域页面按需复用）；既有 services/map/map.service（getMapInfo/getSimpleMaps/fetchMapSites 选项服务）未改动；i18n.ts 四表各增 1 行；联验实证增改删/上传/下载/版本/发布（安全对象）全链路，推送执行留专用环境 |
| 2026-09-19 | 跨地图关联页 + crossMap 服务落地 | P10 交付：① 新增 `services/cross-map/cross-map.service`（六 operation：pageCrossMaps GET+query / createCrossMap / updateCrossMap（int64 id 定位+crossMaps 整体替换）/ deleteCrossMap / getCrossMapStations（页面私有站点选项）/ fetchElevatorOptions（getElevators，contracts 第 5 节电梯行 owner=P10）；owner=P10）；② `features/cross-map`（CrossMapModal：草稿保留+useTabDirtyGuard+失败留稿+顶层清空；名称 64 字；至少两行+地图查重（恰好一行命中，空值跳过）+行级必填；**行级站点独立加载（修复旧版全局一份串选项缺陷）+AbortController 防乱序+切换地图清站点并重载+下拉展开幂等重查**；失效地图/站点/电梯以「原名称（已不在当前选项中）」合成选项保显示（Form.useWatch 驱动重算），失效值不阻止提交）；③ `pages/map-through/CrossMaps` 整页重写（Apex request+列偏好 cross-map:main+名称筛选+confirmCommand 破坏性删除确认；G09 不开放排序；**展开行子表 Apex data 模式**四列核对旧版）；④ **契约缺口 G22：create/update 后端强制「多地图关联节点角度不能为空」（4000540）**——校验所选站点在地图数据中的 angle 属性，OpenAPI CrossMapParam 与旧实现均无此字段，前端无法适配只能如实透传（详见 gaps）；⑤ 路由解除 pending，meta.i18nNamespaces=['crossMap']；⑥ crossMap 四语言分片 33 key 同构（en 旧真译沿用+插值改 {{var}}，繁日韩 B1 基线） | P10（本轮 run） | 消费者影响：cross-map 服务暂无其他消费者（P11 点边组合按需复用行级选项模式）；fetchSimpleMaps（T00 共享）只消费不改动；i18n.ts 四表各增 1 行；联验实证增改删/选项/筛选全链路（无 G18 类缺陷），删除不存在 id 亦 200 扩展 G21 |
| 2026-09-19 | 多地图点边组合页 + node-edge-group 服务落地 | P11 交付：① 新增 `services/node-edge-group/node-edge-group.service`（五 operation：pageSystemNodeEdgeGroups GET+query / createSystemNodeEdgeGroup / updateSystemNodeEdgeGroup（int64 id 定位+mapNodeEdgeGroupIds 整体替换）/ deleteSystemNodeEdgeGroup / getAllSimpleNodeEdgeGroups（页面私有选项 owner=P11）；owner=P11）；② `features/node-edge-group`（NodeEdgeGroupModal：草稿保留+useTabDirtyGuard+失败留稿+顶层清空；名称 64 字；至少一个点边组合；**勾选态与已选集合分离受控——选择取消不污染草稿**；**失效关联识别并阻止错误保存**——合成条目保原名称+「已不可用」标注+存在失效项提交拦截，较 P04 更严的 P11 专项差异化实现，加载中/失败不误判失效）；③ `pages/map-through/PointEdgeCombination` 整页重写（Apex request+列偏好 node-edge-group:main+名称筛选+confirmCommand 破坏性删除确认；G09 不开放排序；行 ID=id int64 经 stringFieldRowId；**展开行子表 Apex data 模式**四列核对旧版、计数缺失留白）；④ 契约发现：deleteSystemNodeEdgeGroup 不存在 id 亦 200（G21 扩展第三例）、create 空集合后端 500+「失败」（G23 登记，前端必填已拦截）；⑤ 路由解除 pending，meta.i18nNamespaces=['nodeEdgeGroup']；⑥ nodeEdgeGroup 四语言分片 36 key 同构（en 旧真译沿用，繁日韩 B1 基线，公共词与 P10 crossMap 分片对齐）；⑦ **P19 引用契约已确认**（组合实体+选项接口，contracts 第 5 节行） | P11（本轮 run） | 消费者影响：node-edge-group 服务暂无其他消费者（P19 将引用，见第 5 节行）；i18n.ts 四表各增 1 行；联验实证增改删/选项/筛选/展开行/列偏好全链路（无 G18 类缺陷），测试数据零残留 |
| 2026-09-19 | 地图推送记录页 + map-push-record 服务落地 | P12 交付：① 新增 `services/map-push-record/map-push-record.service`（三 operation：pageMapPushRecords GET+query 平铺 / rePushMap / cancelPushMap；owner=P12）；② `features/map-push-record`（MapPushSubRecordTable 展开行子表：Apex data 模式+受控 PaginationState 前端筛选分页（车辆名称关键字+推送状态，筛选回首页/页码兜底回退）+五状态 Tag+失败/等待原因 Tooltip；pushState.ts 可取消纯函数）；③ `pages/map-through/MapPushNotificationRecords` 整页重写（Apex request+列偏好 map-push-record:main+操作列默认钉右；推送结果列=子记录状态计数 Tag 汇总、未知枚举原值灰 Tag；**useVisiblePolling 约 5 秒可见串行轮询——命令状态追踪页（P09 推送受理≠子记录终态，状态核实依赖持续重查），联验实测 6000ms 节拍与 P05 一致**；旧手动刷新按钮按按钮纪律废弃；记录级/子记录级重推与取消升级 confirmCommand 命令确认+行级防连点+受理语义提示+失败不自动补发）；④ 权限：后端下发 map-push-record:view+re-push 两码（重推/取消共用，旧实现同边界）；⑤ 路由解除 pending，meta.i18nNamespaces=['mapPushRecord']；⑥ mapPushRecord 四语言分片 30 key 同构（en 旧真译沿用+插值改 {{var}}，繁日韩 B1 基线，公共词与 P09 mapList 分片对齐）；⑦ 带令牌联验：查询链路全实证（GET 平铺/空态/越界页码/守卫回跳+登录落点闭环/列偏好闭环/五语言/双主题双宽度四组截图）；重推/取消命令执行留专用环境（车辆副作用，联验环境 total=0），子表与确认层真实数据运行时验证同条件登记 | P12（本轮 run） | 消费者影响：map-push-record 服务暂无其他消费者（P09 推送语义引用本页为状态核实页，无代码依赖）；i18n.ts 四表各增 1 行；无新增 G 编号（GET 平铺随 G04 既有口径） |
| 2026-09-19 | 调度中心页 + dispatch-config 服务落地 | P13 交付：① 新增 `services/dispatch-config/dispatch-config.service`（getTaskConfigs GET 全量/batchEditConfigs POST 数组整批；owner=P13；editConfig 不迁移）；② `features/dispatch-config`（configEditor.ts 纯函数：开闭区间范围解析 parseFloat 修复 double 边界+值双向转换+未知类型只读；DispatchConfigTable.tsx 每分类独立实例：Apex data 模式+列偏好 dispatch-config:main+配置值列常驻控件按行 configValueType 异构——Apex 内建 editor 单列单类型不适用行级异构）；③ `pages/dispatch-hub/DispatchHub` 整页重写（Tabs 分类+草稿模型 baseline/tabRows+useTabDirtyGuard；**保存两阶段：预读冲突检查（服务端≠基线中止+列冲突项+基线刷新+草稿保留）→batchEditConfigs 整批→本地提交值合成新基线**；**重置=草稿回退最近一次确认保存的基线（纯前端），旧版恢复默认值写后端语义按 TASKS.md 验收明文废弃**）；④ 权限：后端下发 dispatch-hub:view/save/reset 三码（save 控保存+单元格可编辑、reset 控重置入口）；⑤ 路由解除 pending，meta.i18nNamespaces=['dispatchConfig']；⑥ dispatchConfig 四语言 16 key 同构（en 旧真译沿用+{{var}}，繁日韩 B1 基线，「保存」走 common 回退）；⑦ 带令牌联验：API 层（三码下发/getTaskConfigs 44 项/写链路还原零净变化/3000620 越界拒绝）+浏览器（守卫闭环/逐行渲染/重置草稿/保存取消零请求/真实提交×2/G13 冲突中止/列偏好闭环（面板确定=应用、×=取消回滚）/五语言/双主题双宽度四组截图/**beforeunload 拦截 reload 草稿不丢实证**） | P13（本轮 run） | 消费者影响：dispatch-config 服务暂无其他消费者；i18n.ts 四表各增 1 行；无新增 G 编号 |

| 2026-09-19 | 电梯设备页 + device-elevator 服务落地 + G06 缺口呈现定版 | P14 交付：① 新增 `services/device-elevator`（10 operation：pageElevators GET+query 平铺 / getElevatorDrivers / getElevatorState / addElevator / updateElevator（原 deviceKey 定位+全量）/ deleteElevator / outerCall / openDoor / closeDoor / clearElevatorOccupy；owner=P14；**innerCall 不迁移（G06）**；getElevators 选项维持 P10 owner 不重复落地）；② `features/device-elevator`（ElevatorFormModal：草稿保留+useTabDirtyGuard+失败留稿+驱动切换 driverProtocol 覆写配置+JSON.parse 真实校验；ElevatorControlModal：外呼/开门/关门参数录入→confirmCommand→发送，无内呼模式）；③ `pages/tri-device/Elevator` 整页重写（Apex request+列偏好 device-elevator:main+操作列默认钉右+行 ID=deviceKey；状态按需查询+受控 Popover（修复行内 setState 重挂载丢展开态缺陷）+行级状态缓存；G09 不开放排序；普通 CRUD 不轮询）；④ 权限：device:elevator 五码下发，view 挂守卫、四按钮码控入口、「状态」不控权；⑤ 路由解除 pending，meta.i18nNamespaces=['deviceElevator']；⑥ deviceElevator 四语言 63 key 同构（en 旧真译沿用+{{var}}，繁日韩 B1 基线补译，繁中台湾惯用语/日文車内呼び業界用语） | P14（本轮 run） | 消费者影响：device-elevator 服务暂无其他消费者（P15–P18 设备页可参考命令确认+状态查询形态）；fetchElevatorOptions（P10）未改动；i18n.ts 四表各增 1 行；G06 在本页按任务卡要求以「灰色标记入口+点击说明+零请求」呈现闭合；无新增 G 编号 |
| 2026-09-19 | 自动门设备页 + device-auto-door 服务落地 | P15 交付：① 新增 `services/device-auto-door`（9 operation：pageAutoDoors GET+query 平铺 / getAutoDoorDrivers / getAutoDoorState / addAutoDoor / updateAutoDoor（原 deviceKey 定位+全量）/ deleteAutoDoor / openDoor / closeDoor / clearAutoDoorOccupy；owner=P15；getAutoDoors 全量选项暂无活跃消费者不落地；doorWay 为电梯语义可选字段自动门不携带）；② `features/device-auto-door`（AutoDoorFormModal：草稿保留+useTabDirtyGuard+失败留稿+驱动切换 driverProtocol 覆写配置+JSON.parse 真实校验；label「设备驱动」「ip地址」旧版原样；**无控制命令参数弹窗——单门设备开门/关门/清占用均无参数**）；③ `pages/tri-device/AutoDoor` 整页重写（Apex request+列偏好 device-auto-door:main+操作列默认钉右+行 ID=deviceKey；状态按需查询+受控 Popover+行级状态缓存，门状态协议原值；清占用确认列明 vehicleNames 占用车辆清单>5 台合并计数；G09 不开放排序；普通 CRUD 不轮询）；④ 权限：device:auto-door 五码下发，view 挂守卫、四按钮码控入口、「状态」不控权；⑤ 路由解除 pending，meta.i18nNamespaces=['deviceAutoDoor']；⑥ deviceAutoDoor 四语言约 60 key 同构（en 旧真译沿用+{{var}}，繁日韩 B1 基线补译，繁中台湾惯用语/日文開扉・閉扉） | P15（本轮 run） | 消费者影响：device-auto-door 服务暂无其他消费者（P16–P18 设备页可参考同构形态）；i18n.ts 四表各增 1 行；与电梯命令族差异登记（无 outerCall/innerCall、开关门无 doorWay 录入）；无新增 G 编号 |
| 2026-09-19 | 充电桩设备页 + device-charge-pile 服务落地 | P16 交付：① 新增 `services/device-charge-pile`（7 operation：pageChargePiles GET+query 平铺 / getChargePileDrivers / addChargePile / updateChargePile（原 deviceKey 定位+全量 ChargePileParam）/ deleteChargePile / startCharge / stopCharge；owner=P16；**无 getChargePileState——状态随分页行记录内嵌（deviceChargePileState.chargePileState）下发，列表刷新即状态重查**；getChargePileDetail 旧仓库无引用、getAllChargePiles 旧仓库唯一消费者为 H02 暂缓页均不落地；命令族仅开始/停止充电，DeviceParam 的 doorWay 为电梯语义不携带）；② `features/device-charge-pile`（ChargePileFormModal：草稿保留+useTabDirtyGuard+失败留稿+驱动切换 driverProtocol 覆写配置+JSON.parse 真实校验；**ip 必填+IPv4 正则（旧实现同规则）**；端口 0..65536 旧版 max 原样迁移（较通用 TCP 上界 65535 宽 1，登记 P16.md）；label「设备IP地址」「设备端口号」旧版原样；无控制参数弹窗）；③ `pages/tri-device/ModbusChargePile/ModbusChargePile` 整页重写（Apex request+列偏好 device-charge-pile:main+操作列默认钉右+行 ID=deviceKey；充电桩状态列沿用旧 ChargeState 既定映射（ERROR/FAULT→错误、IDLE→空闲、CHARGING→充电中、FULL→充满、OFFLINE→离线），未知枚举原值；开始/停止充电升级 confirmCommand，成功后刷新列表（旧实现同语义）且提示不显示成充电完成；删除升级 confirmCommand danger；G09 不开放排序；普通 CRUD 不轮询）；④ 权限：device:charge-pile 五码下发，view 挂守卫、四按钮码控入口；⑤ 路由解除 pending（charge-pie 历史拼写保留），meta.i18nNamespaces=['deviceChargePile']；⑥ deviceChargePile 四语言 50 key 同构（en 旧真译沿用+{{var}}，繁日韩 B1 基线补译，充电桩=充電樁/充電ステーション/충전기 与 menu 分片对齐，空闲=空閒/アイドル/대기 与 vehicleInfo 分片对齐） | P16（本轮 run） | 消费者影响：device-charge-pile 服务暂无其他消费者（P17–P18 设备页可参考同构形态）；i18n.ts 四表各增 1 行；命令族差异登记（无开关门/清占用/呼叫、状态随行内嵌无独立查询）；无新增 G 编号 |
| 2026-09-19 | 交通灯设备页 + device-traffic-light 服务落地 | P17 交付：① 新增 `services/device-traffic-light`（6 operation：pageTrafficLights GET+query 平铺 / fetchTrafficLightDrivers（getDrivers）/ addTrafficLight / updateTrafficLight（原 deviceKey 定位+全量）/ deleteTrafficLight / testTrafficLight；owner=P17；getTrafficLights 旧仓库无活跃消费者不落地；deviceConfig 为通用 map，旧键集 {url,requestParam,responseSuccessExpression} 原样承载，新契约顶层 ip/port/deviceStatus 旧 UI 无不发明 UI）；② `features/device-traffic-light`（TrafficLightsFormModal：草稿保留+useTabDirtyGuard+失败留稿+驱动切换 driverProtocol 覆写请求参数（4 空格缩进）+JSON.parse 真实校验；编辑回显 2 空格缩进旧版同；label「到点通知」对应 syncWaitResponse 旧版表单原样）；③ `pages/tri-device/TrafficLights` 整页重写（Apex request+列偏好 device-traffic-light:main+操作列默认钉右+行 ID=deviceKey；8 列逐列核对旧版；测试升级 confirmCommand（A21 设备控制类）+失败诊断如实呈现不用动画假装测试通过；删除升级 confirmCommand danger；G09 不开放排序；普通 CRUD 不轮询）；④ 权限：device:traffic-light 五码下发，view 挂守卫、四按钮码控入口（测试归 operate）；⑤ 路由解除 pending，meta.i18nNamespaces=['deviceTrafficLight']；⑥ deviceTrafficLight 四语言 41 key 同构（en 旧真译沿用，繁日韩 B1 基线补译，交通灯=交通號誌/信号機/신호등 与 menu 分片对齐） | P17（本轮 run） | 消费者影响：device-traffic-light 服务暂无其他消费者（P18 设备页可参考同构形态）；i18n.ts 四表各增 1 行；命令差异登记（测试=连通性请求非状态查询、delete/test 仅 deviceKey）；无新增 G 编号 |
| 2026-09-19 | 风淋门设备页 + device-air-shower 服务落地 | P18 交付：① 新增 `services/device-air-shower`（8 operation：pageAirShowerDoors GET+query 平铺 / fetchAirShowerDoorDrivers / fetchAirShowerDoorState / addAirShowerDoor / updateAirShowerDoor（原 deviceKey 定位+全量）/ deleteAirShowerDoor / openDoor / closeDoor（doorWay=FRONT/BACK 必填）；owner=P18；shower/clearAirShowerDoorOccupy/getAirShowerDoors 旧仓库无本页可达消费者不落地）；② `features/device-air-shower`（AirShowerFormModal：草稿保留+useTabDirtyGuard+失败留稿+驱动切换 driverProtocol 覆写设备配置+JSON.parse 真实校验+无 deviceStatus 字段不发明 UI；AirShowerControlModal：门类型 FRONT/BACK 必选，标题/label/校验文案旧版原样）；③ `pages/tri-device/AirShowerDoor` 整页重写（Apex request+列偏好 device-air-shower:main+操作列默认钉右+行 ID=deviceKey；9 列逐列核对旧版含按钮顺序 状态/编辑/删除/操作项；状态=受控 Popover+行级缓存五字段枚举原值；开门/关门升级 弹窗→confirmCommand→发送；删除升级 confirmCommand danger；G09 不开放排序；普通 CRUD 不轮询）；④ 权限：device:air-shower-door 五码下发，view 挂守卫、四按钮码控入口（状态不控权）；⑤ 路由解除 pending，meta.i18nNamespaces=['deviceAirShower']；⑥ deviceAirShower 四语言同构（en B1 基线补译 Air Shower Door，繁日韩 B1 基线补译，风淋门=風淋門/エアシャワードア/에어샤워 도어 与 menu 分片对齐，开/关门=開扉·閉扉/문 열기·문 닫기 沿用 P15） | P18（本轮 run） | 消费者影响：device-air-shower 服务暂无其他消费者；i18n.ts 四表各增 1 行；命令差异登记（开门/关门 doorWay 双门语义必填，与自动门单门不携带为有意差异；openDoor 缺 doorWay 后端 500 实证）；无新增 G 编号 |
| 2026-09-19 | 避障模板页 + obstacle-avoidance 服务落地 | P22 交付：① 新增 `services/obstacle-avoidance`（4 operation：pageObstacleAvoidance GET+query 平铺 / createObstacleAvoidance / updateObstacleAvoidance（int64 id 定位+parameters 整体替换）/ deleteObstacleAvoidance（POST+?id= 按 OpenAPI query 声明）；owner=P22；getObstacleAvoidanceList 唯一活跃消费者为 H02 暂缓页不落地）；② `features/obstacle-avoidance`（obstacleTemplateData：14 预置模板×64 选项 `${key}_index_N` 规则等价迁移（value 变更会破坏历史回显，文件头注明不得改动）；ObstacleTemplateModal 双模式弹窗：草稿保留+useTabDirtyGuard+失败留稿+显式清空；Transfer 14 模板选择（titles/actions/搜索占位旧版逐项一致）；参数配置表按「业务表格统一 Apex」以 data 模式承载（P07/P13 同款行内控件）；类型下拉 label=`${文案} (${value})` 识别具体标识+下拉底部自定义添加（popupRender=antd v6 对旧 dropdownRender 替代，value 按 `${key}_index_${nextIndex}` 规则生成）；**编辑回填算法逐分支等价旧实现**（avoid 精确/前缀匹配模板 key→name 匹配模板名→历史自定义项 key_index_N 还原→完全不匹配旧实现静默丢弃，本实现保留提交语义但丢弃计数状态文本可见化——P22 专项「未知值按契约处理」不静默伪装，不发明旧版没有的编辑控件）；③ `pages/obstacle-avoidance/ObstacleAvoidance` 整页重写（Apex request+列偏好 obstacle-avoidance:main+操作列默认钉右+行 ID=id int64；2 列逐列核对旧版（避障名称弹性+操作 200 旧 fixed right）；展开行子表 Apex data 模式 3 列（避障参数名称/避障参数类型/是否启用避障参数 enable 两态映射、avoid 协议原值、缺失留白）；删除升级 confirmCommand danger（旧 Popconfirm 仅一问按 DoD 8 升级）；G09 不开放排序；普通 CRUD 不轮询）；④ 权限：obstacle-avoidance 四码下发，view 挂守卫、三按钮码控入口；⑤ 路由解除 pending，meta.i18nNamespaces=['obstacleTemplate']；⑥ obstacleTemplate 四语言同构（en 旧真译沿用+插值改 {{var}}（'自定义{index}'→'自定义{{index}}' 与 P10 crossMap '地图 {{index}}' 同形态），繁日韩 B1 基线补译，14 模板名四语言旧真译逐条沿用，公共词与 P11 nodeEdgeGroup 分片对齐） | P22（本轮 run） | 消费者影响：obstacle-avoidance 服务暂无其他消费者；i18n.ts 四表各增 1 行；预置模板数据 value 规则为跨任务稳定契约（P20/P21 工艺任务如消费避障模板展示，avoid 原值不猜语义）；登记 G24（Apex 唯一数据列隐藏边界行为，见 gaps）；无其他新增 G 编号 |

| 2026-09-20 | 订单报表服务落地 + 统计口径共享 | P33 交付：① 新增 `services/report-order`（POST orderQuantityStatistics/orderEfficiencyStatistics，查询性质 POST 归只读；startTime/endTime 未选发空串=旧实现等价；效率契约无 orderStates）；② 新增 echarts@6.1.0（官方 npm 正式版，按需注册；P33 为首个图表页，P34–P37 可复用同一依赖与「React 层标题+口径提示/echarts 绘图」结构）；③ 统计口径 owner 交接：features/analyze-visual/order-statistics/statistics.ts 为数量按状态求和与效率 Σ各类型平均值（秒）的唯一计算源，P35 接入统计报表时消费此口径不另写第二份；④ 路由 analyze-visual-order-statistics 解除 pending（i18nNamespaces=['report-order','orderRecord']，fallback 复用 orderRecord 枚举译文）；⑤ report-order 四语言分片全量真译（无新增缺失登记） | P33（本轮 run） | 消费者影响：echarts 为新增依赖（lock 文件已提交）；orderRecord 命名空间被 P33 fallback 消费（只读复用，P03 owner 无感）；登记 P33 页旧路由注释不可达而规格/TASKS 明确迁移的范围事实（tasks/P33.md） |

## 9. 冻结登记（T00.9）

1. **冻结范围**：第 1–7 节契约为 B1/B2 页面任务的消费基线；实现文件位置与导出以 tasks/T00.md「文件归属」节为准，两者经 T00.9 核对一致。
2. **变更规则**：冻结后新增/变更契约（含公共导出签名变化）由 owner 在变更日志登记，评估并记录全部消费者；页面任务发现契约缺陷报统筹，由原 owner 或指定接任者修复，不在页面内私有变通。
3. **放行状态**：B1/B2 所需机制（登录/会话/权限/路由守卫/表格公共资源/页签/轮询/传输/地图/五语言）实现与机制级验证齐备；**真实登录成功路径、带令牌授权只读联验受 G03 外部阻塞（需测试账号凭据），B1 放行前必须补验**——放行判定以补验证据为准，本登记不构成放行。
| 2026-09-19 | 车辆动作页 + agv-action-manage 服务落地 | P23 交付（代码+门禁全绿；带令牌联验受阻 G25 待补验）：① 新增 `services/action/agv-action-manage`（4 operation：pageAgvActions GET 平铺 / addAgvAction / updateAgvAction（int64 id 定位整体提交）/ deleteAgvAction（**body {id}**，OpenAPI delete body=AGVActionParam 与避障 ?id= 形态不同）；owner=P23；getAGVActions 已由 P03 代建共享服务不重复落地；importExcel/exportExcel 旧仓库零 UI 消费者不落地）；② `pages/action-control/AGVAction` 整页重写（Apex request+列偏好 agv-action:main+操作列默认钉右+行 ID=id int64；5 列逐列核对旧版：动作类型/动作描述/阻塞类型（协议原值，未知枚举原值）/动作参数（单元格 Descriptions column=1 旧同形态，非字符串 value JSON 文本展示层转换 toValueDisplayText 共享工具）/操作 200；删除升级 confirmCommand danger（DoD 8）；G09 不开放排序；普通 CRUD 不轮询）；③ `features/action-control/components/ActionFormModal` 双模式弹窗（草稿保留+useTabDirtyGuard+失败留稿+显式清空；四字段+Form.List 参数行 11/11/2 全宽无标签列与旧版结构逐像素一致；**P23 专项「已有动作参数不因未知枚举丢失」：blockingType 未知原值动态并入 options+value 非字符串 getValueProps 仅转显示保留原值提交**）；④ 权限：action:vehicle:view 挂守卫（既有码）、add/update/delete 三按钮码控入口（下发实证待 G25 解除）；⑤ 路由解除 pending，meta.i18nNamespaces=['agvAction']；⑥ agvAction 四语言同构（en 旧真译逐条沿用，繁日韩 B1 基线补译，「アクション/액션」与 menu 对齐，公共词与 P22 obstacleTemplate 分片对齐） | P23（本轮 run） | 消费者影响：agv-action-manage 服务暂无其他消费者（P24/P20 按 contracts 第 5 节动作行契约消费）；i18n.ts 四表各增 1 行；共享工具新增 utils/display/valueDisplay（toValueDisplayText，两处消费）；共享类型未改动（P03 的 agv-action.service 保持原样，管理行类型独立放宽 blockingType）；登记 G25（root 凭据失效，见 gaps） |
| 2026-09-19 | P23 带令牌联验补验通过 + G25 定性修正 | ① API 层 17 项全过（权限四码下发/GET 平铺分页/无令牌 1000000 对照/创建落库一致/参数 value 字符串+数字+数组+对象四形态保形/编辑全字段落库无 G18 类缺陷/筛选命中与空态/删除 body {id} 自清理终态审计零残留）；② 浏览器整页（守卫回跳带 redirect+登录落点闭环/列表 2 条真实渲染/必填拦截 3 项零请求/新增全链路/编辑回显 SOFT+参数/删除确认五要素+取消零请求+danger+最后实例纪律复验/列偏好闭环四步（隐藏-持久-恢复默认-清偏好后代码默认钉右 sticky right:0 回归；data-pinned 属性仅在偏好保存时写入，代码默认钉右以 sticky 呈现）/五语言 html lang 逐语言/双主题双宽度四组截图 .run-lock/shots-p23/）；③ P23 专项实证：未改动行提交原始 value 对象保形；blockingType 未知值后端 500 拒绝（受控枚举，前端防御性 options 并入自然路径不触发）；④ **G25 定性修正：root 凭据从未失效，联验脚本登录字段名 userName→username 笔误**（前端 auth.service 即 username），修正后立即登录成功 | P23（补验轮） | 消费者影响：无代码改动（纯验证轮+脚本字段名修正）；contracts 第 2 节权限行与第 5 节动作行改已实证；gaps G25 关闭；AGENTS 第 5 节沉淀修正版 RO 垫片（borderBoxSize 结构）与联验脚本字段名教训 |
| 2026-09-19 | 版本管理页 + system-version 服务落地 | P25 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/system/system-version`（6 operation：getSystemVersions 全集/restartSystem/rollback/deletePendingJar/downloadSystemVersionJar Blob/uploadSystemVersion multipart；owner=P25；**G08 替代核实：旧 pageSystemVersions/updateJarRestart 新 OpenAPI 不存在，消费点由 getSystemVersions/restartSystem 替代**）；② `features/system-version`（TransferProgressModal 真实进度弹窗——**旧 FakeProgressModal 伪进度按任务卡移除**，已知总字节显百分比否则走马灯仅显已接收 MB；VersionUploadButton 旧 UpdateVersion 等价迁移（axios onUploadProgress+传输管理器 markProcessing「100% 后服务端处理中」+取消二次确认+beforeunload 仅上传中+成功 800ms 绿条+刷新列表；差异登记：旧 60s 停滞自动中止不落地，axios 无停滞事件，取消+网络错误兜底保留）；useTransferSpeed 3 秒滑动窗口速度/ETA）；③ `pages/system-involve/VersionControl` 整页重写（Apex request 客户端切片分页（getSystemVersions 全集无分页参数，不虚构服务端分页语义）+列偏好 system-version:main+行 ID=gitBuildId+操作列默认钉右；8 列逐列核对旧版（版本类型 Tag CURRENT 绿/BACKUP 灰/PENDING 金+未知枚举原值/时间列 displayDateTime）；重启/回滚/删除统一 confirmCommand 升级（DoD 8）；重启/回滚成功 dispatch(sessionExpired) 会话守卫跳登录（旧清 token 同语义）、失败/断连如实呈现不宣布成功不自动重试；无手动刷新按钮）；④ 权限：system:version:view 挂守卫、五按钮码控入口（**六码下发已实证**）；⑤ 路由解除 pending，meta.i18nNamespaces=['systemVersion']；⑥ systemVersion 四语言同构（en 旧真译逐条沿用+ETA 插值 {n}→{{n}} 形态修正，繁日韩 B1 基线补译，公共词与 P08 vehicleAlarmCode 分片对齐） | P25（本轮 run） | 消费者影响：system-version 服务仅 P25 消费；i18n.ts 四表各增 1 行；传输管理器 markProcessing 首次消费者（「服务端处理中」语义落地）；登记 G21 幂等族第四例（deletePendingJar 不存在 buildId 亦 200）；上传成功闭环需真实构建产物留专用环境（restartSystem/rollback 真实执行同样留专用环境） |
| 2026-09-19 | 系统日志页 + system-log 服务落地 + 请求层 Blob 错误解包修复 | P26 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/system/system-log`（3 operation：pageSystemLogs GET+query 平铺 / getSystemLogTypes / downloadSystemLog Blob；owner=P26）；② `pages/system-involve/SystemLog` 整页重写（Apex request 服务端分页+列偏好 system-log:main+行 ID=name+2 列逐列核对旧版+行多选下载 zip 经传输管理器页内真实进度行（旧 FakeProgressModal 伪进度按任务卡移除）+default.zip 兜底+Blob 释放；A09 条件提交清空选择）；③ **契约变更实证：类型集合为模块名（vehicleState/auth/device/dispatcher/driver/imap/system），旧默认 NORMAL 不在集合且缺 logType 空查——默认类型改为集合就位后取第一项、起始查询延迟到默认值就位**；④ **请求层修复（services/request/request.ts 响应拦截器）：responseType='blob' 通道收到 JSON 业务错误（本页 7000130 且仍带 content-disposition 附件头）按文本解析后统一 unwrapResult——真实业务码/消息如实抛出、绝不落伪文件（DoD 9）**；消费者影响：Blob 下载全部消费者（P03 导出/P08/P09/P25 下载）仅错误路径文案从「响应不符合调度协议」变真实业务消息，成功路径不变，lint/typecheck/build 全绿+本页下载成功链路复验；⑤ 权限 system:log:view 挂守卫+download 按钮码（两码下发已实证）；⑥ 路由解除 pending 挂 systemLog 命名空间；⑦ systemLog 四语言同构（en 旧真译沿用+{{msg}} 插值修正，繁日韩 B1 基线补译，公共词与 P03/P08 对齐） | P26（本轮 run） | 消费者影响：system-log 服务仅 P26 消费；i18n.ts 四表各增 1 行；请求层修复见 ④（错误路径语义修复非破坏）；IAB 沉淀：主题切换后截图合成层旧帧伪象（以 DOM 计算样式为准）+列偏好「恢复默认」需 Enter 应用 |
| 2026-09-19 | 系统设置页 + brand 服务扩展 + 外壳品牌消费接线 | P27 交付（代码+门禁全绿+带令牌联验通过+勾选）：① `services/system/brand` 扩展（P01 建、P27 增 upsertSystemImage PUT multipart 字段 file + notifyBrandResourcesChanged/subscribeBrandResourcesChanged 模块级 EventTarget 通知）；② 新建 `hooks/useBrandImage`（Blob→objectURL、品牌变更订阅重拉、替换前/卸载时精确释放）；③ `pages/system-involve/SystemSetting` 整页重写（单 Tab 图片配置+三张品牌图卡片 headerLogo/loginBackground/favicon，预览+上传+按钮码控入口，无表格无列偏好无轮询——装饰性资源非业务记录；不新增通用设置 CRUD）；④ **契约实证：systemLogos 族后端完全无需认证（无令牌 PUT 受理 code=200；无令牌 GET file 无资源 404=资源状态非会话拒绝）——登录前可用装饰资源设计语义，前端入口由菜单码+三按钮码控制；后端不校验图片内容（伪图片受理，展示层 Blob 校验+img onError 双防线不伪装有图）**；⑤ `Header.tsx` 公共文件改动：顶栏品牌图标消费真实 headerLogo（失败/无资源回退静态 /favicon.ico）+ 动态 favicon（有资源替换 link[rel=icon]）——旧 app.tsx getInitialState 同源行为补齐；⑥ 权限 system:setting:view 挂守卫+三上传按钮码控入口（四码下发已实证）；⑦ 路由解除 pending 挂 system-branding 命名空间；⑧ system-branding 四语言同构（en 旧真译沿用+{{label}} 插值修正，繁日韩 B1 基线补译） | P27（本轮 run） | 消费者影响：Header 常驻每布局页新增两个品牌 GET（无认证装饰层静默失败），BasicLayout 结构/页签栏/状态区零改动；Login.tsx 消费不变（挂载即拉取=下次进入新资源，登出→登录页新背景已实证）；i18n.ts 四表各增 1 行；上传不接 scope.signal/不登记传输管理器（小文件秒级，旧版 axios 同语义，A16 长传输条款不适用已登记） |
| 2026-09-20 | 操作日志页 + sys-log 服务落地 | P28 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/system/sys-log`（单 operation：pageSysLogs POST+JSON body；owner=P28）；② `pages/system-involve/OperationLog` 整页重写（Apex request POST 服务端分页+列偏好 operation-log:main+代码默认钉位 title 左/requestTime 右+行 ID=id+11 列逐列核对旧版+筛选五条件空裁剪+时间秒级序列化）；③ `features/operation-log`：JsonPreviewCell（缺失留白/JSON 空值原文/非 JSON 原文三分态+Popover 格式化+复制，旧 JsonViewer 等价迁移按专项验收收敛）+moduleOptions（14 枚举旧常量等价迁移）；④ 契约实证：id 安全整数自增/requestTime 恒 23 字符毫秒串/targetName 精确匹配（与旧注释不符登记）/未知枚举 500/无令牌 1000000；⑤ 权限 system:operation-log:view 单码挂守卫（只读页无按钮码，下发已实证）；⑥ 路由解除 pending 挂 operationLog 命名空间；⑦ operationLog 四语言同构（en 旧真译沿用，繁日韩 B1 基线补译模块 14 枚举，公共词与 P26 对齐） | P28（本轮 run） | 消费者影响：sys-log 服务仅 P28 消费；i18n.ts 四表各增 1 行；无公共文件改动 |
| 2026-09-20 | license 域增补 + software-license 命名空间 | P29 交付（代码+门禁全绿+带令牌联验通过+勾选）：① `services/license-activation` 增 fetchLicense（POST getLicense 无 body）+ LicenseProofDto（P02 域内扩展，消费者仅 P29）；② 新增 `features/software-license`（useLicenseInfo 三态 hook：scope 中止+页签重激活自动重查自愈；deriveLicenseStatus 纯函数：部署时区日界口径升级=旧版浏览器本地时区收敛登记，缺失=未知+剩余天数留白）；③ SoftwareInformation 整页重写（无表格无轮询；antd Statistic 空串数值化为 0 缺陷经 formatter 直出修复——新增页面消费 Statistic 留白注意点）；④ 路由摘 pending 挂双命名空间 software-license+license-activation；⑤ i18n 新增 software-license 四语言分片（22 键） | P29（本轮 run） | 消费者影响：license-activation 服务仅增不改（P02 消费者零影响）；Statistic 留白手法供后续统计页参考；其余无公共文件改动 |
| 2026-09-20 | 数据库备份页 + database-backup 服务落地 + 权限收敛 | P30 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/system/database-backup`（三 operation 全 GET：getDataBases/getDataBaseBackupFiles/downloadDataBaseBackupFile；owner=P30）；② `pages/system-involve/DatabaseBackupManagement` 整页重写（Apex request 模式全集客户端切片+库下拉默认选第一+清空不重查（旧 effect 判空同语义）+5 列逐列核对+列偏好 database-backup:main+下载 GET Blob+TransferProgressModal 真实进度+传输管理器+取消二次确认——旧 a 标签直下按 A16 升级，旧提示语随形态移除登记）；③ **权限收敛（规格 5.12-12）：旧 .umirc.ts 漏写 access 但旧权限清单真实存在 system:database-backup:view（PERM+菜单映射）——新增 PERM 常量挂路由守卫，download 按钮码既有；不当公开页不凭空造码**；④ 契约实证：下载流式无 Content-Length→不确定进度路径/PGDM 魔数/无令牌下载 7000140 直达业务逻辑（安全观察项登记第 5 节）；⑤ 路由摘 pending 挂 database-backup 命名空间；⑥ i18n 新增 database-backup 四语言分片（24 键，en 旧真译沿用，繁日韩 B1 基线与 systemVersion 下载术语对齐） | P30（本轮 run） | 消费者影响：database-backup 服务仅 P30 消费；permission.constants.ts 增 1 常量（PERM 对象尾部区段，既有消费不受影响）；i18n.ts 四表各增 1 行；TransferProgressModal 第二消费者（P25 建，纯展示组件零改动） |
| 2026-09-20 | 用户管理页 + access-user 服务落地 | P31 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/access-user`（七 operation：pageUsers GET 平铺/addUser/updateState/resetPassword/assignRoles/deleteUser POST body + getRoles GET?userId=（owner=P31 建，P32 复用））；② `pages/access-management/UserManagement` 整页重写（Apex request GET 服务端分页+列偏好 access-user:main+行 ID=id+5 列逐列核对旧版默认左对齐+root 行只读 Tag 收敛+搜索 trim 回首页）；③ `features/access-user`：UserCreateModal（三字段校验+MD5 摘要+关闭销毁草稿）+RoleAssignModal（双查回显+checkStrictly+controller 防乱序）；状态切换行级覆盖层乐观更新等价迁移；删除升级 confirmCommand（P04 同款登记）；④ **模板遗留 REST /users 链整删零残留**（pages/services/features/types system/user 四目录+en-US/system.ts 死键 23 个清理，role/menu 键保留归 P32/P43）；⑤ 契约实证：addUser 形态对照（两/三字段均受理+confirm 一致性校验）/1000140/1000130/pageUsers 含 password 字段安全观察项/addUser 暂态 500 G20 第二例；⑥ 权限 auth:user:view（root 专属）挂守卫+四按钮码下发实证；⑦ 路由解除 pending 挂 access-user 命名空间；⑧ access-user 四语言分片（en 旧真译沿用+插值形态修正，繁日韩 B1 基线，术语与 menu 对齐）；联验缺陷修复：useTranslation 命名空间名不一致致 en 分片未消费（统一 access-user 复验全绿） | P31（本轮 run） | 消费者影响：getRoles 服务 P32 复用（owner 交接登记）；i18n.ts 四表各增 1 行；router/definitions.tsx 单消费者解除 pending；en-US/system.ts 仅删死键不触 role/menu 消费；模板 system/user 链删除经消费者核查（仅链内互引）零外部影响 |
| 2026-09-20 | 角色管理页 + access-role 服务落地 | P32 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/access-role`（六 operation：pageRoles GET 平铺/addRole/updateRole POST body/delete **POST+query id body 空对象**/getPermissions GET?roleId=/assignPermissions POST body；owner=P32）；② `pages/access-management/RoleManagement` 整页重写（Apex request GET 服务端分页+列偏好 access-role:main+行 ID=id+6 列逐列核对旧版默认左对齐+时间列 170（150 截断联验修复）+搜索 trim 回首页+前端不按行收敛内置角色=旧同边界）；③ `features/access-role`：RoleEditModal（单弹窗双模式+三字段校验+回填教训等价）+PermissionAssignModal（**三态持久半选 SPEC D1–D12 完整等价迁移**+双查 controller 防乱序+权限时效下次登录生效不伪造刷新）；删除升级 confirmCommand（P04/P31 同款登记）；④ **模板遗留 REST /roles 链整删零残留**（pages/services/features/types system/role 四目录+en-US/system.ts role 死键 19 个清理，menu 键保留归 P43）；⑤ 契约实证：回显树组装须含祖先（对照实验反例=前端 D4 不变量天然满足）/空半选父节点原样返回=SPEC R3 关闭/空半选提交仅父级 id=D1 实证；⑥ 权限 auth:role:view（root 专属）挂守卫+四按钮码下发实证；⑦ 路由解除 pending 挂 access-role 命名空间；⑧ access-role 四语言分片（en 旧真译沿用含全选/取消全选三态词，繁日韩 B1 基线与 menu/access-user 对齐） | P32（本轮 run） | 消费者影响：access-role 服务仅 P32 消费（getRoles 归属不变）；i18n.ts 四表各增 1 行；router/definitions.tsx 单消费者解除 pending；en-US/system.ts 仅删 role 死键不触 menu 消费；模板 system/role 链删除经消费者核查（仅链内互引）零外部影响 |
| 2026-09-20 | 三方交管页 + tripartite-traffic 服务落地 | P19 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/tripartite-traffic`（六 operation：pageTripartiteTraffics GET 平铺/add·update·delete 均 body 完整实体（删除=旧整行提交同形态）/getSimpleTripartiteTrafficEdgeGroups 无参选项（owner=P19，不复用 P11 getAllSimple=不同 operation）/testCommunication body {applyType,areaCode,systemCode:"rxx"}）；② `pages/tri-traffic/TriTraffic` 整页重写（Apex request GET 服务端分页+列偏好 tripartite-traffic:main+行 ID=id+5 列逐列核对旧版默认左对齐+点边组合列无映射显示原值=已删除组合不可静默替换+搜索 trim 回首页）；③ `features/tri-traffic`：TrafficEditModal（单弹窗双模式+四块表单+扩展参数 Form.List url 整组校验+listToJson/jsonToList 等价旧 transform 双函数+value 非字符串守恒+关闭销毁草稿；补 Switch valuePropName=checked 修复旧版回显不亮=差异收敛登记）；检测升级 confirmCommand 确认层标识目标/仿真+真实结果反馈（专项验收）；删除升级 confirmCommand danger（B3 先例同款）；④ 契约实证：模拟测试双业务码 3200150/3200120（后者=后端真实向 extendParam.url 发起请求，回环拒绝端口限定副作用）/getSimpleTripartiteTrafficEdgeGroups 与 P11 getAllSimple 数据源不同观察项；⑤ 权限 traffic:tripartite:view 守卫+四按钮码五码下发实证；⑥ 路由解除 pending 挂 tripartite-traffic 命名空间；⑦ tripartite-traffic 四语言分片（en 旧真译沿用+失败插值形态修正，繁日韩 B1 基线补译真实译文，术语与 menu/nodeEdgeGroup 对齐） | P19（本轮 run） | 消费者影响：tripartite-traffic 服务仅 P19 消费；i18n.ts 四表各增 1 行；router/definitions.tsx 单消费者解除 pending；零模板遗留链（旧来源为自研页非模板页） |
| 2026-09-20 | 动作分组页 + agv-action-group-manage 服务落地 | ① 新增 `services/action/agv-action-group-manage`（4 operation：pageAgvActionGroups GET 平铺 / addAgvActionGroup / updateAgvActionGroup（int64 id 定位整组提交，拖拽重排与弹窗编辑共用）/ deleteAgvActionGroup（body {id}）；owner=P24；getAGVActionGroups 旧页零 UI 消费者不落地）；② `pages/action-control/AGVActionGroup` 整页重写（Apex request+列偏好 agv-action-group:main+操作列默认钉右+行 ID=id int64；3 列逐列核对旧版：动作组名称/动作组动作（单元格内 DndContext 横向拖拽 Tag 旧同形态+无权限传感器禁用+提交中锁行防连拖+失败 reload 对齐服务端权威顺序）/操作 200；删除升级 confirmCommand danger；G09 不开放排序）；③ `features/action-control/components/ActionGroupModal`（关闭销毁草稿=旧同语义+失效动作合成选项保留原标识+提交拦截 P11 同款）与 DraggableActionTag（旧 DraggableTag 等价迁移）；④ 权限：action-group:view 挂守卫、add/update/delete 三按钮码（四码下发实证）；⑤ 路由解除 pending，meta.i18nNamespaces=['agvActionGroup']；⑥ agvActionGroup 四语言同构（en 旧真译 18 键逐条沿用，繁日韩 B1 基线补译，「動作分組/アクショングループ/액션 그룹」与 menu 对齐） | P24（本轮 run） | 消费者影响：agv-action-group-manage 服务暂无其他消费者（P20 按 contracts 第 5 节分组行契约消费）；i18n.ts 四表各增 1 行；登记三条契约发现（重名 5000000/删除被引用动作 5000010 引用完整性/update 静默过滤不存在 id=前端拦截价值实证）；IAB 垫片可靠路径沉淀（goto 无 Apex 页→注入→SPA 菜单导航）与多页签容器限定教训见 tasks/P24.md §4.2 |
| 2026-09-20 | 任务工艺页 + order-template-manage 服务落地 | P20 交付（代码+门禁全绿+带令牌联验通过+勾选）：① 新增 `services/order-template/order-template-manage`（4 operation：pageOrderTemplates GET 平铺 / createOrderTemplate / updateOrderTemplate（int64 id 整模板替换）/ deleteOrderTemplate（**body {orderTemplateKey} 按调度 key 定位**）；owner=P20；getOrderTemplates 旧页零 UI 消费者不落地（P03 代建共享服务）；createOrderRecords 旧页零消费者不落地）；② `pages/mission-cluster/MissionCreate` 整页重写（Apex request GET 服务端分页+列偏好 order-template:main+行 ID=id+4 列逐列核对旧版默认左对齐+搜索 256px trim 回首页；**展开行双层子表均 Apex data 模式**（子任务 4 列行 ID=id/动作 4 列行 ID=「动作类型+卡内序号」复合；动作参数 Descriptions 旧同形态）；删除升级 confirmCommand danger；G09 不开放排序）；③ `features/mission-cluster/components/MissionTemplateModal` 三态弹窗（创建/编辑/复制=旧 isModify+modifyRow 组合同语义，复制回显走 create 不带 id；指定车辆/分组互斥显示+切换互斥清理；子任务 Form.List 至少 1 条+地图切换清站点+站点按 mapId 缓存共享+编辑回显合成缓存条目；动作卡阻塞类型未知原值并入+参数 value 守恒（P23 同款）；打开按模式回填依赖 open=P32 教训；关闭销毁草稿）；④ 契约实证：**后端允许模板重名（与 P24 组名 5000000 相反）**/互斥字段收敛提交/update 无 G18 类缺陷；脚本倒序清理陷阱教训登记（P24 倒序 dups[1:] 误删链，非后端缺陷）；⑤ 权限 mission-flow:view 挂守卫+add/copy/update/delete 四按钮码五码下发实证（**历史码值交叉：任务工艺=mission-flow:* 族**）；⑥ 路由解除 pending 挂 orderTemplate 命名空间；⑦ orderTemplate 四语言分片（en 旧真译 58 键逐条沿用+插值形态修正，繁日韩 B1 基线补译真实译文，术语与 orderRecord/agvAction/crossMap 对齐） | P20（本轮 run） | 消费者影响：order-template-manage 服务暂无其他消费者（P21 按 contracts 第 5 节共享行消费 fetchOrderTemplates）；i18n.ts 四表各增 1 行；router/definitions.tsx 单消费者解除 pending；【观察项登记】清列偏好+reload 后代码 defaultPinning 未在 DOM 激活（传导链对照实验隔离：显式偏好钉右正常激活，仅「无存储→首帧默认值」场景，跨页样板级 P14–P24 同代码，建议 V01 统一收口） |
