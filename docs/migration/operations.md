# 操作映射总台账（operations）

> 由统筹者汇总维护；页面任务只更新自己的 `tasks/<ID>.md`，由统筹者把已合并实现的映射汇总到这里。
> 列：旧路由/组件/操作 → 新实现 → method/path/operationId → 权限 → 状态/证据。
> T00.1 初始化（2026-09-17）：尚无已合并页面实现，全部待各任务交付后登记。

| 任务 | 旧入口/操作 | 新实现 | Method + Path | operationId | 权限 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P02 | AuthorizeIngress/硬件码展示 | `pages/authorize-ingress` + `features/license-activation/hooks/useHardwareInfo` | GET /fms/v1/auth/license/getHardwareInfo | getHardwareInfo | 无需认证（实证） | 已合并；验证已通过 | tasks/P02.md（联验 2026-09-18） |
| P02 | AuthorizeIngress/硬件码复制 | `features/license-activation/utils/copyText` | — | — | 登录 | 已合并；验证已通过 | 剪贴板读回实证 |
| P02 | AuthorizeIngress/激活提交 | `features/license-activation/components/ActivationForm`（P29 复用） | POST /fms/v1/auth/license/softwareActivation | softwareActivation | 登录（security None） | 已合并；读链路通过，**写副作用待专用环境** | tasks/P02.md |
| P02 | （旧）成功跳 /over-look | activationConfirmed + resolveLandingPath 按权限导航 | — | — | 登录 | 已合并；落点机制实证 | tasks/P02.md |
| P41 | UnAccess/403 反馈+退出登录 | `pages/un-access` + logout 服务 | POST /fms/v1/auth/authorize/logout | logout | 登录 | 已合并；验证已通过 | tasks/P41.md（联验 2026-09-18） |
| P42 | NotFound/404 反馈+恢复路径 | `pages/error/NotFound`（公开 /404 与受保护根内 `*` 兜底共用） | — | — | 公开或登录 | 已合并；验证已通过 | tasks/P42.md（联验 2026-09-18） |
| P42 | （旧）首页按钮 → /over-look | 「首页」+ resolveLandingPath 落点导航（replace）；未登录「去登录」 | — | — | 登录或未登录分支 | 已合并；验证已通过 | tasks/P42.md |
| P42 | ServerError /500 + 两级错误边界 | `pages/error/ServerError` + `RouterErrorBoundary` + `PageErrorBoundary`（error 分片基座常载） | — | — | 登录（/500 守卫） | 已合并；错误边界运行时呈现待自然场景复核（资源齐备静态核对通过） | tasks/P42.md |
| H01 | Overlook/调度监控入口 | `pages/overlook/Overlook` 渲染统一暂缓说明（MigrationPending 玻璃面板）；`migrationDeferred` 标记不作落点/回跳候选 | — | — | overview:view（菜单/直访一致） | 已合并；验证已通过（零业务请求/零 WebSocket/查询上下文保留） | tasks/H01.md（联验 2026-09-18） |
| H02 | MapNestModify/地图编辑入口 | `pages/map-through/MapNestModify` 渲染统一暂缓说明（复用 MigrationPending）；`migrationDeferred` 标记，旧 Konva 编辑器不迁移（P09/P07 资源归属不受影响） | — | — | map-edit:view（菜单/直访一致） | 已合并；验证已通过（零业务请求/查询上下文保留/五语言/两主题） | tasks/H02.md（联验 2026-09-18） |
| H03 | RecordPlayback/录制回放入口 | `pages/record-playback/RecordPlayback` 渲染统一暂缓说明（复用 MigrationPending）；`migrationDeferred` 标记，旧 Konva 回放/导入导出/时间轴/轮询整体不迁移（P03/P34 资源归属不受影响） | — | — | record-playback:view（菜单/直访一致） | 已合并；验证已通过（零业务请求/查询上下文保留/五语言/两主题） | tasks/H03.md（联验 2026-09-18） |

| P03 | OrderRecord/状态统计 | `features/order-record/components/OrderStatisticsBar` + useVisiblePolling（约 5s 可见串行） | GET /fms/v1/dispatcher/orderRecord/orderRecordStateStatistic | orderRecordStateStatistic | order-record:view | 已验证（带令牌真实计数+轮询递增实证，2026-09-18） | tasks/P03.md（2026-09-18） |
| P03 | OrderRecord/列表分页筛选+导出 | `pages/order-record/OrderRecord`（Apex request 模式+列设置面板）+ OrderSearchForm + api.downloadGet/transferManager | GET /fms/v1/dispatcher/orderRecord/pageOrderRecords；GET …/exportOrderRecords | pageOrderRecords / exportOrderRecords | order-record:view | 已验证（带令牌：真实数据/分页/筛选命中/导出 4362 字节单条 xlsx+RFC5987；G04 平铺后端接受实证；列偏好闭环+缺陷修复） | tasks/P03.md |
| P03 | RecordTable/取消+移队列+跳过+继续 | confirmCommand 确认 + OrderCancelModal（取消原因必填） | POST /fms/v1/dispatcher/orderTask/orderTaskOperate | orderTaskOperate | order-record:operate | 前端完成；写操作待专用环境；data 语义随写操作联调 | tasks/P03.md |
| P03 | MockDispatchModal/模拟分配（真实仿真，标注仿真） | MockDispatchModal（防重复提交） | POST /fms/v1/dispatcher/orderTask/mockDispatch | mockDispatch | order-record:check | 前端完成；待专用环境验收 | tasks/P03.md |
| P03 | CreateOrderModal/创建任务 | CreateOrderModal（互斥约束/按行站点选项/草稿保留） | POST /fms/v1/dispatcher/orderRecord/createOrderRecord | createOrderRecord | order-record:create | 弹窗渲染/选项/草稿已验证；提交待专用环境验收 | tasks/P03.md |
| P03 | OrderInfoModal/详情快速预览（mission 分页+动作子表） | OrderInfoModal + MissionActionsTable（Apex；P38 起迁入 features/order-detail 为「壳+OrderDetailPanel」） | POST /fms/v1/dispatcher/orderRecord/getOrderRecordDetail | getOrderRecordDetail | order-record:view | 已验证（带令牌主体+mission+动作子表；P38 往返已联验闭环） | tasks/P03.md；tasks/P38.md |
| P05 | VehicleDisplay/列表分页+名称标识搜索 | `pages/vehicle-deploy/VehicleDisplay`（Apex request 模式+列设置面板+选择列）+ useVisiblePolling（约 5s 可见串行） | GET /fms/v1/dispatcher/vehicle/pageVehicles | pageVehicles | vehicle-list:view | 已验证（带令牌真实 56 辆/分页/筛选命中；G04 平铺后端接受实证；轮询保持筛选） | tasks/P05.md（2026-09-18） |
| P05 | VehicleDisplay/详情快速预览 | `features/vehicle-list/components/VehicleDetailDrawer`（24 项，数据取行记录零请求） | —（pageVehicles 行内数据） | — | 详情不限权（view） | 已验证（坐标三位小数/空值留白/JSON 原文）；P39 独立详情往返已联验（2026-09-18 补验：抽屉→完整详情→页签→往返列表保留） | tasks/P05.md |
| P05 | VehicleModal/接入车辆（新增） | `features/vehicle-list/components/VehicleFormModal`（草稿保留+useTabDirtyGuard；未关联选项 getUnRelationSimpleVehicles） | POST /fms/v1/dispatcher/vehicle/addVehicle；GET …/getUnRelationSimpleVehicles | addVehicle / getUnRelationSimpleVehicles | vehicle-list:add | 弹窗/选项/草稿已验证（未关联真实空集合）；提交待专用环境 | tasks/P05.md |
| P05 | VehicleModal/编辑车辆 | VehicleFormModal 编辑模式（按行重填，agvKey 禁用；已修复编辑→新增残留缺陷） | POST /fms/v1/dispatcher/vehicle/updateVehicle | updateVehicle | vehicle-list:update | 预填已验证；提交待专用环境 | tasks/P05.md |
| P05 | Switch 调度状态翻转 | confirmCommand → verifyVehicleFresh 核验 → updateVehicle 全量翻转（旧实现同通道） | POST /fms/v1/dispatcher/vehicle/updateVehicle | updateVehicle | vehicle-list:enable（无权限 disabled） | 确认框/核验链路已验证；执行待专用环境 | tasks/P05.md |
| P05 | 删除车辆 | confirmCommand（danger）→ deleteVehicle → 清理选择集 | POST /fms/v1/dispatcher/vehicle/deleteVehicle | deleteVehicle | vehicle-list:delete | 确认框已验证；执行待专用环境 | tasks/P05.md |
| P05 | 单车指令（暂停/继续）+ 批量指令（一键暂停/继续/启用/禁用） | 操作/一键操作 Dropdown → confirmCommand（无勾选=全部车辆）→ 核验（单车）→ operate；整批接受诚实文案（A15） | POST /fms/v1/dispatcher/vehicle/vehicleOperate；POST …/allVehicleOperate | vehicleOperate / allVehicleOperate | vehicle-list:operate / vehicle-list:batch-operate | 确认框（含跨页对象清单/全部车辆语义）已验证；执行待专用环境 | tasks/P05.md |
| P38 | 直访 /order-info 按任务上下文查询（主体+mission 分页+动作展开） | `pages/order-info/OrderInfo`（工作区页签默认形态，features/order-detail/OrderDetailPanel 单请求喂两区域） | POST /fms/v1/dispatcher/orderRecord/getOrderRecordDetail | getOrderRecordDetail | order-record:view（守卫） | 已验证（带令牌主体 17 项+mission 2 条+动作子表；不存在任务 code=200+data=null 实证→「任务不存在」反馈） | tasks/P38.md（联验 2026-09-18） |
| P38 | 详情参数（orderKey）+ 实体页签隔离 + 独立窗口 | 命名参数 `?orderKey=`（buildOrderInfoPath/parseOrderInfoSearch 单一真相，裸值 `?KEY` 与 `?KEY=` 兼容）；不同任务独立页签；「独立窗口」按钮经 openStandaloneWindow 打开同一路径 | — | — | order-record:view（新窗口同守卫） | 已验证（守卫回跳带参数/页签隔离/往返快照/独立窗口渲染） | tasks/P38.md |
| P38 | （旧）列表弹窗「完整详情」入口（D08 新增导航便利，旧系统无） | OrderInfoModal footer「完整详情」→ 关弹窗 + navigate /order-info?orderKey=… | — | — | order-record:view | 已验证（弹窗回归+导航+新页签生成+返回快照） | tasks/P38.md |
| P39 | 直访 /vehicle-info 按车辆标识查询实时状态（33 叶子字段递归展平三列 Descriptions，空值留白/枚举映射/时间戳秒级） | `pages/vehicle-info/VehicleInfo`（工作区页签默认形态；features/vehicle-detail/VehicleDetailPanel：getVehicleState + useVisiblePolling 约 5s 可见轮询，失败清空远端区域退避重查，不存在态停轮询） | GET /fms/v1/dispatcher/vehicle/getVehicleState | getVehicleState | vehicle-list:view（守卫） | 已验证（带令牌真实车辆完整字段渲染/不存在车辆 code=200+data=null 实证→「车辆不存在」反馈/缺参数零请求） | tasks/P39.md（联验 2026-09-18） |
| P39 | 详情参数（vehicleKey）+ 实体页签隔离 + 独立窗口 + 五语言 | 命名参数 `?vehicleKey=`（buildVehicleInfoPath/parseVehicleInfoSearch 单一真相，裸值 `?KEY` 与 `?KEY=` 兼容）；不同车辆独立页签；「独立窗口」按钮；vehicleInfo 四语言分片 67 key 同构（en 旧真译沿用，繁日韩 B1 基线）；P05 列表抽屉新增「完整详情」入口（宿主 VehicleDisplay 组装跨域导航） | — | — | vehicle-list:view（新窗口同守卫） | 已验证（守卫回跳带参数/两 vehicleKey 页签隔离/独立窗口渲染/五语言 html lang/裸值兼容/双主题双宽度截图）；抽屉点击链路经 RO 垫片补验通过（IAB ResizeObserver/rAF 抑制为环境问题，登记 P39.md 与 AGENTS 第 5 节） | tasks/P39.md |
| P04 | VehicleGroup/列表分页+搜索+展开行组内车辆 Tag | `pages/vehicle-deploy/VehicleGroup`（Apex request 模式+列设置面板+expandable 展开行；G09 不开放排序） | GET /fms/v1/dispatcher/vehicleGroup/pageVehicleGroups（G04 平铺 query） | pageVehicleGroups | vehicle-group:view（守卫） | 已验证（带令牌：空集合如实呈现/新增后行渲染/筛选命中与空结果/展开行 Tag；XHR 抓包平铺形态实证） | tasks/P04.md（2026-09-18） |
| P04 | GroupModal/新增分组（名称+Transfer 选车） | `features/vehicle-group/components/GroupFormModal`（草稿保留+useTabDirtyGuard；Transfer 复用共享选项 getSimpleVehicles；失效关联合成条目保留原标识+「已不可用」标注） | POST /fms/v1/dispatcher/vehicleGroup/addVehicleGroup；GET …/vehicle/getSimpleVehicles | addVehicleGroup / getSimpleVehicles | vehicle-group:add（无权限隐藏） | 已验证（真实新增成功+后端落库一致；Transfer 56 项真实加载；草稿保留闭环） | tasks/P04.md |
| P04 | GroupModal/编辑分组 | GroupFormModal 编辑模式（按行重填，simpleAGVs key 回填/缺失回退 agvKeys） | POST /fms/v1/dispatcher/vehicleGroup/updateVehicleGroup | updateVehicleGroup | vehicle-group:update（无权限隐藏） | 前端已验证（XHR 请求体正确+code=200+自动刷新）；**后端缺陷 P04-G1：groupName 不落库**（仅 updateTime 更新；vehicleKeys 生效），登记 gaps.md 待后端修复复验 | tasks/P04.md |
| P04 | 删除分组 | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteVehicleGroup → 刷新 | POST /fms/v1/dispatcher/vehicleGroup/deleteVehicleGroup | deleteVehicleGroup | vehicle-group:delete（无权限隐藏） | 已验证（取消路径保留；确认路径真实删除+列表清空+后端 total=0 自清理） | tasks/P04.md |
| P06 | VehicleType/列表分页+名称编码双筛选 | `pages/vehicle-deploy/VehicleType`（Apex request 模式+列设置面板 carrier-type:main；G09 不开放排序；无轮询无刷新按钮） | POST /fms/v1/dispatcher/carrier/pageCarriers（body 平铺） | pageCarriers | carrier:view（守卫；后端权限树真实 MENU 码，P06 核对登记） | 已验证（带令牌：历史数据渲染/筛选命中与空态/守卫回跳带 redirect 且登录落点闭环） | tasks/P06.md（2026-09-19） |
| P06 | CarrierModal/新增载具类型（名称+编码+尺寸） | `features/carrier-type/components/CarrierFormModal`（草稿保留+useTabDirtyGuard+失败留稿；名称/编码 64 字上限；长度/宽度 InputNumber min=1，mm 协议原样） | POST /fms/v1/dispatcher/carrier/createCarrier | createCarrier | carrier:add（无权限隐藏） | 已验证（请求体 XHR 抓包平铺正确+真实落库一致；必填四条齐出；低于 min 的 0 被表单拦截） | tasks/P06.md |
| P06 | CarrierModal/编辑载具类型 | CarrierFormModal 编辑模式（按行重填四字段；编辑目标清除复位干净基线） | POST /fms/v1/dispatcher/carrier/updateCarrier（id 定位+全量字段） | updateCarrier | carrier:update（无权限隐藏） | 已验证（请求体正确；名称与尺寸真实落库，列表回读一致——carrier 无 G18 类缺陷） | tasks/P06.md |
| P06 | 删除载具类型 | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteCarrier → 刷新 | POST /fms/v1/dispatcher/carrier/deleteCarrier | deleteCarrier | carrier:delete（无权限隐藏） | 已验证（取消路径行保留；确认路径真实删除+后端自清理恢复 1 条历史数据） | tasks/P06.md |
| P07 | NodeMapping/列表分页+映射名称筛选 | `pages/vehicle-deploy/NodeMapping`（Apex request 模式+列偏好 node-mapping:main；G09 不开放排序；无轮询无刷新按钮；行 ID 恒取 mappingKey） | GET /fms/v1/dispatcher/agvNodeMapping/pageAGVNodeMapping（GET+query） | pageAgvNodeMappings | node-mapping:view（守卫；旧 SPEC §6 后端已下发，本轮登录响应实证四码） | 已验证（带令牌：真实数据渲染/筛选命中与空态/守卫回跳带 redirect 且登录落点闭环） | tasks/P07.md（2026-09-19） |
| P07 | MappingModal/新增节点映射（名称+关联AGV+地图组/映射行/获取建议） | `features/node-mapping/components/MappingFormModal`（草稿保留+useTabDirtyGuard+失败留稿+顶层清空；名称 64 字上限；行三态纯函数；半填高亮+自动开过滤；行表格 Apex data 模式+antd 受控分页器；关联AGV 必填——后端拒绝空 agvKeys 见 G19） | POST /fms/v1/dispatcher/agvNodeMapping/saveAGVNodeMapping | saveAgvNodeMapping | node-mapping:add（无权限隐藏） | 已验证（请求体 XHR 抓包平铺正确+真实落库一致；建议合并 4 行；半填拦截；选点弹窗回填 D3 语义） | tasks/P07.md |
| P07 | MappingModal/编辑节点映射（全量回填+整体替换） | MappingFormModal 编辑模式（按行重填+预拉各地图节点；失效节点并入选项保显示） | POST /fms/v1/dispatcher/agvNodeMapping/updateAGVNodeMapping（mappingKey+全量分组，整体替换） | updateAgvNodeMapping | node-mapping:update（无权限隐藏） | 已验证（请求体 mappingKey+全量正确；改名真实落库，列表回读一致——无 G18 类缺陷） | tasks/P07.md |
| P07 | 删除节点映射 | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteAgvNodeMapping → 刷新 | POST /fms/v1/dispatcher/agvNodeMapping/deleteAGVNodeMapping | deleteAgvNodeMapping | node-mapping:delete（无权限隐藏） | 已验证（取消路径行保留；确认路径真实删除+后端自清理恢复 2 条历史数据） | tasks/P07.md |
| P07 | 地图选点二级弹窗 | `features/node-mapping/components/MapPickerModal`（ReadOnlyMap 单选节点+回显定位不闪烁+footer 当前行/已选+失效标注「已不在地图上」+确定禁用=无选中） | GET /fms/v1/dispatcher/map/getMapInfo（ReadOnlyMap 内部 fetchMapGraph） | —（消费 T00 共享组件） | — | 已验证（回显选中/坐标点击命中/footer 实时/确定回填映射点仅未手填跟随/取消不写回） | tasks/P07.md |
| P08 | AlarmCodeManagement/列表分页+告警码筛选 | `pages/system-involve/AlarmCodeManagement`（Apex request 模式+列偏好 alarm-code:main；G09 不开放排序；无轮询无刷新按钮；行 ID=String(id)） | GET /fms/v1/dispatcher/vehicleAlarmCode/pageVehicleAlarmCodes（GET+query） | pageVehicleAlarmCodes | vehicle-alarm-code:view（守卫；后端权限树 MENU id=27） | 已验证（带令牌：真实数据 193 条渲染/筛选命中与空态/守卫回跳带 redirect 且登录落点闭环） | tasks/P08.md（2026-09-19） |
| P08 | AlarmCodeModal/新增车辆告警码（告警码+多语言行） | `features/vehicle-alarm-code/components/AlarmCodeFormModal`（草稿保留+useTabDirtyGuard+失败留稿+顶层清空；告警码 128 字上限；语言/描述必填、建议选填；行残行过滤旧兜底同款） | POST /fms/v1/dispatcher/vehicleAlarmCode/addVehicleAlarmCode | addVehicleAlarmCode | vehicle-alarm-code:add（无权限隐藏） | 已验证（创建 toast+落库一致；重复码 2000430 失败留稿实证；2000450 records 实际必填已适配） | tasks/P08.md |
| P08 | AlarmCodeModal/编辑车辆告警码（全量回填+id 定位） | AlarmCodeFormModal 编辑模式 | POST /fms/v1/dispatcher/vehicleAlarmCode/updateVehicleAlarmCode | updateVehicleAlarmCode | vehicle-alarm-code:update（无权限隐藏） | 已验证（回填双行 desc/hint；改名+全量真实落库，无 G18 类缺陷） | tasks/P08.md |
| P08 | 删除车辆告警码 | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteAlarmCode → 刷新（**按 alarmCode 字符串定位**，协议原样） | POST /fms/v1/dispatcher/vehicleAlarmCode/deleteVehicleAlarmCode | deleteVehicleAlarmCode | vehicle-alarm-code:delete（无权限隐藏） | 已验证（取消路径行保留；确认路径真实删除+后端自清理恢复 193 条） | tasks/P08.md |
| P08 | 上传告警码文件（全量覆盖） | 类型校验→confirmCommand（danger「继续上传」）→ beginTransfer 登记（切页继续/关页提示/真实字节进度）→ uploadAlarmCodeFile | POST /fms/v1/dispatcher/vehicleAlarmCode/uploadVehicleAlarmCodeFile（multipart/form-data 字段 file，G05 实证） | uploadVehicleAlarmCodeFile | vehicle-alarm-code:upload（无权限隐藏） | 已验证（确认框五要素/取消零请求/确认真实请求+成功 toast+总数不变；伪文件 500 拒绝且数据不变） | tasks/P08.md |
| P08 | 下载告警码文件 | beginTransfer 登记 → downloadAlarmCodeFile（POST Blob 通道 api.downloadPost）；JSON/HTML 不存伪文件（DoD 9） | POST /fms/v1/dispatcher/vehicleAlarmCode/downVehicleAlarmCodeFile | downVehicleAlarmCodeFile | vehicle-alarm-code:download（无权限隐藏） | 已验证（真实请求+成功 toast；content-disposition 文件名 AGV_Alarm_Code.xlsx 优先本地默认名兜底） | tasks/P08.md |
| P09 | MapList/列表分页+名称标识筛选 | `pages/map-through/MapList`（Apex request 模式+列偏好 map-list:main；G09 不开放排序；无轮询无刷新按钮；行 ID=mapId；query 命中语义=名称模糊属后端行为） | GET /fms/v1/dispatcher/map/pageMapInfos（GET+query） | pageMaps | map-list:view（守卫；前序轮登录响应实证十码下发） | 已验证（带令牌：真实数据 6 条渲染/筛选命中与空态/守卫回跳带 redirect 且登录落点闭环） | tasks/P09.md（2026-09-19） |
| P09 | MapModal/新增地图（名称+状态+楼层） | `features/map-list/components/MapFormModal`（草稿保留+useTabDirtyGuard+失败留稿+顶层清空；名称 64 字上限；楼层 -200~200 整数） | POST /fms/v1/dispatcher/map/createMap | createMap | map-list:add（无权限隐藏） | 已验证（创建 toast+落库一致+列表刷新；必填三条齐出） | tasks/P09.md |
| P09 | MapModal/编辑地图（名称禁用，mapKey 定位只提交 mapState/floor） | MapFormModal 编辑模式（按行重填；旧实现同边界） | POST /fms/v1/dispatcher/map/updateMap | updateMap | map-list:update（无权限隐藏） | 已验证（回填+名称禁用；状态/楼层真实落库，无 G18 类缺陷） | tasks/P09.md |
| P09 | 删除地图（**后端规则：先禁用再删除**，ENABLED 删除 3000210 如实透传） | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteMap（mapId 定位）→ 刷新 | POST /fms/v1/dispatcher/map/deleteMap | deleteMap | map-list:delete（无权限隐藏） | 已验证（取消路径行保留；ENABLED 拒绝透传+行保留；禁用后确认删除+后端自清理；不存在目标 3000100） | tasks/P09.md |
| P09 | 导入调度地图(.zip)/导入车载地图(.bin) | `MapUploadButton`：beforeUpload 扩展名校验→beginTransfer 登记（切页继续/关页提示/真实字节进度，旧伪进度条废弃）；新增性操作无确认（旧同） | POST /fms/v1/dispatcher/map/uploadMap；POST …/uploadVehicleMap（multipart/form-data 字段 file，G05 同 P08 形态） | uploadDispatcherMapFile / uploadVehicleMapFile | map-list:upload-dispatcher-map / map-list:upload-vehicle-map（无权限隐藏） | 已验证（.txt 扩展名拒绝零请求；伪 zip 真实请求→500 如实透传零净变化；真实 zip 回传=追加源地图未发布版本零线上影响；伪 bin→4000060 前序轮同证） | tasks/P09.md |
| P09 | 版本管理弹窗（版本分页+防抖搜索+发布） | `VersionModal`（Apex request 按 mapId 隔离；是否发布列按 currentMapInfoVersion；**发布按钮禁用依据=currentMapInfoVersion 并旧字段 published（新后端不下发，适配实证）**；发布 confirmCommand+行级防连点） | GET /fms/v1/dispatcher/mapVersion/pageMapInfoVersions；POST …/publishMapInfoVersion | pageMapVersions / publishMapVersion | map-list:version / map-version:update / map-version:publish（无权限隐藏） | 已验证（版本渲染/搜索命中/发布按钮 current=true 禁用实证/确认框五要素+取消零请求）；**发布对生产地图的执行留专用环境；publish 对不存在 id 返回 200 登记 G21** | tasks/P09.md |
| P09 | 推送版本到车辆 + 下载版本文件 | `PushVersionModal` 二层弹窗（Transfer 复用 getSimpleVehicles 56 项+至少一辆校验+SLAM 开关默认关+受理语义文案）；下载 beginTransfer+DoD9 伪文件防线+文件名 content-disposition 优先本地兜底（两分支均有实证） | POST /fms/v1/dispatcher/mapVersion/pushMapInfoVersion；POST /fms/v1/dispatcher/map/downloadMap（api.downloadPost Blob） | pushMapVersion / downloadMapFile | —（推送入口旧实现无码）/ map-version:download（无权限隐藏） | 已验证（弹窗结构/选项加载/空选校验/取消零请求；下载真实 465KB zip；DEV 版本下发 content-disposition、6车间 V15 不下发）；**推送执行留专用环境（车辆副作用）** | tasks/P09.md |
| P09 | 版本「编辑」导航 H02 + 拉取地图 G07 禁用 | 先关弹窗再导航 `/map-through/map-nest-modify?mapId=…&mapVersionId=…`（暂缓说明页，不携入编辑器）；拉取按钮 disabled+Tooltip 说明（downloadMapInfo 未在 OpenAPI，不猜替语义） | —（消费 H02 路由） | — | map-version:update | 已验证（带参导航闭环+暂缓页渲染；disabled+Tooltip 真实指针实证） | tasks/P09.md |
| P10 | CrossMaps/列表分页+名称筛选+展开行子表 | `pages/map-through/CrossMaps`（Apex request 模式+列偏好 cross-map:main；G09 不开放排序；行 ID=deviceKey；**展开行子表 Apex data 模式**：地图名称/地图ID/节点名称/节点ID 逐列核对旧版） | GET /fms/v1/dispatcher/crossMap/pageCrossMaps（GET+query） | pageCrossMaps | cross-map:view（守卫；登录响应实证四码下发） | 已验证（带令牌：空态/创建后渲染/展开行子表真实数据/筛选命中与空态/守卫回跳带 redirect 登录落点闭环） | tasks/P10.md（2026-09-19） |
| P10 | CrossMapModal/新增关联（名称+电梯+至少两行地图站点） | `features/cross-map/components/CrossMapModal`（草稿保留+useTabDirtyGuard+失败留稿+顶层清空；名称 64 字；至少两行+地图查重+行级必填；**行级站点独立加载防乱序，切换地图清站点并重载**；失效地图/站点/电梯合成选项保原标识） | POST /fms/v1/dispatcher/crossMap/createCrossMap | createCrossMap | cross-map:add（无权限隐藏） | 已验证（选项真实加载/站点联动加载 1691 站点/查重与必填拦截/提交成功落库一致+列表刷新；**后端 4000540 节点角度校验登记 G22**——站点须在地图数据中配置 angle，OpenAPI/旧实现均无此字段，前端如实透传） | tasks/P10.md |
| P10 | CrossMapModal/编辑关联（全字段可改，id 定位整体替换） | CrossMapModal 编辑模式（按行重填+逐行重新加载站点；失效项合成选项保显示；失效值不阻止提交——P07 判例） | POST /fms/v1/dispatcher/crossMap/updateCrossMap（int64 id+crossMaps 整体替换） | updateCrossMap | cross-map:update（无权限隐藏） | 已验证（回显含站点名称非裸 ID；改名+换站点真实落库一致，无 G18 类缺陷；列表实时更新） | tasks/P10.md |
| P10 | 删除跨地图关联 | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteCrossMap（int64 id 定位）→ 刷新 | POST /fms/v1/dispatcher/crossMap/deleteCrossMap | deleteCrossMap | cross-map:delete（无权限隐藏） | 已验证（取消路径行保留；确认删除+后端自清理；**不存在 id 亦 200 → G21 扩展登记**） | tasks/P10.md |
| P10 | 站点选项（弹窗行级按地图加载） | `fetchCrossMapStations`（页面私有；AbortController 行内防乱序；缺 id 条目过滤；名称自然排序） | GET /fms/v1/dispatcher/crossMap/getCrossMapStations?mapId= | fetchCrossMapStations | —（页面内选项） | 已验证（按地图隔离 6车间 1691/测试大地图 463；联动加载/搜索选中/展开重查恢复） | tasks/P10.md |
| P10 | 电梯选项（跨地图电梯下拉，owner=P10 contracts 第 88 行） | `fetchElevatorOptions`（全量；缺 deviceKey 条目过滤；DTO 仅声明本页消费字段） | GET /fms/v1/device/elevator/getElevators | fetchElevatorOptions | —（页面内选项） | 已验证（联验经 addElevator 虚拟驱动构造测试电梯后真实加载；验证后删除现场零残留） | tasks/P10.md |
| P11 | PointEdgeCombination/列表分页+名称筛选+展开行子表 | `pages/map-through/PointEdgeCombination`（Apex request 模式+列偏好 node-edge-group:main；G09 不开放排序；行 ID=id int64 经 stringFieldRowId 收敛；**展开行子表 Apex data 模式**：点边组合名称/所属地图/节点数量/边数量逐列核对旧版，计数缺失留白） | GET /fms/v1/dispatcher/systemNodeEdgeGroup/pageSystemNodeEdgeGroups（GET+query） | pageSystemNodeEdgeGroups | point-edge-combination:view（守卫；登录响应实证四码下发） | 已验证（带令牌：空态/创建后渲染/展开行子表真实明细/筛选命中与空态/守卫回跳带 redirect 登录落点闭环） | tasks/P11.md（2026-09-19） |
| P11 | NodeEdgeGroupModal/新增组合（名称+Transfer 选点边组合） | `features/node-edge-group/components/NodeEdgeGroupModal`（草稿保留+useTabDirtyGuard+失败留稿+顶层清空；名称 64 字；至少一个点边组合；**勾选态与已选集合分离受控——选择取消不污染草稿**；失效关联合成条目保原名称+「已不可用」标注） | POST /fms/v1/dispatcher/systemNodeEdgeGroup/createSystemNodeEdgeGroup | createSystemNodeEdgeGroup | point-edge-combination:add（无权限隐藏） | 已验证（选项真实加载 19 条/必填拦截两错齐出/勾选取消不污染已选/提交成功落库一致+顺序保持+列表刷新；空集合后端 500 登记 G23） | tasks/P11.md |
| P11 | NodeEdgeGroupModal/编辑组合（全字段可改，id 定位整体替换） | NodeEdgeGroupModal 编辑模式（按行重填，已选 id 从明细嵌套 nodeEdgeGroups[].nodeEdgeGroup.id 提取回显为名称；nodeEdgeGroupKey 后端生成不可改不提交） | POST /fms/v1/dispatcher/systemNodeEdgeGroup/updateSystemNodeEdgeGroup（int64 id+mapNodeEdgeGroupIds 整体替换） | updateSystemNodeEdgeGroup | point-edge-combination:update（无权限隐藏） | 已验证（回显完整非裸 ID；移除一项+改名整体替换落库一致，key 不变顺序保持，无 G18 类缺陷；列表实时更新） | tasks/P11.md |
| P11 | 删除多地图点边组合 | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteSystemNodeEdgeGroup（int64 id 定位）→ 刷新 | POST /fms/v1/dispatcher/systemNodeEdgeGroup/deleteSystemNodeEdgeGroup | deleteSystemNodeEdgeGroup | point-edge-combination:delete（无权限隐藏） | 已验证（取消路径行保留；确认删除+后端自清理；**不存在 id 亦 200 → G21 扩展登记**） | tasks/P11.md |
| P11 | 点边组合选项（弹窗 Transfer 数据源，owner=P11） | `fetchAllSimpleNodeEdgeGroups`（全量；缺 id 条目过滤；仅弹窗打开时加载） | GET /fms/v1/dispatcher/systemNodeEdgeGroup/getAllSimpleNodeEdgeGroups | fetchAllSimpleNodeEdgeGroups | —（页面内选项） | 已验证（19 条真实数据「独占区」系列；P19 引用契约已确认：LittleSimpleMapNodeEdgeGroup{id:string,name:string}，见 P11.md 第 5 节） | tasks/P11.md |
| P12 | MapPushNotificationRecords/列表分页（接口仅分页无筛选，G09 不开放排序） | `pages/map-through/MapPushNotificationRecords`（Apex request 模式+列偏好 map-push-record:main+操作列默认钉右；行 ID=id int64 经 stringFieldRowId；**推送结果列=子记录状态计数 Tag 汇总**，未知枚举显示原值灰 Tag；**useVisiblePolling 约 5 秒可见串行轮询**——命令状态追踪页，P05 样板；旧手动刷新按钮按按钮纪律废弃） | GET /fms/v1/dispatcher/mapPushRecord/pageMapPushRecords（GET+query 平铺，G04 同款实证） | pageMapPushRecords | map-push-record:view（守卫；登录响应实证两码下发） | 已验证（带令牌：GET 平铺/My-Plus 形态/total=0 真实空态渲染/轮询 6000ms 节拍/越界页码/无令牌 1000000 对照/守卫回跳带 redirect 登录落点闭环） | tasks/P12.md（2026-09-19） |
| P12 | 记录级重新推送/取消推送（整批全部车辆） | confirmCommand 命令确认（对象=子记录车辆清单>5 台合并计数+影响说明+提交≠完成附注；取消仅该批存在 WAITING/RUNNING 子记录时开放入口）→ rePushMap/cancelPushMap（只传 mapPushRecordId）→ 受理语义提示+刷新 | POST /fms/v1/dispatcher/mapPushRecord/rePushMap、POST /fms/v1/dispatcher/mapPushRecord/cancelPushMap | rePushMap、cancelPushMap | map-push-record:re-push（重推/取消共用一码，后端无取消专用码；无权限隐藏） | 前端完成（确认层代码路径+防连点交付；**命令执行留专用环境**——影响现场车辆副作用，联验环境 total=0 不可运行时验证） | tasks/P12.md |
| P12 | 子记录级重推/取消推送（单台车辆）+ 展开行子表前端筛选分页 | `features/map-push-record/components/MapPushSubRecordTable`（**Apex data 模式**：车辆子记录全量随主行下发+受控 PaginationState 前端筛选分页（车辆名称关键字+状态），筛选回首页/页码兜底；列=车辆名称/推送状态 Tag/完成时间/失败原因/等待原因 Tooltip/操作；仅 WAITING/RUNNING 显示取消） | 子记录级：POST rePushMap / cancelPushMap（mapPushRecordId+mapPushSubRecordIds:[id]）；查询随主行数据下发（无子级查询接口） | rePushMap、cancelPushMap（子记录级） | map-push-record:re-push（共用） | 前端完成（子表组件+筛选分页+受控分页兜底交付；**真实数据下子表展开与操作运行时验证留专用环境**，与记录级命令同条件） | tasks/P12.md |
| P13 | DispatchHub/分类配置查询（6 分类 44 项全量，无分页 G09 不开放排序） | `pages/dispatch-hub/DispatchHub`（antd Tabs 按 configType 分组+Apex data 模式表格封装 `features/dispatch-config/components/DispatchConfigTable`（每分类独立实例防 ref 串扰+列偏好 dispatch-config:main+行 ID=configKey）；列=配置名称/默认值/配置值/配置值范围/配置值单位 逐列核对旧版，空值留白） | GET /fms/v1/dispatcher/taskConfig/getTaskConfigs（一次全量） | getTaskConfigs | dispatch-hub:view（守卫；登录响应实证三码下发） | 已验证（带令牌：6 分类 44 项渲染与 API 逐行一致/类型分布 int21-bool17-double2-string4/守卫回跳带 redirect 登录落点闭环/五语言/双主题双宽度） | tasks/P13.md（2026-09-19） |
| P13 | 可编辑配置值+参数校验+草稿 | 配置值列常驻编辑控件按行 configValueType 异构（int/double=InputNumber 带 min/max 开闭区间解析（parseFloat 修复 double 边界）/bool=Switch/enum=Select 单选/select=Select 多选/string=Input；未知类型只读原值）；草稿=tabRows 全分类常驻+基线 diff，useTabDirtyGuard 脏页签（**beforeunload 拦截 reload 实证**）；后端范围校验 3000620 为最终防线实证 | —（编辑为前端态，提交走批量接口） | — | dispatch-hub:save（无 save 权限单元格退化只读文本，旧实现同边界） | 已验证（编辑→dirty→按钮亮起/重置草稿回退基线零请求/beforeunload 草稿不丢） | tasks/P13.md |
| P13 | 批量保存（整批，当前分类） | confirmCommand 确认（对象=当前分类全部配置项>5 合并计数+影响含计数）→ **预读冲突检查**（重新 getTaskConfigs 与基线比对，服务端值≠基线即中止+列出冲突项+基线刷新+草稿保留，G13 防无条件覆盖）→ batchEditConfigs 整批提交 → 成功本地提交值合成新基线（整批 200=落库确认），失败如实透传输入保留，不承诺原子事务不伪造逐项成败 | POST /fms/v1/dispatcher/taskConfig/batchEditConfigs（body=TaskConfigEditParam 数组） | batchEditConfigs | dispatch-hub:save（无权限隐藏） | 已验证（真实提交×2 终态零净变化+落库复核一致；**G13 冲突中止真实验证**：页面内 fetch 模拟他人修改→保存中止+冲突提示+草稿保留+请求计数不变；后端范围外值 3000620 拒绝实证） | tasks/P13.md |
| P13 | 重置（草稿回退基线，语义变更） | confirmCommand danger 确认（对象=仅修改项）→ 纯前端回退到最近一次确认保存的基线，不发请求；**旧版「恢复默认值并批量保存」破坏性语义废弃**（TASKS.md 专项验收明文=重置回到最近一次确认保存的基线），默认值列保留作参考 | —（纯前端） | — | dispatch-hub:reset（无权限隐藏；旧版同位置同权限码） | 已验证（重置确认五要素+值回基线+按钮回灰零请求） | tasks/P13.md |
| P14 | Elevator/列表分页+名称标识筛选（G09 不开放排序） | `pages/tri-device/Elevator/Elevator`（Apex request 模式+列偏好 device-elevator:main+操作列默认钉右；行 ID=deviceKey；列=设备名称/设备标识/楼层/设备状态（启用/禁用）/设备驱动（driverKey 映射名称，未命中显原 key）/IP地址/端口/设备配置（JSON Popover 预览）/网络状态（ONLINE 绿/OFFLINE 红/未知原值灰 Tag/缺失留白）逐列核对旧版） | GET /fms/v1/device/elevator/pageElevators（GET+query 平铺，G04 同款） | pageElevators | device:elevator:view（守卫；登录响应实证五码下发） | 已验证（带令牌：基线 total=0 真实空态/创建后渲染逐列一致/驱动列名称映射/筛选命中/守卫回跳带 redirect 登录落点闭环/五语言/双主题双宽度） | tasks/P14.md（2026-09-19） |
| P14 | 驱动集合（表单下拉+驱动列名称映射，owner=P14） | `fetchElevatorDrivers`（全量；经 useStaticOptions 消费：scope 取消/防乱序/失败清空；失败列表回退原 key、弹窗下拉显示加载态） | GET /fms/v1/device/elevator/getElevatorDrivers | fetchElevatorDrivers | —（页面内数据源） | 已验证（2 条真实驱动「虚拟电梯/Modbus-tcp电梯」；DeviceDriver 三字段与 OpenAPI 一致） | tasks/P14.md |
| P14 | ElevatorFormModal/新增电梯（驱动参数校验） | `features/device-elevator/components/ElevatorFormModal`（草稿保留+useTabDirtyGuard+失败留稿+显式清空；名称/ip 64 字；楼层 ±200 整数、端口 0..65535 旧同边界；deviceStatus Radio 默认启用；选中驱动即以 driverProtocol 覆写设备配置 JSON；配置 JSON.parse 真实解析校验失败留稿） | POST /fms/v1/device/elevator/addElevator（body=ElevatorParam） | addElevator | device:elevator:add（无权限隐藏） | 已验证（必填拦截 6 错齐出零请求/下拉 2 驱动真实加载/配置自动填充/提交 code=200 落库一致 deviceKey 后端生成） | tasks/P14.md |
| P14 | ElevatorFormModal/编辑电梯（原 deviceKey 定位整体提交） | 编辑模式按行重填（配置 4 空格 JSON 回显，旧实现同形态；表单无 deviceKey 输入框，提交回带原值定位） | POST /fms/v1/device/elevator/updateElevator（body=ElevatorParam 含原 deviceKey） | updateElevator | device:elevator:update（无权限隐藏） | 已验证（回显完整含驱动名称非裸 key；改名+楼层+状态+配置整体提交 code=200 全字段落库，无 G18 类缺陷；列表实时更新） | tasks/P14.md |
| P14 | 删除电梯 | confirmCommand（danger，对象+影响+提交≠完成附注）→ deleteElevator（DeviceParam）→ 刷新 | POST /fms/v1/device/elevator/deleteElevator | deleteElevator | device:elevator:delete（无权限隐藏） | 已验证（确认红键+取消路径；确认删除+后端自清理终态审计 total=0 零残留） | tasks/P14.md |
| P14 | 电梯状态（详情状态，按需查询不轮询） | 「状态」按钮 → **受控 Popover**（页面级 open 状态，行内 setState 重建列定义重挂载不丢展开态）+ 行级缓存（修复旧版全行共享一份状态串扰）；Descriptions 六项=在线状态/当前楼层/运行状态/前门/后门/占用车辆，**枚举显示协议原值**（联验实测 UNKNOWN/ONLINE）缺失留白；失败清空该行区域+错误透传 | GET /fms/v1/device/elevator/getElevatorState?deviceKey= | fetchElevatorState | —（查看行为不控权，旧实现同边界） | 已验证（真实响应 UNKNOWN/ONLINE/null 原值与留白渲染；**受控 open 修复实证**） | tasks/P14.md |
| P14 | 外呼/开门/关门命令 | ElevatorControlModal 参数录入（外呼=当前楼层 ±200 整数；开关门=doorWay FRONT/BACK）→ confirmCommand（对象+影响含具体参数+提交≠完成附注）→ 发送；受理语义提示（结果以「状态」查询为准）；失败不自动补发 | POST /fms/v1/device/elevator/outerCall（ElevatorOperationParam 仅 deviceKey+currentFloor）、POST openDoor/closeDoor（DeviceParam+doorWay） | outerCall、openDoor、closeDoor | device:elevator:operate（无权限隐藏） | 前端完成（弹窗参数校验+确认层五要素+取消零请求实证；**命令执行留专用环境**——现场设备副作用，与 P05/P09/P12 同条件） | tasks/P14.md |
| P14 | 清除占用电梯车辆 | confirmCommand danger（打断占用语义）→ clearElevatorOccupy（DeviceParam）→ 受理语义提示（旧版直接执行无确认，按 DoD 8 升级） | POST /fms/v1/device/elevator/clearElevatorOccupy | clearElevatorOccupy | device:elevator:operate（无权限隐藏） | 前端完成（确认层交付；**命令执行留专用环境**——影响现场调度副作用） | tasks/P14.md |
| P14 | 内呼入口（G06 缺口呈现） | 操作菜单保留「电梯上楼（内呼）」：灰色标记+Tooltip+点击 warning「内呼接口暂不可用…」，**零请求**；不用 antd disabled（禁用项抑制 Tooltip 致说明不可达，联验实测）；不用外呼猜替；innerCall 服务层不提供 | —（G06：接口未出现在 OpenAPI） | — | device:elevator:operate（入口仅 operate 权限可见，旧实现同边界） | 已验证（菜单五项结构+灰色标记+点击说明+XHR 计数不变零请求） | tasks/P14.md |
| P15 | AutoDoor/列表分页+名称标识筛选（G09 不开放排序） | `pages/tri-device/AutoDoor/AutoDoor`（Apex request 模式+列偏好 device-auto-door:main+操作列默认钉右；行 ID=deviceKey；列=设备名称/设备标识/驱动/启用状态（启用/禁用）/IP地址/端口/设备配置（JSON Popover 预览）/网络状态（ONLINE 绿/OFFLINE 红/未知原值灰 Tag/缺失留白）逐列核对旧版，列头文案同旧版「驱动/启用状态」） | GET /fms/v1/device/autoDoor/pageAutoDoors（GET+query 平铺，G04 同款） | pageAutoDoors | device:auto-door:view（守卫；登录响应实证五码下发） | 已验证（带令牌：基线 total=0 真实空态/创建后渲染逐列一致/驱动列名称映射/筛选命中/守卫回跳带 redirect 登录落点闭环/五语言/双主题双宽度） | tasks/P15.md（2026-09-19） |
| P15 | 驱动集合（表单下拉+驱动列名称映射，owner=P15） | `fetchAutoDoorDrivers`（全量；经 useStaticOptions 消费：scope 取消/防乱序/失败清空；失败列表回退原 key、弹窗下拉显示加载态） | GET /fms/v1/device/autoDoor/getAutoDoorDrivers | fetchAutoDoorDrivers | —（页面内数据源） | 已验证（1 条真实驱动「Modbus-tcp自动门」MODBUS_AUTO_DOOR；DeviceDriver 三字段与 OpenAPI 一致） | tasks/P15.md |
| P15 | AutoDoorFormModal/新增自动门（驱动参数校验） | `features/device-auto-door/components/AutoDoorFormModal`（草稿保留+useTabDirtyGuard+失败留稿+显式清空；名称/ip 64 字；端口 0..65535 旧同边界；无楼层字段=自动门语义；deviceStatus Radio 默认启用；选中驱动即以 driverProtocol 覆写设备配置 JSON；配置 JSON.parse 真实解析校验失败留稿；label「设备驱动」「ip地址」旧版原样） | POST /fms/v1/device/autoDoor/addAutoDoor（body=AutoDoorAddParam） | addAutoDoor | device:auto-door:add（无权限隐藏） | 已验证（必填拦截 5 项齐出零请求/下拉真实加载/配置自动填充真实协议模板/提交 code=200 落库一致 deviceKey 后端生成） | tasks/P15.md |
| P15 | AutoDoorFormModal/编辑自动门（原 deviceKey 定位整体提交） | 编辑模式按行重填（配置 4 空格 JSON 回显，旧实现同形态；表单无 deviceKey 输入框，提交回带原值定位） | POST /fms/v1/device/autoDoor/updateAutoDoor（body=AutoDoorUpdateParam 含原 deviceKey） | updateAutoDoor | device:auto-door:update（无权限隐藏） | 已验证（回显完整含驱动名称非裸 key；改名+状态+端口+配置整体提交 code=200 全字段落库，无 G18 类缺陷；列表实时更新） | tasks/P15.md |
| P15 | 删除自动门 | confirmCommand（danger，对象+引用检查影响+提交≠完成附注）→ deleteAutoDoor（DeviceParam）→ 刷新 | POST /fms/v1/device/autoDoor/deleteAutoDoor | deleteAutoDoor | device:auto-door:delete（无权限隐藏） | 已验证（确认红键+取消路径；确认删除+后端自清理终态审计 total=0 零残留） | tasks/P15.md |
| P15 | 自动门状态（按需查询不轮询） | 「状态」按钮 → **受控 Popover**（页面级 open 状态，行内 setState 重挂载不丢展开态）+ 行级缓存；Descriptions 两项=门状态（doorState **协议原值**：UNKNOWN/ERROR/DOOR_OPENED/DOOR_OPENING/DOOR_CLOSED/DOOR_CLOSING，联验实测 UNKNOWN）+网络状态（ONLINE/OFFLINE 映射）；缺失留白；失败清空该行区域+错误透传 | GET /fms/v1/device/autoDoor/getAutoDoorState?deviceKey= | fetchAutoDoorState | —（查看行为不控权，旧实现同边界） | 已验证（真实响应 UNKNOWN/OFFLINE 原值与映射渲染） | tasks/P15.md |
| P15 | 开门/关门命令 | 菜单项（关门/开门，顺序同旧实现）→ confirmCommand（对象+影响+提交≠完成附注；**单门设备无参数录入，与电梯 doorWay 弹窗为有意差异**）→ 发送；受理语义提示（结果以「状态」查询为准）；失败不自动补发 | POST /fms/v1/device/autoDoor/openDoor、POST closeDoor（DeviceParam，doorWay 不携带） | openDoor、closeDoor | device:auto-door:operate（无权限隐藏） | 前端完成（确认层五要素+取消零请求实证；**命令执行留专用环境**——现场设备副作用，与 P05/P09/P12/P14 同条件） | tasks/P15.md |
| P15 | 清除占用自动门车辆 | confirmCommand danger（打断占用语义；行数据 vehicleNames 非空时影响列明占用车辆清单，>5 台合并计数）→ clearAutoDoorOccupy（DeviceParam）→ 受理语义提示（旧版直接执行无确认，按 DoD 8 升级） | POST /fms/v1/device/autoDoor/clearAutoDoorOccupy | clearAutoDoorOccupy | device:auto-door:operate（无权限隐藏） | 前端完成（确认层交付+取消零请求；**命令执行留专用环境**——影响现场调度副作用） | tasks/P15.md |
| P16 | ModbusChargePile/列表分页+名称搜索（G09 不开放排序） | `pages/tri-device/ModbusChargePile/ModbusChargePile`（Apex request 模式+列偏好 device-charge-pile:main+操作列默认钉右；行 ID=deviceKey；列=设备标识/设备名称/设备状态（启用/禁用）/关联的驱动（driverKey 映射名称，未命中显原 key）/设备的IP地址/设备的端口号/设备配置信息（JSON Popover 预览）/充电桩状态（**随行记录内嵌下发**：已知枚举按旧 ChargeState 既定映射展示，未知原值，缺失留白）逐列核对旧版，列序同旧版设备标识在前） | GET /fms/v1/device/chargePile/pageChargePiles（GET+query 平铺，G04 同款） | pageChargePiles | device:charge-pile:view（守卫；登录响应实证五码下发） | 已验证（带令牌：基线 total=0 真实空态/创建后渲染逐列一致/驱动列名称映射/离线状态映射渲染/守卫回跳带 redirect 登录落点闭环/五语言/双主题双宽度） | tasks/P16.md（2026-09-19） |
| P16 | 驱动集合（表单下拉+驱动列名称映射，owner=P16） | `fetchChargePileDrivers`（全量；经 useStaticOptions 消费：scope 取消/防乱序/失败清空；失败列表回退原 key、弹窗下拉显示加载态） | GET /fms/v1/device/chargePile/getChargePileDrivers | fetchChargePileDrivers | —（页面内数据源） | 已验证（1 条真实驱动「中力充电桩」ZHONG_LI_CHARGE_PILE；DeviceDriver 三字段与 OpenAPI 一致） | tasks/P16.md |
| P16 | ChargePileModal/新增充电桩（驱动参数+IP 格式校验） | `features/device-charge-pile/components/ChargePileFormModal`（草稿保留+useTabDirtyGuard+失败留稿+显式清空；名称/ip 64 字；**ip 必填+IPv4 正则（旧实现同规则「请输入正确的IP地址」）**；端口 0..65536 旧版 max 原样迁移；无楼层字段=充电桩语义；deviceStatus Radio 默认启用；选中驱动即以 driverProtocol 覆写设备配置 JSON；配置 JSON.parse 真实解析校验失败留稿；label「设备IP地址」「设备端口号」旧版原样） | POST /fms/v1/device/chargePile/addChargePile（body=ChargePileParam） | addChargePile | device:charge-pile:add（无权限隐藏） | 已验证（必填拦截 5 项齐出零请求/下拉真实加载/配置自动填充真实协议模板 localPort 9999/提交 code=200 落库一致 deviceKey 后端生成） | tasks/P16.md |
| P16 | ChargePileModal/编辑充电桩（原 deviceKey 定位整体提交） | 编辑模式按行重填（配置 4 空格 JSON 回显，旧实现同形态；表单无 deviceKey 输入框，提交回带原值定位——旧实现 getFieldsValue(true) 同语义） | POST /fms/v1/device/chargePile/updateChargePile（body=ChargePileParam 含原 deviceKey） | updateChargePile | device:charge-pile:update（无权限隐藏） | 已验证（回显完整；改名+IP+端口+状态+配置整体提交 code=200 全字段落库，无 G18 类缺陷；列表实时更新） | tasks/P16.md |
| P16 | 删除充电桩 | confirmCommand（danger，对象+引用检查影响+提交≠完成附注）→ deleteChargePile（DeviceParam）→ 刷新（旧版 Popconfirm 仅一问，按 DoD 8 升级） | POST /fms/v1/device/chargePile/deleteChargePile | deleteChargePile | device:charge-pile:delete（无权限隐藏） | 已验证（确认红键+取消路径零请求；确认删除+后端自清理终态审计 total=0 零残留） | tasks/P16.md |
| P16 | 开始充电/停止充电命令 | 操作项菜单（开始/停止充电，顺序同旧实现）→ confirmCommand（对象+影响+提交≠完成附注；**无参数录入，DeviceParam 仅 deviceKey，doorWay 为电梯语义不携带**）→ 发送；成功后刷新列表（状态随行记录内嵌下发，列表刷新即状态重查，旧实现同语义）；**受理 ≠ 充电完成，成功提示不显示成充电完成**；失败不自动补发 | POST /fms/v1/device/chargePile/startCharge、POST stopCharge（DeviceParam） | startCharge、stopCharge | device:charge-pile:operate（无权限隐藏） | 前端完成（确认层五要素+取消零请求实证；**命令执行留专用环境**——现场充电设备副作用，与 P05/P09/P12/P14/P15 同条件） | tasks/P16.md |
| P16 | 未落地 operation（旧仓库无本页可达消费者） | getChargePileDetail 旧仓库全量无引用；getAllChargePiles 旧仓库唯一消费者为 H02 暂缓页（地图编辑器第三方设备标识选择器），随其消费页面落地 | —（OpenAPI 存在，本页不落地） | getChargePileDetail / getAllChargePiles | — | 已核对不落地（P15 同纪律：不因 API 存在增加旧不可达业务） | tasks/P16.md |


## 汇总状态口径

- 页面任务记录中的「操作映射」表是权威明细；本表只汇总已合并到工作分支的实现。
- 状态沿用 TASKS.md：实施状态（未开始/进行中/前端完成/受阻/已合并）与验证状态（未验证/部分通过/已通过/不适用）分开记录。
| P17 | TrafficLights/列表分页+名称key搜索（G09 不开放排序） | `pages/tri-device/TrafficLights/TrafficLights`（Apex request 模式+列偏好 device-traffic-light:main+操作列默认钉右；行 ID=deviceKey；列=设备标识/设备名称/请求地址（deviceConfig.url）/请求参数（deviceConfig.requestParam 紧凑 JSON，空值留白——旧版「-」占位按空值纪律改留白）/响应成功表达式/同步等待响应（是/否，缺失留白）/创建时间/操作 逐列核对旧版） | GET /fms/v1/device/trafficLight/pageTrafficLights（GET+query 平铺，G04 同款） | pageTrafficLights | device:traffic-light:view（守卫；登录响应实证五码下发） | 已验证（带令牌：基线 total=0 真实空态/创建后渲染逐列一致/守卫回跳带 redirect 登录落点闭环/五语言/双主题双宽度） | tasks/P17.md（2026-09-19） |
| P17 | 驱动集合（表单下拉，owner=P17） | `fetchTrafficLightDrivers`（GET getDrivers 全量；经 useStaticOptions 消费：scope 取消/防乱序/失败清空；失败仅弹窗下拉失去数据源，列表不消费驱动集合） | GET /fms/v1/device/trafficLight/getDrivers | fetchTrafficLightDrivers | —（页面内数据源） | 已验证（1 条真实驱动「http-交通灯」HTTP_TRAFFIC_LIGHT；DeviceDriver 三字段与 OpenAPI 一致） | tasks/P17.md |
| P17 | TrafficLightsFormModal/新增交通灯 | `features/device-traffic-light/components/TrafficLightsFormModal`（草稿保留+useTabDirtyGuard+失败留稿+显式清空；名称 64 字；请求地址/响应成功表达式必填；**label「到点通知」对应 syncWaitResponse 旧版表单原样**；选中驱动即以 driverProtocol 覆写请求参数 JSON（4 空格缩进旧版同）；请求参数 JSON.parse 真实解析校验失败留稿；**deviceConfig 为通用 map，键集 {url,requestParam,responseSuccessExpression} 旧提交形态原样承载；新契约顶层 ip/port/deviceStatus 旧 UI 无不发明**） | POST /fms/v1/device/trafficLight/addTrafficLight（body=DeviceTrafficLightAddParam） | addTrafficLight | device:traffic-light:add（无权限隐藏） | 已验证（必填拦截 4 项齐出零请求/下拉真实加载/请求参数自动填充真实协议模板/提交 code=200 落库一致 deviceKey 后端生成） | tasks/P17.md |
| P17 | TrafficLightsFormModal/编辑交通灯（原 deviceKey 定位整体提交） | 编辑模式按行重填（请求参数 2 空格 JSON 回显，旧实现同形态；表单无 deviceKey 输入框，提交回带原值定位） | POST /fms/v1/device/trafficLight/updateTrafficLight（body=DeviceTrafficLightUpdateParam 含原 deviceKey） | updateTrafficLight | device:traffic-light:update（无权限隐藏） | 已验证（回显完整；改名+翻转同步等待响应+改 deviceConfig 三键整体提交 code=200 全字段落库，无 G18 类缺陷；列表实时更新） | tasks/P17.md |
| P17 | 删除交通灯 | confirmCommand（danger，对象+引用检查影响+提交≠完成附注）→ deleteTrafficLight（DeviceTrafficLightParam）→ 刷新（旧版 Popconfirm 仅一问，按 DoD 8 升级） | POST /fms/v1/device/trafficLight/deleteTrafficLight | deleteTrafficLight | device:traffic-light:delete（无权限隐藏） | 已验证（确认红键+取消路径零请求；确认删除+后端自清理终态审计 total=0 零残留） | tasks/P17.md |
| P17 | 连通性测试（A21 设备控制类操作） | confirmCommand（对象+影响+提交≠完成附注，非 danger）→ testTrafficLight（DeviceTrafficLightParam）→ **code=200 即旧语义「交通灯测试成功，连通性正常」；失败经 apiErrorMessage 透传后端诊断（实证 code=8000010「HTTP请求异常」如实呈现，未知结果保留诊断信息，不用动画假装测试通过）**；行内 loading 在途防连点；不自动重试（旧实现点击直接执行，按 DoD 8+A21 升级；旧「交通灯测试失败：」warning 分支由请求层业务码抛错统一承接） | POST /fms/v1/device/trafficLight/testTrafficLight | testTrafficLight | device:traffic-light:operate（无独立 test 码，归 operate 粗粒度码，旧实现同语义；无权限隐藏） | 已验证（确认层五要素+取消零请求+真实发送失败诊断如实呈现；对象为自建记录+回环拒绝端口，副作用限定测试后端本机） | tasks/P17.md |
| P17 | 未落地 operation（旧仓库无本页可达消费者） | getTrafficLights（GET 全量）旧仓库仅 api 层有封装、全量页面无引用 | —（OpenAPI 存在，本页不落地） | getTrafficLights | — | 已核对不落地（P15/P16 同纪律：不因 API 存在增加旧不可达业务） | tasks/P17.md |
