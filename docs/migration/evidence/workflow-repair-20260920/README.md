# 自动任务工作流修复记录

本轮由用户授权修复自动化执行机制，未实施 P21 或后续业务任务。用户明确确认 P21 原运行已停止后，保存恢复依据并取得本轮独占锁；固定目录和 `apple-rxx` 分支保持不变。

## 修复结果

- ZCode 既有自动任务“调度系统迁移”的提示词已通过界面保存为读取 `docs/migration/AUTOMATION_PROMPT.md` 的稳定入口，并只读核对保存结果。仍暂停，每小时第 05 分触发；未点击立即运行，也未创建其他自动任务。
- 保留全部 48 项队列、39 项历史实现和 P21 当前代码。实施、验收、放行、推送独立记录；原勾选转为待复核，不能将此解释为业务代码被回退。恢复后从 T00 开始每轮复核一项历史门禁，再继续 P21。
- 统一控制脚本负责原子锁、修订号检查点、结束记录、证据矩阵校验、推送确认与恢复。提交后未推送先交付，前项延后验收逐任务补齐后再进入 V01。
- 历史状态和 112 张截图已归档并生成哈希索引；按用户最新要求，截图仅本地留存并由 .gitignore 排除，Git 只保留文字记录、脚本和索引。仅标记历史待复核，不作为新验收通过。现有文档中的明文凭据已脱敏，不改 Git 历史。
- 精简 AGENTS 的过时验证经验，统一 TASKS/SPEC 中实施阶段、表格重试和自动查询约定；未改变业务验收范围。

## 验证

| 检查 | 结果与边界 |
| --- | --- |
| `pnpm check` | 通过 lint、typecheck、structure；结构检查覆盖 474 个源文件 |
| `pnpm build` | 通过；保留现有大 chunk 提示，未进行无关拆包 |
| `node --check scripts/migration-runner.mjs` | 语法通过 |
| `pnpm migration:check` | 48 项完整，投影一致，文档凭据模式检查通过 |
| 控制脚本命令行集成烟测 | 10 项通过，见 `smoke-results.json`；可用 `python docs/migration/evidence/workflow-repair-20260920/smoke-runner.py` 复现 |
| 后端及业务 UI | 本轮未执行，不宣称 P21 或历史业务验收通过 |

烟测在系统临时目录创建小型独立 Git 仓库，以本地裸仓库为远端；没有网络推送、业务后端请求或真实队列修改。验收矩阵夹具仅验证控制器行为。未新增单元测试。

本轮修复保存在当前工作区，未提交或推送；RUN_STATE 的已同步提交仍指向原 P21 基线。后续自动轮不得把本轮未提交修复当成自身业务改动夹带提交。结束回执保存在 `../runs/workflow-repair-20260920-0919.json`。
