# T013 验收证据 — 内存草稿、离开保护与容量准入

- 日期：2026-09-16 第八轮（隔离验证；目标 dev server 5219，`APEX_DEV_LEGACY_TARGET/APEX_DEV_PROXY_TARGET=http://10.11.2.67:8888` 真实旧后端，root/root 登录）
- 浏览器：ZCode 内置 Chromium（IAB），视口默认；语言 zh-CN；探针页 `/dev/page-session-probe`（临时路由，副本 `probe/PageSessionProbe.tsx`，收敛后移除）
- 状态：**V01—V05 通过；V04 全流程通过；V06/V07 部分证据、下轮补验**（详见文末剩余项）

## 场景结果

| ID | 场景 | 结果 | 证据要点 |
| --- | --- | --- | --- |
| V01 | 保存/重置解除草稿保护；无稿初始值不误标脏 | ✅ | 初始「无草稿（无稿初始值不误标为脏）」；置脏 →「已登记草稿：验证表单草稿（构成保护条件）」；解除 → 恢复无草稿 |
| V02a | 关闭取消（草稿保护） | ✅ | 弹窗「关闭页签前请确认」列出 页面会话探针·草稿·验证表单草稿 + 停止等待警示；点「留在当前页」→ 弹窗关闭（modalRoots 1→0）、页签未关、草稿保留 |
| V02b | 关闭确认（草稿保护） | ✅ | 「停止等待并关闭」→ 页签自 tablist 消失，落至首个有权入口 /order-record（空页签兜底） |
| V02c | 关闭最后一个页签兜底 | ✅ | 关闭唯一页签后 URL replace 到 /order-record，任务管理页签播种，无空白宿主页 |
| V03a | 批量关闭取消=原子 | ✅ | 5 页签（1 带草稿 + 4 干净）点 Dock 废纸篓 → 「批量关闭页签前请确认」仅列出受保护页签；取消 → 5 页签全部保留（无部分关闭） |
| V03b | 批量关闭确认 | ✅ | 确认后 4 个可关页签一次全部关闭，仅剩 /order-record 并激活 |
| V03c | 写入任务保护（执行中/待确认） | ✅ | 可控写入 running 时构成保护；停止等待 → unknown（stopped-waiting）；弹窗列出「待确认·探针可控写入（验证离开保护）」；确认后页签关闭、任务记录清除、队列清空 |
| V04a | 容量全保护：打开前提示 | ✅ | 10 个带草稿页签填满缓存后再开新页签：`useBlocker` 拦截，URL 停留原页（mu4ajfb1-8），弹「页签容量已满」——导航提交前提示，未先导航丢状态 |
| V04b | 容量准入取消/放行 | ✅ | 「留在当前页」→ 不创建新页签；「仍要打开」→ 导航完成（mu4ajfb1-9 创建），全部带草稿页签仍保留（受保护不被淘汰；对照：干净页 v05/cap0 已被 LRU 淘汰出缓存） |
| V05 | 跨布局（满幅视图）返回 | ✅ | /over-look 暂缓 overlay 切入（不生成页签）→ 页签栏点回 v05：草稿保留、实例标记不变（l6raz4，缓存宿主未销毁） |
| V06 | 干净页 LRU 重建后轻量状态恢复 | ⏳ 下轮补验 | 轻量状态已存入会话 store（CAP-LIGHT/V06-LIGHT），但两次构造均因页签未被实际淘汰（V04 保护逻辑恰好保留了实例）或整页状态被保留而未观察到重建恢复路径；机制未证伪，场景未走通 |
| V07 | 浏览器刷新/关闭原生保护 | ⏳ 下轮补验 | 存在保护条件时首次 `tab.reload()` 被抑制（页面状态原样保留，符合 beforeunload 生效特征），但 IAB 未暴露可断言的对话框对象（getJsDialog=null）；需下轮用干净状态复验并留存明确证据 |

## 过程发现（已修复）

1. **StrictMode 僵尸弹窗**：命令式 `modal.confirm`（effect 内发起）在双挂载下实例累积（1→2→4）且 destroy 失效，导致确认不生效——改为声明式 `<Modal open>` 渲染（随队列挂载/卸载）后 V02/V03 全部复验通过。
2. **容量弹窗过期绑定**：被拦后快速连续导航会堆叠 blocked 状态，原实现弹窗只绑定首个 blocked 导航（proceed/reset 指向过期导航、残留弹窗）——改为按 `blockerKey`（state+location.key）重建弹窗绑定当前导航。人工单次导航路径正常；堆叠场景仅探针循环构造。
3. **刷新后登录态恢复两次失败**（真实后端）：token 在存储但 restore 未通过 → /login?redirect=...；属 T005 身份恢复范围（其合同 §7 已登记缓存快照渲染路径存疑），非本卡改动引入（未触碰 auth 层），归 T090 真实环境复核。

## 版本绑定

- 目标仓库：HEAD `7628d59` + 本卡未提交差异（含临时探针页与 `/dev/page-session-probe` 路由，下轮验证完成后移除）。
- `pnpm typecheck`：通过（实现后、每次修复后、探针页加入后均 exit 0）。

---

## 第九轮复验与缺陷修复（2026-09-17 00:00—00:55，隔离环境）

- 环境：与第八轮相互独立。mock 旧后端（`evidence/T011/mock-task-server.mjs`，:9389，root/root）+ 第八轮遗留 dev server（:5217，旧协议代理已指向 mock）；Chrome IAB 1280×720，zh-CN，Asia/Shanghai。页面脚本驱动（与第八轮同口径）。
- 版本绑定：第八轮未提交差异 + 本轮两处缺陷修复（见下）；`pnpm typecheck` 通过。第八轮 V01—V05 证据针对其改动版本，本轮对修复后版本全部复验。

| 场景 | 结果 | 要点 |
| --- | --- | --- |
| V01 | ✅ 复验 | 置脏→关闭弹「关闭页签前请确认」列草稿条目（截图 `V01-close-confirm-draft.png`）；留在当前页→页签不关、草稿保留；解除草稿→无弹窗直接关闭 |
| V02 | ✅ 复验 | 可控写入 running→关闭弹窗列「执行中·探针可控写入（验证离开保护）」；确认后页签关闭 |
| V03 | ✅ 复验 | 11 个受保护页签批量关闭：取消→12 页签全保留（无部分关闭）；确认→一次全部关闭并跳首个有权入口 |
| V04 | ✅ 修复后复验 | 缓存满且候选全受保护时开新页签：单实例弹窗、取消不创建、确认后打开且无残留（modals 1→0） |
| V05 | ✅ 复验 | 置脏→满幅 overlay（页签栏隐藏）→返回：草稿保留 |
| V06 | ⏳ 未走通（与第八轮同结论） | 干净页轻量状态已入 store，但两次构造均未观察到实例重建（实例反复激活刷新 activatedSeq / pushState 伪象干扰），机制未证伪 |
| V07 | ⚠️ 部分 | 登记方向 ✅（修复后：草稿登记→beforeunload 拦截生效；无保护→不拦截）；解除方向出现一次未解除现象待复验；真实 reload 原生弹窗未采集 |
| 退出保护 | ⚠️ 部分 | 置脏后退出：弹「退出登录前请确认」并列出草稿 ✅；取消后端态异常（探针页签被关闭并跳 /order-record，草稿随之清除）待复验 |

### 过程发现（本轮新增修复，均复验）

1. **容量确认僵尸弹窗**：命令式 `modal.confirm` 在连续拦截下堆叠残留（观测 1→3 个实例且 destroy 不生效）。第八轮以 `blockerKey` 重建缓解；本轮改为声明式 `<Modal open>`（随 blocked 状态挂载/卸载，React 保证清理，与 LeaveGuardHost 同模式），单次与连续导航路径均复验通过。
2. **beforeunload 守卫漏同步草稿**：`LeaveGuardHost` 守卫同步 effect 只依赖任务快照与确认队列，草稿登记/解除不触发同步——补 `subscribePageSession` 快照依赖后，登记方向复验通过（解除方向见 V07 待复验）。
