# P29 软件信息页动作冻结与迁移记录（T018）

> 冻结依据：快照 `snapshot/dd/src/pages/SystemInvolve/SoftwareInformation/index.tsx` + `index.less` @ e570b8df；目标入口 `src/pages/system-involve/SoftwareInformation/SoftwareInformation.tsx`（路由 `system-involve-software-information`，menuCode `system:software:view`）。

| # | 动作 | 源行为 | 目标实现 | 验证 |
| --- | --- | --- | --- | --- |
| A1 | 许可证查询 | useRequest 挂载 POST getLicense；失败 message.error(获取激活信息出错+err) | `usePageQuery`（页签 scope：关闭/隐藏中止、代际隔离、失败保留旧数据）；失败 toast 一次 | V01 真实读取 ✅ |
| A2 | 状态与剩余天数 | 空值/非法日期=未知；自然日差 <0 已过期、≤30 即将到期、否则已激活 | `getLicenseStatus` 同规则（dayjs startOf day 差）；Tag 颜色 error/warning/success/default | V01 状态「Activated」、剩余 470 天 ✅ |
| A3 | 统计与明细展示 | 最大 AGV 数量、剩余天数、激活/过期日期（YYYY-MM-DD HH:mm:ss，非法原样）、激活码（固定高滚动）、硬件信息（pre 滚动） | 同布局移植；配色桥接 `--app-*` 主题变量（源 --ant-color-* 变量在本工程不可用，antd 6 未开 cssVar） | V01/V07 ✅ |
| A4 | 复制激活码/硬件信息 | 剪贴板复制 + `{label}已复制`/复制失败提示 | 共用 `copyTextToClipboard`；词条入 system 命名空间 | V03 同链路 ✅（P02 页验证） |
| A5 | 激活软件入口（banner + 空态） | `hasPerm(system:software:activate)` 两处共用，无权隐藏（root 短路） | `useAuth().hasButton(BUTTON_PERM.SYSTEM_SOFTWARE_ACTIVATE)`（常量新增 `BUTTON_PERM`） | V01 root 可见 ✅；无权隐藏分支为纯判定消费（T005 已验证快照语义） |
| A6 | 激活弹窗 | Modal + Form 必填 TextArea；destroyOnClose；maskClosable=false | 同源（destroyOnHidden 对齐 antd 6 语义） | V02 ✅ |
| A7 | 激活提交 | POST softwareActivation；成功 message+关弹窗+refresh；失败 message.error(原文) | `submitWriteTask`（tabKey=本页页签）；成功 onSuccess（仅明确成功且纪元一致）：提示+关弹窗+`reload()`；`failed`→原文 toast 弹窗保留；`unknown`→warning+页面内联待确认提示，许可重新读取成功即清除（核查路径） | V02 真实失败 ✅；成功分支归 V08（T090）；unknown 分支逻辑走查 |

差异记录：
1. 查询/写入分别接 T010/T011 合同（源 ahooks useRequest 不迁移）。
2. 激活成功不触发身份重查（沿源行为：仅刷新许可证；身份 `activated` 快照刷新由下次登录/detail 恢复承担；若挂起状态下到达本页的场景被 T015 路由强制排除）。
3. 源样式 antd CSS 变量桥接为全局主题变量，视觉随双主题（源页面本就跟随主题，语义一致）。
