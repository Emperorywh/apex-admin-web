# 动作记录模板（页面首任务复制为 actions/P<xx>.md 后填写）

> 依据 SPEC 附录C 与 §3.2：页面实施前逐动作冻结，无独立 API 的交互也必须列项；
> 复制/启停/拖拽等复用接口的交互独立列项。实现与联调阶段补"实际结果/证据/状态"，
> 不把本模板的"要求"勾成"已通过"。公共动作记录在 actions/global.md（G 编号）。

```
# P<xx> <页面名> 动作冻结记录
- 源版本：docs/migration/reference/snapshot/dd/<源文件相对路径> @ <源HEAD>
- 目标：pages/... features/... services/...（按仓库分层）
- 阶段边界：<复杂页拆分时记录本卡交付子集与后续卡，如 T030→T032>

| 动作编号 | 动作/子功能 | 旧API（可复用/无独立API须注明） | 来源→目标 | 权限（菜单/按钮/root/直访） | 契约（方法/地址/参数/DTO/错误/时间口径） | 行为差异 | 验证（场景ID/浏览器/结果） | 证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P<xx>-A01 | ... | ... | ... | ... | ... | ... | ... | ... | 未开始 |

## 展示字段与查询条件（列表页）
## 枚举与校验
## 弹窗/嵌套内容清单
## 刷新关系（成功后刷新哪些列表/统计）
```

状态取值：未开始／实施中／待外部条件／已验证（待外部条件必须写明具体依赖）。
