# P02 软件授权页动作冻结与迁移记录（T018）

> 冻结依据：快照 `snapshot/dd/src/pages/AuthorizeIngress/index.tsx` + `index.less` @ e570b8df；目标入口 `src/pages/authorize-ingress/AuthorizeIngress/AuthorizeIngress.tsx`（路由 `authorize-ingress` 为 T007 预置覆盖层视图，T018 补齐页面实现）。

| # | 动作 | 源行为 | 目标实现 | 验证 |
| --- | --- | --- | --- | --- |
| A1 | 硬件码查询 | 挂载 GET getHardwareInfo；成功填 `data`，失败 message.error(查询硬件信息出错+msg) | `usePageQuery` 承载（无轮询，覆盖层无 scope 按常驻可见）；失败经 `apiErrorMessage` toast 一次 | V03 真实读取 ✅ |
| A2 | 复制硬件码 | 剪贴板复制（安全上下文 navigator.clipboard，否则 execCommand 降级），成功 message.success(复制成功) | `copyTextToClipboard`（`src/utils/clipboard.ts`，P02/P29 共用唯一实现，源逻辑原样迁移） | V04 toast ✅（剪贴板实际内容未断言，见证据边界） |
| A3 | 激活码输入 | TextArea showCount/allowClear/autoSize，空码禁用激活按钮 | 同源（antd Input.TextArea，trim 后判空） | V03 disabled ✅ |
| A4 | 激活提交 | POST softwareActivation；code=200 跳转，否则 warning(信封 message)；异常 error(激活出错+err) | `submitWriteTask`（tabKey=null 会话级动作，零自动重发）；信封明确拒绝→`failed`→toast 后端原文；超时/网络中断→`unknown`→内联待确认提示+可重试，不伪装成败 | V05 真实失败 ✅；unknown 分支逻辑走查（无自然触发条件，归 T090） |
| A5 | 激活成功去向 | `history.push('/over-look')` | **SPEC P02 修正**：不再跳暂缓的调度监控；调 T015 `resumeAfterSoftwareAuthorizationActivated()`，`resumed` → `resolveFirstAccessiblePath(identity)` replace 导航首个有权且本轮已实现页面；`still-unauthorized`/`unreachable` 留页重试提示；`session-expired` 交 SessionHost 回登录 | V08 未执行（无有效激活码 + detail 恒 500，归 T090） |
| A6 | 返回登录 | `history.replace('/login')`（裸跳，不清会话） | **会话模型升级**：接 T017 `useProtectedLogout()`（G01 统一出口：离开确认→二次确认→真实 logout→sessionExpired 收敛） | V06 取消/确认/重登回跳 ✅ |
| A7 | 自包含暗色视觉 | 深蓝科技风 less + 局部 ConfigProvider darkAlgorithm（colorPrimary #35c8ff、borderRadius 2） | `AuthorizeIngress.module.css`（源样式原样移植，色值不变）+ 同参数局部 darkAlgorithm | V07 计算样式 ✅（基底 #040a18 不随主题） |

差异记录：
1. 激活成功去向由 `/over-look` 改为首个有权已实现页面（SPEC §4/P02 明确修正，恢复编排接 T015 合同 §3）。
2. 「返回登录」由裸跳升级为受保护退出（目标系统会话模型下避免遗留半会话；消费 global.md G01，不改其合同）。
3. 写入接入 T011 任务控制器：源一次性 promise 换为停等/待确认/零重发语义（结果未知内联提示，可重试）。
