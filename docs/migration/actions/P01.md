# P01 登录页动作冻结与迁移记录（T017）

> 冻结依据：快照 `snapshot/dd/src/pages/Login/index.tsx` @ e570b8df；目标入口 `src/pages/auth/Login/Login.tsx` + `src/features/auth/components/LoginForm`（基础表单/分流为 T005/T007 已交付实现，本卡补齐剩余动作）。

| # | 动作 | 源行为 | 目标实现 | 验证 |
| --- | --- | --- | --- | --- |
| A1 | 表单登录 | username+password 必填；密码 MD5 后 POST login；成功存 accessInfo | `useLogin` → auth.service.login（T005）；MD5 32 位小写 | T005 隔离 ✅ + 本卡 V02 真实登录 ✅ |
| A2 | 登录失败提示 | message.warning(信封 message) / message.error(登录出错+err) | LoginForm catch → apiErrorMessage message.error | T005 ✅（本卡未改动该路径） |
| A3 | 激活状态分流 | `activated === false`（严格）→ /authorize-ingress；否则首权页 | LoginForm 既有判定 → AuthorizeIngress / resolveFirstAccessiblePath（T007） | T005/T007 ✅；真实未激活账号缺条件归 T090 |
| A4 | 首个有权页面跳转 | getFirstAccessiblePath(permissionsTree, username)，root 短路 | `resolveFirstAccessiblePath(snapshot)`（T007 合同） | 本卡 V02：root → /order-record ✅ |
| A5 | 语言切换（页面级） | 登录页右上角独立 Dropdown（源仅 zh/en） | `LanguageMenu` 五语共享组件，`dispatch(localeChanged)` 统一时序（T016） | V06：五语菜单 + en-US 全页翻译 + html lang ✅ |
| A6 | 浏览器语言探测 | 无存储偏好时 en* → en-US | i18n 启动逻辑（T016 normalizeLanguage） | T016 V01 ✅ |
| A7 | 登录背景配置 | fetchSystemImage("loginBackground") → blob URL；失败/非图片回退默认；卸载 revoke | `useSystemImage('loginBackground','')`；空回退透出全局 Wallpaper | V01：真实 401KB 图应用 ✅；回退分支经 favicon 404 同链路佐证 ✅ |
| A8 | 过期提示 | sessionStorage token_expired → notification.warning，读取即清 | auth.service 失效收敛统一标记 `apex-admin:session-expired`；登录页读取即清并提示 | V03（隔离）：标记→提示→清除 ✅；真实过期会话归 T090 |
| A9 | 主动退出（登录页无入口，外壳 G01） | — | 见 actions/global.md G01 | 见 global.md |
| A10 | 回跳地址 | 源用整页 href 跳首权页 | redirect 参数 + replace（T007 路由合同，SPA 内导航） | V05 后回登录页带 redirect ✅ |

差异记录：源登录成功后 `window.location.href` 整页刷新；目标改为 SPA 内 replace 导航（T007 路由宿主合同，页签/缓存由会话层接管）。源语言菜单仅 zh/en 两项，目标按 D16 直接暴露五语（T016 已记录的有意增强）。
