# T017 — 迁移登录、用户菜单及外壳图片读取

## 认领记录

- **认领标识**：ZCode 自动调度轮（2026-09-17）
- **开始/结束时间**：2026-09-17（本轮，约 70 分钟，超预算主要耗在弹窗僵尸问题定位与修复）
- **输入版本**：apple-admin @ 47adfa5（T016 提交后，工作区干净）
- **本次范围**：
  1. 登录页（P01）：语言切换入口、登录背景配置（fetchSystemImage + 默认回退）、会话过期提示；激活分流/首权跳转/MD5 密码转换沿用 T005/T007 已有实现核对；
  2. 用户菜单（G01—G03）：主动退出（confirmSessionExit + T015 编排顺序保持）、本人改密（updateUserPassword 外壳方法 + PasswordModal）、语言入口五语化；
  3. 外壳图片（G04）：fetchSystemImage 服务、headerLogo/favicon/loginBackground 三位置消费、默认资源回退、按位置失效通知合同（供 T074 消费）。

## 本次完成内容

1. **P01 登录页**：`useSystemImage('loginBackground')` 背景配置（真实后端图片已验证应用）；登录页右上角 `LanguageMenu` 五语入口（与 Header 共享）；`apex-admin:session-expired` 过期提示（auth.service 5 处失效收敛统一标记，登录页即读即清）。动作冻结记录 `actions/P01.md`（A1—A10）。
2. **G01 主动退出**：抽取 `useProtectedLogout`（confirmSessionExit → 二次确认 → logout → sessionExpired），Header 与改密成功回调用同一链路；新增 `logoutConfirmBus` + `LogoutConfirmHost`（声明式确认，修复既有命令式 modal.confirm 僵尸弹窗问题，见下）。
3. **G02 本人改密**：`PasswordModal`（源校验：字母+数字 4-16、两次一致、MD5 上送）；`updateUserPassword` 落 `src/services/system/user/user.service.ts`（跨任务分工表指定归属，模板方法保持原状待 T078 替换）。菜单项替换模板「个人中心」为「修改密码」（源菜单无个人中心，SPEC §1）。
4. **G03 语言入口**：`src/components/LanguageMenu`（五语原生写法，`localeChanged` 统一时序），Header 与登录页共用。
5. **G04 外壳图片**：`system-setting.service`（fetchSystemImage/downloadBinary + 失效通知）+ `useSystemImage`（回退/revoke/失效重拉）+ `SystemFavicon`（App 根部）；Header 品牌图接入；合同 `contracts/system-image.md`。
6. **五语词条**：common（改密弹窗 10 key）、auth（过期提示 2 key），旧 JSON 优先、缺项人工补译（zh-TW/ja-JP/ko-KR）。

## 改动文件

- 新增：`src/services/system-involve/system-setting/{system-setting.service.ts,system-setting.service.types.ts}`、`src/hooks/useSystemImage.ts`、`src/components/SystemFavicon/SystemFavicon.tsx`、`src/components/LanguageMenu/LanguageMenu.tsx`、`src/features/auth/components/{PasswordModal/PasswordModal.tsx,LogoutConfirmHost/LogoutConfirmHost.tsx}`、`src/features/auth/hooks/useProtectedLogout.ts`、`src/features/auth/logoutConfirmBus.ts`
- 修改：`src/pages/auth/Login/{Login.tsx,Login.module.css}`、`src/layouts/BasicLayout/components/Header/Header.tsx`（品牌图/共享语言菜单/改密+受保护退出）、`src/layouts/SessionHost/SessionHost.tsx`（挂 LogoutConfirmHost）、`src/App/App.tsx`（SystemFavicon）、`src/services/system/user/user.service.ts`（+updateUserPassword）、`src/services/auth/auth.service.ts`（+dispatchSessionExpired 过期标记，导出 SESSION_EXPIRED_NOTICE_KEY）、`src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/{common,auth}.ts`
- 文档：`docs/migration/contracts/system-image.md`、`docs/migration/actions/P01.md`、`docs/migration/actions/global.md`（G01—G04）、`docs/migration/evidence/T017/{verification.md,login-bg.png}`、本文件

## 版本依据

- 旧源快照：`snapshot/dd/src/pages/Login/index.tsx`、`components/ActionsRender`、`components/PasswordModal`、`app.tsx` @ e570b8df（placementKey=headerLogo/loginBackground/favicon 与 ImageSettings 一致）
- 接口：SPEC 附录B（fetchSystemImage/updateUserPassword（外壳）/login/logout/detail）；后端 10.11.2.67:8888（default_OpenAPI.json）

## 验证结果

- **代码检查**：`pnpm typecheck` 通过（实现后与修复后各一次）。
- **隔离验证**（详见 `evidence/T017/verification.md`）：V01 登录前读图回退/真实背景应用 ✅；V02 登录+首权跳转+品牌图 ✅；V03 过期提示（隔离标记）✅；V04 改密闭环（校验→真实写入→受保护退出→新密码登录→恢复原密码）✅；V05 受保护退出取消/确认 ✅；V06 登录页五语 ✅；V07 主题弹窗复用 T016 V04 证据 ◐。
- **真实验证补充**：改密与登出为对测试后端的真实写入（root 密码已恢复原值，恢复请求 code 200）。

## 正常修复（随本卡）

- **退出确认僵尸弹窗**：Header 既有 `modal.confirm` 在验证中出现无法销毁的弹窗实例（SessionHost 注释中 T013 同源教训，容量确认已改声明式）。改为 `LogoutConfirmHost` 声明式宿主 + `logoutConfirmBus`；修复第一版 useSyncExternalStore 快照不稳定导致的无限渲染。
- 退出确认文案键复用既有词条，未改既有已验收行为（T013/T015 编排顺序保持）。

## 阻塞与剩余项

1. **真实过期会话**（A8 全链/V03 真实半场）：后端 detail 恒 500、无效 token 不返回 1000000（T015 已记录），归 **T090** 定向补验。
2. **真实未激活账号**（A3 激活分流）：测试环境无未激活账号，归 **T090**；分流判定代码为 T005 已验证实现，本卡未改。
3. **上传联动**：`uploadSystemImage` + 失效通知消费归 **T074**（合同 §2 已给调用方式）；favicon 位置当前后端 404，替换路径待 T074 联调。
4. **V07 主题弹窗截图与弹窗动画完成度**：IAB rAF 节流环境限制（evidence 已记录），归 **T098** 真实浏览器复核。
5. Accept-Language 等请求头真实抓包仍归 T090（T016 遗留，与本卡无新增）。

## 下次继续位置

- 后续页面卡可直接消费：`LanguageMenu`（G03）、`useProtectedLogout`（G01）、`notifySystemImagesReplaced`（T074）。
- T074 实现上传时同步补 G04 联动证据；T098 复核 P01/G01—G04 时引用 `actions/P01.md`、`actions/global.md`。
