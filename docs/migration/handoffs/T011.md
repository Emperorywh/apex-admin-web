# T011 交接记录 — 实现会话级写入与文件传输任务

- **认领标识**：ZCode 调度轮次 2026-09-16 第七轮（apple-admin 分支，单写者串行；认领时工作区干净、HEAD `2ed3c66`，前六轮已结束无遗留认领）
- **开始时间**：2026-09-16 21:55（约）；**结束时间**：2026-09-16 22:45（约 50 分钟，含阅读/实现/验证/交接，符合单卡预算）
- **认领时输入**：T003/T005/T007/T010 产物就绪、隔离验证通过；消费 `contracts/legacy-request.md`（写请求单次发送/取消两段语义）、`contracts/identity.md`（会话纪元）、`contracts/routing-host.md`（SessionHost T011 挂载点）、`contracts/page-query.md`（查询/写入边界）；SPEC §6.3、§9.1、§10、A11/A13。
- **本次范围**：稳定会话层写入任务控制器 + 文件传输控制器 + 页面订阅接口；SessionHost 挂载点接线；隔离验证后交页面接业务参数。不新增操作中心、不改后端。

## 本次完成内容

1. **会话任务层**（新增 `src/services/session-tasks/`）：写入任务状态机（queued→running→success/failed/unknown/cancelled，结果语义按 §6.3——仅信封可判读拒绝记 failed，超时/丢回执/停止等待记 unknown 并保留对象/动作/提交时间）；XHR 上传引擎（沿旧 `uploadWithProgress`：100ms 节流+末次必发、连接/上传 60s 停滞中止、服务端处理阶段停用停滞、字节 100% 不算成功、速度/ETA/不定进度）；传输阶段 preparing→uploading→server-processing→终态；取消后 bytesSent>0 即 resultUnknown。
2. **稳定会话层挂载**：`SessionTasksHost` 挂入 SessionHost 的 T011 注释位（与 PageCacheHost 同级）；订阅 `auth.epoch`，纪元变化清空全部记录；落定时刻纪元不一致的迟到回执丢弃、onSuccess 不调用。
3. **订阅接口**：`useSessionTasksSnapshot/useTabWriteTasks/useTabTransferTasks/useHasActiveTransfers`（useSyncExternalStore，快照引用稳定）；`submitWriteTask/stopWaitingForWrite/dismissWriteTask/startTransferTask/cancelTransferTask/dismissTransferTask`，统一出口 `@/services/session-tasks`。
4. **隔离验证** V01—V07 全部通过（切页/全屏回执、丢回执、停止等待、业务失败、旧会话迟到、上传后处理与阶段反馈、取消/失败/不可判读/服务端处理超时、无自动重发），证据见 `evidence/T011/T011-evidence.md`。
5. **验证期发现并修复两处缺陷**（详见证据文档）：writeTasks 在途状态未置 running；`toApiError` 将 legacyCall 抛出的裸 ApiError 降级为 CLIENT.UNKNOWN（会误判取消/业务失败），已在 `request.ts` 增加 isApiError 形状透传。

## 改动文件

- 新增：`src/services/session-tasks/{sessionTask.types.ts,sessionTaskStore.ts,writeTasks.ts,uploadEngine.ts,transferTasks.ts,useSessionTasks.ts,index.ts}`、`src/layouts/SessionHost/SessionTasksHost.tsx`、`docs/migration/contracts/page-tasks.md`、`docs/migration/evidence/T011/{T011-evidence.md,mock-task-server.mjs,mock-task-server-log-segment1.json,mock-task-server-log-segment2.json,probe/TaskProbe.tsx}`、本文件
- 修改：`src/layouts/SessionHost/SessionHost.tsx`（挂载 SessionTasksHost）、`src/services/request/request.ts`（toApiError 识别裸 ApiError）、`src/services/request/legacy/legacyRequest.ts`（新增 `buildLegacyRequestHeaders`/`reportLegacyRequestError` 供 XHR 引擎复用同一鉴权/语言头与事件总线）、TASKS.md
- 验证用临时文件（提交前已移除）：`src/pages/dev/task-probe/TaskProbe.tsx`、`src/router/definitions.tsx` 临时路由（副本留 `evidence/T011/probe/`）

## 版本依据

- 目标仓库：HEAD `2ed3c66` + 本卡未提交差异（提交后以该提交为准）；`pnpm typecheck` 通过（实现后、移除临时探针后各复跑，均 exit 0）。
- SPEC 依据：§6.3、§9.1、§10.1；A11/A13 相关语义（写入单次发送、结果待确认、传输阶段、取消语义）。

## 验证结果（详见 evidence/T011/T011-evidence.md，全部为【隔离验证】）

V01 切页/全屏回执不断+原页恢复订阅 ✅｜V02 丢回执→unknown/receipt-lost（服务端 aborted=true，单次）✅｜V03 停止等待→stopped-waiting ✅｜V03b 业务失败→failed 保留原文 ✅｜V04 旧会话迟到→记录清空不复活、onSuccess 不触发、无重发 ✅｜V05 上传后处理+阶段反馈+处理期不误判停滞 ✅｜V06 取消（服务端已收全量→resultUnknown）/失败/HTML 不可判读 ✅｜V07 服务端处理超时 61s→unknown ✅｜全程无自动重发（服务端逐 tag 恰一次请求）✅。

## 阻塞

无外部阻塞。环境限制：环回环境浏览器网络缓冲吞没 30MB 请求体，**uploading 阶段真实停滞无法隔离构造**（停滞检测代码沿旧实现原样迁移）——真实大文件停滞行为归 T090/T096 复核；V01 中 overlay 期间页面 UI 冻结为 PageCacheHost 既有语义，非本层缺陷。

## 剩余项与下次继续位置

- 本卡实现/资料＝**产物就绪**；验收＝**隔离验证通过**。无剩余实现项。
- 下游入口：**T013**（依赖 T007✓+T011✓）可领取——消费 `useSessionTasksSnapshot`/dismiss API 做离开保护与容量准入；**T016**（T001✓+T007✓）可领取；T015 后续经 stop/cancel/dismiss 组合接线失效编排。
- 交接给页面卡（T030/T035/T037/T066/T071/T074/T077 等）：写入/上传一律走 `@/services/session-tasks`；`run` 必须透传 `ctx.signal`；`tabKey` 用当前页签 key；上传扩展名/字段名校验按源契约在页面侧做；待确认记录展示重译按 `actionKey`（合同 `contracts/page-tasks.md`）。
- T090/T096 复核项：真实上传停滞与超大文件内存边界、服务端处理超时时长（现沿旧下载通道 60s）。

## 复现方式（供后续核对）

`node docs/migration/evidence/T011/mock-task-server.mjs`（9389）→ `APEX_DEV_LEGACY_TARGET=http://127.0.0.1:9389 APEX_DEV_PROXY_TARGET=http://127.0.0.1:9389 pnpm dev --port 5217` → root/root → 从 `evidence/T011/probe/` 恢复探针页与 `/dev/task-probe` 路由 → 按证据文件场景表操作；桩日志：`/dev/log?n=50`。
