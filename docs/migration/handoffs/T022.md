# T022 交接记录 — 补齐展开子表与动态行高能力

- **认领标识**：ZCode 调度轮次 2026-09-17 第二轮（apple-admin 分支，单写者串行）
- **起止时间**：2026-09-17；实际耗时约 115 分钟（远超 50 分钟预算：IAB 渲染帧停摆迫使切换真实桌面 Chrome + CDP 驱动验证，含定位"嵌套表被 `last()` 误选"等测试脚本问题的时间；实现本身约 25 分钟）
- **认领时输入**：目标仓 HEAD `b2bfe25`（工作区干净）；表格库 C:\yangwenhua\ApexTableReact HEAD `ae34e0a`（工作区干净，= npm `apex-table-react@0.1.0`）；前置：T021 合同 `contracts/table-capabilities.md`、T003 请求层、探针页 `src/pages/dev/ApexTableProbe/`；真实旧后端 `http://10.11.2.67:8888`（root/root）。
- **本次范围**：核对库 0.1.0 展开实现；探针页新增三个真实后端只读消费样例（成员表 / 任务多层展开 / 分页×展开规模）；按退出条件完成真实浏览器验证并更新合同。

## 完成内容

1. **库能力核对（未改库）**：`internal/expansion.ts` + `ExpandedRow` 已具备受控/非受控展开、`rowExpandable`、详情行动态测量（虚拟器 measureElement）、密度变化重测（`useLayoutEffect[safeHeight]→measure()`）、隐藏恢复锚点、`scrollToRow` 实测偏移定位——无需要改库的缺口，所有验证发现都在消费侧解决。
2. **探针样例 `src/pages/dev/ApexTableProbe/ExpansionProbe.tsx`**（临时路由随收尾移除，真实后端只读，零 mock）：
   - 样例一 成员表：`pageVehicleGroups` → `simpleAGVs` 嵌套成员子表（非虚拟内容高、可排序、空成员不可展开）；
   - 样例二 任务多层展开：`pageOrderTemplates` 模板 → 任务子表（独立客户端分页 5/10/页）→ 动作子表三级嵌套；动作行身份用 `actionType#序号` 组合（真实数据 actionType 重复、actionId 全空，裸键触发 `duplicateRowId` 拒绝渲染）；
   - 样例三 分页×展开规模：`pageOrderFlows` 79 行×8 页服务端分页 + 子流程子表。
   - `ApexTableProbe.tsx` 改为纵向滚动容器并修复 flex 收缩压扁表格的问题（顶层子元素 `flexShrink: 0`——表格根元素被压缩后视口测量为 0、行不渲染且无错误提示）。
3. **合同更新** `contracts/table-capabilities.md`：§4 展开行能力改为已验证；§5 新增限制 6（重复业务键组合行身份，供 T059/T024 消费）、7（滚动容器 flexShrink:0）、8（嵌套子表非虚拟模式）；§6 补 T022 验证结论与剩余项。

## 改动文件

- 新增：`src/pages/dev/ApexTableProbe/ExpansionProbe.tsx`、`docs/migration/evidence/T022/`（本文件 + 5 张截图）、`docs/migration/handoffs/T022.md`
- 修改：`src/pages/dev/ApexTableProbe/ApexTableProbe.tsx`、`docs/migration/contracts/table-capabilities.md`、`TASKS.md`（本卡状态行）

## 验证结果（详见 evidence/T022/T022-evidence.md）

- 环境：真实旧后端（root）+ 真实桌面 Chrome 152（CDP 驱动；IAB guest 本轮渲染帧停摆 rAF=0/ResizeObserver 不投递，为环境问题非代码缺陷，已如实记录）；类型检查目标仓 exit 0、库仓 exit 0（未改库）。

| 场景 | 结果 |
| --- | --- |
| V01 成员表（展开/收起/成员子表 6 行/详情高 359px） | ✅ |
| V02 三级展开（任务子表分页独立 10→4 行、主表不动；动作第三级） | ✅ |
| V03 长子表动态行高（19 动作、详情 1254px、滚动到底可达无错位） | ✅ |
| V04 分页×展开（跨页后展开状态按行 ID 保留、回到原页详情恢复 aria-expanded=true） | ✅ |
| V05 语言（展开行→Collapse row）与视口尺寸变化重排 | ✅ |
| V06 父子交互（子表排序/点击不误触父行展开） | ✅ |
| 200 行分页（真实后端可展开父表最大 79 行×8 页；198 行分页归 T021-V01；更大规模展开由库演示覆盖） | ✅（口径如左） |
| 密度切换 UI 级演示 | ⏳ 未执行，归 T028 随页补验 |

## 阻塞

无阻塞本卡。环境事项（复现路径）：IAB guest 渲染帧停摆时，可按本轮方式用独立 profile + `--remote-debugging-port` 启动桌面 Chrome、CDP 驱动验证；`.env.local` 不被 vite 读取，旧后端代理须内联 `APEX_DEV_LEGACY_TARGET`。

## 剩余项与下次继续位置

- **验收状态＝部分验证通过**：V01—V06 与 200 行口径通过；密度切换 UI 级演示归 **T028**（首个表格业务页）随页补验（与 T021-V07 列偏好演示同轮处理）；更大规模展开联验归 T095A。
- 三个暂缓探针样例消费提示已入合同 §5/§6；正式页面（T059 动作表等）必须用组合行身份，禁裸 actionType。
- 探针页 `dev/apex-table-probe` 为临时路由，T028 完成真实页验收后随 V07 补验一并移除。
- 服务实现责任不因演示改变：`vehicle-group`/`template`/`order-flow` 的 service 层仍归 T028/T059(T061)/T063（TASKS §5 分工）。
