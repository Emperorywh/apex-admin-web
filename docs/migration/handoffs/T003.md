# T003 交接记录 — 统一旧JSON协议、分页与二进制下载

- **认领标识**：ZCode 调度轮次 2026-09-16 第二轮（apple-admin 分支，单写者串行）
- **开始时间**：2026-09-16 16:45（约）；**结束时间**：2026-09-16 17:35（约 50 分钟，预算内收敛）
- **认领时输入**：目标仓库 HEAD `5128d5c`（干净）；T001 产物就绪、隔离验证通过；TASKS.md 其余任务均未开始、无遗留认领；SPEC §6.1—6.3、§10.2、附录B.1；旧源码快照 `dd/src/api/{request,httpShared,index}.ts`、`app.tsx` 拦截器、`OrderRecord/RecordTable/SearchForm` 下载实现。

## 本次完成内容

1. **旧协议通道**（新增，不改 `/api/v1` 新协议行为）：
   - `legacyRequest.ts`：唯一 axios 实例（baseURL 空、`/fms`、`/rcsFlow` 路径原样、withCredentials、JSON 15s/下载 60s 超时）；`legacyGet/legacyPost/legacyPut` 信封解包；`downloadBinary`（blob+响应头+文件名，JSON 业务错误嗅探不落损坏文件）；`nativeDownload`（无鉴权头 GET 原生移交）；`setLegacyToken` 头部来源；`subscribeLegacyEvents` 事件总线（session-expired / authorization-required / request-error）；`createLegacyErrorDeduper` 提示去重器。全通道无重试/无重放（SPEC §6.3）。
   - `legacyProtocol.ts`：纯函数——信封形状守卫与成功判据（code===200 且 message==='success'）、分页转换（records/current/size/total/pages→items/page/pageSize/total/pages）、0 基页码换算、有效页回正、`toRowId` 行身份规范化（禁止 Number 往返）、单一 Bearer 组装、Content-Disposition 文件名解析（RFC5987 优先）、失败→ApiError 映射（bizCode/bizMessage 原文保留）。
   - `legacy.types.ts` + `request.types.ts` 增量（ApiError 可选 bizCode/bizMessage，对现有消费方无影响）。
2. **认证/授权事件合同**：1000000/1001000 先发事件再抛错；本层不做跳转、不清会话，编排交 T005/T015。
3. **合同文档**：`contracts/legacy-request.md`（调用方式、成功/失败语义、分页、ID、下载与内存限制、待真实核验项）。

## 改动文件

- 新增：`src/services/request/legacy/{legacyRequest.ts, legacyProtocol.ts, legacy.types.ts}`、`docs/migration/contracts/legacy-request.md`、`docs/migration/evidence/T003/{T003-evidence.md, mock-server-log.jsonl}`、`docs/migration/handoffs/T003.md`
- 修改：`src/services/request/request.types.ts`（ApiError 加可选 bizCode/bizMessage）、`TASKS.md`（本卡状态）

## 版本依据

- 目标仓库：基线 `5128d5c` + 本卡未提交差异（提交后以该提交为准）；`pnpm typecheck` 通过（exit 0，antd 6.6.4，Node v22.23.1）。
- 旧源码依据：快照 `e570b8df`——`httpShared.ts`（BIZ_CODE 1000000/1001000、buildAuthHeaders）、`app.tsx` 拦截器（信封路由、无成功解包）、`request.ts`（GET/POST/PUT/DELETE 薄封装）、`SearchForm/index.tsx`（blob+getResponse 下载与文件名解析）、`typing.d.ts`（pageNo 1 起、ResponseType 分页形状）。

## 验证结果（详见 evidence/T003/T003-evidence.md，全部为【隔离验证】）

| 场景 | 结果 |
| --- | --- |
| T003-V01 信封成功 / 业务失败 / 分页映射 / 大整数ID | 通过（字符串大整数 ID 原样保留；裸数字 >2^53 在 JSON.parse 丢精度，已证实并记入合同限制） |
| T003-V02 认证 1000000 / 授权 1001000 / HTML 形状守卫事件 | 通过（事件+抛错，事件各一次） |
| T003-V03 下载成功（RFC5987 中文文件名）/ 两类文件型业务错误 | 通过（错误不返回 blob，不产生损坏文件） |
| T003-V04 写请求不重放 | 通过（发送前中止服务端计数 0；传输中止恰 1 次无重试；正常写 1 次） |
| T003-V05 头部注入 | 通过（单一 Bearer 不重复、Accept-Language=zh-CN、路径无 /api/v1 前缀） |
| T003-V06 原生移交 | 通过（锚点 GET 无 Authorization 头，query 正确编码） |

验证方法：临时 mock 旧后端（`%TEMP%\t003-mock\mock.mjs`，不入仓库）+ dev 代理（`APEX_DEV_LEGACY_TARGET`）+ 内置浏览器页面内动态 import 真实模块执行；服务端视角以 mock 逐请求日志为准（已存档脱敏）。

## 受影响消费者（现有入口可迁移性）

现有新协议消费方（`features/auth`、`features/dashboard`、`features/order-record`、`features/profile`、`features/system/*`、`services/auth|dashboard|order-record|profile|system`、`layouts/BasicLayout/Header`、`types/*`）**不受影响**——仅 `ApiError` 增加两个可选字段。旧协议通道尚无业务调用方；业务接口接入自 T005（auth）起按合同消费。

## 阻塞

无阻塞本卡的项。外部条件照常由责任卡跟进（不阻塞开发）：

- 旧后端地址/账号未提供（T090）：真实鉴权、CORS/Cookie、真实文件头与 ID 下发类型待核。
- token 是否自带 Bearer 前缀由 T005 登录合同确认（本层已兼容两种形态）。

## 剩余项与下次继续位置

- 本卡实现/资料＝**产物就绪**；验收＝**隔离验证通过**（真实环境联调不属本卡退出条件，按 SPEC §13.2 由 T090 条件下的集成验收覆盖）。
- 下游入口：**T005**（实现依赖 T003 ✓）接 `setLegacyToken`/事件；T026 共享查询、T010 查询、T011 写入任务均按 `contracts/legacy-request.md` 消费。
- 合同中"待真实环境核验"清单在 T090 条件具备后由消费卡定向补验，无需重跑本卡隔离场景（代码不变时证据可复用）。

## 复现方式（供后续核对）

`node mock.mjs`（mock，端口 9377，脚本在会话临时目录，如需重建按 evidence 场景表重写）→ `APEX_DEV_LEGACY_TARGET=http://127.0.0.1:9377 pnpm dev --port 5199` → 浏览器控制台 `import('/src/services/request/legacy/legacyRequest.ts')` 后按 evidence 场景表执行。
