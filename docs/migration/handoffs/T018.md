# T018 交接记录 — 迁移软件授权入口与许可证信息

> 只记录：本次完成内容、改动文件、版本依据、验证结果、阻塞、精确剩余项和下次继续位置。

- **认领标识**：ZCode 调度轮次 2026-09-17 T018 轮（apple-admin 分支，单写者串行；认领时工作区干净、HEAD `ea2b3e1`，上一轮 T017 已提交收口、无遗留认领）
- **开始时间**：2026-09-17 T018 轮启动；**结束时间**：同日（本轮约 55 分钟，略超 50 分钟预算：验证期发现并补正 dev 代理注入方式的环境问题，见「环境发现」）
- **认领时输入**：T007/T015/T016/T013 产物就绪且隔离验证通过；消费 `contracts/invalidation-orchestration.md`（§3 恢复接口）、`contracts/identity.md`、`contracts/page-query.md`、`contracts/page-tasks.md`、`contracts/language-theme.md`；SPEC §6.2、P02/P29、A02。
- **本次范围**：P02 授权页（/authorize-ingress 覆盖层）与 P29 软件信息页完整迁移；共用激活服务 `system-involve/software-information`（TASKS §5 本域唯一责任卡）；激活成功接 `resumeAfterSoftwareAuthorizationActivated()` 恢复编排与有权入口导航；两页动作冻结记录 actions/P02.md、actions/P29.md。
- **本轮状态**：**实现/资料＝产物就绪**；**验收＝部分验证通过**（V01—V07 对真实旧后端读写通过，证据 docs/migration/evidence/T018/；V08 激活成功恢复全链、V09 自然 1001000 挂起分流因外部条件缺失未执行，定向补验归 T090）。

## 本次完成内容

1. **激活服务**（新增 `src/services/system-involve/software-information/`，service+types）：getHardwareInfo（GET，data 为硬件码字符串）、getLicense（POST 无请求体，LicenseInfoRecord）、softwareActivation（POST，须由写入任务执行器透传 signal）；信封失败抛 ApiError（bizMessage 保后端原文），1001000/1000000 由事件桥收敛（本服务不处理）。
2. **P02 授权页**（替换占位实现 + module.css）：深蓝科技风自包含视觉原样移植（局部 ConfigProvider darkAlgorithm 同源参数）；硬件码查询（usePageQuery 无轮询）、复制（新公共工具）、激活提交接 T011 写入任务（tabKey=null 会话级、零自动重发、unknown 转内联待确认）；激活成功调 T015 恢复接口，`resumed` → `resolveFirstAccessiblePath(identity)` replace 导航（SPEC P02 修正：不跳暂缓监控）；「返回登录」接 T017 useProtectedLogout（G01 统一出口）。
3. **P29 软件信息页**（替换占位实现 + module.css）：许可证查询接 usePageQuery（页签 scope）；状态/剩余天数同源规则；横幅/统计/明细布局移植，`--ant-color-*` 桥接为 `--app-*` 主题变量；激活弹窗接写入任务（tabKey=本页页签，成功后 reload 许可证）；激活入口按 `useAuth().hasButton(BUTTON_PERM.SYSTEM_SOFTWARE_ACTIVATE)` 隐藏；unknown 记录内联提示并以「重新读取许可证成功」为核查完成清除。
4. **公共配套**：`src/utils/clipboard.ts`（P02/P29 共用复制，execCommand 降级原样迁移）；`BUTTON_PERM` 常量（permission.constants.ts，首个按钮码，值 `system:software:activate` 沿源不改名）。
5. **五语与路由**：auth 命名空间（P02 词条 15 key）与 system 命名空间（P29 词条 31 key）补 en-US/zh-TW/ja-JP/ko-KR（zh-CN key 即文案）；路由 meta 补 `i18nNamespaces: ['auth']`（authorize-ingress）与 `['system']`（software-information）。

## 改动文件

- 新增：`src/services/system-involve/software-information/software-information.service.ts`、`software-information.service.types.ts`、`src/utils/clipboard.ts`、`src/pages/authorize-ingress/AuthorizeIngress/AuthorizeIngress.module.css`、`src/pages/system-involve/SoftwareInformation/SoftwareInformation.module.css`、`docs/migration/actions/P02.md`、`docs/migration/actions/P29.md`、`docs/migration/evidence/T018/T018-evidence.md`、本文件
- 修改：`src/pages/authorize-ingress/AuthorizeIngress/AuthorizeIngress.tsx`、`src/pages/system-involve/SoftwareInformation/SoftwareInformation.tsx`（占位 → 完整实现）、`src/constants/permission.constants.ts`（BUTTON_PERM）、`src/router/definitions.tsx`（两路由 meta 补 i18nNamespaces）、`src/i18n/locales/{en-US,zh-TW,ja-JP,ko-KR}/{auth,system}.ts`（T018 词条）、`docs/migration/environment.md`（§5 代理注入方式补正）、TASKS.md

## 版本依据

- 目标仓库：HEAD `ea2b3e1` + 本卡差异（提交后以 T018 提交为准）；`pnpm typecheck` 通过（exit 0）。
- 真实旧后端 `http://10.11.2.67:8888`（root/root 登录、getHardwareInfo/getLicense 真实读取、softwareActivation 真实写入失败路径信封 `1001020 激活失败`）；detail 对有效 token 仍恒 500（与 T015 记录一致）。

## 验证结果（详见 evidence/T018/T018-evidence.md，真实后端读写验证）

V01 P29 许可证读取与三语渲染 ✅｜V02 P29 激活真实写入失败（写入任务 failed 分支）✅｜V03 P02 直访+硬件码 ✅｜V04 复制接线 ✅｜V05 P02 激活失败 ✅｜V06 返回登录取消/确认/回跳 ✅｜V07 双主题表现 ✅｜V08 激活成功→恢复→导航 ◌ 未执行｜V09 自然挂起分流 ◌ 未执行。

## 阻塞

无外部阻塞。V08/V09 为外部条件缺失（无有效激活码；真实后端已激活且 detail 恒 500、无法自然产生 1001000），解除方式归 T090：提供可用激活码与可产生 1001000/可用 detail 的测试条件后，按 actions/P02.md A5 与 evidence V08/V09 步骤定向补验。

## 环境发现（已补正 environment.md §5）

`vite.config.ts` 在配置期直读 `process.env.APEX_DEV_LEGACY_TARGET`，`.env.local` 不生效（实测配置后 `/fms` 仍 404）；正确方式为内联环境变量启动。T090 及后续真实联调卡按此执行。

## 剩余项与下次继续位置

1. **T090 定向补验**：V08 激活成功全链（`resumeAfterSoftwareAuthorizationActivated` → `resumed` → 首权页导航；依赖有效激活码 + detail 可用）；V09 自然 1001000 → 守卫/SessionHost 强制 → P02 渲染 → 激活成功恢复（同条件）。
2. **复制场景边界**：系统剪贴板实际内容未断言（IAB 权限限制）；execCommand 降级路径待内网 http 部署实测（T090/T098 可覆盖）。
3. **分域复核**：P02/P29 五语×双主题全矩阵与组合证据归 T098（A01/A07/A15）；本卡证据版本绑定 T018 提交。
4. **无既有证据退回**：本卡改动均为新增页面实现/占位替换/词条与 meta 追加，不影响 T007/T013/T015/T017 既有验收场景条件；T015 恢复接口被本卡按计划消费（其 V04『路由返回由 T018 消费』项即本卡 V08，随 T090 一并补验）。

## 下次继续位置

- 激活成功分支代码入口：`src/pages/authorize-ingress/AuthorizeIngress/AuthorizeIngress.tsx` `handleActivationSuccess()`；恢复接口合同 `docs/migration/contracts/invalidation-orchestration.md` §3。
- 服务唯一入口：`src/services/system-involve/software-information/software-information.service.ts`（TASKS §5：`system-involve/software-information` 域由 T018 维护，无其他责任方）。
