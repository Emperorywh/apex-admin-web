# T007 交接记录 — 统一路由、对象页签与稳定会话缓存宿主

- **认领标识**：ZCode 调度轮次 2026-09-16 第四轮（apple-admin 分支，单写者串行；认领时工作区干净、无遗留认领）
- **开始时间**：2026-09-16 19:05（约）；**结束时间**：2026-09-16 20:20（约 75 分钟，含隔离验证与两轮缺陷修复，超出 50 分钟预算——验证阶段发现并修复了阻断性启动缺陷，选择完成闭环而非留下半成品，见"阻塞"）
- **认领时输入**：目标仓库 HEAD `e17b710`（干净）；T001/T003/T005 产物就绪、隔离验证通过；消费 `contracts/identity.md`（setPermissionMenuTree 注入点、纪元 API）、`contracts/legacy-request.md`；SPEC §8—9、§1.2、P41/P42、A04/A06/A09/A16；旧源码快照 `e570b8df`——`.umirc.ts`（access 映射）、`src/constants/permission.ts`（PERM/MENU_TREE/ROOT_ONLY_CODES）、`src/access.ts`（ROOT_ONLY 对非 root 全拦截）、`pages/OrderInfo/index.tsx` 与 `pages/VehicleInfo/index.tsx`（裸 query=整个 search）。

## 本次完成内容

1. **路由鉴权（含 T005 移交缺陷修复）**：definitions 全量接入 `meta.menuCode`（镜像旧 .umirc access；两处源遗漏按 SPEC §8.2 修正：载具类型=carrier:view、数据库备份=system:database-backup:view）；守卫按 认证 → rootOnly → menuCode 逐层拦截，越权统一落 P41；user/role 管理 `rootOnly`。**根因修复**：① store.ts migrate 将 identity 置 `undefined` 绕过全部 `=== null` 判定（改为 null + 守卫/useAuth 改 falsy 判定）；② 守卫只等 rehydrate 不等 restoreSession（新增 `awaitSessionRestored()` 恢复完成门，硬刷新不再误弹登录）。
2. **权限树绑定**：`src/router/permissionTree.ts` 从 definitions 派生全量菜单结构并注入 `setPermissionMenuTree`（祖先填充生效）。
3. **默认入口与安全返回**：`firstAccessible.ts` 解析"首个有权且本轮已实现的业务页"（跳过暂缓、rootOnly）；根 index 改动态 loader；LoginForm/NotFound/ServerError/RouterErrorBoundary/DeferredPage 统一消费；移除指向暂缓页的静态 FALLBACK_PATH；P41 双文案（`?scope=deferred-only` 区分仅暂缓权限）。
4. **菜单**：DockMenu 按 `filterMenuByPermission(nodes, hasMenu, isRoot)` 渲染期过滤（叶子持码、rootOnly 非 root 隐藏、空分组隐藏）；暂缓菜单行内/Dock 分区标注"下一轮实现"。
5. **暂缓入口**：三模块 loadPage 改指统一 `DeferredPage`（标题按 pathname 反查路由定义；"下一轮实现"文案+返回本轮可用页面/退出登录），原 Overlook/MapNestModify/RecordPlayback 文件保留待下一轮；P41 页从占位改为完整双文案实现。
6. **稳定会话宿主**：新增 `SessionHost` 包裹全部受保护路由（router.tsx），页签缓存宿主提升到布局切换之上；全屏/暂缓/404/无权限/软件授权为 `hideInTabs+noCache` 会话内视图——外壳 `shellBare` 隐藏（顶栏/Dock 插槽 display:none、工作区满幅），页签 Activity 转 hidden（DOM/state 保留、查询暂停）；原 BasicLayout.tsx/.module.css 删除（组件目录沿用）。**T011 挂载点**已留注（SessionHost 内 PageCacheHost 旁）。
7. **对象页签身份**：order-info/vehicle-info 接 `objectParam`（orderTaskKey/vehicleKey）；`objectTab.ts` 规范化 key（兼容裸 query，其余参数不参与身份，缺失参数落稳定错误页签不发请求）；TabsBar 标题带对象后缀；同对象聚焦、异对象独立缓存。两页为最小身份入口（本卡阶段边界，真实详情归 T030/T070）。
8. **模板入口清理**：移除模板额外仪表盘路由（§1.3，页面文件保留）；affixTab 播种随之为空（机制保留）。
9. **词条**：en-US common 命名空间新增暂缓/P41/对象页签词条（五语补齐归 T016，沿用 T005 先例）。

## 改动文件

- 新增：`src/constants/permission.constants.ts`、`src/router/{permissionTree.ts,firstAccessible.ts,objectTab.ts}`、`src/layouts/SessionHost/{SessionHost.tsx,SessionHost.module.css}`、`src/pages/deferred/DeferredPage/DeferredPage.tsx`、`docs/migration/contracts/routing-host.md`、`docs/migration/evidence/T007/{T007-evidence.md,mock-server.mjs,mock-server-log.jsonl}`、本文件
- 重写：`src/router/{guard.ts,projections.tsx,redirect.ts(删除静态默认入口)}`、`src/pages/un-access/UnAccess/UnAccess.tsx`、`src/pages/order-info/OrderInfo/OrderInfo.tsx`、`src/pages/vehicle-info/VehicleInfo/VehicleInfo.tsx`
- 修改：`src/router/{definitions.tsx,router.types.ts,router.tsx}`、`src/store/store.ts`（migrate 修复）、`src/hooks/useAuth.ts`、`src/layouts/BasicLayout/components/{PageCacheHost/PageCacheHost.tsx,TabsBar/TabsBar.tsx,DockMenu/DockMenu.tsx,DockMenu/DockMenu.module.css}`、`src/features/auth/components/LoginForm/LoginForm.tsx`、`src/pages/error/{NotFound/NotFound.tsx,ServerError/ServerError.tsx}`、`src/components/RouterErrorBoundary/RouterErrorBoundary.tsx`、`src/services/auth/auth.service.ts`（恢复完成门）、`src/i18n/locales/en-US/common.ts`、`src/constants/route.constants.ts`、TASKS.md
- 删除：`src/layouts/BasicLayout/BasicLayout.tsx`、`src/layouts/BasicLayout/BasicLayout.module.css`、`src/constants/route.constants.ts` 的 FALLBACK_PATH

## 版本依据

- 目标仓库：基线 `e17b710` + 本卡未提交差异（提交后以该提交为准）；`pnpm typecheck` 通过（exit 0，本轮三次增量修改后均复跑）。
- 旧源码依据：快照 `e570b8df`——`.umirc.ts`（逐路由 access）、`constants/permission.ts`（PERM/MENU_TREE）、`access.ts`（root 短路 + ROOT_ONLY_CODES 全拦截）、OrderInfo/VehicleInfo（裸 query）。

## 验证结果（详见 evidence/T007/T007-evidence.md，全部为【隔离验证】）

V01 未登录硬刷新重定向（含缺陷修复）✅｜V02/V02b 登录回跳与默认入口 ✅｜V03/V08 退出确认+页签清空 ✅｜V04 对象页签规范化/并存/聚焦 + 裸 query 兼容 ✅｜V05a—f op 菜单过滤/无权直访/rootOnly/暂缓直访与返回 ✅｜V06 跨布局实例保留（DOM 标记法）+ 外壳隐藏 + 页签挂起 ✅｜V07 未匹配路径 404 与安全返回 ✅｜P41 双文案分流 ✅。mock 日志全程仅 auth 请求——被拒页面/暂缓模块/对象详情均未发出业务请求。

## 阻塞

无外部阻塞。**预算偏差说明**：验证阶段 V01 复现 T005 移交缺陷并定位为两处启动时序/状态根因（identity undefined、守卫不等恢复），属本卡承诺交付的"守卫鉴权接线"阻断项，故延长验证至修复闭环；总耗时约 75 分钟，超预算约 25 分钟。

## 剩余项与下次继续位置

- 本卡实现/资料＝**产物就绪**；验收＝**隔离验证通过**（真实环境验收待 T090）。
- 未在本轮采集实操作证据：全屏监控页进入与菜单别名 replace 语义（机制同 overlay 已验证；归 T089/T093/T106）；五语完整补齐（T016）；detail 真实形状（T090 合同第 5 节）。
- 下游入口：**T010**（实现依赖 T003✓+T007✓）与 **T011**（T005✓+T007✓+T003✓）——消费 `contracts/routing-host.md`（T011 挂载点在第 4 节）；**T016**（T001✓+T007✓）可补五语；**T013**（T007✓，但依赖 T011 未就绪，暂不可领）。
- 交接给 T010/T011：SessionHost 即"稳定会话层"，查询/任务控制器挂在宿主层级；纪元 API 见 identity 合同第 3 节。
- 交接给 T013/T015：sessionExpired 已统一清页签（tabsSlice extraReducer）；overlay 视图挂起机制已就位，撤权清理接线归 T015。
- 交接给 T017：G01 退出（Header 确认弹窗+登出失败保留会话）与 G03 语言入口现状沿用模板实现，其编排验收归 T017。

## 复现方式（供后续核对）

`node docs/migration/evidence/T007/mock-server.mjs`（端口 9388）→ `APEX_DEV_LEGACY_TARGET=http://127.0.0.1:9388 pnpm dev --port 5217` → 账号 root/root（特权）与 op/op（仅 vehicle-group:view+overview:view+auth:user:view）。场景表见 `evidence/T007/T007-evidence.md`。注意：验证中曾因浏览器缓存 Vite 旧模块出现伪象，复现时先硬刷新或重启 dev server。
