# T016 交接记录 — 补齐五语运行时与公共控件主题桥接

- **认领标识**：ZCode 调度轮次 2026-09-17（apple-admin 分支，单写者串行）
- **开始时间**：2026-09-17；**结束时间**：同日，约 55 分钟（阅读+实现+验证+交接）
- **认领时输入**：目标仓库 HEAD `3445579`（工作区干净）；T001/T007 产物就绪且隔离验证通过；前置合同 routing-host.md、legacy-request.md；旧词条 `reference/snapshot/dd/src/locales/*.json`。
- **本次范围**：五语运行时（zh-CN/en-US/zh-TW/ja-JP/ko-KR）、现有 8 命名空间公共词条四语补齐、antd/dayjs/请求语言/主题桥接、公共语言主题合同；不重做外壳，领域词条随页面迁移。

## 完成内容

1. **i18n.ts 五语运行时**：`SUPPORTED_LANGUAGES` 扩至五语；`normalizeLanguage` 归一 ja/ko/zh-TW；`DAYJS_LOCALE_MAP` 五语映射（对齐旧 app.tsx）；资源懒加载改 `import.meta.glob('./locales/*/*.ts')` 自动登记（新增语言/命名空间零代码改动）；切换时序保持「预加载→changeLanguage→持久化→dayjs→html lang」。
2. **App.tsx**：`ANTD_LOCALE_MAP` 五语（对齐旧 app.tsx ANTD_LOCALE_MAP），ConfigProvider locale 随 settings.locale。
3. **Header.tsx**：语言菜单五语原生标签；ClockText 中文格式分支扩展 `startsWith('zh')`（简/繁共用）。
4. **词条迁移**：32 个资源文件（4 语言 × 8 命名空间）、285 key 全部有译文。生成方式：目标 en-US key 为基准 → 旧 JSON（含 menu 嵌套键）命中优先；缺失或「译文=简中原文」未译占位人工补译 250 key（清单 evidence/T016/manual-translations.json）。一次性生成脚本运行后已删除，未入库。
5. **请求语言**：legacyRequest 已直读 `i18next.language`（T003 既有实现），本卡核对无需改动。
6. **主题桥接**：useTheme/data-theme/buildAppTheme 既有双体系沿用并写入合同；验证深色下公共弹窗。
7. **合同**：contracts/language-theme.md（语言清单/资源接入/切换时序/Accept-Language/主题/表格与图表接入约定/已验证限制）。

## 改动文件

- 修改：`src/i18n/i18n.ts`、`src/App/App.tsx`、`src/layouts/BasicLayout/components/Header/Header.tsx`、`TASKS.md`（T016 状态行）
- 新增：`src/i18n/locales/{zh-TW,ja-JP,ko-KR}/{auth,common,dashboard,error,menu,orderRecord,profile,system}.ts`（24 个文件）、`docs/migration/contracts/language-theme.md`、`docs/migration/evidence/T016/{T016-evidence.md,manual-translations.json,V04-dark-shell-en.png,V04-dark-dialog-en.png}`、本文件

## 验证结果（详见 evidence/T016/T016-evidence.md）

| 场景 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过（exit 0） |
| T016-V01 五语逐一切换（文案/文档标题/html lang/antd 内置文案/dayjs 时钟/持久化） | 通过【隔离验证】 |
| T016-V02 快速连切竞态（en→ja→ko 无混语） | 通过【隔离验证】 |
| T016-V03 刷新后语言持久化 | 通过【隔离验证】 |
| T016-V04 深色主题公共弹窗（截图 2 张） | 通过【隔离验证】 |
| T016-V05 编辑不丢稿（DOM 哨兵，切语言不重建节点） | 通过【隔离验证】 |
| T016-V06 Accept-Language 真实抓包 | **未执行**（隔离环境无旧后端），归 T090 定向补验 |

验证会话为隔离种子（root 短路身份、detail 不可达保留快照分支），未连接真实后端、无业务写入；结束已清理种子并恢复浅色/zh-CN。

## 阻塞

无阻塞本卡的项。

## 剩余项与下次继续位置

- Accept-Language 真实抓包归 T090；登录页语言入口（G03）与图片读取归 T017；表格 locale 落库归 T021（合同 §6 已留约定）；图表标签/主题消费归 T081（合同 §7）。
- order-record 模板页枚举徽标/「状态」列头中文为 T066 范围（词条已备于 orderRecord ns）。
- 下游入口：T017（依赖 T016 ✓ 已满足）按 contracts/language-theme.md §1/§3 接语言入口；T021 按 §6 接表格。

## 消费提示（对后续卡生效）

1. 新增词条流程：页面直接 `t('中文文案')` → 四个非默认语言文件补同 key；zh-CN 永不加资源文件。
2. 语言切换入口必须 `dispatch(localeChanged(lang))` 走 App.tsx 统一时序，禁止直调 `i18next.changeLanguage`。
3. 旧系统 UI 语言菜单仅两项，目标五语菜单为按 D16 的有意增强（合同 §8 已记录）。
