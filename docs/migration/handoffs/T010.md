# T010 交接记录 — 建立可见性驱动的查询与轮询

- **认领标识**：ZCode 调度轮次 2026-09-16 第六轮（apple-admin 分支，单写者串行；认领时工作区干净、HEAD `dd75254`，第五轮已结束无遗留认领）
- **第六轮范围**：仅补验收——V03b（页签栏切回立即请求）、V04（可见态发慢请求后关闭页签，日志现 aborted=true）、document 隐藏分支（最小化浏览器窗口）；不改查询控制器实现。验证场景：`evidence/T010/T010-evidence.md` 同名场景。
- **第六轮起止**：开始 2026-09-16 21:05（约）；结束 2026-09-16 21:35（约 30 分钟，含环境准备与文档）。提交前 `pnpm typecheck` exit 0（临时探针移除后复跑）。
- **开始时间**：2026-09-16 19:50（约）；**结束时间**：2026-09-16 20:45（约 55 分钟，略超 50 分钟预算——超时主因为浏览器自动化的点击管线在遮挡环境超时，改用页面脚本驱动多耗约 10 分钟）
- **认领时输入**：T001/T003/T005/T007 产物就绪、隔离验证通过；消费 `contracts/legacy-request.md`（请求/分页/取消语义）、`contracts/routing-host.md`（SessionHost/Activity 挂起语义）；SPEC §6.3、§9.2、A07/A12。

## 本次完成内容

1. **统一查询接口 `usePageQuery`**（新增 `src/hooks/page-query/`）：可见性驱动（Activity 激活 ∧ document 可见，任一不满足即中止在途+撤销轮询；恢复立即请求、不积攒任务）；过期响应隔离（代际守卫 + 每代独立 AbortController，条件切换/隐藏/卸载联动 scope signal 中止，取消静默）；轮询以“上次请求落定”为基准重排（同一查询不并发堆叠，单链单定时器）；`enabled` 门控（非法参数/无权不发请求）、`failureCount` 连续失败计数、错误态不清旧数据（降级展示）；卸载后响应不落地。查询控制器不接管写入/传输（归 T011）。
2. **轮询策略常量 `PAGE_POLLING`**：任务记录 1s（interval）、实时看板完成后 5s、服务器资源完成后 2s（§9.2 源频率唯一来源）；其余页面不擅自增加轮询。
3. **五个现有消费方迁移**（公共 API 不变）：useOrderList、useRoleList、useUserList、useMenuTree、useDashboardOverview（30s 沿用当前目标实现并注明源频率归 T084）。顺带修复：旧模式切条件不中止在途请求（现在会中止）。
4. **隔离验证基建**：`evidence/T010/mock-query-server.mjs`（可控 tag/delay/fail 查询桩 + 新协议用户列表桩 + 中止标记日志）与临时探针页（已从 src 移除，副本留 `evidence/T010/probe/`）。

## 改动文件

- 新增：`src/hooks/page-query/{usePageQuery.ts,pageQuery.types.ts,useDocumentVisible.ts,index.ts}`、`docs/migration/contracts/page-query.md`、`docs/migration/evidence/T010/{T010-evidence.md,mock-query-server.mjs,mock-query-server-log.jsonl,probe/QueryProbe.tsx}`、本文件
- 修改：`src/features/order-record/hooks/useOrderList.ts`、`src/features/system/role/hooks/useRoleList.ts`、`src/features/system/user/hooks/useUserList.ts`、`src/features/system/menu/hooks/useMenuTree.ts`、`src/features/dashboard/hooks/useDashboardOverview.ts`、TASKS.md
- 验证用临时文件（提交前已移除）：`src/pages/dev/query-probe/QueryProbe.tsx`、`src/router/definitions.tsx` 临时路由

## 版本依据

- 目标仓库：HEAD `4e5f1a3` + 本卡未提交差异（提交后以该提交为准）；`pnpm typecheck` 通过（实现后、移除临时探针后各复跑一次，均 exit 0）。
- SPEC 依据：§6.3、§9.2；A07/A12 相关语义（过期覆盖、可见轮询、无重复链）。

## 验证结果（详见 evidence/T010/T010-evidence.md，全部为【隔离验证】）

V01 慢请求竞态（旧条件 3s 慢响应落地但不覆盖新条件）✅｜V02 连续失败（按轮询继续、failureCount/错误可见、旧数据保留、恢复清零、无堆叠无 toast）✅｜V03a 隐藏暂停（overlay/切页后零请求）✅｜V05 无重复链（全程日志单链、双实例独立）✅｜V06 轮询不重置条件 ✅｜V03b/V04/document 分支第五轮未取证，已于第六轮补齐通过（见下方“第六轮完成情况”）。

## 阻塞

无外部阻塞。环境因素：内嵌浏览器面板遮挡时定时器被钳制（1s 轮询实测 2.0s），前台真实频率待 T090 复核；浏览器自动化点击管线超时改用页面脚本驱动；内嵌面板隐藏不触发 visibilitychange，document 分支经宿主窗口真实最小化取证。

## 第六轮完成情况（验收收尾，2026-09-16）

1. **V03b 恢复立即请求 ✅**：主导航切走后隐藏 84.6 秒零请求；点击页签栏“查询探针”切回，348/349ms 内 A0/B0 恢复请求同时到达服务端；不轮询实例 B 计数恰 +1（StrictMode 首帧双发后恒定单发/次恢复）。
2. **V04 可见态关闭中止在途 ✅**：delay=5000 在途 535ms 后关闭页签，服务端 `delay=5000 ms=5014 aborted=True`；页签移除、探针 DOM 卸载。
3. **document 隐藏分支 ✅**：内嵌面板隐藏不触发 visibilitychange（environment 因素，非代码缺陷），改经宿主窗口真实最小化取证：页面 `document.hidden=true`；9 秒在途请求于 2.1s 时被最小化中止，服务端 `delay=9000 ms=9011 aborted=True`；隐藏期零请求；窗口还原后 9—10ms 内两实例各重发一次。顺带证实条件切换中止的服务端标记（`delay=8000 ms=8007 aborted=True`）。
4. 验证方式：mock 服务端日志为唯一时间线依据（全程 97 条已存 `mock-query-server-log.jsonl`）；页签操作经页面脚本驱动（Playwright 定位器在遮挡面板超时，与第五轮一致）；本卡未改动 `src/` 任何实现文件，临时探针页与路由提交前已移除并复跑 `pnpm typecheck`（exit 0）。
5. 第六轮改动文件（均为文档/证据，无 src 改动）：`docs/migration/evidence/T010/{T010-evidence.md, mock-query-server.mjs（aborted 判定加 res.destroyed 兜底）, mock-query-server-log.jsonl（第六轮全程日志）, probe/QueryProbe.tsx（同步为 B 实例不轮询版本）}`、`docs/migration/contracts/page-query.md`（§4 移除未取证说明）、`TASKS.md`（T010 验收状态）、本文件。

## 剩余项与下次继续位置

- 本卡实现/资料＝**产物就绪**；验收＝**隔离验证通过**（2026-09-16 第六轮补齐 V03b/V04/document 分支后，V01—V06 及 document 分支全部有隔离证据；前台真实轮询频率与真实环境复核归 T090，见环境因素）。
- 本卡无剩余实现/验证项，无后续认领入口。下游入口：**T011**（T005✓+T007✓+T003✓）消费本合同时应注意“写入不得绑定查询 AbortSignal”；**T013**（依赖 T007✓+T011）待 T011 就绪；**T016**（T001✓+T007✓）可领取。
- 交接给页面卡（T030/T066/T084 等）：查询一律走 `@/hooks/page-query`；轮询频率只取 `PAGE_POLLING` 常量；分页回正/筛选重置编排是页面职责（T003 合同 §4）；P03 的 1s 轮询由 T066 接真实接口时打开 `PAGE_POLLING.taskRecords`。
- 交接给 T095A（A12 验收）：单链不堆叠、隐藏暂停的隔离证据已就位（evidence/T010/），10 页签规模与资源趋势留待该卡。

## 复现方式（供后续核对）

`node docs/migration/evidence/T010/mock-query-server.mjs`（9389）→ `APEX_DEV_LEGACY_TARGET=http://127.0.0.1:9389 APEX_DEV_PROXY_TARGET=http://127.0.0.1:9389 pnpm dev --port 5217` → root/root → 按证据文件说明挂回探针路由；桩控制与日志：`/dev/control`、`/dev/log?n=40`。
