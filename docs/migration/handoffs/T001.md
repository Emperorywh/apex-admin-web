# T001 交接记录 — 冻结三仓基线、交接资料与运行环境

- **认领标识**：ZCode 调度轮次 2026-09-16（apple-admin 分支，单写者串行）
- **开始时间**：2026-09-16（本轮）；**结束时间**：同日，约 45 分钟（50 分钟预算内）
- **认领时输入**：目标仓库 HEAD `ebe6f0a`（干净）；TASKS.md 全部任务"未开始"、无遗留认领；SPEC 调研基线：旧项目 `70762c19`、表格库 `b208762d`。
- **本次范围**：三仓基线与类型检查现状记录；旧源码/表格库只读快照与相对路径 manifest；动作/交接模板；antd 锁定与代理、环境示例调整；路由入口索引。

## 完成内容

1. **baseline.md**（docs/migration/baseline.md）：三仓实际 HEAD 与 SPEC 调研基线差异清单——旧项目 `e570b8df`（+1 提交：任务统计报表利用率优化，涉 T085 域）、表格库 `ae34e0a`（+2 提交：行展开已提交并发布 npm `0.1.0`，涉 T021/T022 域）；依赖锁现状；`pnpm typecheck` 前后两次通过记录；49 个 `loadPage` 入口索引（3 个暂缓入口标注）；缺资料登记 5 项。
2. **只读快照**：`docs/migration/reference/snapshot/dd/`（658 文件，7.2MB，源 `e570b8df`）与 `snapshot/apex-table-react/`（93 文件，3.4MB，源 `ae34e0a`）；两仓快照时工作区均干净；排除 `.git/node_modules/dist/缓存/.npmrc(防凭据)/*.local/*.log`，全树 find 复核无残留；目录已置只读权限。
3. **reference/manifest.md**：相对路径定位索引（旧项目关键资料、表格库关键资料、发布物版本、快照使用规则），不依赖原机器绝对路径。
4. **antd 锁定**：package.json `^6.6.1` → 精确 `6.6.4`（2026-09-16 registry 核实最新稳定版，与 SPEC §5.2 基准一致），pnpm-lock 同步，锁定后 typecheck 通过。
5. **代理与环境示例**：vite.config.ts 新增 `/fms`、`/rcsFlow` 旧协议前缀代理（单一 `APEX_DEV_LEGACY_TARGET` 驱动，未配置不注册，防误连旧 .umirc.ts 内网生产地址）；新增 `.env.example`。
6. **模板**：actions/TEMPLATE.md（SPEC 附录C 字段）、handoffs/_TEMPLATE.md。
7. **environment.md**：运行条件（Node ≥22.22.0 / pnpm 11.21.0 / frozen-lockfile 安装）、代理行为与实测表、WS 归属说明（`/websocket/getDispatcherMonitor` 属暂缓模块，交 T106 按消费者核验）、外部条件与解除方式。

## 改动文件

- 修改：`package.json`、`pnpm-lock.yaml`、`vite.config.ts`、`TASKS.md`（T001 状态行）
- 新增：`.env.example`、`docs/migration/baseline.md`、`docs/migration/environment.md`、`docs/migration/reference/manifest.md`、`docs/migration/reference/snapshot/{dd,apex-table-react}/`、`docs/migration/actions/TEMPLATE.md`、`docs/migration/handoffs/{_TEMPLATE.md,T001.md}`、`docs/migration/evidence/T001/T001-evidence.md`

## 验证结果（详见 evidence/T001/T001-evidence.md）

| 场景 | 结果 |
| --- | --- |
| T001-V01 typecheck（antd 6.6.1 基线 / 6.6.4 锁定后） | 两次均通过（exit 0）【隔离验证】 |
| T001-V02 快照复杂页/库源码可定位 + 只读 | 通过【隔离验证】 |
| T001-V02b 快照排除项核查 | 通过 |
| T001-V03 冻结安装复现条件 | 通过（Node v22.23.1、pnpm 11.21.0、lock 已更新）；**全新目录冷装演练未做**，登记为可选补验 |
| T001-V04 代理实测 | 未配置旧目标：/fms、/rcsFlow → 200 html（SPA 回退）；配置不可达目标：两前缀 → 502（代理接管）；/api → 502（转发、本地后端未启动）【隔离验证】 |

未执行：浏览器端功能验收（本卡无业务页）、真实旧后端连通（地址未提供，SPEC §14）——不记为通过。

## 阻塞

无阻塞本卡的项。外部条件照常由责任卡跟进：旧后端地址/账号（T090）、表格库正式发布授权（T107）、网关（T106）。

## 剩余项与下次继续位置

- 本卡产物就绪；验收＝隔离验证通过，无强制剩余项。
- 可选补验（不阻塞下游）：全新目录 `pnpm install --frozen-lockfile` 冷装演练；旧前缀代理对接真实旧后端的状态/内容类型核验（并入 T090/T106 环境）。
- 下游入口：T003（实现依赖 T001 ✓）按 `docs/migration/baseline.md` §2/§3 与 environment.md §2 起步；T005 依赖 T003。旧项目行展开能力已入 npm `0.1.0`，T021 核对能力时直接按已发布版本核。
- 快照刷新规则：原仓库再变动时按 manifest §5 记录新 HEAD 后刷新，不静默覆盖。

## 消费提示（对后续卡生效的基线事实）

1. 旧前缀大小写：`/fms`、`/rcsFlow`（F 大写，以快照 .umirc.ts 为准）。
2. path 拼写保留源样：`vehicle-diplay`、`charge-pie`（definitions.tsx 已如此，勿"修正"）。
3. P08 页面目录在 `pages/system-involve/AlarmCodeManagement`，路由挂在 vehicle-deploy 下（入口索引已标注）。
4. 本会话曾探测到 5173 端口有会话外既有 dev 进程，本卡未触碰；共享工作区串行约定继续有效。
