# T021 交接记录 — 接入 ApexTable 分页、跨页选择、主题与五语

- **认领标识**：ZCode 调度轮次 2026-09-17（apple-admin 分支，单写者串行）
- **开始时间**：2026-09-17；**结束时间**：同日，约 55 分钟（超出 50 分钟预算 5 分钟，主要耗在演示环境的 dev server 代理排查与弹窗输入通道）
- **认领时输入**：目标仓库 HEAD `ebdd997`（干净）；表格库 C:\yangwenhua\ApexTableReact HEAD `ae34e0a`（工作区干净，与 npm 发布 `apex-table-react@0.1.0` 对应）；前置合同 T016 §6、T013、T003 §4/§5。
- **本次范围**：落依赖 `apex-table-react@0.1.0`；目标仓薄适配（五语包、主题桥、会话内存列偏好、request 分页与跨页选择样例）；临时探针页演示；table-capabilities.md。

## 完成内容

1. **依赖**：`apex-table-react@0.1.0`（npm 正式版）入 package.json + pnpm-lock；integrity `sha512-mXq9blpaI88…kQ==`、`dist/index.js` sha256 `4c36f36a…4c4be`；本轮未改库（分页/选择/列设置/展开/编辑库均已具备，无缺口需要补库）。
2. **薄适配 `src/components/ApexTable/`**（唯一消费入口，页面禁直引库名）：
   - `apexTableLocales.ts` 五语 ApexLocale 包（zh-CN 复用库 zhCN；en/zh-TW/ja/ko 本仓补译，含函数型文案与读屏名）；
   - `useApexTableLocale.ts` i18next→normalizeLanguage→语言包，切语言自动重渲（T016 合同 §6 唯一映射落位）；
   - `apexTableTheme.css` `--apex-table-*`→`--app-*` 令牌映射，`.apex-table.apex-table` 特异性稳定覆盖库默认（深色随 data-theme 实测生效）；
   - `tableColumnPreferences.ts` 会话内存列偏好（库 localStorage 适配器 + 进程内 Map 存储 + auth.epoch 清空；`useSessionColumnLayout` 自动防抖保存/恢复/resetToDefault）——对齐 SPEC §7.2"仅会话内记忆"；
   - `legacyTableRequest.ts` `createLegacyTableRequest`（0 基→旧协议 1 基页码、signal 下传、rowCount=后端 total）与 `useCrossPageSelection`（页面级持有选择状态）。
3. **临时探针**：`src/pages/dev/ApexTableProbe/` + 路由 `dev/apex-table-probe`（hideInMenu，收尾移除，同 T013 先例）；真实后端 pageVehicles 只读查询，零 mock。
4. **合同**：`docs/migration/contracts/table-capabilities.md`（版本锚点、能力矩阵、消费样例、5 条已验证限制、剩余项）。

## 改动文件

- 新增：`src/components/ApexTable/{index.ts,apexTableLocales.ts,useApexTableLocale.ts,apexTableTheme.css,tableColumnPreferences.ts,legacyTableRequest.ts}`、`src/pages/dev/ApexTableProbe/ApexTableProbe.tsx`、`docs/migration/contracts/table-capabilities.md`、`docs/migration/evidence/T021/`、`docs/migration/handoffs/T021.md`
- 修改：`package.json`、`pnpm-lock.yaml`、`src/router/definitions.tsx`（探针路由）、`TASKS.md`（本卡状态行）

## 验证结果（详见 evidence/T021/T021-evidence.md）

| 场景 | 结果 |
| --- | --- |
| V01 服务端分页（真实 198 条/20 页/页大小切换/翻页） | ✅【真实后端只读】 |
| V02 跨页选择保留（P1×2→P2×1 计数 3、ID 合并） | ✅ |
| V03 表头复选=仅当前页全选（A08 无全库全选） | ✅ |
| V04 清空选择 | ✅ |
| V05 五语包（en/ja/zh-TW/ko/zh-CN 文案+读屏名） | ✅ |
| V06 主题桥接深/浅（计算样式+渲染截图） | ✅（修复特异性覆盖后） |
| V07 列布局会话记忆 UI 级演示 | ⏳ 未执行（IAB 输入通道无法驱动列设置弹窗输入框，非业务缺陷） |
| 类型检查 | 目标仓 exit 0；库仓 exit 0 |

## 阻塞

无阻塞本卡。环境事项：5173 会话外遗留 dev 进程未触碰；旧协议代理需 shell 环境变量 `APEX_DEV_LEGACY_TARGET`（`.env.local` 不被 vite.config 读取），本轮 :5221 自建实例验证。

## 剩余项与下次继续位置

- **验收状态＝部分验证通过**：V01—V06 通过；V07 列偏好 UI 级演示因环境输入通道限制未执行——归 **T028**（首个表格业务页）接入时随页补验（改动列宽/显隐→离开回来保留→恢复默认），不阻塞其开工。
- 五语×两主题全组合视觉、真实大表性能归 T099—T105 分域复核；展开/编辑合同分别由 T022/T024 交付；正式包切换归 T107。
- 消费提示：业务页只从 `@/components/ApexTable` 导入；行身份用 `getRowId`+`toRowId`；筛选清空选择与末页回正是页面职责（合同 §3）；单表品牌覆盖用 `style` 属性，勿写第三套 CSS。
- 探针页 `dev/apex-table-probe` 为临时路由，T028 完成真实页验收后移除（与 V07 补验同轮处理亦可）。
