# 全模式生效（Vite dev 与 build 均加载）的真实环境配置；模板说明见 .env.example。
# 本地临时差异（如指向另一台后端）用 .env.local / .env.*.local 覆盖，不入库。

# axios 实例 baseURL：真实后端 API 前缀（apex-admin，/api/v1）；开发经 Vite dev server
# 的 /api 代理转发，生产走同源 /api/v1 网关；生产如需其他地址，在部署流水线以环境变量覆盖
VITE_API_BASE_URL=/api/v1

# 仅 Vite dev server 使用，不以 VITE_ 前缀暴露给客户端；替换为真实后端地址
# （用 127.0.0.1 而非 localhost：Windows 上 localhost 可能先解析为 ::1，IPv4-only 后端会连不上）
PROXY_TARGET=http://127.0.0.1:8000
