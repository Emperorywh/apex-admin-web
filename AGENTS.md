# 项目开发规范

适用于整个仓库。修改前查阅当前代码与配置；已有不符合规范的实现不作为新增代码的范例。

## 技术栈与工具

- 使用 React、TypeScript、Vite、Ant Design 和 Lucide React；沿用现有路由、状态管理、国际化和请求方案。
- 使用 pnpm，运行环境、依赖版本、格式和静态检查以项目配置为准。依赖变更同步更新锁文件，不新增重复工具链。

## 代码组织

- 按业务功能组织代码，相关组件、Hook、状态、类型、服务和资源就近放置。仅将真正通用的能力提升为应用级共享模块，不建立集中收纳业务类型的全局目录。
- 每个组件、页面和布局使用独立的同名目录，专属内容放在目录内；共享组件不得附带定义在其他组件文件中。
- 业务目录使用有业务含义的名称，组件使用 PascalCase，Hook 使用 `useXxx`。样式优先使用 CSS Modules，复用现有设计令牌。
- 页面负责布局与组合，组件负责展示与交互，复杂业务流程和纯数据规则从组件中提取。按实际需要拆分，不预建空目录或无用抽象。
- 类型按最小合理作用域定义，跨功能使用时从所属业务模块导入。优先复用与推导，纯类型使用类型导入，不用 `any` 绕过检查；外部数据在服务或模型边界校验、转换。

## 状态与副作用

- 状态保留在实际使用处，必要时共享；可计算的数据直接派生，持久化仅保存明确需要的数据。
- 复用现有请求封装、类型化 Redux Hook 和统一路由配置，不重复实现基础设施。
- Effect 明确依赖并清理资源；缓存页面的持续副作用随页面激活状态暂停、恢复。页面请求处理取消，主动取消不提示操作失败。

## 国际化

- 所有变更都检查国际化影响。支持 `zh-CN`、`en-US`，以中文文案为 key，中文直接显示 key，不单独维护中文资源。
- 用户可见文案及无障碍文案通过翻译函数输出，使用所属命名空间；动态文案使用插值，英文翻译与中文 key 同步维护。
- 沿用现有资源加载和语言切换机制，命名空间与路由声明同步更新；确认直接访问、缓存页面和语言切换时中英文展示正确。

## 注释与维护

- 注释使用简体中文，说明职责、业务规则、非直观逻辑和必要的设计原因，不逐行复述代码。
- 修改前阅读相关文档和注释，优先原位更新、合并重复说明、删除失效内容，保持与实现一致。
- 只实现当前需求。替换或迁移时同步更新引用并删除旧实现、无效资源和翻译，不为本仓库已被替换的旧实现保留兼容层、转发或备用分支。

## 验证与交付

- 永远不要编写单元测试，也不添加单元测试框架或配置。
- 根据变更范围执行静态检查、类型检查、格式检查、构建及必要的实际交互验证，命令以项目现有脚本为准。
- 纯文档修改只检查内容、链接和格式，无需运行应用构建。
- 交付时说明实际修改、已执行的验证及未通过的原因；未执行的检查不得报告为通过。
