## 1. 简体中文注释：覆盖全部新增与修改代码

### 覆盖范围

所有新增、修改的代码都必须有对应的简体中文注释。

### 注释内容

注释应说明代码的职责、业务含义和必要的设计原因。根据实际逻辑补充输入输出约束、状态变化、边界条件、副作用或资源清理要求，不要求每条注释机械包含所有项目。

React 代码重点解释组件用途、重要 Props 的含义、状态之间的关系、事件处理规则，以及 Effect 存在的原因和清理行为。样式注释重点解释布局目的、交互状态和不直观的尺寸约束。

## 2. 全新项目：只保留当前有效实现

### 禁止历史兼容

不得为了兼容项目旧版本而增加兼容层、适配分支、字段别名、旧接口转发、新旧数据格式探测或双读双写逻辑。

替换实现后，不保留备用旧组件、废弃函数、重复状态、旧 Props、旧导出、被注释掉的代码或只服务于旧实现的开关。不建立 `legacy`、`compat`、`deprecated` 等用于保存历史实现的结构，也不要把旧实现改名后继续留在仓库中。

不要以“以后可能用到”“保险起见”“先保留旧逻辑”为理由增加代码。面向当前明确需求实现，不预建多版本架构或没有实际用途的扩展层。

## 3. 文档与注释：原位更新，只保留最新内容

### 更新方式

修改文档或注释前，先阅读相关章节与上下文，并查找同一规则的其他表述。优先在原有位置替换、合并或删除，而不是默认追加到文件末尾。

新要求替代旧要求时，直接重写旧段落；内容不再适用时删除；多处重复时保留一个主要说明位置，其他位置按需引用。只有确实出现独立的新主题，且现有结构无法合理容纳时，才新增章节或文件。