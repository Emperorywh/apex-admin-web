# 环境变量示例：复制为 .env.development / .env.production 或 .env.*.local 后按需修改。
# 本仓库不提交 .env.*.local。

# axios 实例的 baseURL，暴露给客户端。
# 开发默认 /api（走 Vite dev server 代理）；生产可使用同源路径或完整 http(s) 地址。
VITE_API_BASE_URL=/api

# 演示模式，暴露给客户端，固定三态（规格 §13.1），启动时校验，非法值使 dev/build 失败：
#   off      不允许 demo；构建时不得包含 demo chunk、账号和假数据（真实生产默认）
#   force    所有受支持请求直接走 demo adapter；可用于无后端的示例部署，例如：
#              VITE_DEMO_MODE=force pnpm build   # 产物内置演示数据，可直接静态托管
#   fallback 先请求真实登录；仅网络级失败时提示并切换 demo 后重放一次登录（开发默认）
VITE_DEMO_MODE=fallback

# 仅 Vite dev server 使用的代理目标，不以 VITE_ 前缀暴露给客户端。
# dev server 将 /api 请求以 changeOrigin 代理到该地址。
PROXY_TARGET=http://localhost:8080
