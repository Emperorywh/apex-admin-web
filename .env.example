# 环境变量示例：复制为 .env.local（已被 .gitignore 忽略，不会提交）后按需修改。
# 各变量与运行条件的完整说明见 docs/migration/environment.md。

# 本地后端（C:\code\apex-admin，FastAPI）dev 代理目标，转发 /api 前缀。
# 未设置时默认 http://localhost:8000。
APEX_DEV_PROXY_TARGET=http://localhost:8000

# 旧调度后端 dev 代理目标，转发 /fms 与 /rcsFlow 两个前缀（rcsFlow 的 F 为大写）。
# 未设置时这两个前缀不注册代理，请求直接 404，避免误连旧配置遗留的内网生产地址。
# 测试地址与账号由用户另行提供后填入，例如 http://10.x.x.x:8888。
# APEX_DEV_LEGACY_TARGET=
