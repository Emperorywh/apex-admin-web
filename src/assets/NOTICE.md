# 资产许可说明（SPEC_UI2 §5.2/§6.5/§15）

## slash-admin 衍生资产（MIT）

本目录（`src/assets/`）下以下来源资产复制自 slash-admin（https://github.com/d3george/slash-admin，MIT License，Copyright (c) 2023 d3george）：

- `icons/` 内除 `ic-role.svg`、`ic-flask.svg` 外的全部 `ic-*.svg` 彩色菜单图标；
- `images/cyan-blur.png`、`images/red-blur.png`（设置抽屉光斑背景）。

许可全文见 `icons/LICENSE.slash-admin`。

## 项目原创增补

- `icons/ic-role.svg`（盾牌）、`icons/ic-flask.svg`（烧瓶）：按 slash-admin 同风格（双色面性、24px 画布、currentColor）由本项目原创绘制，许可与本项目一致。

## Inter 字体（SIL OFL 1.1）

`fonts/inter-latin-wght-normal.woff2` 复制自依赖包 `@fontsource-variable/inter`
（代码 MIT / 字体 SIL Open Font License 1.1），仅取拉丁子集自托管，无 CDN 依赖；
许可全文见 `fonts/LICENSE.inter`（随资产一并分发）。字体文件升级 = 升级依赖后重拷同名文件。
