/**
 * 实时轮询集中配置（T00.6，规格 8.2 / D14）。
 *
 * 「可配置间隔及退避上限在实现中集中定义」——本文件是全站唯一调整点，
 * 实时页面（P34 看板 / P05 车辆 / P39 车辆详情 / P40 服务器资源）一律经
 * useVisiblePolling 消费，不得各自私设定时器。
 *
 * 普通配置列表不轮询（进入/手动刷新/操作后刷新），不受本配置影响。
 */

/** 基础轮询间隔：一次请求完成后到下一次请求的静默时长（约 5 秒） */
export const POLL_BASE_INTERVAL_MS = 5_000

/** 失败退避倍率：连续失败时下一轮间隔 = 上一轮间隔 × 倍率 */
export const POLL_BACKOFF_MULTIPLIER = 2

/** 退避上限：失败重试间隔的最大值（约 1 分钟），避免后端恢复初期被打穿 */
export const POLL_MAX_BACKOFF_MS = 60_000
